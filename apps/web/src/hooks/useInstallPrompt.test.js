"use strict";
// Component will be created in Plan 03 — test stubs only
// Requirements: PWA-02 (Android install prompt), PWA-03 (iOS detection)
describe('useInstallPrompt [PWA-02, PWA-03]', () => {
    it.todo('exports isInstallable, isIOS, isStandalone, triggerInstall');
    it.todo('isStandalone defaults to false in jsdom');
});
