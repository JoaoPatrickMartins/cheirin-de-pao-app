// BannerInsuficiente component tests — Wave 0 stub (Fase 4)
// Requirements: SCHED-08 (alerta de crédito insuficiente)
import { describe, it } from 'vitest'
import { render } from '@testing-library/react'

describe('BannerInsuficiente', () => {
  it.todo('renderiza quando requerido maior que saldo')
  it.todo('retorna null quando requerido menor ou igual ao saldo')
  it.todo('chama onComprar ao clicar em Comprar créditos')
  it.todo('chama onAjustar com o valor do saldo ao clicar em Usar saldo')
})
