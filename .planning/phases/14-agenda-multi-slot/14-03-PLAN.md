---
phase: 14-agenda-multi-slot
plan: 03
type: execute
wave: 2
depends_on:
  - 14-01
  - 14-02
files_modified:
  - apps/api/src/modules/schedules/schedules.repository.ts
  - apps/api/src/modules/schedules/schedules.service.ts
autonomous: true
requirements:
  - MSCHED-02
  - MSCHED-04

must_haves:
  truths:
    - "createDailyOrders() gera 2 Orders separados (com deliveryTime '06:30' e '15:30') quando schedule.days tem 2 slots com qty > 0"
    - "createDailyOrders() gera 1 Order quando um slot tem saldo suficiente e outro não — transações são independentes"
    - "createDailyOrders() modo legado (schedule.days === null) continua gerando 1 Order sem deliveryTime"
    - "getConsumoSemanal() soma todos os slots de todos os dias em modo multi-slot"
    - "sendEveReminders() inclui 'às HH:MM' no texto do push quando order.deliveryTime não é null"
    - "sendEveReminders() NÃO inclui 'null' no texto quando order.deliveryTime é null"
    - "sendLowCreditNotifications() usa getConsumoSemanal() para comparar saldo com consumo total"
    - "processAutoBuy() usa getConsumoSemanal() para decidir se deve recarregar"
    - "repository.upsert() aceita body com 'days' e salva apenas days (sem weeklyQty obrigatório)"
  artifacts:
    - path: "apps/api/src/modules/schedules/schedules.service.ts"
      provides: "getConsumoSemanal() helper + createDailyOrders() multi-slot + sendEveReminders() com deliveryTime"
      contains: "function getConsumoSemanal"
    - path: "apps/api/src/modules/schedules/schedules.repository.ts"
      provides: "upsert() backward-compatible para multi-slot e legado"
      contains: "isMultiSlot"
  key_links:
    - from: "schedules.service.ts"
      to: "schedules.repository.ts"
      via: "this.repo.upsert(userId, condominiumId, data)"
      pattern: "repo.upsert"
    - from: "schedules.service.ts"
      to: "schedules.schema.ts"
      via: "import WeeklyQty, ScheduleBody"
      pattern: "WeeklyQty"
---

<objective>
Implementar suporte multi-slot no backend do módulo schedules. Duas mudanças principais:

1. **Repository:** Atualizar `upsert()` para lidar com body multi-slot (`days` presente) — salva apenas `days` no campo correspondente sem exigir `weeklyQty`; modo legado inalterado.

2. **Service:** Extrair helper `getConsumoSemanal(schedule)` (D-09) e refatorar `createDailyOrders()` para gerar 1 Order por slot quando `schedule.days !== null` (D-08). Atualizar `sendEveReminders()` com null-check em `Order.deliveryTime` (D-10). Atualizar `processAutoBuy()` e `sendLowCreditNotifications()` para usar `getConsumoSemanal()`.

Purpose: Faz os 7 testes do plano 01 passarem (RED → GREEN). Após este plano, o backend suporta plenamente o modo multi-slot enquanto mantém backward-compat com schedules legados.

Output: schedules.repository.ts e schedules.service.ts atualizados; suite de testes verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-agenda-multi-slot/14-CONTEXT.md
@.planning/phases/14-agenda-multi-slot/14-RESEARCH.md
</context>

<interfaces>
<!-- ScheduleBody após plano 02 (schedules.schema.ts atualizado): -->
```typescript
export const DaysSchema = z.record(z.string(), WeeklyQtySchema)
// DaysSchema: Record<string, WeeklyQty> — ex: { "06:30": {seg:2,...}, "15:30": {seg:1,...} }

export const ScheduleBodySchema = z.object({
  days: DaysSchema.optional(),
  weeklyQty: WeeklyQtySchema.optional(),
  deliveryTime: z.string().optional(),
  notifyReconfigure: z.boolean().default(false),
}).refine(/* ... */)

export type ScheduleBody = z.infer<typeof ScheduleBodySchema>
// { days?: Record<string,WeeklyQty>, weeklyQty?: WeeklyQty, deliveryTime?: string, notifyReconfigure: boolean }
```

<!-- schema.prisma após plano 02 (campos nullable): -->
```prisma
model Schedule {
  weeklyQty    Json?   // nullable após plano 02
  deliveryTime String? // nullable após plano 02
  days         Json?
}

model Order {
  deliveryTime String? // NOVO campo — adicionado no plano 02
}
```

<!-- Estrutura atual de createDailyOrders (schedules.service.ts): -->
```typescript
// Loop atual (modo único, legado):
for (const schedule of schedules) {
  const weeklyQty = schedule.weeklyQty as WeeklyQty
  const qty = weeklyQty[dayKey] ?? 0
  if (qty === 0) continue
  // verifica saldo, cria $transaction com order.create + user.update + creditTransaction.create
}
```

<!-- Estrutura atual de upsert (schedules.repository.ts): -->
```typescript
upsert(userId: string, condominiumId: string, data: ScheduleBody) {
  return this.prisma.schedule.upsert({
    where: { userId_condominiumId: { userId, condominiumId } },
    update: { weeklyQty: data.weeklyQty, deliveryTime: data.deliveryTime, notifyReconfigure: data.notifyReconfigure, isActive: true },
    create: { userId, condominiumId, weeklyQty: data.weeklyQty, deliveryTime: data.deliveryTime, notifyReconfigure: data.notifyReconfigure, isActive: true },
  })
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar repository.upsert() para modo multi-slot</name>
  <files>apps/api/src/modules/schedules/schedules.repository.ts</files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.repository.ts (arquivo completo — ler antes de editar)
    - apps/api/src/modules/schedules/schedules.schema.ts (verificar tipo ScheduleBody atual após plano 02)
    - .planning/phases/14-agenda-multi-slot/14-RESEARCH.md (seção "Repository upsert atualizado" — padrão a seguir)
  </read_first>
  <action>
    Modificar o método `upsert()` em schedules.repository.ts para discriminar entre modo multi-slot e legado.

    Detecção: `const isMultiSlot = 'days' in data && data.days !== undefined`

    Quando `isMultiSlot === true`:
    - `update`: salva `{ days: data.days, notifyReconfigure: data.notifyReconfigure, isActive: true }` — NÃO salva weeklyQty/deliveryTime (preserva valores legados existentes no documento)
    - `create`: cria com `{ userId, condominiumId, days: data.days, notifyReconfigure: data.notifyReconfigure, isActive: true }` — weeklyQty e deliveryTime são nullable agora (plano 02), não precisam ser passados

    Quando `isMultiSlot === false` (modo legado):
    - `update` e `create`: comportamento atual inalterado (weeklyQty + deliveryTime)

    IMPORTANTE: no `create` multi-slot, weeklyQty e deliveryTime são nullable após plano 02 — não precisa de objeto placeholder zerado. Prisma Client aceita omitir campos nullable no create.

    Verificar se TypeScript aceita o tipo após a mudança de schema (`weeklyQty: Json?`). Se houver erro de tipo no campo `weeklyQty` no modo legado (data.weeklyQty pode ser undefined segundo o tipo ScheduleBody), usar operador `!` ou cast `as WeeklyQty` para o mode legado após a verificação `isMultiSlot === false`.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -E "schedules.repository|ScheduleBody" | head -10</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` sem erros em schedules.repository.ts
    - `grep "isMultiSlot" apps/api/src/modules/schedules/schedules.repository.ts` retorna resultado
    - Método upsert() presente com dois caminhos (multi-slot e legado)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implementar getConsumoSemanal() + createDailyOrders() multi-slot + sendEveReminders() null-safe</name>
  <files>apps/api/src/modules/schedules/schedules.service.ts</files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.service.ts (arquivo completo — ler antes de qualquer edição)
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (verificar o que os testes do plano 01 esperam exatamente — especialmente deliveryTime nos data do order.create)
    - .planning/phases/14-agenda-multi-slot/14-CONTEXT.md (D-08, D-09, D-10 — definições exatas)
    - .planning/phases/14-agenda-multi-slot/14-RESEARCH.md (Pattern 2, Pattern 3, Pitfall 2, Pitfall 5)
  </read_first>
  <action>
    Realizar quatro mudanças em schedules.service.ts:

    MUDANÇA 1 — Extrair função helper `getConsumoSemanal` (D-09):
    Adicionar função module-level (não método de classe) logo após as funções utilitárias
    `getTomorrowDayKey` e `getTomorrowDate`. Assinatura:
    `function getConsumoSemanal(schedule: { days: unknown; weeklyQty: unknown }): number`

    Lógica:
    - Se `schedule.days` é truthy: cast para `Record<string, WeeklyQty>`, usa `Object.values(days).flatMap(wq => Object.values(wq)).reduce((sum, v) => sum + (v as number), 0)`
    - Senão: cast `schedule.weeklyQty` para `WeeklyQty`, usa `Object.values(wq).reduce((sum, v) => sum + (v as number), 0)`

    MUDANÇA 2 — Refatorar `createDailyOrders()` (D-08):
    Dentro do loop `for (const schedule of schedules)`, adicionar bifurcação ANTES do código atual:

    ```
    if (schedule.days) {
      // MODO MULTI-SLOT: iterar por slot
      const days = schedule.days as Record<string, WeeklyQty>
      for (const [slotTime, weeklyQtyMap] of Object.entries(days)) {
        const qty = weeklyQtyMap[dayKey] ?? 0
        if (qty === 0) continue
        // Verificar saldo — ANTES de entrar na transação (Pitfall 2)
        const user = await this.repo.findUserById(schedule.userId)
        if (!user) continue
        if (user.creditBalance < qty) {
          // enviar push de alerta (mesmo código do modo legado) e continue
          continue
        }
        // Transação independente por slot (Pitfall 2 — NÃO agrupar ambos os slots numa transação)
        await this.prisma.$transaction(async (tx) => {
          await tx.order.create({ data: {
            userId: schedule.userId, type: 'SCHEDULED', quantity: qty,
            scheduledDate, status: 'SCHEDULED',
            deliveryTime: slotTime,  // NOVO campo Order.deliveryTime
          }})
          await tx.user.update({ where: { id: schedule.userId }, data: { creditBalance: { decrement: qty } } })
          await tx.creditTransaction.create({ data: {
            userId: schedule.userId, type: 'DELIVERY', quantity: -qty,
            description: `Entrega agendada para ${scheduledDate.toISOString().split('T')[0]} às ${slotTime}`,
          }})
        })
      }
      continue  // pular o bloco legado abaixo
    }
    // MODO LEGADO: código atual inalterado
    const weeklyQty = schedule.weeklyQty as WeeklyQty
    // ...resto do código atual...
    ```

    MUDANÇA 3 — Atualizar `sendEveReminders()` (D-10, Pitfall 5):
    Modificar a interpolação do texto do push de véspera:
    - ANTES: `notification.contents = { pt: \`Lembrete: ${order.quantity} pães agendados para amanhã.\` }`
    - DEPOIS: 
      ```
      const timeStr = order.deliveryTime ? ` às ${order.deliveryTime}` : ''
      notification.contents = { pt: `Lembrete: ${order.quantity} pães${timeStr} amanhã.` }
      ```
    - Aplicar o mesmo null-check no `body` de `createAndTrim` para manter consistência.

    MUDANÇA 4 — Atualizar `processAutoBuy()` e `sendLowCreditNotifications()`:
    - Em `processAutoBuy()`: substituir o bloco de cálculo `consumoSemanal` por chamada a `getConsumoSemanal(schedule)` nos dois lugares onde aparece (modo 'acabar').
    - Em `sendLowCreditNotifications()`: substituir o bloco de cálculo `consumoSemanal` por chamada a `getConsumoSemanal(schedule)`.

    ATENÇÃO: busca do usuário (`this.repo.findUserById`) no loop multi-slot deve ocorrer DENTRO
    de cada iteração de slot, não fora — cada slot precisa verificar saldo atualizado após
    a transação do slot anterior ter decrementado o creditBalance.
  </action>
  <verify>
    <automated>cd apps/api && npm test -- schedules.service 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `cd apps/api && npm test -- schedules.service` mostra todos os 7 novos testes como PASS (GREEN)
    - Testes existentes continuam verdes (nenhuma regressão)
    - `grep "getConsumoSemanal" apps/api/src/modules/schedules/schedules.service.ts` retorna pelo menos 4 ocorrências (definição + 3 usos)
    - `grep "deliveryTime: slotTime" apps/api/src/modules/schedules/schedules.service.ts` retorna resultado
    - `npx tsc --noEmit` sem erros em schedules.service.ts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron → Prisma | createDailyOrders() lê schedules sem filtro de userId externo — operação interna segura |
| schedule.days → Order.deliveryTime | Chave do campo days (HH:MM) é copiada diretamente para Order.deliveryTime — sem sanitização necessária (string de horário) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Elevation of Privilege | createDailyOrders() — userId | mitigate | userId sempre do schedule.userId (lido do banco) — nunca de input externo; padrão JWT já estabelecido em fases anteriores |
| T-14-03-02 | Tampering | Transação independente por slot | mitigate | Cada slot tem sua própria `prisma.$transaction` — saldo insuficiente em 1 slot não afeta o outro (Pitfall 2 do RESEARCH.md implementado) |
| T-14-03-03 | Information Disclosure | sendEveReminders null check | mitigate | `order.deliveryTime ? \` às ${order.deliveryTime}\` : ''` — null nunca aparece no texto do push (Pitfall 5) |
</threat_model>

<verification>
Suite completa verde:

```bash
cd apps/api && npm test -- schedules.service 2>&1 | grep -E "PASS|FAIL|✓|✗" | head -30
```

Verificação de TypeScript:

```bash
cd apps/api && npx tsc --noEmit 2>&1 | grep -v "^$" | head -20
```

Verificação de presença das mudanças:

```bash
grep -c "getConsumoSemanal" apps/api/src/modules/schedules/schedules.service.ts && \
grep -c "deliveryTime: slotTime" apps/api/src/modules/schedules/schedules.service.ts && \
grep -c "isMultiSlot" apps/api/src/modules/schedules/schedules.repository.ts
```

Esperado: 4+ ocorrências de getConsumoSemanal, 1 de deliveryTime: slotTime, 1+ de isMultiSlot.
</verification>

<success_criteria>
- `cd apps/api && npm test -- schedules.service` — todos os 7 novos testes GREEN, zero regressões
- `npx tsc --noEmit` sem erros em schedules.service.ts e schedules.repository.ts
- schedules.service.ts contém função `getConsumoSemanal` reutilizada em 3+ funções do serviço
- createDailyOrders() contém bifurcação `if (schedule.days)` → loop de slots com `deliveryTime: slotTime`
- sendEveReminders() usa null-check `order.deliveryTime ?` antes de interpolar no texto
- repository.upsert() discrimina por `isMultiSlot` com dois caminhos de update/create
</success_criteria>

<output>
Criar `.planning/phases/14-agenda-multi-slot/14-03-SUMMARY.md` ao concluir.
</output>
