---
phase: 14-agenda-multi-slot
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
autonomous: true
requirements:
  - MSCHED-02
  - MSCHED-04

must_haves:
  truths:
    - "createDailyOrders() gera 2 Orders independentes quando schedule.days tem 2 slots com qty > 0"
    - "createDailyOrders() gera 0 Orders quando ambos os slots têm qty = 0 no dia"
    - "createDailyOrders() gera apenas 1 Order (manhã) quando saldo insuficiente para tarde"
    - "createDailyOrders() modo legado (schedule.days === null) continua funcionando sem mudanças"
    - "getConsumoSemanal() retorna soma total de todos os slots em modo multi-slot"
    - "sendEveReminders() inclui deliveryTime no texto quando order.deliveryTime !== null"
    - "sendEveReminders() NÃO inclui 'null' literal no texto quando order.deliveryTime é null"
    - "sendLowCreditNotifications() usa getConsumoSemanal() para calcular consumo em modo multi-slot"
  artifacts:
    - path: "apps/api/src/modules/schedules/__tests__/schedules.service.test.ts"
      provides: "7 novos testes MSCHED-02/04 em RED state"
      contains: "createDailyOrders_multiSlot"
  key_links:
    - from: "schedules.service.test.ts"
      to: "schedules.service.ts"
      via: "import SchedulesService"
      pattern: "from '../schedules.service.js'"
---

<objective>
Adicionar 7 testes unitários ao arquivo de testes existente do SchedulesService, cobrindo os comportamentos multi-slot definidos em MSCHED-02 e MSCHED-04. Os testes devem estar em estado RED (failing) pois as implementações ainda não existem — o plano 03 fará os testes passarem.

Purpose: Garantir cobertura automatizada dos cenários críticos de multi-slot antes da implementação, conforme Wave 0 do VALIDATION.md. Os testes documentam o contrato esperado das funções `createDailyOrders`, `getConsumoSemanal`, `sendEveReminders` e `sendLowCreditNotifications`.

Output: Arquivo de testes atualizado com 7 novos describe/it blocks. Suite existente deve continuar passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-agenda-multi-slot/14-CONTEXT.md
@.planning/phases/14-agenda-multi-slot/14-RESEARCH.md
@.planning/phases/14-agenda-multi-slot/14-VALIDATION.md
</context>

<interfaces>
<!-- Arquivo existente — ler antes de modificar para entender estrutura de mocks -->
<!-- apps/api/src/modules/schedules/__tests__/schedules.service.test.ts -->

Tipos relevantes já declarados no arquivo:
```typescript
type ScheduleShape = {
  id: string
  userId: string
  condominiumId: string
  isActive: boolean
  weeklyQty: object
  deliveryTime: string
  notifyReconfigure: boolean
}
// ATENÇÃO: adicionar campo `days` opcional: days?: object | null

type OrderShape = {
  id: string
  userId: string
  quantity: number
  scheduledDate: Date
  status: string
}
// ATENÇÃO: adicionar campo `deliveryTime` opcional: deliveryTime?: string | null

type UserShape = {
  id: string
  creditBalance: number
  condominiumId: string | null
  autoRecharge: object | null
  oneSignalPlayerId: string | null
}
```

Função helper de mock existente:
```typescript
function createMockFastify(overrides?: {
  schedules?: ScheduleShape[]
  users?: Record<string, UserShape>
  createOrderFn?: ReturnType<typeof vi.fn>
  updateUserFn?: ReturnType<typeof vi.fn>
  createTransactionFn?: ReturnType<typeof vi.fn>
}): FastifyInstance
```

Nota: o mock de `prisma.order.findMany` precisará ser extendido para cobrir `sendEveReminders` (retorna OrderShape[] com campo `deliveryTime`).
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Adicionar 7 testes MSCHED-02/04 em estado RED</name>
  <files>apps/api/src/modules/schedules/__tests__/schedules.service.test.ts</files>
  <read_first>
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (arquivo completo — ler antes de qualquer edição para entender estrutura de mocks, tipos declarados e padrões de describe/it)
    - apps/api/src/modules/schedules/schedules.service.ts (código atual de createDailyOrders, sendEveReminders, sendLowCreditNotifications — entender o que cada função faz hoje)
    - .planning/phases/14-agenda-multi-slot/14-VALIDATION.md (lista exata dos 7 nomes de testes requeridos em Wave 0 Requirements)
  </read_first>
  <behavior>
    Testes a adicionar (nomes exatos per VALIDATION.md):

    1. `createDailyOrders_multiSlot_cria2Orders_quandoAmbosSlotsTêmQty`
       - schedule.days = { "06:30": { seg:2, ...todos os dias com 2 }, "15:30": { seg:1, ...todos os dias com 1 } }
       - schedule.weeklyQty = { seg:0, ...zeros } (placeholder)
       - user.creditBalance = 20 (suficiente para ambos os slots)
       - Esperado: createOrderFn chamada 2x com deliveryTime "06:30" e "15:30" respectivamente

    2. `createDailyOrders_multiSlot_criaApenasOrdersManhã_quandoTardeInsuficiente`
       - schedule.days = { "06:30": { todos os dias: 2 }, "15:30": { todos os dias: 5 } }
       - user.creditBalance = 3 (suficiente para manhã qty=2, insuficiente para tarde qty=5)
       - Esperado: createOrderFn chamada 1x (apenas manhã com deliveryTime "06:30")

    3. `createDailyOrders_legado_continuaFuncionando_quandoDaysNulo`
       - schedule.days = null (modo legado)
       - schedule.weeklyQty = { seg:2, ter:2, ...todos os dias com 2 }
       - user.creditBalance = 10
       - Esperado: createOrderFn chamada 1x sem campo deliveryTime (ou deliveryTime ausente/undefined)

    4. `getConsumoSemanal_retornaSomaTotal_modoMultiSlot`
       - Testar a função helper diretamente via comportamento do serviço em processAutoBuy ou sendLowCreditNotifications
       - schedule.days = { "06:30": { seg:2,ter:0,qua:2,qui:0,sex:2,sab:0,dom:0 }, "15:30": { seg:1,ter:0,qua:1,qui:0,sex:1,sab:0,dom:0 } }
       - consumoSemanal esperado: (2+2+2)+(1+1+1) = 9
       - Verificar via sendLowCreditNotifications: user.creditBalance = 8 (< 9) → push enviado; se creditBalance = 10 (>= 9) → push NÃO enviado

    5. `sendEveReminders_textoPush_inclui_deliveryTime_quandoDisponível`
       - Mock prisma.order.findMany retorna order com deliveryTime: "06:30", quantity: 2
       - user.oneSignalPlayerId = "player-1"
       - Esperado: contents.pt contém "06:30" e NÃO contém "null"

    6. `sendEveReminders_textoPush_SEM_null_quandoDeliveryTimeNulo`
       - Mock prisma.order.findMany retorna order com deliveryTime: null, quantity: 3
       - user.oneSignalPlayerId = "player-1"
       - Esperado: contents.pt NÃO contém "null" nem "undefined"

    7. `sendLowCreditNotifications_usaGetConsumoSemanal_modoMultiSlot`
       - schedule.days = { "06:30": { seg:3, ...todos 3 }, "15:30": { seg:3, ...todos 3 } }
       - user.creditBalance = 10 (< consumoSemanal = 42 → 3*7 + 3*7)
       - Esperado: createNotificationMock chamado (push de low credit enviado)
       - Com creditBalance = 50 (>= 42): createNotificationMock NÃO chamado

    Todos os 7 testes devem estar em estado RED (failing) ao final deste plano, pois
    as implementações multi-slot não existem ainda em schedules.service.ts.

    Manter os testes existentes intactos — não alterar nenhum describe/it já presente.

    O mock `createMockFastify` deve ser extendido para suportar:
    - `ScheduleShape.days?: object | null` (campo opcional)
    - `prisma.order.findMany` retornando orders com `deliveryTime?: string | null`
    - Parâmetros de sobrescrita para `prisma.order.findMany` e `prisma.user.findUnique` nos testes de sendEveReminders
  </behavior>
  <verify>
    <automated>cd apps/api && npm test -- schedules.service 2>&1 | tail -40</automated>
  </verify>
  <done>
    - 7 novos testes adicionados ao arquivo existente
    - Testes existentes continuam passando (verde)
    - 7 novos testes estão failing (RED) com mensagens de erro sobre comportamento não implementado
    - `cd apps/api && npm test -- schedules.service` mostra os 7 testes como FAIL
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → service | Testes chamam SchedulesService com mocks de Prisma e OneSignal — sem I/O real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-SC | Tampering | npm install (Wave 0) | accept | Nenhum pacote novo instalado — apenas edição de arquivo existente |
</threat_model>

<verification>
Suite existente verde + 7 novos testes em RED:

```bash
cd apps/api && npm test -- schedules.service
```

Output esperado: testes existentes PASS, 7 novos testes FAIL com mensagem clara sobre
comportamento multi-slot ausente (ex: "Expected createOrderFn to be called twice").
</verification>

<success_criteria>
- `cd apps/api && npm test -- schedules.service` executa sem crash
- Testes existentes (createDailyOrders legado, upsertSchedule, processAutoBuy) continuam verdes
- 7 novos testes aparecem como FAIL (RED state confirmado)
- Nenhum import novo de fora do projeto adicionado ao arquivo de testes
</success_criteria>

<output>
Criar `.planning/phases/14-agenda-multi-slot/14-01-SUMMARY.md` ao concluir.
</output>
