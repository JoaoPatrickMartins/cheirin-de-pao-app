---
phase: 14-agenda-multi-slot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/modules/schedules/schedules.schema.ts
autonomous: true
requirements:
  - MSCHED-02
  - MSCHED-04

must_haves:
  truths:
    - "Order.deliveryTime String? existe no schema — orders multi-slot têm deliveryTime preenchido, orders legados têm null"
    - "Schedule.deliveryTime é String? (nullable) — upsert multi-slot não falha por campo obrigatório ausente"
    - "Schedule.weeklyQty é Json? (nullable) — upsert multi-slot usa objeto zerado como placeholder sem TypeScript error"
    - "ScheduleBodySchema aceita body { days, notifyReconfigure } sem error 400"
    - "ScheduleBodySchema aceita body { weeklyQty, deliveryTime, notifyReconfigure } sem error 400"
    - "ScheduleBodySchema rejeita body vazio (sem days nem weeklyQty+deliveryTime) com mensagem clara"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Order.deliveryTime String? + Schedule.deliveryTime String? + Schedule.weeklyQty Json?"
      contains: "deliveryTime   String?"
    - path: "apps/api/src/modules/schedules/schedules.schema.ts"
      provides: "ScheduleBodySchema backward-compatible com DaysSchema"
      contains: "DaysSchema"
  key_links:
    - from: "apps/api/src/modules/schedules/schedules.repository.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "Prisma Client gerado após db push"
      pattern: "deliveryTime.*String?"
    - from: "apps/api/src/modules/schedules/schedules.schema.ts"
      to: "schedules.repository.ts"
      via: "export type ScheduleBody"
      pattern: "export type ScheduleBody"
---

<objective>
Realizar duas mudanças de schema/contrato necessárias para a fase multi-slot:

1. **Schema Prisma:** Tornar `Schedule.deliveryTime` e `Schedule.weeklyQty` nullable (`String?` e `Json?`); adicionar `Order.deliveryTime String?`. Executar `db push` [BLOCKING].

2. **Zod Schema:** Substituir `ScheduleBodySchema` por versão backward-compatible que aceita tanto `{ days, notifyReconfigure }` (multi-slot) quanto `{ weeklyQty, deliveryTime, notifyReconfigure }` (legado).

Purpose: Desbloqueante para o plano 03 (service + repository). O `db push` atualiza o Prisma Client gerado, tornando os tipos TypeScript corretos para os campos nullable.

Output: schema.prisma atualizado, db push executado, schedules.schema.ts com DaysSchema + ScheduleBodySchema refatorado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-agenda-multi-slot/14-CONTEXT.md
@.planning/phases/14-agenda-multi-slot/14-RESEARCH.md
</context>

<interfaces>
<!-- Estado atual dos campos relevantes no schema.prisma (linhas 192-221): -->

```prisma
model Schedule {
  id                String   @id @default(auto()) @map("_id") @db.ObjectId
  userId            String   @db.ObjectId
  condominiumId     String   @db.ObjectId
  weeklyQty         Json        // MUDAR para Json?
  deliveryTime      String      // MUDAR para String?
  notifyReconfigure Boolean  @default(false)
  isActive          Boolean  @default(true)
  days              Json?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  @@unique([userId, condominiumId])
}

model Order {
  id             String      @id @default(auto()) @map("_id") @db.ObjectId
  userId         String      @db.ObjectId
  type           OrderType
  quantity       Int
  scheduledDate  DateTime
  status         OrderStatus
  deliveryListId String?     @db.ObjectId
  courierId      String?     @db.ObjectId
  condominiumId  String?     @db.ObjectId
  // ADICIONAR: deliveryTime String?
  createdAt      DateTime    @default(now())
  updatedAt      DateTime    @updatedAt
}
```

<!-- Estado atual de schedules.schema.ts: -->

```typescript
// ATUAL — single-slot apenas:
export const WeeklyQtySchema = z.object({
  seg: z.number().int().min(0).max(12),
  ter: z.number().int().min(0).max(12),
  qua: z.number().int().min(0).max(12),
  qui: z.number().int().min(0).max(12),
  sex: z.number().int().min(0).max(12),
  sab: z.number().int().min(0).max(12),
  dom: z.number().int().min(0).max(12),
})

export const ScheduleBodySchema = z.object({
  weeklyQty: WeeklyQtySchema,
  deliveryTime: z.enum(['06:30', '07:00', '07:30', '08:00']),
  notifyReconfigure: z.boolean().default(false),
})

export type ScheduleBody = z.infer<typeof ScheduleBodySchema>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar schema.prisma e executar db push [BLOCKING]</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (ler completo — identificar linha exata de Schedule.deliveryTime, Schedule.weeklyQty e posição de inserção de Order.deliveryTime)
  </read_first>
  <action>
    Editar apps/api/prisma/schema.prisma realizando três mudanças cirúrgicas:

    1. Em modelo Schedule: alterar `deliveryTime String` para `deliveryTime String?`
    2. Em modelo Schedule: alterar `weeklyQty Json` para `weeklyQty Json?`
    3. Em modelo Order: adicionar linha `deliveryTime   String?` após o campo `condominiumId String? @db.ObjectId` e antes de `createdAt DateTime @default(now())`

    Após editar o schema, executar o db push (MongoDB Atlas remoto — nunca `prisma migrate dev`):

    ```
    cd apps/api && npx prisma db push --accept-data-loss
    ```

    O flag `--accept-data-loss` é necessário para o ambiente não-interativo (sem TTY).
    O push torna os campos `Schedule.deliveryTime`, `Schedule.weeklyQty` e `Order.deliveryTime` nullable
    no Prisma Client gerado — documentos MongoDB existentes não são afetados (MongoDB ignora
    campos ausentes em documentos existentes; o Prisma Client simplesmente retornará null para
    esses campos em documentos que não os têm).

    Não alterar nenhum outro campo do schema — mudanças cirúrgicas apenas.
  </action>
  <verify>
    <automated>cd apps/api && npx prisma validate 2>&1 && grep -n "deliveryTime.*String?" apps/api/prisma/schema.prisma | wc -l</automated>
  </verify>
  <done>
    - `npx prisma validate` sai com código 0 (schema válido)
    - `grep "deliveryTime.*String?" apps/api/prisma/schema.prisma` retorna 2 linhas (Schedule e Order)
    - `grep "weeklyQty.*Json?" apps/api/prisma/schema.prisma` retorna 1 linha (Schedule)
    - `npx prisma db push --accept-data-loss` completou sem erro
    - Arquivo `apps/api/node_modules/.prisma/client/` atualizado (Prisma Client regenerado)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refatorar ScheduleBodySchema para aceitar formato multi-slot e legado</name>
  <files>apps/api/src/modules/schedules/schedules.schema.ts</files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.schema.ts (arquivo atual completo)
    - apps/api/src/modules/schedules/schedules.repository.ts (usa ScheduleBody — verificar impacto da mudança de tipo)
    - apps/api/src/modules/schedules/schedules.controller.ts (usa ScheduleBodySchema.parse — verificar onde é chamado)
    - .planning/phases/14-agenda-multi-slot/14-RESEARCH.md (Pattern 1 e Pitfall 3 — aviso sobre z.union e alternativa com .refine)
  </read_first>
  <behavior>
    - ScheduleBodySchema com body `{ days: {"06:30":{seg:2,...}}, notifyReconfigure: false }` → parse() retorna objeto sem error
    - ScheduleBodySchema com body `{ weeklyQty: {seg:2,...}, deliveryTime: "06:30", notifyReconfigure: false }` → parse() retorna objeto sem error
    - ScheduleBodySchema com body `{}` → parse() lança ZodError com mensagem sobre campos obrigatórios
    - WeeklyQtySchema mantém min(0) max(12) por campo — DaysSchema aplica WeeklyQtySchema para cada slot
    - ScheduleBody type inferido do schema — usado no repository e controller sem mudança de chamada
  </behavior>
  <action>
    Substituir o conteúdo de apps/api/src/modules/schedules/schedules.schema.ts mantendo
    WeeklyQtySchema inalterado e adicionando DaysSchema + refatorando ScheduleBodySchema.

    Usar a abordagem de schema unificado com .refine (Pattern 1 / Pitfall 3 do RESEARCH.md):

    - Exportar `DaysSchema = z.record(z.string(), WeeklyQtySchema)` — chave HH:MM, valor WeeklyQty
    - `ScheduleBodySchema` passa a ser z.object com todos os campos opcionais + .refine que exige
      presença de `days` OU de `weeklyQty + deliveryTime`
    - `deliveryTime` no schema legado: mudar de `z.enum([...])` para `z.string()` — slots são
      configuráveis pelo admin (RESEARCH.md Pitfall 3 e warning após Pattern 1)
    - Exportar `DaysSchema` e `type ScheduleBody` como antes (export type deve continuar existindo)

    Estrutura dos campos no z.object:
    - `days: DaysSchema.optional()`
    - `weeklyQty: WeeklyQtySchema.optional()`
    - `deliveryTime: z.string().optional()`
    - `notifyReconfigure: z.boolean().default(false)`

    Mensagem do .refine: 'Forneça days (multi-slot) ou weeklyQty+deliveryTime (single-slot)'

    Não alterar schedules.repository.ts nem schedules.controller.ts neste plano — o repositório
    será atualizado no plano 03.
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit 2>&1 | grep -E "schedules.schema|ScheduleBody|DaysSchema" | head -20</automated>
  </verify>
  <done>
    - `npx tsc --noEmit` não produz erros em schedules.schema.ts
    - `grep "DaysSchema" apps/api/src/modules/schedules/schedules.schema.ts` retorna resultado
    - `grep "deliveryTime.*z.string" apps/api/src/modules/schedules/schedules.schema.ts` retorna resultado (não mais z.enum)
    - `grep "export type ScheduleBody" apps/api/src/modules/schedules/schedules.schema.ts` retorna resultado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| body HTTP → ScheduleBodySchema | Body do PUT /schedules/me deve ser validado antes de qualquer acesso ao banco |
| Prisma Client → MongoDB Atlas | db push envia schema ao Atlas remoto — operação irreversível sem rollback automático |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | DaysSchema — chaves de horário | accept | Aceitar qualquer string HH:MM no schema — cron ignora horários inativos; validação estrita adicionaria query extra ao banco por benefício marginal no MVP (documentado em RESEARCH.md Security) |
| T-14-02-02 | Tampering | WeeklyQty por slot | mitigate | WeeklyQtySchema mantém min(0) max(12) por campo — DaysSchema aplica WeeklyQtySchema para cada slot, limitando overflow de quantidade |
| T-14-02-03 | Elevation of Privilege | userId no body PUT | mitigate | userId sempre extraído do JWT (request.user.id) no controller — nunca do body; padrão já estabelecido (T-10-02-01) |
</threat_model>

<verification>
Verificação final do plano 02:

```bash
cd apps/api && npx prisma validate && \
  grep -c "deliveryTime.*String?" apps/api/prisma/schema.prisma && \
  grep -c "weeklyQty.*Json?" apps/api/prisma/schema.prisma && \
  grep -c "DaysSchema" apps/api/src/modules/schedules/schedules.schema.ts && \
  npx tsc --noEmit 2>&1 | grep -v "^$" | head -10
```

Resultado esperado: prisma validate OK, 2 ocorrências de deliveryTime String?, 1 de weeklyQty Json?, DaysSchema presente, tsc sem erros em schedules.schema.ts.
</verification>

<success_criteria>
- schema.prisma contém `Order.deliveryTime String?` e `Schedule.deliveryTime String?` e `Schedule.weeklyQty Json?`
- `npx prisma db push` executou com sucesso (sem error de conexão ou validação)
- `schedules.schema.ts` exporta `DaysSchema`, `WeeklyQtySchema`, `ScheduleBodySchema`, `WeeklyQty`, `ScheduleBody`
- `ScheduleBodySchema.parse({ days: {"06:30":{seg:2,ter:0,qua:0,qui:0,sex:0,sab:0,dom:0}}, notifyReconfigure: false })` não lança exceção
- `ScheduleBodySchema.parse({ weeklyQty: {seg:2,ter:0,qua:0,qui:0,sex:0,sab:0,dom:0}, deliveryTime: "06:30", notifyReconfigure: false })` não lança exceção
- `npx tsc --noEmit` sem novos erros de TypeScript
</success_criteria>

<output>
Criar `.planning/phases/14-agenda-multi-slot/14-02-SUMMARY.md` ao concluir.
</output>
