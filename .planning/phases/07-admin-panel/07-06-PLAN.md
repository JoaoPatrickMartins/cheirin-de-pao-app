---
phase: 07-admin-panel
plan: 06
type: execute
wave: 1
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - apps/api/src/plugins/cron.ts
  - apps/api/src/server.ts
  - apps/api/src/modules/admin-orders/admin-orders.route.ts
  - apps/api/src/modules/admin-orders/admin-orders.controller.ts
  - apps/api/src/modules/admin-orders/admin-orders.service.ts
autonomous: true
requirements: [ADMO-01, ADMO-02, ADMO-03, ADMO-04, ADMO-10, ADMO-11, UI-09]

must_haves:
  truths:
    - "server.ts registra todos os 9 novos modulos admin via fastify.register"
    - "cron.ts tem um 4o cron '0 * * * *' que chama adminSettingsService.processCutoff()"
    - "GET /admin/dashboard retorna 4 KPIs: breadsTodayCount, revenueToday, clientsCount, condominiumsCount"
    - "GET /admin/orders/delivery-status retorna entregas do dia agrupadas por condominio"
    - "GET /admin/orders/division-suggestion retorna sugestao de divisao entre entregadores ativos"
  artifacts:
    - path: "apps/api/src/server.ts"
      provides: "9 novos modulos admin registrados"
    - path: "apps/api/src/plugins/cron.ts"
      provides: "Cron 4 de cutoff a cada hora cheia"
    - path: "apps/api/src/modules/admin-orders/admin-orders.service.ts"
      provides: "getDashboard, getDeliveryStatus, getDivisionSuggestion"
  key_links:
    - from: "cron.ts"
      to: "admin-settings.service.ts"
      via: "cronPlugin instancia AdminSettingsService e chama processCutoff() a cada XX:00"
      pattern: "processCutoff"
    - from: "admin-orders.service.ts getDivisionSuggestion"
      to: "prisma.user (role=COURIER, isBlocked=false)"
      via: "algoritmo hibrido: balanceia paes totais entre entregadores ativos"
      pattern: "role.*COURIER"
---

<objective>
Concluir a camada API: registrar todos os 9 modulos novos em server.ts, adicionar cron de corte em cron.ts e estender admin-orders com 3 endpoints de dashboard, status de entregas e sugestao de divisao.

Purpose: Plano de fechamento da Wave 1 API — integra todos os modulos em um servidor funcional e adiciona os endpoints que alimentam o AdminPainel e AdminEntregas do frontend.

Output: cron.ts + server.ts modificados + admin-orders estendido com dashboard e divisao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-RESEARCH.md
@.planning/phases/07-admin-panel/07-PATTERNS.md

<interfaces>
<!-- cron.ts padrao existente (de PATTERNS.md):
import cron from 'node-cron'
if (process.env.NODE_ENV === 'test') { fastify.log.info('[cron] ambiente de teste'); return }  // OBRIGATORIO
// Adicionar apos o cron 3 existente:
const adminSettingsService = new AdminSettingsService(fastify)
cron.schedule('0 * * * *', async () => { await adminSettingsService.processCutoff() },
  { timezone: 'America/Sao_Paulo', name: 'cutoff-check' })
-->

<!-- server.ts padrao de registro (de PATTERNS.md):
import { adminCondominiumsRoute } from './modules/admin-condominiums/admin-condominiums.route.js'
// ... demais imports
await fastify.register(adminCondominiumsRoute)
// ... demais registers
Ordem recomendada: adminSettingsRoute, adminCondominiumsRoute, adminCombosRoute, adminSuppliersRoute,
  adminCouriersRoute, adminClientsRoute, adminSupplierOrdersRoute, adminFinancialRoute, adminPaymentsRoute
-->

<!-- admin-orders existente (Fase 5+6):
O modulo admin-orders.route.ts ja existe com:
- PATCH /admin/orders/:id/status (Fase 5)
- PATCH /admin/orders/assign-courier (Fase 6)
Estender com 3 endpoints novos — NAO remover os existentes
-->

<!-- Algoritmo de divisao de entregadores (de CONTEXT.md D-10):
D-10: Hibrido — prioriza condomínios inteiros por entregador; divide quantidade se necessario para balancear.
Criterio: equilibrar total de paes entre entregadores ativos.
Implementacao sugerida:
1. Buscar entregadores ativos (role=COURIER, isBlocked=false)
2. Buscar Orders do dia seguinte agrupadas por condominiumId com soma de quantity
3. Ordenar condominios por quantity desc
4. Greedy assignment: alocar cada condominio ao entregador com menor total atual
5. Retornar { courierId, courierName, condominiums: [{ condominiumId, name, quantity }], total: number }[]
-->

<!-- GET /admin/dashboard — KPIs do AdminPainel:
breadsTodayCount: soma de quantity em Orders com scheduledDate=hoje, status != CANCELLED
revenueToday: soma de amount em Payment com status=PAID e createdAt=hoje
clientsCount: count de Users com role=CLIENT e isBlocked=false
condominiumsCount: count de Condominiums com isActive=true
Tambem retornar: cutoffTime (Setting key=cutoffTime), revenueByType (combos vs avulso hoje)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registrar 9 modulos em server.ts e adicionar cron de corte em cron.ts</name>
  <files>
    apps/api/src/server.ts,
    apps/api/src/plugins/cron.ts
  </files>
  <read_first>
    - apps/api/src/server.ts — ler arquivo completo para ver imports existentes e posicao dos registers
    - apps/api/src/plugins/cron.ts — ler completo para ver 3 crons existentes e onde adicionar o 4o
    - apps/api/src/modules/admin-settings/admin-settings.route.ts — verificar nome do export (criado em 07-02)
    - .planning/phases/07-admin-panel/07-PATTERNS.md — secao server.ts e cron.ts padroes exatos
  </read_first>
  <action>
    server.ts: adicionar 9 imports dos novos modulos apos os imports existentes (usar .js ao final de cada import path — padrao ESM do projeto). Registrar na seguinte ordem apos os registers existentes:
    `await fastify.register(adminSettingsRoute)`, `adminCondominiumsRoute`, `adminCombosRoute`, `adminSuppliersRoute`, `adminCouriersRoute`, `adminClientsRoute`, `adminSupplierOrdersRoute`, `adminFinancialRoute`, `adminPaymentsRoute`.

    cron.ts: ler o arquivo para encontrar o ultimo cron existente. Adicionar APOS ele (antes do fastify.log.info de finalizacao se existir):
    - Import AdminSettingsService no topo do arquivo (importar de '../modules/admin-settings/admin-settings.service.js')
    - Dentro do cronPlugin, apos os outros crons: `const adminSettingsService = new AdminSettingsService(fastify)` e o schedule `'0 * * * *'` com try/catch interno. Guard NODE_ENV=test ja deve estar no inicio do plugin — nao duplicar.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/api 2>&1 | tail -20</automated>
  </verify>
  <done>server.ts tem os 9 imports e registers novos; cron.ts tem o 4o schedule '0 * * * *'; build TypeScript sem erros</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Estender admin-orders com dashboard, delivery-status e division-suggestion</name>
  <files>
    apps/api/src/modules/admin-orders/admin-orders.route.ts,
    apps/api/src/modules/admin-orders/admin-orders.controller.ts,
    apps/api/src/modules/admin-orders/admin-orders.service.ts
  </files>
  <read_first>
    - apps/api/src/modules/admin-orders/admin-orders.route.ts — ler completo para ver rotas existentes
    - apps/api/src/modules/admin-orders/admin-orders.controller.ts — ler completo para ver handlers existentes
    - apps/api/src/modules/admin-orders/admin-orders.service.ts — ler completo para ver metodos existentes
    - apps/api/prisma/schema.prisma — models Order (condominiumId), User (role, isBlocked), Payment, Setting, Condominium
    - .planning/phases/07-admin-panel/07-CONTEXT.md — D-10 (algoritmo hibrido de divisao), D-11 (sugestao on-demand)
  </read_first>
  <behavior>
    GET /admin/dashboard:
    - breadsTodayCount: sum(Order.quantity) WHERE scheduledDate=hoje BRT AND status != CANCELLED
    - revenueToday: sum(Payment.amount) WHERE status=PAID AND createdAt=hoje BRT
    - clientsCount: count(User) WHERE role=CLIENT AND isBlocked=false
    - condominiumsCount: count(Condominium) WHERE isActive=true
    - cutoffTime: Setting key='cutoffTime'.value (ou '20:00' se nao existir)
    - revenueByType: { combos: number, avulso: number } — como ADMF-03 mas para hoje

    GET /admin/orders/delivery-status:
    - Buscar Orders de hoje (scheduledDate=hoje BRT) status != CANCELLED
    - Agrupar por condominiumId: total agendado, total DELIVERED
    - Para cada grupo: buscar nome do Condominium
    - Retornar: [{ condominiumId, condominiumName, scheduled: N, delivered: N }]

    GET /admin/orders/division-suggestion:
    - Buscar entregadores ativos: User WHERE role=COURIER AND isBlocked=false
    - Buscar Orders de amanha BRT agrupadas por condominiumId (sum quantity, nao CANCELLED)
    - Algoritmo greedy: ordenar condominios por quantity desc; para cada um, alocar ao entregador com menor total atual
    - Retornar: [{ courierId, courierName, condominiums: [{ condominiumId, condominiumName, quantity }], total: N }]
  </behavior>
  <action>
    Nos 3 arquivos existentes do modulo admin-orders, ADICIONAR (nao remover ou substituir os existentes):

    admin-orders.service.ts: adicionar metodos getDashboard(), getDeliveryStatus(), getDivisionSuggestion(). Para getDashboard: calcular inicio e fim do dia BRT com `new Date()` ajustado. Para getDivisionSuggestion: implementar o algoritmo greedy descrito acima — sem bibliotecas externas, pura logica em memoria.

    admin-orders.controller.ts: adicionar handlers dashboard(), deliveryStatus(), divisionSuggestion(). Role check ADMIN em todos os 3.

    admin-orders.route.ts: adicionar 3 rotas ANTES das rotas :id para evitar conflito de parametros:
    - `GET /admin/dashboard` (nao dentro de /admin/orders para ser um endpoint separado de facil acesso)
    - `GET /admin/orders/delivery-status`
    - `GET /admin/orders/division-suggestion`
    Todas com preHandler: [fastify.authenticate].
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=@cheirin-de-pao/api 2>&1 | tail -10</automated>
  </verify>
  <done>3 novos endpoints em admin-orders; GET /admin/dashboard retorna 403 sem token; suite API verde; build TypeScript sem erros</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron → AdminSettingsService | Cron executa com privilegios de servidor — falha silenciosa e critica para nao derrubar o servidor |
| Client → /admin/dashboard | KPIs financeiros exigem autenticacao ADMIN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Elevation of Privilege | GET /admin/dashboard, /admin/orders/delivery-status, /admin/orders/division-suggestion | mitigate | Role check ADMIN inline no controller + preHandler authenticate |
| T-07-06-02 | Denial of Service | Cron processCutoff falhando em loop | mitigate | try/catch com fastify.log.error dentro do callback do cron — servidor nao e derrubado por erro no cron |
| T-07-06-03 | Tampering | Algoritmo de divisao manipulado via query params | mitigate | Endpoint e GET sem body; sem params de usuario afetando a logica de balanceamento |
| T-07-06-04 | Information Disclosure | server.ts expondo novos endpoints sem autenticacao | mitigate | preHandler: [fastify.authenticate] em todas as rotas novas — confirmado na verificacao |
| T-07-SC | Tampering | npm installs | accept | Nenhuma instalacao nova — apenas modificacao de arquivos existentes |
</threat_model>

<verification>
1. Build TypeScript: `npm run build --workspace=@cheirin-de-pao/api` sem erros
2. 9 registers em server.ts: `grep -c "fastify.register(admin" apps/api/src/server.ts`
3. Cron de cutoff: `grep "cutoff-check\|processCutoff" apps/api/src/plugins/cron.ts`
4. Dashboard endpoint: `grep "/admin/dashboard" apps/api/src/modules/admin-orders/admin-orders.route.ts`
5. Suite Vitest API verde: `npm run test --workspace=@cheirin-de-pao/api`
</verification>

<success_criteria>
- server.ts registra 9 modulos admin novos
- cron.ts tem 4o cron '0 * * * *' chamando processCutoff()
- admin-orders tem GET /admin/dashboard, /admin/orders/delivery-status, /admin/orders/division-suggestion
- Build TypeScript sem erros
- Suite Vitest API verde
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-06-SUMMARY.md` ao concluir
</output>
