---
phase: 07-admin-panel
plan: 04
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.route.ts
  - apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.controller.ts
  - apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.service.ts
  - apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.schema.ts
  - apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.repository.ts
  - apps/api/src/modules/admin-supplier-orders/pdf-generator.ts
  - apps/api/src/modules/admin-supplier-orders/excel-generator.ts
autonomous: true
requirements: [ADMO-05, ADMO-06, ADMO-07, ADMO-08, ADMO-09]

must_haves:
  truths:
    - "GET /admin/supplier-orders/draft retorna lista de condomínios com totais de pães para o dia seguinte"
    - "POST /admin/supplier-orders cria PurchaseOrder DRAFT + PurchaseOrderItems por fornecedor"
    - "PATCH /admin/supplier-orders/:id/finalize muda status para FINALIZED"
    - "GET /admin/supplier-orders/:id/pdf retorna Buffer Content-Type application/pdf"
    - "GET /admin/supplier-orders/:id/excel retorna Buffer Content-Type vnd.openxmlformats"
    - "GET /admin/supplier-orders retorna histórico de pedidos FINALIZED"
  artifacts:
    - path: "apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.service.ts"
      provides: "getDraft, create, finalize, getHistory, getPdfBuffer, getExcelBuffer"
    - path: "apps/api/src/modules/admin-supplier-orders/pdf-generator.ts"
      provides: "generatePdf(data) retorna Promise<Buffer> via pdfmake v0.3 getBuffer()"
    - path: "apps/api/src/modules/admin-supplier-orders/excel-generator.ts"
      provides: "generateExcel(data) retorna Promise<Buffer> via exceljs writeBuffer()"
  key_links:
    - from: "pdf-generator.ts"
      to: "pdfmake/js/index.js"
      via: "await pdf.getBuffer() — Promise async sem callback"
      pattern: "getBuffer"
    - from: "admin-supplier-orders.service.ts"
      to: "prisma.purchaseOrder + prisma.purchaseOrderItem"
      via: "create usa $transaction para cabeçalho + items atomicamente"
      pattern: "prisma.purchaseOrder.create"
---

<objective>
Implementar o módulo admin-supplier-orders: fluxo completo do pedido ao fornecedor com cálculo de draft (totais por condomínio), criação, finalização, histórico e geração de PDF (pdfmake v0.3) e Excel (exceljs).

Purpose: Cobre ADMO-05..09 — geração do pedido ao fornecedor é o fluxo mais crítico do painel admin. PDF e Excel são dependências do step 3 "Pronto" na UI do AdminPedido.

Output: 7 arquivos TypeScript incluindo os geradores de arquivo binário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-RESEARCH.md
@.planning/phases/07-admin-panel/07-PATTERNS.md

<interfaces>
<!-- PurchaseOrder model (schema existente):
model PurchaseOrder {
  id String @id; date DateTime; totalQuantity Int; cutoffTime DateTime;
  status PurchaseOrderStatus (DRAFT|FINALIZED)
}
-->

<!-- PurchaseOrderItem model:
model PurchaseOrderItem {
  id String @id; purchaseOrderId String @db.ObjectId; supplierId String @db.ObjectId;
  quantity Int; unitPrice Float
}
-->

<!-- pdfmake v0.3 CRITICO — de RESEARCH.md Pattern 2:
import pdfmake from 'pdfmake/js/index.js'   (import path EXATO — nao 'pdfmake')
pdfmake.addFonts({ Helvetica: { normal: 'Helvetica', bold: 'Helvetica-Bold' } })
const pdf = pdfmake.createPdf(docDefinition)
const buffer = await pdf.getBuffer()    (Promise — SEM callback, SEM vfs_fonts.js)
-->

<!-- exceljs — de RESEARCH.md Pattern 3:
import ExcelJS from 'exceljs'
const workbook = new ExcelJS.Workbook()
const sheet = workbook.addWorksheet('Pedido ao Fornecedor')
sheet.columns = [{ header, key, width }]
rows.forEach(r => sheet.addRow(r))
const buffer = await workbook.xlsx.writeBuffer()
-->

<!-- SupplierOrderData type (compartilhado entre pdf e excel generator):
interface SupplierOrderData {
  date: string            // ex: "15/06/2026"
  items: Array<{ supplier: string; quantity: number; unitPrice: number; total: number }>
  grandTotal: number      // total de paes
  grandTotalBrl: string   // ex: "R$ 50,00"
}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geradores de PDF e Excel</name>
  <files>
    apps/api/src/modules/admin-supplier-orders/pdf-generator.ts,
    apps/api/src/modules/admin-supplier-orders/excel-generator.ts
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-RESEARCH.md — Pattern 2 (pdfmake v0.3 completo) e Pattern 3 (exceljs)
    - .planning/phases/07-admin-panel/07-RESEARCH.md — Pitfall 1 (pdfmake v0.2 vs v0.3)
    - apps/api/src/modules/admin-supplier-orders/__tests__/admin-supplier-orders.service.test.ts — stub existente
  </read_first>
  <behavior>
    pdf-generator.ts:
    - Exporta `generatePdf(data: SupplierOrderData): Promise<Buffer>`
    - Retorna Buffer com minimo de 100 bytes para input valido
    - Usa Helvetica built-in (sem vfs_fonts.js)

    excel-generator.ts:
    - Exporta `generateExcel(data: SupplierOrderData): Promise<Buffer>`
    - Retorna Buffer com minimo de 100 bytes para input valido
    - Sheet "Pedido ao Fornecedor" com 4 colunas
  </behavior>
  <action>
    Criar pdf-generator.ts:
    - Import exato: `import pdfmake from 'pdfmake/js/index.js'` (nao 'pdfmake')
    - Export type SupplierOrderData (reutilizado pelo service)
    - Chamar addFonts com Helvetica antes de createPdf
    - docDefinition: defaultStyle font Helvetica, content com titulo "Pedido ao Fornecedor", data, tabela (widths ['*', 'auto', 'auto', 'auto'], body com header e rows dos items + linha de total)
    - `const pdf = pdfmake.createPdf(docDefinition)` seguido de `return pdf.getBuffer()` (sem callback)

    Criar excel-generator.ts:
    - Import: `import ExcelJS from 'exceljs'`
    - new ExcelJS.Workbook(), addWorksheet
    - sheet.columns com 4 colunas: Fornecedor (30), Paes (10), Preco/Pao (12), Total (12)
    - forEach nos items + addRow com { supplier, quantity, unitPrice, total }
    - Row final "TOTAL" com grandTotal e grandTotalBrl
    - `return workbook.xlsx.writeBuffer() as Promise<Buffer>`

    Atualizar stub em admin-supplier-orders.service.test.ts: adicionar `it('generatePdf retorna Buffer maior que 0')` importando generatePdf real e chamando com data de exemplo.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=@cheirin-de-pao/api -- --reporter=verbose 2>&1 | grep -E "(admin-supplier|PASS|FAIL)"</automated>
  </verify>
  <done>Dois arquivos criados; teste de buffer passa verde; gerador PDF retorna Buffer maior que 100 bytes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Modulo admin-supplier-orders (service + route + controller + schema + repository)</name>
  <files>
    apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.route.ts,
    apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.controller.ts,
    apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.service.ts,
    apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.schema.ts,
    apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.repository.ts
  </files>
  <read_first>
    - apps/api/src/modules/admin-orders/admin-orders.route.ts — padrao de rota
    - apps/api/src/modules/admin-orders/admin-orders.controller.ts — padrao de controller com role check
    - apps/api/src/modules/payments/payments.repository.ts — padrao de repository
    - apps/api/prisma/schema.prisma — models Order (condominiumId adicionado), PurchaseOrder, PurchaseOrderItem, Supplier
    - apps/api/src/modules/admin-supplier-orders/pdf-generator.ts — SupplierOrderData type
  </read_first>
  <behavior>
    - GET /admin/supplier-orders/draft: buscar Orders com scheduledDate amanha (BRT: hoje+1 dia inicio/fim), status != CANCELLED; agrupar por condominiumId somando quantity; buscar nome do Condominium para cada id; retornar array `{ condominiumId, condominiumName, quantity }[]` ordenado por nome
    - POST /admin/supplier-orders: body `{ items: [{ supplierId, quantity }], cutoffTime?: string }`. Criar PurchaseOrder (status DRAFT, date=amanha) + PurchaseOrderItems em $transaction. Retornar id do PurchaseOrder criado.
    - PATCH /admin/supplier-orders/:id/finalize: buscar PurchaseOrder por id (404 se nao existir), verificar status=DRAFT (400 se ja FINALIZED), update status para FINALIZED
    - GET /admin/supplier-orders/:id/pdf: buscar PurchaseOrder + PurchaseOrderItems + Suppliers; chamar generatePdf; reply com Content-Type application/pdf + Content-Disposition attachment filename="pedido.pdf"
    - GET /admin/supplier-orders/:id/excel: idem com generateExcel; Content-Type application/vnd.openxmlformats-officedocument.spreadsheetml.sheet + filename="pedido.xlsx"
    - GET /admin/supplier-orders: listar PurchaseOrders com status=FINALIZED, orderBy date desc
  </behavior>
  <action>
    admin-supplier-orders.schema.ts: `CreateSupplierOrderSchema = z.object({ items: z.array(z.object({ supplierId: z.string(), quantity: z.number().int().min(1) })).min(1, 'Pelo menos um item'), cutoffTime: z.string().optional() })`.

    admin-supplier-orders.repository.ts: findById (com items), findHistory (status FINALIZED), create ($transaction), finalize (update status).

    admin-supplier-orders.service.ts: getDraft calcula amanha BRT como `new Date()` ajustado com `+3*3600*1000` para BRT simplificado (ou usar `Intl.DateTimeFormat('pt-BR', { timeZone: 'America/Sao_Paulo' })` para obter data correta). getPdfBuffer e getExcelBuffer buscam PurchaseOrder + items + suppliers e chamam os geradores.

    admin-supplier-orders.controller.ts: handlers getDraft, create, finalize, getHistory, getPdf, getExcel. Para getPdf/getExcel: nao usar reply.status(200).send() — usar reply.header(...).send(buffer) diretamente.

    admin-supplier-orders.route.ts: rotas com preHandler: [fastify.authenticate]. Ordem: GET /admin/supplier-orders/draft (antes de /:id para nao conflitar), POST /admin/supplier-orders, GET /admin/supplier-orders, PATCH /admin/supplier-orders/:id/finalize, GET /admin/supplier-orders/:id/pdf, GET /admin/supplier-orders/:id/excel.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=@cheirin-de-pao/api 2>&1 | tail -10</automated>
  </verify>
  <done>5 arquivos criados; suite Vitest API verde; GET /admin/supplier-orders retorna 403 sem token; modulo admin-supplier-orders completo com 7 arquivos totais</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Apenas ADMIN pode acessar supplier-orders e baixar PDF/Excel |
| API → pdfmake/exceljs | Geracao de arquivo server-side com dados do banco — sem input do usuario no conteudo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Elevation of Privilege | GET /admin/supplier-orders/:id/pdf e /excel | mitigate | preHandler: [fastify.authenticate] + role check ADMIN no controller antes de gerar arquivo |
| T-07-04-02 | Information Disclosure | Download de PDF/Excel sem autenticacao | mitigate | Endpoints protegidos por authenticate plugin — sem token = 401 |
| T-07-04-03 | Tampering | POST /admin/supplier-orders com supplierId invalido | mitigate | Zod valida array de items; service verifica Supplier existe via findUnique (404 se nao existir) |
| T-07-04-04 | Tampering | PATCH /admin/supplier-orders/:id/finalize duplicado | mitigate | Service verifica status=DRAFT antes de finalizar — retorna 400 se ja FINALIZED |
| T-07-SC | Tampering | npm installs | accept | pdfmake e exceljs ja instalados no projeto (RESEARCH.md verificado) |
</threat_model>

<verification>
1. Suite Vitest API verde: `npm run test --workspace=@cheirin-de-pao/api`
2. Modulo existe: `ls apps/api/src/modules/admin-supplier-orders/`
3. Geradores exportados: `grep -l "generatePdf\|generateExcel" apps/api/src/modules/admin-supplier-orders/`
4. Role check: `grep -c "role !== 'ADMIN'" apps/api/src/modules/admin-supplier-orders/admin-supplier-orders.controller.ts`
</verification>

<success_criteria>
- pdf-generator.ts e excel-generator.ts exportam funcoes que retornam Buffer
- admin-supplier-orders tem endpoints GET draft, POST create, PATCH finalize, GET pdf, GET excel, GET history
- Todos os endpoints retornam 403 sem autenticacao ADMIN
- Suite Vitest API verde
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-04-SUMMARY.md` ao concluir
</output>
