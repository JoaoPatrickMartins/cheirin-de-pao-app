---
phase: 07-admin-panel
plan: 10
type: execute
wave: 2
depends_on: ["07-07"]
files_modified:
  - apps/web/src/pages/admin/tabs/AdminClientes.tsx
  - apps/web/src/components/admin/ClientDetailView.tsx
autonomous: true
requirements: [ADMG-08, ADMG-09, ADMG-10, ADMG-11]

must_haves:
  truths:
    - "AdminClientes exibe lista de clientes com saldo de creditos, condominio e apartamento"
    - "Chips de filtro horizontais exibem Todos + nomes dos condominios para filtrar clientes"
    - "Ao selecionar um cliente, exibe sub-tela ClientDetailView com historico somente leitura (D-02)"
    - "Botao Bloquear/Desbloquear muda isBlocked do cliente via PATCH /admin/clients/:id/block"
    - "Card bloqueado tem opacity 0.6 e icone ban"
  artifacts:
    - path: "apps/web/src/pages/admin/tabs/AdminClientes.tsx"
      provides: "Lista com chips de filtro, navegacao para detalhe (sub=detalhe D-02)"
    - path: "apps/web/src/components/admin/ClientDetailView.tsx"
      provides: "Detalhe somente leitura: saldo, ultima compra, agendamento + toggle bloquear"
  key_links:
    - from: "AdminClientes.tsx chips"
      to: "GET /admin/clients?condominiumId="
      via: "filtroCondominio state atualiza query param no apiFetch"
      pattern: "condominiumId"
    - from: "ClientDetailView.tsx botao bloquear"
      to: "PATCH /admin/clients/:id/block"
      via: "apiFetch com method PATCH + dialog de confirmacao antes"
      pattern: "clients.*block"
---

<objective>
Implementar AdminClientes (lista com filtro por condominio via chips + sub-tela de detalhe via stack de estado D-02) e ClientDetailView (detalhe somente leitura com historico e toggle de bloqueio).

Purpose: Cobre ADMG-08..11 — gestao de clientes e o fluxo de bloquear/desbloquear.

Output: 2 arquivos React/TypeScript; navegacao via sub state interno (D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-UI-SPEC.md
@.planning/phases/07-admin-panel/07-PATTERNS.md

<interfaces>
<!-- Sub-estado de AdminClientes (D-02, de PATTERNS.md):
type AdminClientesSub = null | 'detalhe'
const [sub, setSub] = useState<AdminClientesSub>(null)
const [selectedId, setSelectedId] = useState<string | null>(null)
if (sub === 'detalhe' && selectedId) {
  return <ClientDetailView clienteId={selectedId} onBack={() => { setSub(null); setSelectedId(null) }} />
}
-->

<!-- GET /admin/clients response shape:
[{ id, name, condominiumId, apartment, block, creditBalance, isBlocked, createdAt, lastPurchaseAt }]
GET /admin/clients?condominiumId={id}  -- filtro por condominio
-->

<!-- GET /admin/clients/:id response shape:
{ id, name, cpf?, phone, email, condominiumId, apartment, block, creditBalance, isBlocked,
  schedule: { weeklyQty: Record<string,number>, deliveryTime, isActive } | null,
  recentOrders: [{ scheduledDate, quantity, status }] }
-->

<!-- GET /admin/condominiums response shape (para chips de filtro):
[{ id, name, type, isActive }]
-->

<!-- Chips de filtro de UI-SPEC.md:
display: flex, gap: 8px, overflowX: auto, paddingBottom: 4px
Cada chip: padding '8px 14px', borderRadius 999px, fontSize 12.5px, Hanken weight=700, whiteSpace: nowrap
Ativo: border '1.5px solid --color-accent', background '--color-gold-soft', color '--color-accent'
Inativo: border '1.5px solid --color-border', background '--color-surface', color '--color-text-sec'
-->

<!-- PATCH /admin/clients/:id/block:
method PATCH, sem body
Resposta: { id, isBlocked: boolean }
Dialogo de confirmacao antes: "Bloquear {nome}?" / "O cliente nao podera fazer pedidos."
-->

<!-- ClientDetailView rows de UI-SPEC.md:
AppBar title='Cliente' onBack={onBack}
Card: avatar user 64px circular surface2, nome Bricolage 20px, condo+apto 13px textSec, Pill 'Bloqueado' se bloqueado
Row wallet 'Saldo de creditos'
Row clock 'Ultima compra' -- formatada em pt-BR
Row calendar 'Agendamento' -- resumo do weeklyQty ou 'Sem agendamento'
Nota: 12px textTer 'O admin apenas visualiza os dados do cliente -- nao edita o cadastro.'
Btn ghost icon=ban 'Bloquear cliente' (ou variant gold icon=check 'Desbloquear cliente')
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminClientes (lista com chips de filtro e navegacao para detalhe)</name>
  <files>
    apps/web/src/pages/admin/tabs/AdminClientes.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao completa Aba Clientes (lista e chips)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Interaction Contracts: Bloquear/Desbloquear cliente
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Copywriting Contract (secao Clientes)
    - .planning/phases/07-admin-panel/07-PATTERNS.md — secao Telas com Sub-estado (padrao AdminClientesSub)
    - apps/web/src/pages/courier/CourierScreen.tsx — padrao de useEffect + apiFetch + lista
    - apps/web/src/components/admin/AdminHead.tsx — reutilizar para o cabecalho
  </read_first>
  <action>
    Criar apps/web/src/pages/admin/tabs/AdminClientes.tsx.

    Estados: `sub: AdminClientesSub = null`, `selectedId: string | null`, `filtroCondominio: string | null`, `clientes: Cliente[]`, `condominios: Condo[]`, `isLoading: boolean`.

    Ao montar: buscar simultaneamente GET /admin/clients e GET /admin/condominiums (para popular chips). Ao trocar filtroCondominio: re-buscar GET /admin/clients?condominiumId={id}.

    Condicao de renderizacao: se `sub === 'detalhe' && selectedId`, renderizar ClientDetailView com clienteId e onBack. Caso contrario, renderizar a lista.

    Tela de lista:
    - AdminHead titulo="Clientes" sub="{clientes.length} cadastrados"
    - Chips: div overflowX auto, gap 8; chip "Todos" (key=null) + um chip por condominio. Chip ativo com borda accent + gold-soft. onClick: setFiltroCondominio(id) ou null para Todos.
    - Lista de cards (Card pad=14 cursor=pointer onClick={() => { setSelectedId(c.id); setSub('detalhe') }}):
      - opacity: c.isBlocked ? 0.6 : 1
      - Avatar user 44x44 surface2 borderRadius=12
      - Nome 14.5px weight=700 + Icon ban size=14 cor=accent (se bloqueado)
      - Linha secundaria: "{condominioName} . {apto} . ult. {data}" -- 12px textTer truncado
      - Coluna direita: creditBalance Bricolage 17px weight=800 (textTer se 0, text se >0) + label "creditos" 10.5px textTer
    - Empty state quando lista vazia: "Nenhum cliente encontrado" + "Tente filtrar por outro condominio."
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | grep -E "(AdminClientes|error|Error)" | head -5</automated>
  </verify>
  <done>AdminClientes.tsx criado; build sem erros; chip de filtro muda o param da query; sub='detalhe' ativado ao clicar no card</done>
</task>

<task type="auto">
  <name>Task 2: ClientDetailView (detalhe somente leitura + toggle bloquear)</name>
  <files>
    apps/web/src/components/admin/ClientDetailView.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao Sub-tela de detalhe do cliente (spec completa)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Interaction Contracts: Bloquear/Desbloquear
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Copywriting Contract (secao Clientes -- dialogs)
    - apps/web/src/components/brand/Icon.tsx -- icones wallet, clock, calendar, ban, check
    - apps/web/src/pages/client/CombosScreen.tsx -- padrao handleSubmit com loading + error (linhas 67-113)
    - apps/api/src/modules/admin-clients/admin-clients.route.ts -- confirmar endpoint PATCH /admin/clients/:id/block
  </read_first>
  <action>
    Criar apps/web/src/components/admin/ClientDetailView.tsx.

    Props: `{ clienteId: string; onBack: () => void }`.

    Estados: `cliente: ClienteDetalhe | null`, `isLoading: boolean`, `isBlocking: boolean`, `showDialog: boolean`.

    Ao montar: buscar GET /admin/clients/{clienteId}. Interface ClienteDetalhe: `{ id, name, phone, email, condominiumId, apartment, creditBalance, isBlocked, schedule: {...} | null, recentOrders: [...] }`.

    Render:
    - AppBar title="Cliente" onBack={onBack} (primitiva existente do projeto)
    - Card de avatar: user 64px circular surface2; nome Bricolage 20px; condo+apto 13px textSec; Pill tone="gold" "Bloqueado" se isBlocked
    - Card de dados (Row components):
      - Row icon wallet: label "Saldo de creditos", value "{creditBalance} paes"
      - Row icon clock: label "Ultima compra", value formatado ou "Sem compras"
      - Row icon calendar: label "Agendamento", value resumo do weeklyQty (ex: "Seg, Qua, Sex — 2 paes") ou "Sem agendamento"
    - Separadores entre rows: height=1px background='var(--color-border-2)'
    - Nota: "O admin apenas visualiza os dados do cliente — nao edita o cadastro." -- 12px textTer
    - Btn ghost full icon=ban "Bloquear cliente" (ou Btn gold full icon=check "Desbloquear cliente")

    Dialog de confirmacao: renderizar como div overlay position=fixed inset=0 background='rgba(0,0,0,0.4)' role="dialog" aria-modal="true" quando showDialog=true. Conteudo: "Bloquear {nome}?" + corpo + botoes Cancelar (ghost) e Confirmar (primary ou gold). Ao confirmar: chamar PATCH /admin/clients/:id/block, atualizar cliente.isBlocked localmente, fechar dialog.

    Tratamento de erro no block: exibir mensagem inline abaixo do botao: "Nao foi possivel alterar. Tente novamente."
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | tail -15</automated>
  </verify>
  <done>ClientDetailView criado; dialog com role=dialog aria-modal=true; toggle bloquear chama PATCH corretamente; build frontend sem erros</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> PATCH /admin/clients/:id/block | Apenas ADMIN autenticado pode bloquear clientes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Elevation of Privilege | PATCH /admin/clients/:id/block | mitigate | apiFetch inclui JWT; servidor verifica role=ADMIN + User.role=CLIENT antes de bloquear |
| T-07-10-02 | Tampering | Filtro por condominiumId via querystring | mitigate | condominiumId e ObjectId do MongoDB -- servidor valida formato; nao e input livre |
| T-07-10-03 | Tampering | Bloqueio acidental sem confirmacao | mitigate | Dialog de confirmacao com role=dialog aria-modal antes de chamar API -- usuario ve "Bloquear {nome}?" explicitamente |
| T-07-10-04 | Information Disclosure | Detalhe do cliente expoe phone/email | mitigate | Endpoint GET /admin/clients/:id exige role=ADMIN; dados encriptados em transit via HTTPS |
| T-07-SC | Tampering | npm installs | accept | Nenhuma instalacao nova neste plano |
</threat_model>

<verification>
1. Build frontend sem erros: `npm run build --workspace=@cheirin-de-pao/web`
2. Sub-estado em AdminClientes: `grep "setSub.*detalhe\|sub.*detalhe" apps/web/src/pages/admin/tabs/AdminClientes.tsx`
3. Chips de filtro: `grep "filtroCondominio\|condominiumId" apps/web/src/pages/admin/tabs/AdminClientes.tsx`
4. Dialog aria-modal: `grep "aria-modal" apps/web/src/components/admin/ClientDetailView.tsx`
5. Toggle bloquear: `grep "clients.*block\|block.*clients" apps/web/src/components/admin/ClientDetailView.tsx`
</verification>

<success_criteria>
- AdminClientes exibe lista com chips de filtro por condominio (Todos + por nome)
- Ao clicar num cliente, sub='detalhe' renderiza ClientDetailView sem trocar de rota (D-02)
- ClientDetailView exibe saldo, ultima compra, agendamento somente leitura
- Botao Bloquear mostra dialog de confirmacao antes de chamar API
- Apos bloqueio: isBlocked atualizado localmente; card fica opacity=0.6 na lista
- Build frontend sem erros TypeScript
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-10-SUMMARY.md` ao concluir
</output>
