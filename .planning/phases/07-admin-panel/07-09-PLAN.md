---
phase: 07-admin-panel
plan: 09
type: execute
wave: 2
depends_on: ["07-07"]
files_modified:
  - apps/web/src/pages/admin/tabs/AdminEntregas.tsx
  - apps/web/src/components/admin/SegmentedControl.tsx
  - apps/web/src/components/admin/DeliveryDivisionCard.tsx
autonomous: true
requirements: [ADMO-04, ADMO-10, ADMO-11]

must_haves:
  truths:
    - "AdminEntregas exibe SegmentedControl com abas Hoje e Historico"
    - "Aba Hoje exibe card Divisao sugerida com drag-and-drop de condominios entre entregadores"
    - "Botao Aprovar divisao dispara PATCH /admin/orders/assign-courier em batch e muda card para verde/Aprovada"
    - "Aba Hoje exibe secao Agendadas vs Realizadas com ProgressBar por condominio"
    - "Drag-and-drop funciona em mobile (TouchSensor delay=250ms tolerance=5)"
  artifacts:
    - path: "apps/web/src/pages/admin/tabs/AdminEntregas.tsx"
      provides: "Aba Entregas com Hoje/Historico, divisao sugerida, progresso por condominio"
    - path: "apps/web/src/components/admin/SegmentedControl.tsx"
      provides: "Componente reutilizavel generico para tabs segmentadas"
    - path: "apps/web/src/components/admin/DeliveryDivisionCard.tsx"
      provides: "Card de divisao de entregadores com dnd-kit drag-and-drop"
  key_links:
    - from: "DeliveryDivisionCard.tsx"
      to: "@dnd-kit/core DndContext + @dnd-kit/sortable SortableContext"
      via: "useDraggable/useSortable para condomínios; onDragEnd move condo entre arrays de entregadores"
      pattern: "DndContext.*onDragEnd"
    - from: "AdminEntregas.tsx botao Aprovar"
      to: "PATCH /admin/orders/assign-courier"
      via: "apiFetch batch por entregador: [{ courierId, orderIds }]"
      pattern: "assign-courier"
---

<objective>
Implementar AdminEntregas: aba com SegmentedControl Hoje/Historico, card de divisao sugerida com drag-and-drop (@dnd-kit), botao de aprovacao em batch e secao de progresso por condominio.

Purpose: Cobre ADMO-04, ADMO-10, ADMO-11 — divisao de entregadores e dashboard de entregas sao funcionalidades unicas do admin sem analog em outras fases.

Output: 3 arquivos React/TypeScript; drag-and-drop com TouchSensor para PWA mobile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-UI-SPEC.md
@.planning/phases/07-admin-panel/07-PATTERNS.md
@.planning/phases/07-admin-panel/07-RESEARCH.md

<interfaces>
<!-- @dnd-kit padrao de RESEARCH.md Pattern 5:
import { DndContext, PointerSensor, TouchSensor, useSensor, useSensors, DragOverlay } from '@dnd-kit/core'
import { SortableContext, useSortable, verticalListSortingStrategy } from '@dnd-kit/sortable'

const sensors = useSensors(
  useSensor(PointerSensor, { activationConstraint: { distance: 8 } }),
  useSensor(TouchSensor, { activationConstraint: { delay: 250, tolerance: 5 } })
)

// Componente arrastavel:
const { attributes, listeners, setNodeRef, transform, isDragging } = useSortable({ id: condo.id })
<div ref={setNodeRef} style={{ opacity: isDragging ? 0.5 : 1, transform: CSS.Transform.toString(transform) }} {...attributes} {...listeners}>
-->

<!-- Estado de divisao:
type Assignment = { courierId: string; courierName: string; condos: CondoItem[] }
interface CondoItem { condominiumId: string; condominiumName: string; quantity: number }

onDragEnd({ active, over }):
  se over.id == courierId diferente do ativo: mover condo do array do entregador atual para o outro
  atualizar totais em tempo real
-->

<!-- PATCH /admin/orders/assign-courier (endpoint existente da Fase 6):
Body: { courierId: string; orderIds: string[] }
Chamar uma vez por entregador que tem condominios atribuidos
Para obter orderIds por condominio: GET /admin/orders/delivery-status retorna { condominiumId, ordersIds[] }
OU: GET /admin/supplier-orders/draft retorna condominiumId -> buscar orderIds separadamente
Simplificacao MVP: ao aprovar, buscar Orders do dia seguinte por condominiumId=condominoIds do entregador
-->

<!-- SegmentedControl de PATTERNS.md (adapter generico):
interface Tab<T extends string> { key: T; label: string }
interface SegmentedControlProps<T extends string> { tabs: Tab<T>[]; value: T; onChange: (v: T) => void }
Altura minima 44px, background surface2, tab ativa: surface + shadowSoft
-->

<!-- Estrutura de dados de delivery-status (de admin-orders GET /admin/orders/delivery-status):
[{ condominiumId, condominiumName, scheduled: N, delivered: N, orderIds: string[] }]
-->

<!-- DeliveryDivisionCard props:
interface DeliveryDivisionCardProps {
  assignments: Assignment[]
  onAssignmentsChange: (next: Assignment[]) => void
  onApprove: () => Promise<void>
  isApproved: boolean
  isApproving: boolean
}
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SegmentedControl (generico reutilizavel)</name>
  <files>
    apps/web/src/components/admin/SegmentedControl.tsx
  </files>
  <read_first>
    - apps/web/src/components/courier/SegmentedControl.tsx — analog exato (copiar e adaptar com generic)
    - .planning/phases/07-admin-panel/07-PATTERNS.md — secao SegmentedControl admin (padrao generico com generic T)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — SegmentedControl Hoje/Historico spec visual
  </read_first>
  <action>
    Criar apps/web/src/components/admin/SegmentedControl.tsx.

    Diferenca do analog: usar generic `<T extends string>` para suportar multiplos usos (Hoje/Historico na AdminEntregas e Dia/Semana/Mes no AdminFinanceiro).

    Props: `{ tabs: Array<{ key: T; label: string }>; value: T; onChange: (v: T) => void }`.

    Estilos identicos ao SegmentedControl do courier: background surface2, borderRadius 13px, padding 4, gap 4. Tab ativa: surface + borderRadius 10px + shadowSoft. Tab inativa: transparent. Altura minima 44px. Font 13.5px Hanken weight=700.

    Exportar como funcao generica: `export function SegmentedControl<T extends string>({ tabs, value, onChange }: SegmentedControlProps<T>)`.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | grep -E "(SegmentedControl|error|Error)" | head -5</automated>
  </verify>
  <done>SegmentedControl.tsx criado como generico; build sem erros TypeScript</done>
</task>

<task type="auto">
  <name>Task 2: DeliveryDivisionCard (dnd-kit) e AdminEntregas (tela completa)</name>
  <files>
    apps/web/src/components/admin/DeliveryDivisionCard.tsx,
    apps/web/src/pages/admin/tabs/AdminEntregas.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-RESEARCH.md — Pattern 5 (dnd-kit completo: DndContext, useSortable, TouchSensor)
    - .planning/phases/07-admin-panel/07-RESEARCH.md — Pitfall 3 (scroll vs drag em mobile)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao Aba Entregas (Hoje, Historico, Divisao sugerida, Agendadas vs Realizadas)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Interaction Contracts (Aprovar divisao D-12, Drag-and-drop D-11)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Copywriting Contract (secao Entregas)
    - apps/web/src/pages/admin/tabs/AdminPainel.tsx — padrao de useEffect + apiFetch + isLoading
    - apps/web/src/components/admin/ProgressBar.tsx — reutilizar em cada condominio
    - apps/api/src/modules/admin-orders/admin-orders.route.ts — PATCH /admin/orders/assign-courier (Fase 6, ja existe)
  </read_first>
  <action>
    Verificar se @dnd-kit/core e @dnd-kit/sortable estao em apps/web/package.json. Se nao estiverem, instalar:
    `npm install @dnd-kit/core @dnd-kit/sortable --workspace=@cheirin-de-pao/web`

    DeliveryDivisionCard.tsx:
    - Import DndContext, PointerSensor, TouchSensor, useSensor, useSensors de '@dnd-kit/core'
    - Import SortableContext, useSortable, verticalListSortingStrategy de '@dnd-kit/sortable'
    - Import CSS de '@dnd-kit/utilities'
    - sensors com PointerSensor (distance: 8) + TouchSensor (delay: 250, tolerance: 5) — CRITICO para PWA mobile
    - Funcao SortableCondo: useSortable({ id: condo.condominiumId }); div com setNodeRef, transform, isDragging
    - DndContext onDragEnd: identificar qual entregador tem o condo ativo e qual e o over; mover condo entre arrays; recalcular totais
    - Visual do card conforme UI-SPEC: borda 1.5px accent quando nao aprovado, borda border2 quando aprovado; spark icon + "Divisao sugerida"; por entregador: avatar user 36px + nome + lista condos + total paes Bricolage 15px
    - Botao "Aprovar divisao": variant gold, full, size sm, icon check; disabled quando isApproving
    - Pill "Aprovada" com icon check: visivel apenas quando isApproved

    AdminEntregas.tsx:
    - AdminHead titulo="Entregas" sub="Controle do dia"
    - SegmentedControl tabs=[{key:'hoje', label:'Hoje'}, {key:'historico', label:'Historico'}] value={segment}
    - Aba Hoje:
      - Buscar GET /admin/orders/division-suggestion no useEffect -> initializar assignments
      - Buscar GET /admin/orders/delivery-status -> lista de progresso por condominio
      - DeliveryDivisionCard com assignments, onAssignmentsChange, onApprove, isApproved, isApproving
      - onApprove: para cada entregador com condos, buscar orderIds via delivery-status; chamar apiFetch('/admin/orders/assign-courier', { method: 'PATCH', body: { courierId, orderIds } }); setIsApproved(true)
      - Secao "AGENDADAS VS REALIZADAS": lista de cards por condominio com nome, pill parcial/completo, ProgressBar
    - Aba Historico: lista de PurchaseOrders finalizados (GET /admin/supplier-orders) — cards com data, totais, pill status

    Textos exatos: usar Copywriting Contract da UI-SPEC.md (secao Entregas).
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | tail -15</automated>
  </verify>
  <done>DeliveryDivisionCard com dnd-kit criado; AdminEntregas completo; build frontend sem erros; TouchSensor configurado com delay=250</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> PATCH /admin/orders/assign-courier | Apenas ADMIN autenticado pode aprovar divisao de entregadores |
| dnd-kit drag handlers | Apenas reordenacao local — sem dados enviados ao servidor ate aprovacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Elevation of Privilege | PATCH /admin/orders/assign-courier sem autenticacao | mitigate | apiFetch inclui JWT; servidor verifica role=ADMIN; 403 sem token |
| T-07-09-02 | Tampering | Drag-and-drop envia dados invalidos | mitigate | Ids de condominio e entregador vem do servidor via GET division-suggestion; sem input textual do usuario |
| T-07-09-03 | Denial of Service | dnd-kit ativando em scroll mobile | mitigate | TouchSensor delay=250ms tolerance=5 previne drag acidental em scroll (RESEARCH.md Pitfall 3) |
| T-07-09-04 | Tampering | Aprovacao duplicada (botao clicado duas vezes) | mitigate | isApproving state desabilita o botao durante a chamada; isApproved oculta o botao apos sucesso |
| T-07-SC | Tampering | npm install @dnd-kit | mitigate | Pacotes verificados em Package Legitimacy Audit (RESEARCH.md) — [OK] sem flags; instalar apenas se nao existirem |
</threat_model>

<verification>
1. Build frontend sem erros: `npm run build --workspace=@cheirin-de-pao/web`
2. dnd-kit instalado: `grep "@dnd-kit" apps/web/package.json`
3. TouchSensor delay: `grep "delay: 250" apps/web/src/components/admin/DeliveryDivisionCard.tsx`
4. Aprovacao em batch: `grep "assign-courier" apps/web/src/pages/admin/tabs/AdminEntregas.tsx`
5. SegmentedControl generico: `grep "extends string" apps/web/src/components/admin/SegmentedControl.tsx`
</verification>

<success_criteria>
- SegmentedControl generico reutilizavel (Hoje/Historico, Dia/Semana/Mes)
- DeliveryDivisionCard com drag-and-drop de condominios entre entregadores; TouchSensor configurado
- AdminEntregas: aba Hoje com divisao sugerida + progresso por condominio; aba Historico com lista de pedidos
- Aprovacao dispara PATCH assign-courier em batch; card muda de border accent para border2 com pill "Aprovada"
- Build frontend sem erros TypeScript
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-09-SUMMARY.md` ao concluir
</output>
