---
phase: 07-admin-panel
plan: 08
type: execute
wave: 2
depends_on: ["07-06", "07-07"]
files_modified:
  - apps/web/src/pages/admin/tabs/AdminPedido.tsx
  - apps/web/src/components/admin/StepBar.tsx
autonomous: true
requirements: [ADMO-05, ADMO-06, ADMO-07, ADMO-08, ADMO-09]

must_haves:
  truths:
    - "AdminPedido exibe StepBar com 4 steps (Conferir/Ajustar/Dividir/Pronto)"
    - "Step 0 lista condominios com totais via GET /admin/supplier-orders/draft"
    - "Step 1 permite ajustar quantidades via Stepper por condominio"
    - "Step 2 divide entre fornecedor principal (75%) e reserva (25%) com steppers acoplados"
    - "Step 3 exibe resumo, botao PDF e botao Excel que disparam download"
    - "Confirmar no step 2 cria PurchaseOrder via POST /admin/supplier-orders e avanca para step 3"
    - "Botao PDF chama GET /admin/supplier-orders/:id/pdf e faz download do arquivo"
  artifacts:
    - path: "apps/web/src/pages/admin/tabs/AdminPedido.tsx"
      provides: "Fluxo de 4 etapas: Conferir -> Ajustar -> Dividir -> Pronto"
    - path: "apps/web/src/components/admin/StepBar.tsx"
      provides: "Barra de 4 steps com preenchimento dourado"
  key_links:
    - from: "AdminPedido.tsx step 0"
      to: "GET /admin/supplier-orders/draft"
      via: "apiFetch('/admin/supplier-orders/draft') no useEffect ao montar"
      pattern: "supplier-orders/draft"
    - from: "AdminPedido.tsx step 2 confirmar"
      to: "POST /admin/supplier-orders"
      via: "apiFetch('/admin/supplier-orders', { method: 'POST', body: JSON.stringify({ items }) })"
      pattern: "supplier-orders.*POST"
    - from: "AdminPedido.tsx step 3 PDF"
      to: "GET /admin/supplier-orders/:id/pdf"
      via: "window.open ou fetch + createObjectURL para download do arquivo"
      pattern: "supplier-orders.*pdf"
---

<objective>
Implementar AdminPedido (fluxo completo de 4 etapas para pedido ao fornecedor) e StepBar (barra de progresso de 4 steps com preenchimento dourado).

Purpose: Cobre ADMO-05..09 no frontend — o fluxo mais extenso da UI admin com 4 steps distintos, steppers acoplados e download de arquivo.

Output: 2 arquivos React/TypeScript; fluxo Conferir->Ajustar->Dividir->Pronto funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-UI-SPEC.md
@.planning/phases/07-admin-panel/07-PATTERNS.md

<interfaces>
<!-- StepBar de UI-SPEC.md:
display: flex, gap: 6px, padding: '0 20px 14px'
4 steps: 'Conferir' | 'Ajustar' | 'Dividir' | 'Pronto'
Cada step: flex:1, textAlign: center
  Barra acima: height 4px borderRadius 99px
    Ativo/concluido (i <= step): background --color-gold (#E3AC3F)
    Pendente: background --color-border
  Label: 11px Hanken weight=700
    Ativo/concluido: cor accent (#B0702A)
    Pendente: cor textTer (#A89A82)
Admin pode clicar em step anterior para voltar (D-09)
Props: { step: 0|1|2|3; onStepClick: (step: number) => void }
-->

<!-- Primitive Stepper existente no projeto:
apps/web/src/components/brand/ tem primitivas Card, Pill, Btn, Field, Stepper, Switch, Row, AppBar
Usar Stepper existente para quantidades -- nao recriar
-->

<!-- Steppers acoplados no step 2 (UI-SPEC.md):
ao avancer de step 1 para 2: setSplit({ p: Math.round(total * 0.75), r: total - Math.round(total * 0.75) })
os dois steppers devem somar sempre 'total'
onChange do p: setSplit({ p: newP, r: total - newP })
onChange do r: setSplit({ p: total - newR, r: newR })
min=0 para ambos
-->

<!-- Download de arquivo (sem dependencia extra):
const res = await apiFetch(`/admin/supplier-orders/${orderId}/pdf`)
const blob = await res.blob()
const url = URL.createObjectURL(blob)
const a = document.createElement('a')
a.href = url
a.download = 'pedido.pdf'
a.click()
URL.revokeObjectURL(url)
-->

<!-- Suppliers para step 2 (buscar ao entrar no step):
const res = await apiFetch('/admin/suppliers')
const suppliers = await res.json()  -- Array<{ id, name, pricePerUnit, isPrincipal }>
Exibir: principal = suppliers.find(s => s.isPrincipal), reserva = primeiro nao-principal
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StepBar (componente de barra de progresso de 4 etapas)</name>
  <files>
    apps/web/src/components/admin/StepBar.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao StepBar (especificacao completa)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Animations: StepBar preenchimento de barra e instantaneo
    - apps/web/src/components/courier/SegmentedControl.tsx — referencia de padrao de componente UI com estado ativo/inativo
  </read_first>
  <action>
    Criar apps/web/src/components/admin/StepBar.tsx.

    Props: `{ step: 0 | 1 | 2 | 3; onStepClick: (i: number) => void }`.

    Constante STEP_LABELS: `['Conferir', 'Ajustar', 'Dividir', 'Pronto']`.

    Para cada label (indice i de 0 a 3):
    - isActive = i === step; isConcluded = i < step; isClickable = i < step (pode voltar para step anterior)
    - div wrapper com flex:1 e textAlign:center; cursor='pointer' quando isClickable; onClick={() => i < step && onStepClick(i)
    - Barra topo: height 4px borderRadius 99px marginBottom 6px; background: i <= step ? '#E3AC3F' : 'var(--color-border)'
    - Label: 11px Hanken weight=700; color: i <= step ? 'var(--color-accent)' : 'var(--color-text-ter)'
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | grep -E "(StepBar|error|Error)" | head -5</automated>
  </verify>
  <done>StepBar.tsx criado; build frontend sem erros TypeScript; props tipadas corretamente</done>
</task>

<task type="auto">
  <name>Task 2: AdminPedido (fluxo 4 etapas completo com download PDF/Excel)</name>
  <files>
    apps/web/src/pages/admin/tabs/AdminPedido.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao completa Aba Pedido (Steps 0, 1, 2, 3 spec visual)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Interaction Contracts (Fluxo Pedido ao Fornecedor D-09)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — Copywriting Contract (textos do Pedido)
    - apps/web/src/pages/courier/CourierScreen.tsx — padrao de useEffect + apiFetch + estado loading
    - .planning/phases/07-admin-panel/07-CONTEXT.md — D-07 (PDF/Excel via backend), D-08 (PurchaseOrder no historico), D-09 (navegacao livre entre steps)
    - apps/web/src/components/admin/StepBar.tsx — recem criado
  </read_first>
  <action>
    Criar apps/web/src/pages/admin/tabs/AdminPedido.tsx.

    Estados necessarios:
    - `step: 0 | 1 | 2 | 3` — etapa atual
    - `draftData: CondoDraft[] | null` — dados do draft (condominios + totais)
    - `adjustedQts: Record<string, number>` — quantidades ajustadas por condominiumId (step 1)
    - `suppliers: Supplier[] | null` — lista de fornecedores (carregada ao entrar no step 2)
    - `split: { p: number; r: number }` — divisao entre principal e reserva (step 2)
    - `orderId: string | null` — id do PurchaseOrder criado apos confirmar step 2
    - `isLoading: boolean; isCreating: boolean; isDownloading: 'pdf' | 'excel' | null`

    Ao montar: buscar GET /admin/supplier-orders/draft + GET /admin/settings/cutoff (para exibir horario no card do step 0).

    Step 0 (Conferir): AdminHead titulo="Pedido ao fornecedor" sub="Para amanha"; StepBar step=0; card de horario de corte (scissors 44px gold-soft + titulo "Horario de corte HH:MM" + pill "Aberto"/"Encerrado"); label "CONSOLIDADO POR CONDOMINIO"; lista de cards (building 42px surface2, nome, N entregas, total Bricolage 18px); rodape fixo: "Total necessario" + valor Bricolage 22px + Btn full size="lg" icon="scissors" "Encerrar corte e gerar pedido" que avanca para step 1 e inicializa adjustedQts com os valores do draft.

    Step 1 (Ajustar): instrucao texto; lista de cards com nome do condo + "base N paes" + Stepper value={adjustedQts[id]} onChange={(v) => setAdjustedQts({...prev, [id]: v})} min=0 max=400; rodape "Total ajustado" + soma total + Btn "Escolher fornecedores" (ao clicar: buscar GET /admin/suppliers + calcular split 75/25 + avancar para step 2).

    Step 2 (Dividir): instrucao; card Fornecedor Principal com Stepper value={split.p} onChange={(v) => setSplit({ p: v, r: total-v })}; card Fornecedor Reserva com Stepper value={split.r} onChange={(v) => setSplit({ p: total-v, r: v })}; rodape + Btn "Finalizar pedido" (ao clicar: POST /admin/supplier-orders com items=[{supplierId: principalId, quantity: split.p}, {supplierId: reservaId, quantity: split.r}], salvar orderId, avancar para step 3).

    Step 3 (Pronto): icone check 72px good-soft; titulo "Pedido gerado" Bricolage 22px; subtitulo "Salvo no historico"; card de resumo com linhas por fornecedor + total; botoes PDF e Excel (chamar apiFetch, blob(), URL.createObjectURL, a.click()); Btn ghost "Voltar ao inicio" (reseta step=0, orderId=null).

    Textos exatos: usar Copywriting Contract da UI-SPEC.md secao Pedido ao fornecedor.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | tail -15</automated>
  </verify>
  <done>AdminPedido.tsx criado com 4 steps; build frontend sem erros; StepBar integrado; download de PDF/Excel via createObjectURL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> API | apiFetch inclui JWT -- sem token, API retorna 401 |
| Download de arquivo | blob() e createObjectURL sao operacoes locais -- sem XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Elevation of Privilege | AdminPedido sem guard | mitigate | Protegido pelo AdminLayout que ja faz guard de role=ADMIN antes de renderizar qualquer aba |
| T-07-08-02 | Information Disclosure | Download de PDF/Excel | mitigate | Endpoint exige JWT + role ADMIN no servidor; browser apenas exibe o arquivo ja autorizado |
| T-07-08-03 | Tampering | Stepper acoplado -- soma invalida | mitigate | Logic de onChange garante que split.p + split.r == total em qualquer mudanca; min=0 em ambos evita negativo |
| T-07-08-04 | Tampering | POST /admin/supplier-orders com items vazios | mitigate | Frontend valida split.p > 0 ou split.r > 0 antes de chamar a API; backend valida via Zod (items.min(1)) |
| T-07-SC | Tampering | npm installs | accept | Nenhuma instalacao nova neste plano |
</threat_model>

<verification>
1. Build frontend sem erros: `npm run build --workspace=@cheirin-de-pao/web`
2. StepBar existe: `ls apps/web/src/components/admin/StepBar.tsx`
3. 4 steps no AdminPedido: `grep -c "step === " apps/web/src/pages/admin/tabs/AdminPedido.tsx`
4. Download via blob: `grep "createObjectURL\|URL.create" apps/web/src/pages/admin/tabs/AdminPedido.tsx`
5. POST supplier-orders: `grep "supplier-orders.*POST\|POST.*supplier-orders" apps/web/src/pages/admin/tabs/AdminPedido.tsx`
</verification>

<success_criteria>
- StepBar renderiza 4 labels com barra gold nos concluidos e accent no texto
- AdminPedido tem 4 steps funcionais com navegacao livre para steps anteriores (D-09)
- Step 0: lista draft do servidor
- Step 1: steppers ajustaveis por condominio
- Step 2: steppers acoplados (soma = total); fornecedores do banco
- Step 3: download PDF e Excel via createObjectURL; botao "Voltar ao inicio" reseta
- Build frontend sem erros TypeScript
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-08-SUMMARY.md` ao concluir
</output>
