---
phase: 07-admin-panel
plan: 07
type: execute
wave: 2
depends_on: ["07-06"]
files_modified:
  - apps/web/src/pages/admin/AdminLayout.tsx
  - apps/web/src/components/admin/AdminBottomNav.tsx
  - apps/web/src/components/admin/AdminHead.tsx
  - apps/web/src/components/admin/KpiCard.tsx
  - apps/web/src/components/admin/BarChart.tsx
  - apps/web/src/components/admin/ProgressBar.tsx
  - apps/web/src/pages/admin/tabs/AdminPainel.tsx
autonomous: true
requirements: [ADMO-03, ADMO-10, UI-09]

must_haves:
  truths:
    - "AdminLayout renderiza AdminBottomNav com 5 abas e exibe a aba ativa por estado interno (D-01)"
    - "Aba ativa na bottom nav usa cor --color-accent icone stroke 2.3"
    - "AdminPainel exibe 4 KpiCards em grade 2x2 com dados do GET /admin/dashboard"
    - "Card banner Pedido de amanha navega para aba pedido ao clicar"
    - "BarChart exibe 7 colunas (seg a dom) com coluna do dia atual em --color-gold"
  artifacts:
    - path: "apps/web/src/pages/admin/AdminLayout.tsx"
      provides: "Container com estado tab + AdminBottomNav + renderizacao condicional das 5 abas"
    - path: "apps/web/src/components/admin/AdminBottomNav.tsx"
      provides: "Bottom nav 5 abas com safe-area + estado ativo"
    - path: "apps/web/src/components/admin/AdminHead.tsx"
      provides: "Cabecalho reutilizavel: avatar espresso BreadMark + subtitulo + titulo"
    - path: "apps/web/src/pages/admin/tabs/AdminPainel.tsx"
      provides: "Dashboard com 4 KPIs + banner Pedido + BarChart + receita por tipo"
  key_links:
    - from: "AdminLayout.tsx"
      to: "AdminBottomNav.tsx"
      via: "prop activeTab={tab} onTabChange={setTab}"
      pattern: "onTabChange"
    - from: "AdminPainel.tsx"
      to: "GET /admin/dashboard"
      via: "apiFetch('/admin/dashboard') no useEffect"
      pattern: "apiFetch.*dashboard"
---

<objective>
Substituir o AdminLayout placeholder e criar os 7 componentes da camada de navegacao e dashboard do admin: AdminLayout, AdminBottomNav, AdminHead, KpiCard, BarChart, ProgressBar e AdminPainel.

Purpose: Primeiro plano frontend — entrega a espinha dorsal da UI admin (navegacao + dashboard). Todos os planos frontend subsequentes dependem do AdminLayout e dos componentes compartilhados criados aqui.

Output: 7 arquivos React/TypeScript; AdminPainel funcional consumindo GET /admin/dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel/07-CONTEXT.md
@.planning/phases/07-admin-panel/07-UI-SPEC.md
@.planning/phases/07-admin-panel/07-PATTERNS.md

<interfaces>
<!-- AdminLayout padrao de PATTERNS.md (linhas completas):
import { useState } from 'react'
import { useAuth } from '../../hooks/useAuth'
import { LoadingScreen } from '../auth/LoadingScreen'
import { Navigate } from 'react-router'
type AdminTab = 'painel' | 'pedido' | 'entregas' | 'clientes' | 'gestao'
export function AdminLayout() {
  const { user, isLoading } = useAuth()
  const [tab, setTab] = useState<AdminTab>('painel')
  if (isLoading) return <LoadingScreen />
  if (!user || user.role !== 'ADMIN') return <Navigate to="/" replace />
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--color-app-bg)', display: 'flex', flexDirection: 'column', paddingBottom: 'calc(56px + env(safe-area-inset-bottom))' }}>
      {tab === 'painel' && <AdminPainel onNavigate={setTab} />}
      {tab === 'pedido' && <div />}
      {tab === 'entregas' && <div />}
      {tab === 'clientes' && <div />}
      {tab === 'gestao' && <div />}
      <AdminBottomNav activeTab={tab} onTabChange={setTab} />
    </div>
  )
}
-->

<!-- AdminBottomNav TABS (de UI-SPEC.md):
painel -> icon='trend' label='Painel'
pedido -> icon='factory' label='Pedido'
entregas -> icon='truck' label='Entregas'
clientes -> icon='users' label='Clientes'
gestao -> icon='settings' label='Gestao'
Aba ativa: color='--color-accent' stroke=2.3 weight=700
Aba inativa: color='--color-text-ter' stroke=2 weight=600
Padrao de nav exato em PATTERNS.md linhas 469-538
-->

<!-- AdminHead padrao de PATTERNS.md:
Avatar 42x42px borderRadius=13px background='#1E1207' com BreadMark size=27 color='#E3AC3F'
Subtitulo: 12.5px Hanken weight=600 cor=textTer
Titulo: Bricolage 20px weight=700 letterSpacing=-0.02em
-->

<!-- KpiCard de UI-SPEC.md:
Card pad=16 borderRadius=22px
Header: Icon size=20 cor=accent + Pill opcional (justifyContent: space-between)
Valor: Bricolage 26px weight=800 marginTop=12 letterSpacing=-0.02em
Label: 12.5px Hanken weight=600 cor=textSec
-->

<!-- BarChart de UI-SPEC.md:
display: flex, alignItems: flex-end, gap: 8, height: 96px
7 colunas (Seg a Dom); coluna do dia atual: cor --color-gold; demais: --color-surface-2
Label de dia: 10.5px Hanken weight=600 cor=textTer
Cada barra: flex: 1, borderRadius: 7px, transition: height 0.3s
-->

<!-- apiFetch de CourierScreen.tsx (padrao existente):
import { apiFetch } from '../../../lib/apiFetch'
const res = await apiFetch('/admin/dashboard')
if (res.ok) { setData(await res.json()) }
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminLayout + AdminBottomNav + AdminHead (componentes de navegacao)</name>
  <files>
    apps/web/src/pages/admin/AdminLayout.tsx,
    apps/web/src/components/admin/AdminBottomNav.tsx,
    apps/web/src/components/admin/AdminHead.tsx
  </files>
  <read_first>
    - apps/web/src/pages/admin/AdminLayout.tsx — ler o placeholder atual para entender o que sera substituido
    - apps/web/src/components/client/ClientTabBar.tsx — analog exato para AdminBottomNav (linhas 1-85)
    - apps/web/src/pages/client/ClientLayout.tsx — analog para AdminLayout (linhas 1-28)
    - apps/web/src/components/brand/Icon.tsx — verificar que icones trend, factory, truck, users, settings existem
    - apps/web/src/components/brand/BreadMark.tsx — para AdminHead
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secoes AdminLayout, AdminBottomNav e AdminHead
    - apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx — atualizar stub para testar componente real
  </read_first>
  <behavior>
    AdminLayout:
    - Estado interno `tab: AdminTab = 'painel'`; sem React Router sub-rotas (D-01)
    - Guard: redirect para '/' se !user ou user.role !== 'ADMIN'
    - As 5 abas: painel renderiza AdminPainel, pedido/entregas/clientes/gestao renderizam div placeholder (serao substituidos nos proximos planos)
    - paddingBottom: 'calc(56px + env(safe-area-inset-bottom))' para safe area iOS

    AdminBottomNav:
    - 5 botoes flex:1 em nav com aria-label='Navegacao administrativa'
    - Aba ativa: color=--color-accent, stroke=2.3, weight=700
    - Aba inativa: color=--color-text-ter, stroke=2, weight=600
    - minHeight=44 em cada botao
    - padding: '8px 6px calc(8px + env(safe-area-inset-bottom, 0px))'

    AdminHead:
    - Props: `{ sub: string; titulo: string }`
    - Avatar: 42x42 borderRadius=13 background='#1E1207' com BreadMark size=27 color='#E3AC3F'
    - Subtitulo: 12.5px Hanken weight=600 cor=#A89A82 (textTer)
    - Titulo: Bricolage 20px weight=700 letterSpacing=-0.02em
  </behavior>
  <action>
    AdminLayout.tsx: substituir o arquivo placeholder atual. Criar diretorio tabs/ vazio se necessario. Importar AdminPainel (criar stub basico `export function AdminPainel({ onNavigate }: { onNavigate: (tab: string) => void }) { return <div /> }` se AdminPainel.tsx nao existir — sera substituido na Task 2). Seguir exatamente o padrao de PATTERNS.md (linhas 392-437).

    AdminBottomNav.tsx: criar em apps/web/src/components/admin/. Seguir exatamente o padrao de PATTERNS.md (linhas 443-538) com a constante TABS correta (5 itens). Icon.tsx existente — importar de '../../brand/Icon'.

    AdminHead.tsx: criar em apps/web/src/components/admin/. Props: `{ sub: string; titulo: string }`. Estrutura de dois elementos: div avatar (espresso + BreadMark) + div texto (p sub + h1 titulo). Usar styles inline conforme UI-SPEC.md.

    Atualizar AdminBottomNav.test.tsx: renderizar com @testing-library/react, verificar 5 botoes com aria-label, verificar que a aba 'painel' tem aria-current='page' quando activeTab='painel'.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=@cheirin-de-pao/web -- --reporter=verbose 2>&1 | grep -E "(AdminBottomNav|PASS|FAIL)"</automated>
  </verify>
  <done>3 arquivos criados/substituidos; teste AdminBottomNav verde; AdminLayout substitui o placeholder; bottom nav tem 5 abas com safe-area</done>
</task>

<task type="auto">
  <name>Task 2: KpiCard + BarChart + ProgressBar + AdminPainel (dashboard)</name>
  <files>
    apps/web/src/components/admin/KpiCard.tsx,
    apps/web/src/components/admin/BarChart.tsx,
    apps/web/src/components/admin/ProgressBar.tsx,
    apps/web/src/pages/admin/tabs/AdminPainel.tsx
  </files>
  <read_first>
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao completa Aba Painel (KpiCards, card atalho, BarChart, receita por tipo)
    - .planning/phases/07-admin-panel/07-UI-SPEC.md — secao Copywriting Contract (textos exatos de Painel)
    - apps/web/src/pages/courier/CourierScreen.tsx — padrao de useEffect + apiFetch + estado (linhas 1-62)
    - .planning/phases/07-admin-panel/07-PATTERNS.md — secao AdminPainel padrao de imports e estrutura
    - apps/web/src/components/brand/Icon.tsx — icones bag, trend, users, building, factory, spark
  </read_first>
  <action>
    KpiCard.tsx: props `{ icon: keyof typeof Ic; value: string | number; label: string; pill?: { text: string; tone: 'good' | 'gold' | 'neutral' } }`. Card pad=16 borderRadius=22px. Header flex justify-between: Icon size=20 cor=accent + Pill opcional. Valor: Bricolage 26px weight=800. Label: 12.5px Hanken weight=600 cor=textSec.

    BarChart.tsx: props `{ data: Array<{ label: string; value: number; highlight?: boolean }>; height?: number }`. Renderiza barras flexbox. Coluna com highlight=true usa background='var(--color-gold)', demais usam 'var(--color-surface-2)'. Altura da barra = (value / max) * height proporcional. Cada barra: flex:1 borderRadius=7px transition='height 0.3s'. Label abaixo: 10.5px Hanken weight=600 cor=textTer.

    ProgressBar.tsx: props `{ value: number; max: number; color?: string }`. Height 7px borderRadius=99px overflow=hidden. Fill: width=(value/max)*100% transition='width 0.3s ease'. Color default='var(--color-gold)'. Cores alternativas: '--color-good' quando completo.

    AdminPainel.tsx: substituir o stub/placeholder. Importar AdminHead, KpiCard, BarChart, ProgressBar, apiFetch. Interface DashboardData: `{ breadsTodayCount: number; revenueToday: number; clientsCount: number; condominiumsCount: number; cutoffTime: string; revenueByType: { combos: number; avulso: number } }`. useEffect chama apiFetch('/admin/dashboard'). Grade 2x2 de KpiCards (gap=12px). Card atalho de Pedido de amanha: background=espresso, onClick={() => onNavigate('pedido')}, BreadMark decorativo position=absolute opacity=0.12 size=120. Card Fornadas por dia: BarChart com 7 colunas (Seg a Dom) — usar `new Date().getDay()` para identificar dia atual. Card Receita por tipo: barra proporcional (combos em gold, avulso em accent opacity 0.5) + legenda.

    Textos exatos: usar Copywriting Contract da UI-SPEC.md (Painel section). Loading state: spinner simples ou skeleton — sem texto.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build --workspace=@cheirin-de-pao/web 2>&1 | tail -15</automated>
  </verify>
  <done>4 arquivos criados; build frontend sem erros TypeScript; AdminPainel renderiza 4 KPIs, banner, BarChart e receita por tipo</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API | apiFetch inclui JWT Bearer — sem token, servidor retorna 401 |
| AdminLayout guard | user.role !== 'ADMIN' redireciona para '/' antes de renderizar qualquer aba |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Elevation of Privilege | AdminLayout sem guard correto | mitigate | Guard explícito: `if (!user || user.role !== 'ADMIN') return <Navigate to="/" replace />` — verificado no acceptance criterion |
| T-07-07-02 | Information Disclosure | KPIs financeiros expostos sem autenticacao | mitigate | apiFetch adiciona JWT automaticamente; servidor retorna 401 sem token; frontend redireciona para login via useAuth |
| T-07-07-03 | Tampering | Injecao de dados no BarChart via apiFetch | mitigate | Dados vem do server autenticado; BarChart apenas renderiza numeros — sem innerHTML ou eval |
| T-07-SC | Tampering | npm installs | accept | Nenhuma instalacao nova — apenas @dnd-kit sera instalado no plano 07-09 |
</threat_model>

<verification>
1. Build frontend sem erros: `npm run build --workspace=@cheirin-de-pao/web`
2. Teste AdminBottomNav verde: `npm run test --workspace=@cheirin-de-pao/web -- --reporter=verbose`
3. Guard presente em AdminLayout: `grep "role !== 'ADMIN'" apps/web/src/pages/admin/AdminLayout.tsx`
4. 5 abas na bottom nav: `grep -c "key:" apps/web/src/components/admin/AdminBottomNav.tsx`
5. apiFetch em AdminPainel: `grep "apiFetch" apps/web/src/pages/admin/tabs/AdminPainel.tsx`
</verification>

<success_criteria>
- AdminLayout substitui o placeholder com estado interno de 5 abas (D-01)
- AdminBottomNav tem 5 botoes com safe-area e cores corretas (accent=ativo, textTer=inativo)
- AdminHead e reutilizavel com props sub+titulo
- AdminPainel consome GET /admin/dashboard e exibe 4 KpiCards, banner, BarChart e receita por tipo
- Build frontend sem erros TypeScript
</success_criteria>

<output>
Criar `.planning/phases/07-admin-panel/07-07-SUMMARY.md` ao concluir
</output>
