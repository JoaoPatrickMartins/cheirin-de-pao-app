---
phase: 08-finalizacao-pagamentos
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
  - 08-05
files_modified:
  - apps/web/src/pages/client/HomeScreen.tsx
  - apps/web/src/pages/client/CardPaymentScreen.tsx
  - apps/web/src/pages/client/CombosScreen.tsx
  - apps/web/src/pages/client/PixWaitingScreen.tsx
  - apps/web/src/pages/client/ClientLayout.tsx
  - apps/web/src/hooks/useOneSignalDeepLink.ts
  - apps/web/src/components/client/ClientTabBar.tsx
autonomous: true
requirements:
  - CRED-11
  - UI-04
  - UI-07
  - UI-08
  - PAY-01
  - PAY-02

must_haves:
  truths:
    - "HomeScreen exibe BannerInsuficiente quando creditBalance === 0 (CRED-09 frontend)"
    - "HomeScreen exibe NextDays com dados reais do GET /schedules/me (próximos 5 dias)"
    - "TodayDelivery card exibe 3 estados completos: SCHEDULED / OUT_FOR_DELIVERY / DELIVERED"
    - "CardPaymentScreen usa <Icon name='arrowL'> no botão back (não símbolo literal ←)"
    - "useOneSignalDeepLink hook navega para /client/creditos quando additionalData.screen === 'creditos'"
    - "ClientLayout chama useOneSignalDeepLink para habilitar deep link de push"
    - "Tab bar ativa aba Créditos em todas as sub-rotas /client/creditos/* (ClientTabBar.tsx auditado)"
    - "CombosScreen exibe BannerInsuficiente na aba combos e na aba avulso quando créditos insuficientes"
  artifacts:
    - path: "apps/web/src/hooks/useOneSignalDeepLink.ts"
      provides: "Hook de deep link OneSignal para navigate(/client/creditos)"
      contains: "useOneSignalDeepLink"
    - path: "apps/web/src/pages/client/HomeScreen.tsx"
      provides: "BannerInsuficiente + NextDays reais + TodayDelivery 3 estados"
      contains: "BannerInsuficiente"
    - path: "apps/web/src/pages/client/ClientLayout.tsx"
      provides: "Chamada ao useOneSignalDeepLink"
      contains: "useOneSignalDeepLink"
    - path: "apps/web/src/components/client/ClientTabBar.tsx"
      provides: "activeState correto para sub-rotas /client/creditos/*"
      contains: "startsWith"
  key_links:
    - from: "apps/web/src/pages/client/HomeScreen.tsx"
      to: "apps/api/src/modules/schedules/schedules.route.ts"
      via: "apiFetch('/schedules/me') via useSchedule hook"
      pattern: "useSchedule|schedules/me"
    - from: "apps/web/src/pages/client/HomeScreen.tsx"
      to: "apps/api/src/modules/orders/orders.route.ts"
      via: "useOrderTracking() chamando GET /orders/today"
      pattern: "useOrderTracking|orders/today"
    - from: "apps/web/src/hooks/useOneSignalDeepLink.ts"
      to: "react-onesignal (window.OneSignal)"
      via: "OS.Notifications.addEventListener('click', handler)"
      pattern: "addEventListener.*click|NotificationClick"
    - from: "apps/web/src/components/client/ClientTabBar.tsx"
      to: "react-router-dom (useLocation)"
      via: "location.pathname.startsWith(tab.path)"
      pattern: "startsWith.*client/creditos|pathname.*creditos"
---

<objective>
Completar os gaps do frontend da Phase 8: HomeScreen (BannerInsuficiente + NextDays reais + TodayDelivery 3 estados), CardPaymentScreen (botão back com Icon), hook useOneSignalDeepLink + wiring no ClientLayout, e auditoria do CombosScreen/PixWaitingScreen/ClientTabBar.

Purpose: Fechar os requisitos UI-04, UI-07, UI-08, CRED-11 e garantir que todas as telas de pagamento estão completas e conformes ao design handoff. Verificar que ClientTabBar ativa a aba Créditos em todas as sub-rotas /client/creditos/*.
Output: HomeScreen completa com saldo real + entrega de hoje + banner de alerta + próximas entregas reais; CardPaymentScreen com botão back correto; deep link de push funcional; auditoria do CombosScreen, PixWaitingScreen e ClientTabBar confirmada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-finalizacao-pagamentos/08-CONTEXT.md
@.planning/phases/08-finalizacao-pagamentos/08-PATTERNS.md
@.planning/phases/08-finalizacao-pagamentos/08-UI-SPEC.md

<interfaces>
<!-- Contratos que o executor precisa para implementar sem exploração adicional -->

De apps/web/src/components/client/BannerInsuficiente.tsx (já existente):
- Props: saldo: number, requerido: number, onComprar: () => void, onAjustar: (qtd: number) => void
- Componente já oculta automaticamente quando requerido <= saldo
- Botão "Usar {saldo}" existe no componente — na HomeA usar onAjustar={() => {}} e ocultar o botão via style={{ display: 'none' }} em wrapper OU adicionar prop hideAjustar?: boolean ao componente

De apps/web/src/hooks/useSchedule.ts (já existente):
- export: useSchedule(creditBalance: number)
- Retorna: { weeklyQty: WeeklyQty, isLoading, error, ... }
- WeeklyQty: { seg: number, ter: number, qua: number, qui: number, sex: number, sab: number, dom: number }

De apps/web/src/hooks/useOrderTracking.ts (já existente):
- export: useOrderTracking()
- Retorna: { order: Order | null, isLoading }
- Order: { id, status: 'SCHEDULED'|'OUT_FOR_DELIVERY'|'DELIVERED', quantity, ... }
- Chama GET /orders/today internamente — link de carga crítico para o TodayDelivery card

De apps/web/src/contexts/AuthContext.tsx:
- useAuth() retorna: { user: { creditBalance, firstName, ... }, updateCreditBalance, ... }

De apps/web/src/hooks/useOneSignalRegister.ts (análogo para useOneSignalDeepLink):
- Usa (window as any).OneSignal para acessar SDK sem double-init
- Padrão de useEffect com cleanup: OS.Notifications.addEventListener + removeEventListener
- Sem imports de 'react-onesignal' direto — usa window.OneSignal

De apps/web/src/components/brand/Icon.tsx:
- Props: name: string, size?: number, stroke?: number, color?: string
- Ícone "arrowL" existe (confirmado em outros arquivos)

De apps/web/src/pages/client/HomeScreen.tsx (estado atual — 425 linhas):
- Lacunas: sem BannerInsuficiente, sem useSchedule/NextDays reais, TodayDelivery a verificar
- Já tem: useOrderTracking, CreditBalanceCard, useAuth

De apps/web/src/pages/client/CardPaymentScreen.tsx:
- Linha 85: botão back usa símbolo "←" puro → substituir por <Icon name="arrowL" size={20}>

De apps/web/src/components/client/ClientTabBar.tsx (a auditar na Task 3):
- Usa location.pathname.startsWith(tab.path) para determinar aba ativa
- Aba Créditos deve ter path: '/client/creditos' — o startsWith cobre /client/creditos, /client/creditos/pix, /client/creditos/cartao, /client/creditos/sucesso automaticamente
- Verificar se path da aba Créditos está exatamente como '/client/creditos' (não '/client/comprar' nem outra variante)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar useOneSignalDeepLink + wiring no ClientLayout</name>
  <files>
    apps/web/src/hooks/useOneSignalDeepLink.ts
    apps/web/src/pages/client/ClientLayout.tsx
  </files>
  <read_first>
    - apps/web/src/hooks/useOneSignalRegister.ts (OBRIGATÓRIO — análogo exato; copiar padrão de acesso via window.OneSignal e estrutura de useEffect com cleanup)
    - apps/web/src/pages/client/ClientLayout.tsx (OBRIGATÓRIO — ver onde useOneSignalRegister é chamado para adicionar useOneSignalDeepLink ao lado)
  </read_first>
  <action>
    Criar apps/web/src/hooks/useOneSignalDeepLink.ts seguindo exatamente o padrão de useOneSignalRegister.ts:
    - Import: useEffect de 'react', useNavigate de 'react-router-dom'
    - Dentro do useEffect: const OS = (window as any).OneSignal; if (!OS) return
    - Registrar: OS?.Notifications?.addEventListener?.('click', handleClick) com try/catch silencioso
    - handleClick: const screen = event?.notification?.additionalData?.screen; if (screen === 'creditos') navigate('/client/creditos')
    - Cleanup: OS?.Notifications?.removeEventListener?.('click', handleClick) com try/catch silencioso
    - Deps do useEffect: [navigate]
    - Export: export function useOneSignalDeepLink(): void

    Em ClientLayout.tsx: localizar a linha onde useOneSignalRegister() é chamado e adicionar useOneSignalDeepLink() imediatamente após. Adicionar import do hook: import { useOneSignalDeepLink } from '../../hooks/useOneSignalDeepLink'.

    NÃO registrar o listener em main.tsx — useNavigate não está disponível fora do router context (Pitfall 4 do RESEARCH.md).

    Gate comportamental: a verificação funcional completa deste hook (push real disparando navegação) ocorre no checkpoint 08-04 Task 1. O verify desta task confirma apenas compilação TypeScript e presença dos padrões via grep.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo apps/web/src/hooks/useOneSignalDeepLink.ts existe
    - grep -c "useOneSignalDeepLink" apps/web/src/pages/client/ClientLayout.tsx retorna >= 2 (import + chamada)
    - grep -c "addEventListener.*click\|click.*addEventListener" apps/web/src/hooks/useOneSignalDeepLink.ts retorna >= 1
    - grep -c "removeEventListener" apps/web/src/hooks/useOneSignalDeepLink.ts retorna >= 1 (cleanup)
    - grep -c "screen.*creditos\|creditos.*screen" apps/web/src/hooks/useOneSignalDeepLink.ts retorna >= 1
    - npx tsc --noEmit em apps/web retorna 0 erros de tipo
    - Confirmação funcional (navegação via push real) delegada ao checkpoint 08-04 Task 1
  </acceptance_criteria>
  <done>
    Hook useOneSignalDeepLink criado com padrão correto (window.OneSignal, cleanup, navigate). ClientLayout o chama ao lado de useOneSignalRegister. TypeScript compila sem erros. Verificação funcional ocorre no checkpoint 08-04.
  </done>
</task>

<task type="auto">
  <name>Task 2: Completar HomeScreen — BannerInsuficiente + NextDays reais + TodayDelivery</name>
  <files>
    apps/web/src/pages/client/HomeScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/HomeScreen.tsx (OBRIGATÓRIO — ler o arquivo inteiro; 425 linhas; identificar exatamente onde TodayDelivery está, onde inserir BannerInsuficiente, e se NextDays já existe ou é placeholder)
    - apps/web/src/components/client/BannerInsuficiente.tsx (OBRIGATÓRIO — verificar props disponíveis; determinar se precisa adicionar hideAjustar ou usar onAjustar={() => {}})
    - apps/web/src/hooks/useSchedule.ts (verificar assinatura exata do hook e tipo WeeklyQty retornado)
    - .projeto/design_handoff_cheirin_pao/app/screens-home.jsx (verificar design completo da HomeA — BreadMark watermark, layout TodayDelivery, NextDays pills)
  </read_first>
  <action>
    Ler HomeScreen.tsx integralmente. Identificar as 3 lacunas:

    LACUNA 1 — BannerInsuficiente (D-09, UI-SPEC §HomeScreen Lacuna 1):
    Inserir após o card TodayDelivery, antes de QuickActions. Condição: (user?.creditBalance ?? 0) === 0.
    Props: saldo={0} requerido={1} onComprar={() => navigate('/client/creditos')} onAjustar={() => {}}
    Para o botão "Usar 0" que não faz sentido na Home: verificar se BannerInsuficiente aceita prop hideAjustar. Se aceitar: usar hideAjustar={true}. Se não aceitar: adicionar prop opcional hideAjustar?: boolean ao BannerInsuficiente.tsx e aplicar style={{ display: 'none' }} ao botão quando hideAjustar=true.
    Importar BannerInsuficiente de '../../components/client/BannerInsuficiente' se não estiver importado.

    LACUNA 2 — TodayDelivery 3 estados (D-04, UI-SPEC §HomeScreen Lacuna 2):
    Verificar se useOrderTracking já está importado e em uso. useOrderTracking chama GET /orders/today internamente — este é o link de carga crítico para o TodayDelivery card. Se o TodayDelivery já renderiza os 3 estados (SCHEDULED/OUT_FOR_DELIVERY/DELIVERED) conforme UI-SPEC: apenas confirmar. Se algum estado estiver incompleto ou com cópia errada: corrigir para:
    - SCHEDULED: label uppercase "AGENDADO" (gold, letterSpacing 0.06em); pill "Agendado" (bg surface-2, textSec)
    - OUT_FOR_DELIVERY: label uppercase "SAINDO DO FORNO"; pill "A caminho" (bg goodSoft, good, dot 6px verde)
    - DELIVERED: label uppercase "ENTREGUE" (card opacity 0.85); pill "Entregue hoje" (bg surface-2, textSec)
    - Sem pedido: "Nenhuma entrega agendada"
    BreadMark watermark no card espresso: position absolute, top -40, right -20, opacity 0.13, size 140, color #E3AC3F

    LACUNA 3 — NextDays com dados reais (D-03, UI-SPEC §HomeScreen Lacuna 3):
    Importar useSchedule de '../../hooks/useSchedule'.
    Chamar: const { weeklyQty, isLoading: scheduleLoading } = useSchedule(user?.creditBalance ?? 0)
    Gerar próximos 5 dias a partir de amanhã (BRT):
    DAY_KEY_MAP = { 0:'dom', 1:'seg', 2:'ter', 3:'qua', 4:'qui', 5:'sex', 6:'sab' }
    DAY_ABBR = { seg:'Seg', ter:'Ter', qua:'Qua', qui:'Qui', sex:'Sex', sab:'Sáb', dom:'Dom' }
    nextDays = Array.from({ length: 5 }, (_, i) => { const d = new Date(); d.setDate(d.getDate() + i + 1); const key = DAY_KEY_MAP[d.getDay()]; return { abbr: DAY_ABBR[key], dayNum: d.getDate(), qty: weeklyQty?.[key] ?? 0, key } })
    Substituir o placeholder estático por renderização real:
    - Se scheduleLoading: manter placeholder atual
    - Se weeklyQty todos zero (sem agendamento): manter placeholder "Configure sua agenda para ver as próximas entregas"
    - Se tem agendamento: renderizar cards por dia conforme UI-SPEC (card 62×auto, radius 16, padding 12px 0; dia abreviado 11.5px textTer weight 600; número Bricolage 20px weight 800; pill gold "{qty} pão" quando qty > 0; "folga" quando qty === 0)
    Header row: "Próximas entregas" (Bricolage 16px weight 700) + botão "Editar agenda" (accent 13px weight 700) que navega para /client/agenda.

    Não alterar CreditBalanceCard, QuickActions, ou qualquer outra seção da HomeScreen.
    Não alterar useOrderTracking ou usePaymentPolling — só observar/usar o que já existe.

    Gate comportamental: a verificação visual completa (BannerInsuficiente aparecendo na tela, NextDays com dados reais, TodayDelivery nos 3 estados) ocorre no checkpoint 08-04 Task 1. O verify desta task confirma apenas compilação TypeScript e presença dos padrões via grep.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "BannerInsuficiente" apps/web/src/pages/client/HomeScreen.tsx retorna >= 2 (import + uso)
    - grep -c "useSchedule\|weeklyQty" apps/web/src/pages/client/HomeScreen.tsx retorna >= 2
    - grep -c "DAY_KEY_MAP\|nextDays\|schedules/me" apps/web/src/pages/client/HomeScreen.tsx retorna >= 1
    - grep -c "SAINDO DO FORNO\|OUT_FOR_DELIVERY\|AGENDADO\|ENTREGUE" apps/web/src/pages/client/HomeScreen.tsx retorna >= 3 (3 estados completos)
    - grep -c "creditBalance.*0\|0.*creditBalance" apps/web/src/pages/client/HomeScreen.tsx retorna >= 1 (condição do banner)
    - npx tsc --noEmit em apps/web retorna 0 erros de tipo
    - Verificação visual e funcional delegada ao checkpoint 08-04 Task 1
  </acceptance_criteria>
  <done>
    HomeScreen completa: BannerInsuficiente inserido com condição creditBalance=0, NextDays renderiza dados reais do agendamento semanal via useSchedule, TodayDelivery tem 3 estados conformes ao handoff. TypeScript compila sem erros. Confirmação visual ocorre no checkpoint 08-04.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix CardPaymentScreen + auditoria ClientTabBar + auditoria CombosScreen e PixWaitingScreen</name>
  <files>
    apps/web/src/pages/client/CardPaymentScreen.tsx
    apps/web/src/components/client/ClientTabBar.tsx
    apps/web/src/pages/client/CombosScreen.tsx
    apps/web/src/pages/client/PixWaitingScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/CardPaymentScreen.tsx (OBRIGATÓRIO — localizar botão back "←" na linha ~85; verificar onReady/onError/onSubmit do Brick)
    - apps/web/src/components/client/ClientTabBar.tsx (OBRIGATÓRIO — localizar o path da aba Créditos; verificar se usa startsWith; confirmar que '/client/creditos' como path cobre todas as sub-rotas)
    - apps/web/src/pages/client/CombosScreen.tsx (OBRIGATÓRIO — verificar BannerInsuficiente presente em ambas as abas, toggle Pix/Cartão, CTA bar, label dinâmica, navegação)
    - apps/web/src/pages/client/PixWaitingScreen.tsx (OBRIGATÓRIO — verificar estados waiting/timeout/rejected/approved; verificar copy do rejected)
    - .projeto/design_handoff_cheirin_pao/app/screens-order.jsx (verificar design para confirmações de copy e visuais)
  </read_first>
  <action>
    ARQUIVO 1 — CardPaymentScreen.tsx (completar):
    Localizar o botão back que usa símbolo "←" (linha ~85). Substituir por:
    - Import Icon de '../../components/brand/Icon' (adicionar se não estiver importado)
    - Container button: minHeight: 44, width: 38, height: 38, borderRadius: 12, border: '1.5px solid var(--color-border)', background: 'var(--color-surface-2)', cursor: 'pointer', display: 'grid', placeItems: 'center'
    - Conteúdo: <Icon name="arrowL" size={20} color="var(--color-text)" />
    - aria-label="Voltar"
    Verificar que onReady oculta o texto "Carregando formulário de pagamento...", onError chama setError com mensagem adequada, e onSubmit extrai token + installments + issuer_id + payment_method_id + payer do formData.

    ARQUIVO 2 — ClientTabBar.tsx (auditar + corrigir se necessário):
    Ler o arquivo inteiro. Localizar a definição da aba Créditos (label 'Créditos' ou equivalente). Verificar:
    (a) O path da aba está definido como '/client/creditos' (não '/client/comprar' nem outra variante)
    (b) O mecanismo de activeState usa location.pathname.startsWith(tab.path) — isso cobre automaticamente /client/creditos/pix, /client/creditos/cartao, /client/creditos/sucesso
    (c) Se o path estiver incorreto (ex: '/client/comprar'): corrigir para '/client/creditos'
    (d) Se o mecanismo não for startsWith mas sim ===: alterar para startsWith para cobrir sub-rotas
    Não alterar nenhum outro aspecto do componente — apenas o path da aba Créditos e o mecanismo de activeState se necessário.

    ARQUIVO 3 — CombosScreen.tsx (auditar):
    Verificar os seguintes pontos da UI-SPEC. Para cada ponto:
    - Se conforme: deixar como está, nenhuma mudança
    - Se não conforme: corrigir para o valor especificado
    Pontos a verificar:
    (a) BannerInsuficiente aparece na aba combos quando selectedCombo.quantity > creditBalance
    (b) BannerInsuficiente aparece na aba avulso quando customQty > creditBalance
    (c) Toggle Pix/Cartão na CTA bar: dois botões, border '1.5px solid var(--color-accent)' quando selecionado
    (d) Botão CTA primário: height 52, bg 'var(--color-accent)', Bricolage 16px weight 700
    (e) Label dinâmica: "Comprar {nomeCombo}" (combos) / "Comprar {N} pão"/"Comprar {N} pães" (avulso singular/plural)
    (f) Link "Quer no automático?" navega para /client/auto-recompra
    (g) Navegação: Pix → POST /payments/pix → navigate('/client/creditos/pix', { state: {paymentId, qrCodeBase64, qrCode, comboQuantity} }); Cartão → navigate('/client/creditos/cartao', { state: {comboId?, customQuantity?, amount} })

    ARQUIVO 4 — PixWaitingScreen.tsx (auditar + completar copy):
    Verificar pontos da UI-SPEC:
    (a) Estado rejected: texto "Pagamento não aprovado. Isso pode ter sido um erro temporário do banco. Tente novamente." — corrigir se diferente
    (b) Botão "Tentar novamente" navega para /client/creditos
    (c) Estado timeout: texto "Não detectamos o pagamento ainda. Verifique o app do banco e tente novamente."
    (d) Botão "Verificar mais tarde" navega para /client/home
    (e) updateCreditBalance(novoSaldo) chamado antes de navegar para /client/creditos/sucesso
    (f) Guard: state.paymentId ausente → redirect para /client/creditos

    NÃO alterar a lógica do usePaymentPolling — apenas verificar que os estados visuais estão corretos.

    Gate comportamental: a verificação funcional completa destes componentes (fluxo de compra real, estados da tab bar, deep link funcionando) ocorre no checkpoint 08-04 Task 1. O verify desta task confirma apenas compilação TypeScript e presença dos padrões via grep.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    <!-- CardPaymentScreen -->
    - grep -c "arrowL\|Icon.*arrowL" apps/web/src/pages/client/CardPaymentScreen.tsx retorna >= 1 (botão back correto)
    - grep -v "^[[:space:]]*//" apps/web/src/pages/client/CardPaymentScreen.tsx | grep -c "←" retorna 0 (símbolo puro removido)

    <!-- ClientTabBar -->
    - grep -c "client/creditos" apps/web/src/components/client/ClientTabBar.tsx retorna >= 1 (path da aba Créditos)
    - grep -c "startsWith" apps/web/src/components/client/ClientTabBar.tsx retorna >= 1 (mecanismo de activeState cobre sub-rotas)
    - grep -c "client/comprar" apps/web/src/components/client/ClientTabBar.tsx retorna 0 (URL inexistente não referenciada na tab bar)

    <!-- PixWaitingScreen -->
    - grep -c "Pagamento não aprovado" apps/web/src/pages/client/PixWaitingScreen.tsx retorna >= 1 (copy rejected correto)
    - grep -c "Tentar novamente\|client/creditos" apps/web/src/pages/client/PixWaitingScreen.tsx retorna >= 1

    <!-- CombosScreen -->
    - grep -c "BannerInsuficiente" apps/web/src/pages/client/CombosScreen.tsx retorna >= 2 (aba combos + aba avulso)

    <!-- TypeScript -->
    - npx tsc --noEmit em apps/web retorna 0 erros de tipo

    - Verificação funcional completa delegada ao checkpoint 08-04 Task 1
  </acceptance_criteria>
  <done>
    CardPaymentScreen com botão back correto (Icon arrowL). ClientTabBar auditada: aba Créditos usa path '/client/creditos' com startsWith — ativa em todas as sub-rotas /client/creditos/*. CombosScreen auditada e conforme UI-SPEC. PixWaitingScreen com copy correto para estado rejected. TypeScript compila sem erros. Confirmação funcional ocorre no checkpoint 08-04.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OneSignal push → Browser (deep link) | Dados de additionalData do push chegam ao handler no cliente |
| Browser → API (schedules) | GET /schedules/me requer JWT válido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-09 | Tampering | useOneSignalDeepLink — additionalData.screen manipulado | accept | O handler navega para rotas internas (/client/creditos) — nenhuma ação destrutiva possível via deep link; risco mínimo |
| T-08-10 | Information Disclosure | HomeScreen exibe creditBalance na UI | accept | Dado do próprio usuário autenticado — creditBalance vem do AuthContext pós-login; sem exposição a terceiros |
| T-08-11 | Spoofing | CardPayment Brick — token não validado no frontend | mitigate | Token enviado para POST /payments/card no backend — validação ocorre na API do MP server-side; frontend não precisa validar |
| T-08-12 | Elevation of Privilege | GET /schedules/me sem autenticação | mitigate | Endpoint protegido pelo fastify.authenticate preHandler — JWT obrigatório; sem autenticação retorna 401 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` → 0 erros de tipo
- `grep -c "BannerInsuficiente" apps/web/src/pages/client/HomeScreen.tsx` >= 2
- `grep -c "useOneSignalDeepLink" apps/web/src/pages/client/ClientLayout.tsx` >= 2
- `grep -c "arrowL" apps/web/src/pages/client/CardPaymentScreen.tsx` >= 1
- `grep -c "client/creditos" apps/web/src/components/client/ClientTabBar.tsx` >= 1 (tab bar cobre sub-rotas)
- `grep -c "startsWith" apps/web/src/components/client/ClientTabBar.tsx` >= 1
- `grep -c "sendLowCreditNotifications" apps/api/src/plugins/cron.ts` >= 2 (vem do 08-02)
- `cd apps/api && npm test` → suite completa verde (vem do 08-02)
</verification>

<success_criteria>
1. useOneSignalDeepLink hook criado e chamado no ClientLayout — deep link de push para /client/creditos funcional
2. HomeScreen completa: BannerInsuficiente (quando creditBalance=0) + NextDays reais (de GET /schedules/me via useSchedule) + TodayDelivery 3 estados completos (via useOrderTracking → GET /orders/today)
3. CardPaymentScreen com botão back usando Icon arrowL (não símbolo literal)
4. ClientTabBar auditada: aba Créditos ativa em /client/creditos, /client/creditos/pix, /client/creditos/cartao, /client/creditos/sucesso (startsWith)
5. CombosScreen auditada: BannerInsuficiente em ambas as abas, navegação correta para pix/cartao
6. PixWaitingScreen auditada: copy do estado rejected corrigido, updateCreditBalance chamado ao aprovar
7. TypeScript compila sem erros de tipo
8. Verificação funcional completa (fluxos de usuário real) ocorre no checkpoint 08-04 Task 1
</success_criteria>

<output>
Criar `.planning/phases/08-finalizacao-pagamentos/08-03-SUMMARY.md` ao concluir
</output>
