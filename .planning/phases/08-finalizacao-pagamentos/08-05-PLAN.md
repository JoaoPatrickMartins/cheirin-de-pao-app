---
phase: 08-finalizacao-pagamentos
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
autonomous: true
requirements:
  - CRED-08
  - CRED-10

must_haves:
  truths:
    - "processAutoBuy respeita o campo weekday: só compra quando o dia do cron coincide com autoRecharge.weekday"
    - "processAutoBuy respeita o modo 'semanal': sem compra para modos diferentes de 'semanal'"
    - "processAutoBuy envia push de confirmação (tipo Pix) ao cliente após compra automática bem-sucedida"
    - "Testes unitários cobrem os 3 cenários acima para processAutoBuy"
  artifacts:
    - path: "apps/api/src/modules/schedules/__tests__/schedules.service.test.ts"
      provides: "3 casos de teste para CRED-08/10 (weekday, modo, push de confirmação)"
      contains: "processAutoBuy"
  key_links:
    - from: "apps/api/src/plugins/cron.ts"
      to: "apps/api/src/modules/schedules/schedules.service.ts"
      via: "schedulesService.processAutoBuy()"
      pattern: "processAutoBuy"
    - from: "apps/api/src/modules/schedules/schedules.service.ts"
      to: "@onesignal/node-onesignal"
      via: "osClient.createNotification(notification) após criação Pix"
      pattern: "createNotification"
---

<objective>
Auditar e cobrir com testes unitários o fluxo completo de compra automática (processAutoBuy): verificar se o weekday do autoRecharge é respeitado no cron, se o modo 'semanal' é verificado, e se um push de confirmação é enviado após compra bem-sucedida.

Purpose: Fechar CRED-08 (compra automática semanal com seletor de dia) e CRED-10 (compra automática executada corretamente quando saldo insuficiente + auto-recharge ativo) com evidência testada. O processAutoBuy já existe — este plano verifica e documenta o comportamento real, adicionando testes para os cenários que o checker identificou como não cobertos.
Output: 3 casos de teste unitários verdes cobrindo processAutoBuy (weekday, modo semanal, push de confirmação após compra).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-finalizacao-pagamentos/08-CONTEXT.md
@.planning/phases/08-finalizacao-pagamentos/08-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa para auditar sem exploração adicional -->

De apps/api/src/modules/schedules/schedules.service.ts (processAutoBuy — a inspecionar):
- processAutoBuy(): itera sobre schedules com autoRecharge ativo
- Verifica consumoSemanal: se creditBalance >= consumoSemanal → skip (sem necessidade de compra)
- Verifica weekday: autoRecharge.weekday deve coincidir com o dia atual do cron
- Verifica mode: autoRecharge.mode deve ser 'semanal'
- Se tudo ok: chama createPix (ou similar) e envia push de confirmação ao cliente
- BUG já corrigido no 08-02: notification.url = '/client/creditos' (não '/client/comprar')

De apps/api/src/modules/schedules/__tests__/schedules.service.test.ts:
- Mock OneSignal: vi.mock('@onesignal/node-onesignal', ...) — JÁ EXISTE no arquivo
- Mock Fastify: createMockFastify() — padrão existente no arquivo
- autoRecharge mock: { active: true, mode: 'semanal', weekday: 'seg', comboId: 'combo-id' }

De apps/api/src/modules/schedules/schedules.repository.ts:
- findUserById(userId: string): retorna { id, creditBalance, oneSignalPlayerId, autoRecharge, ... }

NOTA: O plano 08-02 (Wave 1, paralelo) corrige o bug de URL em processAutoBuy. Se 08-05 rodar em paralelo com 08-02, o arquivo schedules.service.ts pode não ter o bug corrigido ainda. Os testes em 08-05 são apenas sobre schedules.service.test.ts — não modificam schedules.service.ts — então não há conflito de file ownership.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auditar processAutoBuy e criar testes unitários para CRED-08/10</name>
  <files>
    apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.service.ts (OBRIGATÓRIO — ler processAutoBuy integralmente: verificar exatamente como weekday é checado, como mode é checado, e se/como push de confirmação é enviado após compra; documentar comportamento real no SUMMARY)
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (OBRIGATÓRIO — ver estado atual dos casos de processAutoBuy: quais já existem, quais faltam)
  </read_first>
  <behavior>
    Antes de escrever os testes, inspecionar processAutoBuy em schedules.service.ts e responder:
    (a) weekday: o código verifica autoRecharge.weekday contra o dia atual? Se sim, como — qual campo e qual comparação?
    (b) mode: o código verifica autoRecharge.mode === 'semanal'? Se sim, onde?
    (c) push de confirmação: após a criação Pix automática, o código envia push com createNotification? Se sim, qual payload?

    Com base na inspeção, escrever os 3 testes:
    - Test 1 (weekday respeitado): mock com autoRecharge.weekday='seg' e dia atual simulado como 'ter' → processAutoBuy NÃO deve disparar compra para este schedule. Assert: createNotification não chamado, createPix/paymentsService não chamado.
    - Test 2 (modo semanal): mock com autoRecharge.mode='mensal' (ou qualquer valor diferente de 'semanal') → processAutoBuy NÃO deve disparar compra. Assert: createNotification não chamado.
    - Test 3 (push de confirmação): mock com autoRecharge ativo + weekday correto + mode='semanal' + creditBalance < consumoSemanal → processAutoBuy DEVE disparar compra e enviar push. Assert: createNotification chamado 1 vez com payload incluindo url='/client/creditos' OU additionalData com screen relevante.

    Se o comportamento real do processAutoBuy for diferente do esperado (ex: não verifica weekday), documentar a discrepância no SUMMARY e criar testes que refletem o comportamento real — não o comportamento esperado. O objetivo é cobertura, não reescrever a implementação.
  </behavior>
  <action>
    Ler schedules.service.ts integralmente, focando em processAutoBuy. Extrair a lógica exata de:
    1. Verificação de weekday: identificar o campo (autoRecharge.weekday) e a comparação usada (ex: new Date().getDay(), comparação com string do dia)
    2. Verificação de mode: identificar a condição (ex: autoRecharge.mode === 'semanal')
    3. Push de confirmação: identificar se existe chamada a createNotification após o processamento e qual o payload

    Ler schedules.service.test.ts para ver os mocks existentes de processAutoBuy (se houver).

    Adicionar o bloco describe('processAutoBuy [CRED-08/10]') com os 3 casos. Para mockar o dia atual nos testes de weekday, usar vi.setSystemTime() ou mockar a chamada interna de new Date() dependendo de como o código é estruturado — se a função usa new Date() diretamente, usar vi.setSystemTime(new Date('2024-01-08')) para simular uma segunda-feira, por exemplo.

    Reutilizar os mocks existentes no arquivo (vi.mock, createMockFastify, mock do OneSignal).

    Se a implementação de processAutoBuy não verificar weekday ou mode, escrever testes que documentam o comportamento atual e adicionar comentário: "// TODO CRED-08: implementação atual não verifica weekday/mode — comportamento documentado, não o esperado".
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npm test -- schedules.service.test.ts 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Comando npm test -- schedules.service.test.ts retorna PASS com 0 falhas
    - grep -c "processAutoBuy" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts retorna >= 3 (describe + múltiplos its)
    - grep -c "weekday\|CRED-08" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts retorna >= 1 (cobertura de weekday documentada)
    - grep -c "semanal\|CRED-10" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts retorna >= 1 (cobertura de modo semanal documentada)
    - npm test (suite completa da API) retorna com 0 failed
    - SUMMARY documenta o comportamento real de processAutoBuy (verifica ou não verifica weekday/mode)
  </acceptance_criteria>
  <done>
    3 casos de teste unitários para processAutoBuy verdes (CRED-08/10 cobertos). Comportamento real de processAutoBuy documentado no SUMMARY — incluindo se weekday e mode são verificados conforme esperado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron backend → Payments API | processAutoBuy cria pagamento Pix automaticamente em nome do usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-16 | Elevation of Privilege | processAutoBuy — compra automática sem consentimento explícito | mitigate | autoRecharge.active só é true quando usuário habilitou via AutoBuyScreen (PUT /users/me/auto-recharge); consentimento registrado em banco |
| T-08-17 | Tampering | processAutoBuy — weekday incorreto causa compra fora do dia desejado | mitigate | Testes unitários criados neste plano verificam que weekday é respeitado; comportamento documentado no SUMMARY |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `cd apps/api && npm test -- schedules.service.test.ts` → PASS com 0 failed
- `grep -c "processAutoBuy" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts` >= 3
- `grep -c "weekday\|semanal" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts` >= 2
- `cd apps/api && npm test` → suite completa verde
</verification>

<success_criteria>
1. 3 casos de teste unitários para processAutoBuy verdes (weekday respeitado, modo semanal verificado, push de confirmação enviado)
2. Comportamento real de processAutoBuy documentado no SUMMARY — incluindo se CRED-08/10 estão plenamente implementados ou se há gaps na implementação (ex: weekday não verificado)
3. Suite completa de testes da API verde sem regressões
</success_criteria>

<output>
Criar `.planning/phases/08-finalizacao-pagamentos/08-05-SUMMARY.md` ao concluir
</output>
