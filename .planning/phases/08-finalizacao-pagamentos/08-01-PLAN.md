---
phase: 08-finalizacao-pagamentos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts
  - apps/api/src/modules/credits/__tests__/credits.service.test.ts
  - apps/api/src/modules/payments/__tests__/payments.service.test.ts
autonomous: false
requirements:
  - CRED-01
  - CRED-02
  - CRED-03
  - CRED-04
  - CRED-05
  - CRED-06
  - PAY-01
  - PAY-02

must_haves:
  truths:
    - "Suite de testes do backend (payments + webhooks + credits) passa completamente"
    - "Caso de teste para reconcilePayment com customQuantity (compra avulsa) existe e é verde"
    - "Credenciais MP sandbox configuradas nas .env — testes end-to-end possíveis"
    - "ngrok instalado e URL configurada no dashboard MP sandbox"
  artifacts:
    - path: "apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts"
      provides: "Cobertura de customQuantity (compra avulsa) no reconcilePayment"
      contains: "customQuantity"
    - path: "apps/api/.env"
      provides: "Credenciais MP sandbox"
      contains: "MP_ACCESS_TOKEN"
    - path: "apps/web/.env"
      provides: "Chave pública MP sandbox"
      contains: "VITE_MP_PUBLIC_KEY"
  key_links:
    - from: "apps/api/src/modules/webhooks/webhooks.service.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "prisma.payment.findUnique + prisma.user.update"
      pattern: "payment\\.findUnique|creditUserBalance"
    - from: "apps/web/src/pages/client/PixWaitingScreen.tsx"
      to: "apps/api/src/modules/payments/payments.route.ts"
      via: "GET /payments/:id/status (polling)"
      pattern: "payments.*status"

user_setup:
  - service: mercado-pago-sandbox
    why: "Testes end-to-end de Pix e cartão requerem credenciais sandbox do MP"
    env_vars:
      - name: MP_ACCESS_TOKEN
        source: "mercadopago.com.br → Minhas integrações → Credenciais de teste → Access Token"
        target_file: apps/api/.env
      - name: MP_WEBHOOK_SECRET
        source: "mercadopago.com.br → Minhas integrações → Credenciais de teste → Client Secret"
        target_file: apps/api/.env
      - name: MP_PUBLIC_KEY
        source: "mercadopago.com.br → Minhas integrações → Credenciais de teste → Public Key"
        target_file: apps/api/.env
      - name: VITE_MP_PUBLIC_KEY
        source: "Mesmo valor que MP_PUBLIC_KEY acima"
        target_file: apps/web/.env
    dashboard_config:
      - task: "Instalar ngrok (se não instalado)"
        location: "Terminal: npm install -g ngrok OU https://ngrok.com/download"
      - task: "Iniciar ngrok e copiar URL HTTPS"
        location: "Terminal: ngrok http 3001 → copiar URL do tipo https://{id}.ngrok.io"
      - task: "Configurar URL de webhook no dashboard MP sandbox"
        location: "mercadopago.com.br → Suas integrações → Webhooks → Criar webhook: URL = https://{ngrok-id}.ngrok.io/webhooks/mercadopago, Eventos = payments"
---

<objective>
Configurar ambiente sandbox do Mercado Pago e verificar cobertura de testes dos módulos de backend existentes (payments, webhooks, credits). Garante que o código dos planos 03-03/03-02 está funcional antes de qualquer correção.

Purpose: Bloquear execução dos testes end-to-end de pagamento até as credenciais serem configuradas. Identificar e cobrir gaps de testes antes de avançar para implementação de novas funcionalidades.
Output: Env vars MP sandbox configuradas, testes de backend completos e verdes (incluindo caso customQuantity), e ngrok ativo para testes de webhook.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-finalizacao-pagamentos/08-CONTEXT.md
@.planning/phases/08-finalizacao-pagamentos/08-RESEARCH.md
@.planning/phases/08-finalizacao-pagamentos/08-PATTERNS.md
@.planning/phases/08-finalizacao-pagamentos/08-VALIDATION.md

<interfaces>
<!-- Contratos relevantes para auditoria e testes -->

De apps/api/src/modules/webhooks/webhooks.service.ts (linhas confirmadas no codebase):
- reconcilePayment(mpPaymentId: string): Promise<void>
- lógica: payment.status === 'PAID' → return (idempotência)
- lógica: payment.customQuantity ?? getComboQuantity(payment.comboId) → quantity
- lógica: creditUserBalance (em $transaction) + updatePaymentStatus

De apps/api/src/modules/payments/payments.service.ts:
- createPix(userId, comboId?, customQuantity?, amount): retorna { paymentId, qrCodeBase64, qrCode }
- createCard(userId, token, ...): retorna { paymentId }
- getPaymentStatus(paymentId): retorna { status: 'PENDING'|'PAID'|'FAILED' }

De apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts:
- Caso 'APROVADO (customQuantity)' JÁ EXISTE (linha 71) — cobrindo comboId=null, customQuantity=5
- Casos confirmados: APROVADO combo, APROVADO avulso, REJECTED, PENDING, PAID (idempotência), sem quantity

De apps/api/src/modules/schedules/schedules.repository.ts:
- findUserById(userId: string): retorna { id, creditBalance, oneSignalPlayerId, autoRecharge, ... }
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configurar credenciais MP sandbox e ngrok</name>
  <what-built>Esta tarefa não pode ser automatizada — requer acesso ao dashboard do Mercado Pago e instalação local do ngrok.</what-built>
  <how-to-verify>
    1. Acesse mercadopago.com.br → login → Minhas integrações → selecione ou crie uma aplicação de teste
    2. Em Credenciais → copie: Access Token (começa com TEST-), Public Key (começa com TEST-)
    3. Em apps/api/.env, adicione (ou atualize se já existem):
       MP_ACCESS_TOKEN=TEST-...
       MP_WEBHOOK_SECRET=TEST-... (Client Secret da mesma aplicação)
       MP_PUBLIC_KEY=TEST-...
    4. Em apps/web/.env, adicione:
       VITE_MP_PUBLIC_KEY=TEST-... (mesmo valor que MP_PUBLIC_KEY)
    5. Execute: ngrok http 3001 (instale se necessário: npm install -g ngrok)
    6. Copie a URL HTTPS do ngrok (ex: https://abc123.ngrok.io)
    7. No dashboard MP → Integrações → Webhooks → Criar webhook:
       - URL: https://{ngrok-id}.ngrok.io/webhooks/mercadopago
       - Eventos: payments
    8. Confirme que o servidor API sobe sem erro: cd apps/api && npm run dev
       Deve aparecer nos logs: "MercadoPago SDK initialized" OU sem erro de MP_ACCESS_TOKEN
  </how-to-verify>
  <resume-signal>Digite "pronto" quando as env vars estiverem no .env e o servidor subir sem erro de MP_ACCESS_TOKEN</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auditar e completar testes de backend (webhooks + credits)</name>
  <files>
    apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts
    apps/api/src/modules/credits/__tests__/credits.service.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts (OBRIGATÓRIO — ver estado atual dos casos de teste antes de qualquer mudança)
    - apps/api/src/modules/credits/__tests__/credits.service.test.ts (OBRIGATÓRIO — verificar cobertura atual de getPricing e checkBalance)
    - apps/api/src/modules/webhooks/webhooks.service.ts (fonte de verdade das implementações testadas)
    - apps/api/src/modules/credits/credits.service.ts (fonte de verdade para getPricing)
  </read_first>
  <behavior>
    - Test 1 (webhooks — já deve existir): reconcilePayment com customQuantity=5, comboId=null → credita 5 pãezinhos e marca PAID
    - Test 2 (webhooks — verificar): reconcilePayment com payment.status=PAID → não chama creditUserBalance (idempotência)
    - Test 3 (credits — verificar): getPricing retorna avulsoUnit, avulsoLimite, bestComboUnitPrice corretamente
    - Test 4 (credits — criar se ausente): getUnitPrice com customQuantity acima de avulsoLimite → retorna erro ou preço maior
  </behavior>
  <action>
    Ler ambos os arquivos de teste integralmente. Para cada caso listado em behavior:
    - Se o caso já existe e passa: marcar como "CONFIRMED OK" em comentário — não alterar
    - Se o caso existe mas está incompleto (ex: missing assertion): adicionar a assertion faltante
    - Se o caso não existe: adicionar o caso seguindo o padrão createMockFastify existente nos arquivos (vi.fn() mocks, beforeEach reset)

    Para webhooks.service.test.ts: o caso customQuantity (CRED-03) já está confirmado por grep (linha 71). Verificar se há assertion de `expect(creditUserBalance).toHaveBeenCalledWith(userId, 5, paymentId)` — adicionar se ausente.

    Para credits.service.test.ts: verificar se `getPricing()` e `checkBalance()` têm cobertura básica. Se arquivo estiver ausente ou vazio, criar o stub mínimo com pelo menos 2 casos de teste.

    Usar `vi.mock`, `describe`, `it`, `expect`, `beforeEach` — padrão do codebase (importado de 'vitest').
    NÃO usar `setInterval`, `Date.now()` reais nos testes — mockar se necessário.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npm test -- webhooks.service.test.ts credits.service.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Comando `npm test -- webhooks.service.test.ts` retorna PASS com todos os testes verdes
    - Grep confirma existência do caso customQuantity: grep -c "customQuantity" apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts retorna >= 1
    - Grep confirma existência de assertion de crédito: grep -c "creditUserBalance\|toHaveBeenCalled" apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts retorna >= 1
    - Nenhum teste marcado como skip ou todo
    - Suite completa do arquivo webhooks passa: 0 failed, N passed
  </acceptance_criteria>
  <done>
    Suite de testes de backend (webhooks + credits) completamente verde. Caso customQuantity confirmado e coberto. Cobertura básica de credits.service confirmada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MP → API (webhook) | Dados do Mercado Pago chegam via POST sem autenticação de sessão |
| Browser → API (payments) | Requisições de pagamento do frontend autenticadas via JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Spoofing | POST /webhooks/mercadopago | mitigate | HMAC-SHA256 com MP_WEBHOOK_SECRET em webhooks.service.ts — já implementado; verificar em auditoria |
| T-08-02 | Tampering | reconcilePayment — duplo crédito | mitigate | Idempotência via payment.status === 'PAID' antes de creditar; $transaction atômica — já implementado |
| T-08-03 | Tampering | customQuantity negativo ou zero | mitigate | Validar `if (!quantity) return` em reconcilePayment; verificar que payments.schema.ts tem min: 1 no campo customQuantity |
| T-08-04 | Denial of Service | MP_ACCESS_TOKEN ausente em startup | mitigate | user_setup blocking previne execução sem credenciais; servidor deve logar erro explícito se token ausente |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — apenas auditoria de código existente |
</threat_model>

<verification>
Suite de testes verdes:
- `cd apps/api && npm test -- webhooks.service.test.ts` → PASS
- `cd apps/api && npm test -- credits.service.test.ts` → PASS (ou PASS com warnings se arquivo novo)
- Env vars presentes: `grep -c "MP_ACCESS_TOKEN" apps/api/.env` retorna >= 1
- Env vars presentes: `grep -c "VITE_MP_PUBLIC_KEY" apps/web/.env` retorna >= 1
</verification>

<success_criteria>
1. Credenciais MP sandbox configuradas nos arquivos .env corretos sem exibir em logs
2. Suite de testes de backend verde com cobertura confirmada de reconcilePayment (incluindo compra avulsa com customQuantity)
3. ngrok ativo com URL HTTPS configurada no dashboard MP sandbox como endpoint de webhook
4. Nenhuma regressão nos testes existentes
</success_criteria>

<output>
Criar `.planning/phases/08-finalizacao-pagamentos/08-01-SUMMARY.md` ao concluir
</output>
