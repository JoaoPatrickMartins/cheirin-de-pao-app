---
phase: 08-finalizacao-pagamentos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/schedules/schedules.service.ts
  - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
  - apps/api/src/plugins/cron.ts
autonomous: true
requirements:
  - CRED-09

must_haves:
  truths:
    - "sendLowCreditNotifications dispara push OneSignal quando creditBalance < consumo semanal e sem auto-recharge"
    - "sendLowCreditNotifications NÃO dispara quando auto-recharge ativo (CRED-10 — compra automática cuida disso)"
    - "processAutoBuy usa URL /client/creditos (não /client/comprar inexistente)"
    - "sendLowCreditNotifications é chamado no cron de meia-noite após processAutoBuy"
    - "Notificação de crédito insuficiente é persistida em Notification com type=LOW_CREDIT"
    - "Testes unitários cobrem 4 cenários de sendLowCreditNotifications"
  artifacts:
    - path: "apps/api/src/modules/schedules/schedules.service.ts"
      provides: "Função sendLowCreditNotifications + bug fix processAutoBuy URL"
      contains: "sendLowCreditNotifications"
    - path: "apps/api/src/modules/schedules/__tests__/schedules.service.test.ts"
      provides: "4 casos de teste para CRED-09"
      contains: "sendLowCreditNotifications"
    - path: "apps/api/src/plugins/cron.ts"
      provides: "Chamada ao sendLowCreditNotifications no cron de meia-noite"
      contains: "sendLowCreditNotifications"
  key_links:
    - from: "apps/api/src/plugins/cron.ts"
      to: "apps/api/src/modules/schedules/schedules.service.ts"
      via: "schedulesService.sendLowCreditNotifications()"
      pattern: "sendLowCreditNotifications"
    - from: "apps/api/src/modules/schedules/schedules.service.ts"
      to: "@onesignal/node-onesignal"
      via: "osClient.createNotification(notification)"
      pattern: "createNotification"
    - from: "apps/api/src/modules/schedules/schedules.service.ts"
      to: "apps/api/src/modules/notifications/notifications.service.ts"
      via: "notificationsService.createAndTrim({ type: 'LOW_CREDIT', ... })"
      pattern: "createAndTrim.*LOW_CREDIT"
---

<objective>
Implementar a notificação push de crédito insuficiente (CRED-09) no cron de meia-noite. Corrigir o bug de URL inexistente no processAutoBuy (CRED-10). Cobrir ambas as funcionalidades com testes unitários.

Nota: CRED-08 (compra automática semanal com seletor de weekday) e CRED-10 (fluxo completo de processAutoBuy — weekday respeitado, modo semanal, push de confirmação após compra bem-sucedida) são cobertos no plano 08-05, que audita e testa processAutoBuy em profundidade. Este plano (08-02) cobre apenas CRED-09 (notificação de crédito insuficiente) e o bug fix de URL do processAutoBuy como efeito colateral.

Purpose: Fechar CRED-09 — o sistema notifica proativamente clientes sem auto-recharge quando o saldo está abaixo do consumo semanal.
Output: Função sendLowCreditNotifications no schedules.service.ts, chamada no cron de meia-noite, 4 testes unitários verdes, bug fix em processAutoBuy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-finalizacao-pagamentos/08-CONTEXT.md
@.planning/phases/08-finalizacao-pagamentos/08-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa para implementar sem exploração adicional -->

De apps/api/src/modules/schedules/schedules.service.ts (já existente — padrões a seguir):
- createOsClient(): função local que retorna new OneSignal.DefaultApi(configuration)
- sendEveReminders(): padrão de loop over schedules + push + notificationsService.createAndTrim
- processAutoBuy(): calcula consumoSemanal com Object.values(weeklyQty).reduce — reutilizar padrão
- LINHA 271 (BUG): notification.url = '/client/comprar' → corrigir para '/client/creditos'

De apps/api/src/modules/schedules/schedules.repository.ts:
- findUserById(userId: string): retorna { id, creditBalance, oneSignalPlayerId, autoRecharge, condominiumId }

De apps/api/src/modules/notifications/notifications.service.ts:
- createAndTrim({ userId, type, title, body }): persiste Notification e mantém últimas 30

De apps/api/prisma/schema.prisma:
- enum NotificationType: LOW_CREDIT (linha 94 — JÁ EXISTE no schema)
- model Notification: { id, userId, type NotificationType, title, body, isRead, createdAt }

De apps/api/src/plugins/cron.ts (estado atual):
- Cron de meia-noite chama: createDailyOrders → processAutoBuy (com try/catch isolado por chamada)
- Padrão: try { await schedulesService.X() } catch { fastify.log.error(...) }

De apps/api/src/modules/schedules/__tests__/schedules.service.test.ts:
- Mock OneSignal: vi.mock('@onesignal/node-onesignal', ...) — JÁ EXISTE no arquivo
- Mock Fastify: createMockFastify() — padrão existente no arquivo
- Estrutura autoRecharge no mock: { autoRecharge: null } ou { autoRecharge: { active: true } }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar sendLowCreditNotifications e corrigir processAutoBuy URL</name>
  <files>
    apps/api/src/modules/schedules/schedules.service.ts
  </files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.service.ts (OBRIGATÓRIO — ler o arquivo inteiro para entender estrutura atual, imports, createOsClient, sendEveReminders como análogo, linha 271 do bug)
    - apps/api/src/modules/schedules/schedules.repository.ts (verificar assinatura exata de findUserById)
    - apps/api/src/modules/notifications/notifications.service.ts (verificar assinatura exata de createAndTrim)
  </read_first>
  <behavior>
    - sendLowCreditNotifications(): para cada schedule ativo, se user.autoRecharge?.active → skip. Se creditBalance >= consumoSemanal → skip. Se consumoSemanal === 0 → skip. Caso contrário: enviar push OneSignal com additionalData.screen='creditos' (D-11), persistir Notification type=LOW_CREDIT.
    - Push OneSignal: notification.app_id = ONESIGNAL_APP_ID, include_subscription_ids = [user.oneSignalPlayerId], headings = { pt: 'Seus créditos estão acabando' }, contents = { pt: 'Você tem {N} crédito(s) e sua semana precisa de {M}. Recarregue agora antes que faltem pães!' }, additionalData = { screen: 'creditos' }. Usar locale 'pt' para consistência com código existente (schedules.service.ts usa 'pt', não 'pt-BR').
    - processAutoBuy bug fix: linha com notification.url = '/client/comprar' → notification.url = '/client/creditos'
    - Cada iteração de schedule envolta em try/catch que loga warn e continua (não aborta o loop inteiro)
  </behavior>
  <action>
    Ler o arquivo completo. Localizar a linha com notification.url = '/client/comprar' em processAutoBuy e corrigir para '/client/creditos' (D-17/D-18 do CONTEXT.md).

    Adicionar o método sendLowCreditNotifications() à classe SchedulesService, após processAutoBuy. Modelo exato está em 08-PATTERNS.md seção "schedules.service.ts — adicionar sendLowCreditNotifications". Seguir o padrão de sendEveReminders para estrutura geral (loop, try/catch por item, log de erro).

    Campos concretos da notificação:
    - notification.app_id = process.env.ONESIGNAL_APP_ID!
    - notification.include_subscription_ids = [user.oneSignalPlayerId]
    - notification.headings = { pt: 'Seus créditos estão acabando' }
    - notification.contents = { pt: `Você tem ${user.creditBalance} crédito(s) e sua semana precisa de ${consumoSemanal}. Recarregue agora antes que faltem pães!` }
    - notification.additionalData = { screen: 'creditos' }

    Persistência via: this.notificationsService.createAndTrim({ userId: schedule.userId, type: 'LOW_CREDIT', title: 'Créditos insuficientes', body: `Você tem ${user.creditBalance} crédito(s) e sua semana precisa de ${consumoSemanal}.` })

    Não adicionar novos imports — OneSignal e NotificationsService já são importados no arquivo.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx tsc --noEmit 2>&1 | head -20 && grep -c "sendLowCreditNotifications" apps/api/src/modules/schedules/schedules.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sendLowCreditNotifications" apps/api/src/modules/schedules/schedules.service.ts retorna >= 2 (definição + export implícito da classe)
    - grep -c "client/creditos" apps/api/src/modules/schedules/schedules.service.ts retorna >= 1 (URL corrigida no processAutoBuy)
    - grep -v "client/comprar" apps/api/src/modules/schedules/schedules.service.ts retorna sem matches (URL bug removida)
    - grep -c "LOW_CREDIT" apps/api/src/modules/schedules/schedules.service.ts retorna >= 1
    - grep -c "screen.*creditos\|creditos.*screen" apps/api/src/modules/schedules/schedules.service.ts retorna >= 1 (deep link D-11)
    - npx tsc --noEmit em apps/api retorna 0 erros (TypeScript compila sem erro)
    - Nota: confirmação comportamental de sendLowCreditNotifications é feita pelos testes da Task 2 — o verify desta task confirma apenas presença e compilação
  </acceptance_criteria>
  <done>
    sendLowCreditNotifications implementado no schedules.service.ts com push OneSignal + persistência LOW_CREDIT + bug fix de URL. TypeScript compila sem erros. Confirmação comportamental delegada à Task 2 (testes unitários).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Testes unitários para sendLowCreditNotifications + wiring no cron</name>
  <files>
    apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
    apps/api/src/plugins/cron.ts
  </files>
  <read_first>
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (OBRIGATÓRIO — ler arquivo inteiro para entender mocks existentes, padrão createMockFastify, mock do OneSignal, estrutura dos casos já existentes)
    - apps/api/src/plugins/cron.ts (OBRIGATÓRIO — ler arquivo inteiro para inserir a chamada no lugar certo, após processAutoBuy no cron de meia-noite)
  </read_first>
  <behavior>
    - Test 1: sendLowCreditNotifications — envia push quando creditBalance=2 < consumoSemanal=5 e sem auto-recharge e tem oneSignalPlayerId
    - Test 2: sendLowCreditNotifications — NÃO envia push quando autoRecharge.active=true
    - Test 3: sendLowCreditNotifications — NÃO envia push quando creditBalance >= consumoSemanal
    - Test 4: sendLowCreditNotifications — NÃO envia push quando oneSignalPlayerId = null (mas persiste Notification)
    - cron.ts: sendLowCreditNotifications é chamado após processAutoBuy dentro do cron de meia-noite
  </behavior>
  <action>
    Em schedules.service.test.ts: adicionar um bloco describe('sendLowCreditNotifications [CRED-09]') com os 4 casos de teste. Reutilizar o mock de OneSignal já existente no arquivo (vi.mock('@onesignal/node-onesignal', ...) e o createMockFastify existente).

    Para os mocks do Prisma necessários, o repositório usa this.prisma.schedule.findMany e this.repo.findUserById. Estruturar os mocks de acordo com o padrão do createMockFastify existente — mock de prisma.schedule.findMany retornando array de schedules com weeklyQty como objeto { seg: 2, ter: 0, qua: 1, qui: 2, sex: 0, sab: 0, dom: 0 }.

    Para o test 1 (envia push): mock user com creditBalance: 2, autoRecharge: null, oneSignalPlayerId: 'osp-id'. Consumo semanal = 5. Assert: createNotification foi chamado 1 vez com notification.additionalData.screen === 'creditos'.

    Para tests 2-4 (não envia): assert expect(createNotification).not.toHaveBeenCalled().

    Em cron.ts: adicionar após o bloco try/catch do processAutoBuy (manter padrão de try/catch isolado):
    try { await schedulesService.sendLowCreditNotifications(); fastify.log.info('[cron] sendLowCreditNotifications concluído') } catch (err) { fastify.log.error({ err }, '[cron] erro em sendLowCreditNotifications — servidor mantido ativo') }

    Atualizar o log de início do cron de meia-noite para incluir sendLowCreditNotifications:
    DE: '[cron] iniciando createDailyOrders + processAutoBuy'
    PARA: '[cron] iniciando createDailyOrders + processAutoBuy + sendLowCreditNotifications'
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npm test -- schedules.service.test.ts 2>&1 | tail -50</automated>
  </verify>
  <acceptance_criteria>
    - Comando npm test -- schedules.service.test.ts retorna PASS com 0 falhas
    - grep -c "sendLowCreditNotifications" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts retorna >= 4 (describe + 4 its)
    - grep -c "sendLowCreditNotifications" apps/api/src/plugins/cron.ts retorna >= 2 (chamada + log)
    - grep -c "client/comprar" apps/api/src/modules/schedules/schedules.service.ts retorna 0 (bug removido)
    - npm test (suite completa) retorna com 0 failed
  </acceptance_criteria>
  <done>
    4 testes unitários de sendLowCreditNotifications verdes (CRED-09 coberto). Cron de meia-noite chama sendLowCreditNotifications após processAutoBuy. Suite completa de testes da API verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron backend → OneSignal API | Backend envia push via ONESIGNAL_REST_API_KEY sem dados do usuário no payload |
| Cron backend → MongoDB | Leitura de schedules/users e escrita de Notification — operações internas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | sendLowCreditNotifications — envio duplicado por loop | mitigate | Loop processa cada schedule uma vez; sem estado persistente entre runs — idempotência natural pelo cron diário |
| T-08-06 | Denial of Service | Cron de meia-noite — falha em sendLowCreditNotifications derruba servidor | mitigate | try/catch isolado por operação em cron.ts — falha em sendLowCreditNotifications não impede createDailyOrders nem processAutoBuy |
| T-08-07 | Information Disclosure | Push de crédito insuficiente expõe saldo do cliente | accept | Saldo é dado do próprio usuário; notificação vai somente para o oneSignalPlayerId do usuário — sem exposição a terceiros |
| T-08-08 | Elevation of Privilege | processAutoBuy com URL /client/comprar (inexistente) | accept | Bug fix já incluído — risco de confusão de navegação corrigido para /client/creditos |
  T-08-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `cd apps/api && npm test` → suite completa verde (0 failed)
- `grep -c "sendLowCreditNotifications" apps/api/src/modules/schedules/schedules.service.ts` >= 2
- `grep -c "sendLowCreditNotifications" apps/api/src/plugins/cron.ts` >= 2
- `grep -v "^#" apps/api/src/modules/schedules/schedules.service.ts | grep -c "client/comprar"` retorna 0 (bug removido)
- `grep -c "LOW_CREDIT" apps/api/src/modules/schedules/schedules.service.ts` >= 1
</verification>

<success_criteria>
1. sendLowCreditNotifications implementado com push OneSignal + persistência LOW_CREDIT + skip para auto-recharge
2. Bug corrigido: processAutoBuy usa /client/creditos (não /client/comprar)
3. Cron de meia-noite chama sendLowCreditNotifications após processAutoBuy com try/catch isolado
4. 4 casos de teste unitários verdes cobrindo CRED-09 (envia / não envia com auto-recharge / não envia com saldo suficiente / não envia sem oneSignalPlayerId)
5. Suite completa de testes da API verde sem regressões
</success_criteria>

<output>
Criar `.planning/phases/08-finalizacao-pagamentos/08-02-SUMMARY.md` ao concluir
</output>
