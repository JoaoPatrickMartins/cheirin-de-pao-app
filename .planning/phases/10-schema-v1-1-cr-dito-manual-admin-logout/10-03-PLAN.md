---
phase: 10-schema-v1-1-cr-dito-manual-admin-logout
plan: 03
type: execute
wave: 2
depends_on:
  - 10-02
files_modified:
  - apps/web/src/pages/admin/ClientDetailView.tsx
  - apps/web/src/components/admin/AdminBottomNav.tsx
  - apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx
  - apps/web/src/pages/courier/CourierScreen.tsx
  - apps/web/src/pages/client/NotificationsScreen.tsx
  - apps/web/src/hooks/useOneSignalDeepLink.ts
autonomous: true
requirements:
  - CREDM-01
  - CREDM-02
  - CREDM-03
  - LGOUT-01
  - LGOUT-02

must_haves:
  truths:
    - "Admin vê botão '+ Adicionar créditos' abaixo do número de créditos no ClientDetailView"
    - "Ao clicar, modal bottom sheet abre com título 'Adicionar Créditos', input de quantidade e 4 chips de motivo"
    - "Confirmar envia POST /admin/clients/:id/grant-credits, modal fecha, toast de sucesso aparece por 2500ms"
    - "Entregador vê ícone LogOut no canto superior direito do header — clicar chama AuthContext.logout() sem dialog"
    - "Admin vê botão 'Sair' fora dos 5 tabs no AdminBottomNav — clicar abre dialog de confirmação"
    - "Dialog admin tem título 'Sair da conta?', botões 'Continuar na conta' e 'Sair' — 'Sair' chama AuthContext.logout()"
    - "NotificationsScreen exibe CREDIT_GRANTED com tom 'gold' e ícone 'coin'"
    - "CTA_CONFIG[CREDIT_GRANTED] = { label: 'Ver saldo', path: '/client/home' }"
    - "useOneSignalDeepLink navega para /client/home quando screen === 'home'"
    - "Teste AdminBottomNav atualizado para 6 botões (5 tabs + botão Sair)"
  artifacts:
    - path: "apps/web/src/pages/admin/ClientDetailView.tsx"
      provides: "Botão grant-credits + modal bottom sheet + toast inline"
      contains: "Adicionar Créditos"
    - path: "apps/web/src/components/admin/AdminBottomNav.tsx"
      provides: "Botão Sair fora do TABS array com dialog de confirmação"
      contains: "Sair da conta"
    - path: "apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx"
      provides: "Teste atualizado para 6 botões"
      contains: "toHaveLength(6)"
    - path: "apps/web/src/pages/courier/CourierScreen.tsx"
      provides: "Ícone LogOut no header com onClick → AuthContext.logout()"
      contains: "logout"
    - path: "apps/web/src/pages/client/NotificationsScreen.tsx"
      provides: "CREDIT_GRANTED em getTone (gold) + getIcon (coin) + CTA_CONFIG"
      contains: "CREDIT_GRANTED"
    - path: "apps/web/src/hooks/useOneSignalDeepLink.ts"
      provides: "Case screen === 'home' → navigate('/client/home')"
      contains: "'home'"
  key_links:
    - from: "ClientDetailView.tsx"
      to: "POST /admin/clients/:id/grant-credits"
      via: "apiFetch com body { quantity, reason }"
      pattern: "grant-credits"
    - from: "AdminBottomNav.tsx (botão Sair)"
      to: "AuthContext.logout()"
      via: "dialog confirm → logout()"
      pattern: "logout"
    - from: "CourierScreen.tsx (ícone logout)"
      to: "AuthContext.logout()"
      via: "onClick direto sem dialog"
      pattern: "logout"
    - from: "useOneSignalDeepLink.ts"
      to: "navigate('/client/home')"
      via: "case screen === 'home'"
      pattern: "client/home"
    - from: "NotificationsScreen.tsx CTA_CONFIG"
      to: "navigate('/client/home')"
      via: "cta.path"
      pattern: "CREDIT_GRANTED"
---

<objective>
Entrega todas as superfícies de UI da Phase 10: modal de concessão de créditos no ClientDetailView, logout do entregador (ícone no header), logout do admin (botão Sair com dialog no AdminBottomNav), suporte a CREDIT_GRANTED na NotificationsScreen e deep link 'home' no useOneSignalDeepLink. Cada feature é implementada de forma isolada para facilitar revisão.

Purpose: Fechar o loop de ponta a ponta — admin concede crédito, cliente recebe push, abre o app, notificação aparece com CTA 'Ver saldo' navegando para /client/home. Logout permite que entregador e admin encerrem sessão com segurança.

Output: 6 arquivos modificados com UI e lógica completas, teste AdminBottomNav atualizado para 6 botões, zero regressões nos testes existentes.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/STATE.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/10-schema-v1-1-cr-dito-manual-admin-logout/10-02-SUMMARY.md

<interfaces>
<!-- Contratos e padrões extraídos do codebase — executor não precisa re-ler esses arquivos -->

useAuth hook — contrato relevante:
  const { user, logout } = useAuth()
  // logout() encerra a sessão e redireciona para login
  // Usar o mesmo import path das outras telas: import { useAuth } from '../../hooks/useAuth'

apiFetch — padrão de uso nas telas admin:
  const res = await apiFetch('/admin/clients/:id/grant-credits', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ quantity, reason }),
  })
  // apiFetch injeta Authorization header automaticamente

Toast inline pattern (de ScheduleScreen.tsx):
  const [toast, setToast] = useState<{ message: string; ok: boolean } | null>(null)
  setToast({ message: `${quantity} crédito(s) adicionado(s) a ${nome}`, ok: true })
  setTimeout(() => setToast(null), 2500)
  // JSX: position: fixed, top: 16, left: 50%, transform: translateX(-50%)
  // z-index: 9999, background: var(--color-espresso), cor de texto branca
  // border-radius: 20px, padding: 10px 20px, font-size: 14px, font-weight: 700

Modal bottom sheet specs (do UI-SPEC aprovado):
  Overlay: position fixed, inset 0, background rgba(0,0,0,0.5), z-index 50
  Container: position fixed, bottom 0, left 0, right 0, background var(--color-app-bg)
             border-radius 20px 20px 0 0, padding 24px 20px calc(32px + env(safe-area-inset-bottom, 0px))
             max-height 80vh, overflow-y auto, z-index 51
  Handle bar: width 36px, height 4px, border-radius 999px, background var(--color-border), margin-bottom 20px
  role="dialog", aria-modal="true", aria-labelledby="modal-grant-title"
  Input de quantidade: autoFocus ao abrir, type="number", min="1"
  Escape fecha o modal (useEffect com keydown listener)

Chips de motivo specs (do UI-SPEC aprovado):
  4 valores: 'Acerto', 'Bonificação', 'Compensação', 'Promoção'
  Layout: flex row, gap 8px, flex-wrap wrap
  Não selecionado: border 1.5px solid var(--color-border), background transparent, color var(--color-text)
  Selecionado: border none, background var(--color-gold), color var(--color-espresso) (#1E1207)
  Estilo base: min-height 44px, padding 8px 16px, border-radius 999px, font-weight 700, font-size 13px
  aria-pressed={motivo === m} em cada chip

Validação do botão confirmar:
  Habilitado apenas quando: quantity >= 1 AND motivo selecionado
  Fora disso: disabled, opacity 0.45

Copywriting confirmado:
  Modal title id: "modal-grant-title", texto: "Adicionar Créditos"
  Trigger button: "+ Adicionar créditos" (ghost pequeno)
  Cancelar: "Descartar"
  Confirmar normal: "Adicionar créditos"
  Confirmar loading: "Confirmando..."
  Toast: `${quantity} crédito(s) adicionado(s) a ${nome}`

Icon logout path (de Icon.tsx linha 32):
  logout: 'M15 12H4M11 8l-4 4 4 4M14 4h5a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1h-5'
  Uso: <Icon name="logout" size={22} color="var(--color-text-ter)" />

AdminBottomNav — estrutura atual:
  type AdminTab = 'painel' | 'pedido' | 'entregas' | 'clientes' | 'gestao'
  const TABS: TabItem[] = [ ...5 tabs... ]
  export function AdminBottomNav({ activeTab, onTabChange }) { ... mapeia TABS ... }
  // Pitfall 6: NÃO adicionar 'logout' ao tipo AdminTab nem ao array TABS
  // Implementar 'Sair' como botão especial APÓS o map de TABS, com useState local para dialog

CourierScreen header — estrutura atual:
  <div style={{ display: 'flex', justifyContent: 'space-between', ... }}>
    <div>  {/* Avatar BreadMark à esquerda */}  </div>
    <div style={{ textAlign: 'right' }}>  {/* Textos à direita */}  </div>
  </div>
  // D-08: adicionar botão logout no canto superior direito do header
  // Mas "direita" já tem textos — reestruturar para: BreadMark | espaço flex | botão logout | textos
  // Ou: manter estrutura e adicionar ícone logout dentro do div da direita, acima dos textos
  // Padrão seguro: adicionar <button> com Icon name="logout" dentro do div de texto, acima dos <p>

NotificationsScreen — funções a modificar:
  function getTone(type: string): Tone {
    // Adicionar: if (['CREDIT_GRANTED'].includes(type)) return 'gold'
    // Antes do return 'neutral'
  }
  function getIcon(type: string) {
    // Adicionar: if (type === 'CREDIT_GRANTED') return 'coin'
    // Antes do return 'repeat' (fallback)
  }
  const CTA_CONFIG: Record<string, { label: string; path: string }> = {
    // Adicionar: CREDIT_GRANTED: { label: 'Ver saldo', path: '/client/home' }
  }

useOneSignalDeepLink — case a adicionar:
  // Após o case 'pedidos', adicionar:
  } else if (screen === 'home') {
    navigate('/client/home')
  }

AdminBottomNav test — linha a atualizar:
  // Linha 15: expect(buttons).toHaveLength(5)  →  toHaveLength(6)
  // Adicionar novo teste verificando o botão Sair e o dialog de confirmação
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modal grant-credits no ClientDetailView + logout no CourierScreen + NotificationsScreen + deeplink</name>
  <files>
    apps/web/src/pages/admin/ClientDetailView.tsx
    apps/web/src/pages/courier/CourierScreen.tsx
    apps/web/src/pages/client/NotificationsScreen.tsx
    apps/web/src/hooks/useOneSignalDeepLink.ts
  </files>
  <read_first>
    - apps/web/src/pages/admin/ClientDetailView.tsx (componente completo — identificar onde fica o card de saldo)
    - apps/web/src/pages/courier/CourierScreen.tsx (header completo — já lido, usar para localizar ponto de inserção)
    - apps/web/src/pages/client/NotificationsScreen.tsx (funções getTone, getIcon e CTA_CONFIG — já lido)
    - apps/web/src/hooks/useOneSignalDeepLink.ts (handleClick switch — já lido)
  </read_first>
  <action>
    ARQUIVO 1 — apps/web/src/pages/admin/ClientDetailView.tsx:

    Ler o arquivo completamente primeiro para localizar o card de saldo (provavelmente exibe cliente.creditBalance). Identificar onde está a seção de saldo para inserir o botão e o modal.

    Adicionar os seguintes estados ao componente (junto com os estados existentes):
      const [showGrantModal, setShowGrantModal] = useState(false)
      const [grantQty, setGrantQty] = useState(1)
      const [grantMotivo, setGrantMotivo] = useState<string | null>(null)
      const [grantLoading, setGrantLoading] = useState(false)
      const [toast, setToast] = useState<{ message: string; ok: boolean } | null>(null)

    Adicionar a função handleGrant ao componente:
      async function handleGrant() {
        if (!grantMotivo || grantQty < 1) return
        setGrantLoading(true)
        try {
          const res = await apiFetch(`/admin/clients/${cliente.id}/grant-credits`, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ quantity: grantQty, reason: grantMotivo }),
          })
          if (res.ok) {
            const updated = (await res.json()) as { creditBalance: number }
            setCliente((prev) => prev ? { ...prev, creditBalance: updated.creditBalance } : prev)
            setShowGrantModal(false)
            setGrantQty(1)
            setGrantMotivo(null)
            setToast({ message: `${grantQty} crédito(s) adicionado(s) a ${cliente.name}`, ok: true })
            setTimeout(() => setToast(null), 2500)
          }
        } catch {
          // silencioso — mostrar toast de erro se desejar
        } finally {
          setGrantLoading(false)
        }
      }

    No JSX — abaixo do número de créditos (D-03), adicionar o botão trigger:
      <button
        onClick={() => setShowGrantModal(true)}
        aria-label="Adicionar créditos"
        style={{ background: 'none', border: '1.5px solid var(--color-border)', borderRadius: 999, padding: '6px 14px', fontFamily: 'var(--font-body)', fontSize: 13, fontWeight: 700, color: 'var(--color-text)', cursor: 'pointer' }}
      >
        + Adicionar créditos
      </button>

    No JSX — no final do return, antes do fechamento da div raiz, adicionar o toast:
      {toast && (
        <div role="status" style={{ position: 'fixed', top: 16, left: '50%', transform: 'translateX(-50%)', zIndex: 9999, background: 'var(--color-espresso)', color: '#fff', borderRadius: 20, padding: '10px 20px', fontSize: 14, fontWeight: 700, whiteSpace: 'nowrap' }}>
          {toast.message}
        </div>
      )}

    No JSX — após o toast, adicionar o modal bottom sheet quando showGrantModal === true:
      Estrutura: overlay div + container div com handle bar, título id="modal-grant-title", input de quantidade (type="number", min="1", autoFocus, value={grantQty}, onChange → setGrantQty(Number(e.target.value))), seção de chips de motivo e botões de ação.

      Overlay: onClick={() => setShowGrantModal(false)}
      Container: role="dialog", aria-modal="true", aria-labelledby="modal-grant-title", onClick={e => e.stopPropagation()}

      Handle bar conforme UI-SPEC (36px × 4px, border-radius 999px, background var(--color-border), margin auto margin-bottom 20px).

      Título h2 id="modal-grant-title": "Adicionar Créditos", font-family var(--font-display), font-weight 700, font-size 19.

      Label + input de quantidade: label "Quantidade", input type="number" min="1" com os estilos padrão de input do projeto.

      Label de chips: "Motivo", flex row gap 8px flex-wrap wrap. Para cada motivo em ['Acerto', 'Bonificação', 'Compensação', 'Promoção']:
        <button
          key={m}
          aria-pressed={grantMotivo === m}
          onClick={() => setGrantMotivo(m)}
          style={{
            minHeight: 44, padding: '8px 16px', borderRadius: 999,
            fontWeight: 700, fontSize: 13, cursor: 'pointer',
            border: grantMotivo === m ? 'none' : '1.5px solid var(--color-border)',
            background: grantMotivo === m ? 'var(--color-gold)' : 'transparent',
            color: grantMotivo === m ? '#1E1207' : 'var(--color-text)',
          }}
        >{m}</button>

      Botões de ação (flex row gap 12 margin-top 24):
        Descartar: background none, border 1.5px solid var(--color-border), onClick={() => setShowGrantModal(false)}
        Adicionar créditos: background var(--color-accent) (dourado), color #1E1207, disabled={!grantMotivo || grantQty < 1 || grantLoading}, opacity={!grantMotivo || grantQty < 1 ? 0.45 : 1}, onClick={handleGrant}
        Texto do botão confirmar: grantLoading ? 'Confirmando...' : 'Adicionar créditos'

      useEffect para fechar com Escape:
        useEffect(() => {
          if (!showGrantModal) return
          const handler = (e: KeyboardEvent) => { if (e.key === 'Escape') setShowGrantModal(false) }
          document.addEventListener('keydown', handler)
          return () => document.removeEventListener('keydown', handler)
        }, [showGrantModal])

    ARQUIVO 2 — apps/web/src/pages/courier/CourierScreen.tsx:

    Adicionar import do useAuth (já importado, verificar): const { logout } = useAuth() — adicionar logout à desestruturação.

    No header, dentro do div de textos (textAlign: right), adicionar um botão logout ANTES dos dois <p>:
      <button
        onClick={logout}
        aria-label="Sair"
        style={{ background: 'none', border: 'none', cursor: 'pointer', padding: 4, display: 'flex', alignItems: 'center', justifyContent: 'flex-end', marginBottom: 4 }}
      >
        <Icon name="logout" size={22} color="var(--color-text-ter)" />
      </button>
    Sem dialog — clique chama logout() diretamente (D-08).

    ARQUIVO 3 — apps/web/src/pages/client/NotificationsScreen.tsx:

    Modificar getTone: adicionar antes do return 'neutral':
      if (['CREDIT_GRANTED'].includes(type)) return 'gold'

    Modificar getIcon: adicionar antes do return 'repeat':
      if (type === 'CREDIT_GRANTED') return 'coin'

    Modificar CTA_CONFIG: adicionar entrada:
      CREDIT_GRANTED: { label: 'Ver saldo', path: '/client/home' },

    ARQUIVO 4 — apps/web/src/hooks/useOneSignalDeepLink.ts:

    No handleClick, após o else if (screen === 'pedidos') { navigate('/client/pedidos') }, adicionar:
      } else if (screen === 'home') {
        navigate('/client/home')
      }
    Atualizar o comentário do threat model para incluir 'home' como rota válida.
  </action>
  <verify>
    <automated>
      cd /home/joaopatrick/Projects/cheirin-de-pao-app && grep -c "grant-credits" apps/web/src/pages/admin/ClientDetailView.tsx && grep -c "Adicionar Créditos" apps/web/src/pages/admin/ClientDetailView.tsx && grep -c "CREDIT_GRANTED" apps/web/src/pages/client/NotificationsScreen.tsx && grep -c "client/home" apps/web/src/hooks/useOneSignalDeepLink.ts && grep -c "logout" apps/web/src/pages/courier/CourierScreen.tsx
    </automated>
  </verify>
  <acceptance_criteria>
    grep "grant-credits" em ClientDetailView.tsx retorna >= 1 (apiFetch call).
    grep "Adicionar Créditos" em ClientDetailView.tsx retorna >= 1 (título do modal).
    grep "CREDIT_GRANTED" em NotificationsScreen.tsx retorna >= 3 (getTone + getIcon + CTA_CONFIG).
    grep "client/home" em useOneSignalDeepLink.ts retorna >= 1 (novo case).
    grep "logout" em CourierScreen.tsx retorna >= 1 (botão no header).
    npx tsc --noEmit em apps/web/ passa sem erros nos 4 arquivos.
  </acceptance_criteria>
  <done>Modal grant-credits funcional, logout entregador implementado, NotificationsScreen suporta CREDIT_GRANTED, deeplink 'home' registrado.</done>
</task>

<task type="auto">
  <name>Task 2: Logout Admin no AdminBottomNav + atualizar teste para 6 botões</name>
  <files>
    apps/web/src/components/admin/AdminBottomNav.tsx
    apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx
  </files>
  <read_first>
    - apps/web/src/components/admin/AdminBottomNav.tsx (completo — já lido nesta sessão)
    - apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx (completo — já lido nesta sessão)
  </read_first>
  <action>
    ARQUIVO 1 — AdminBottomNav.tsx:

    Adicionar imports necessários no topo:
      import { useState } from 'react'
      import { useAuth } from '../../hooks/useAuth'  // verificar path relativo correto para o componente

    Dentro da função AdminBottomNav, adicionar:
      const { logout } = useAuth()
      const [showLogoutDialog, setShowLogoutDialog] = useState(false)

    NÃO modificar o tipo AdminTab — não adicionar 'logout' (Pitfall 6).
    NÃO adicionar 'Sair' ao array TABS (Pitfall 6).

    No JSX — dentro do <nav>, após o map de TABS (após o fechamento do TABS.map), adicionar o botão Sair como elemento irmão:
      <button
        onClick={() => setShowLogoutDialog(true)}
        aria-label="Sair"
        style={{
          flex: 1,
          minHeight: 44,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 4,
          background: 'none',
          border: 'none',
          cursor: 'pointer',
          padding: '5px 0',
        }}
      >
        <Icon name="logout" size={22} stroke={2} color="var(--color-text-ter)" />
        <span style={{ fontFamily: 'var(--font-body)', fontSize: 10, fontWeight: 600, color: 'var(--color-text-ter)', lineHeight: 1 }}>
          Sair
        </span>
      </button>

    Após o fechamento do <nav>, adicionar o dialog de confirmação condicional (fora do nav — dialog é um overlay):
      {showLogoutDialog && (
        <div
          role="dialog"
          aria-modal="true"
          aria-labelledby="dialog-logout-title"
          style={{ position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', zIndex: 200, display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '0 24px' }}
        >
          <div style={{ background: 'var(--color-surface)', borderRadius: 20, padding: '28px 24px', width: '100%', maxWidth: 360 }}>
            <h2 id="dialog-logout-title" style={{ fontFamily: 'var(--font-display)', fontWeight: 700, fontSize: 19, color: 'var(--color-text)', margin: '0 0 8px' }}>
              Sair da conta?
            </h2>
            <p style={{ fontFamily: 'var(--font-body)', fontSize: 14, color: 'var(--color-text-sec)', margin: '0 0 24px' }}>
              Você será redirecionado para a tela de login.
            </p>
            <div style={{ display: 'flex', gap: 12 }}>
              <button
                onClick={() => setShowLogoutDialog(false)}
                style={{ flex: 1, minHeight: 44, borderRadius: 999, border: '1.5px solid var(--color-border)', background: 'none', fontFamily: 'var(--font-body)', fontWeight: 700, fontSize: 15, color: 'var(--color-text)', cursor: 'pointer' }}
              >
                Continuar na conta
              </button>
              <button
                onClick={logout}
                style={{ flex: 1, minHeight: 44, borderRadius: 999, border: 'none', background: 'var(--color-espresso)', fontFamily: 'var(--font-body)', fontWeight: 700, fontSize: 15, color: '#fff', cursor: 'pointer' }}
              >
                Sair
              </button>
            </div>
          </div>
        </div>
      )}

    O componente retorna um Fragment (<>) englobando o nav e o dialog, ou usa uma div wrapper — escolher Fragment para não alterar o layout do nav.

    ARQUIVO 2 — AdminBottomNav.test.tsx:

    Adicionar mock do useAuth antes dos testes para evitar erro de contexto:
      vi.mock('../../hooks/useAuth', () => ({
        useAuth: () => ({ logout: vi.fn() }),
      }))
    Ajustar o import path relativo conforme a localização do arquivo de teste.

    Atualizar o primeiro teste:
      Linha que diz toHaveLength(5) → toHaveLength(6)
      Atualizar comentário: "Deve renderizar exatamente 6 botões de navegação (5 tabs + botão Sair)"

    Adicionar novo teste ao describe('AdminBottomNav'):
      it('renderiza botão Sair com aria-label correto', () => {
        const onTabChange = vi.fn()
        render(<AdminBottomNav activeTab="painel" onTabChange={onTabChange} />)
        const sairButton = screen.getByRole('button', { name: 'Sair' })
        expect(sairButton).toBeDefined()
      })

    Manter os 2 outros testes existentes sem alteração.
  </action>
  <verify>
    <automated>
      cd /home/joaopatrick/Projects/cheirin-de-pao-app && npx vitest run apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx --reporter=verbose 2>&1 | tail -20
    </automated>
  </verify>
  <acceptance_criteria>
    Todos os testes de AdminBottomNav passam (0 failed).
    grep "toHaveLength(6)" apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx retorna 1.
    grep "Sair da conta" apps/web/src/components/admin/AdminBottomNav.tsx retorna 1 (dialog title).
    grep "logout" apps/web/src/components/admin/AdminBottomNav.tsx retorna >= 2 (useAuth e onClick).
    AdminTab type NÃO inclui 'logout' — grep "'logout'" apps/web/src/components/admin/AdminBottomNav.tsx | grep "AdminTab" retorna 0.
    npx tsc --noEmit em apps/web/ passa sem erros nos 2 arquivos.
  </acceptance_criteria>
  <done>Botão Sair com dialog no AdminBottomNav; teste atualizado para 6 botões; todos os testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Modal → POST /admin/clients/:id/grant-credits | Input de quantity e reason do formulário admin; validado pelo Zod no backend (Plan 02) |
| useOneSignalDeepLink → navigate() | additionalData.screen vem do push OneSignal; apenas navega para rotas internas |
| Dialog logout → AuthContext.logout() | Ação destrutiva de sessão; confirmação explícita exigida para admin (D-09) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Spoofing | Modal grant-credits | mitigate | apiFetch injeta token JWT automaticamente; backend valida role ADMIN |
| T-10-03-02 | Tampering | useOneSignalDeepLink additionalData.screen | accept | Apenas navega para rotas internas fixas (/client/home); nenhuma ação destrutiva possível via deep link |
| T-10-03-03 | Denial of Service | Logout sem confirmação (entregador) | accept | Logout de entregador é de baixo risco; re-autenticação via OTP é simples |
| T-10-03-04 | Spoofing | Dialog logout admin | mitigate | Dialog de confirmação previne logout acidental; texto claro "Sair da conta?" com CTA destrutivo separado |
| T-10-03-05 | Information Disclosure | Modal fecha sem enviar | accept | Estado local limpo ao fechar; quantity e reason não persistidos no cliente |
| T-10-03-SC | Tampering | npm/npx installs | accept | Sem novas dependências neste plan; apenas edições em arquivos existentes |
</threat_model>

<verification>
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npx vitest run apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx 2>&1 | grep -E "passed|failed"
grep -c "Adicionar Créditos" apps/web/src/pages/admin/ClientDetailView.tsx
grep -c "grant-credits" apps/web/src/pages/admin/ClientDetailView.tsx
grep -c "CREDIT_GRANTED" apps/web/src/pages/client/NotificationsScreen.tsx
grep -c "client/home" apps/web/src/hooks/useOneSignalDeepLink.ts
grep -c "logout" apps/web/src/pages/courier/CourierScreen.tsx
grep "toHaveLength" apps/web/src/components/admin/__tests__/AdminBottomNav.test.tsx
</verification>

<success_criteria>
- Todos os testes do AdminBottomNav passam (0 failed), incluindo o novo teste do botão Sair
- toHaveLength(6) presente no teste (5 tabs + 1 Sair)
- Modal grant-credits renderizável com overlay, handle bar, input, 4 chips e botões
- Toast "N crédito(s) adicionado(s) a Nome" aparece 2500ms após grant bem-sucedido
- Ícone logout visível no header do CourierScreen, chama logout() diretamente
- Dialog admin 'Sair da conta?' com botões 'Continuar na conta' e 'Sair'
- CREDIT_GRANTED em getTone retorna 'gold', getIcon retorna 'coin', CTA_CONFIG aponta /client/home
- useOneSignalDeepLink navega para /client/home quando screen === 'home'
- Zero erros TypeScript em todos os 6 arquivos modificados
</success_criteria>

<output>
Criar .planning/phases/10-schema-v1-1-cr-dito-manual-admin-logout/10-03-SUMMARY.md ao concluir.
</output>
