---
phase: 10-schema-v1-1-cr-dito-manual-admin-logout
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts
autonomous: true
requirements:
  - CREDM-01
  - CREDM-02
  - CREDM-03
  - LGOUT-01
  - LGOUT-02

must_haves:
  truths:
    - "prisma db push executa sem erros e TransactionType.ADMIN_GRANT existe no schema"
    - "NotificationType.CREDIT_GRANTED existe no schema"
    - "CreditTransaction possui campos adminId e reason opcionais"
    - "User possui campo mpCustomerId opcional"
    - "model SavedCard existe com os 8 campos especificados em D-07"
    - "Condominium possui campo deliverySlots String[]"
    - "Schedule possui campo days Json?"
    - "Os test stubs para grantCredits existem no arquivo de testes do AdminClientsService"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Schema v1.1 com todos os campos de D-01 e D-02"
      contains: "ADMIN_GRANT"
    - path: "apps/api/prisma/schema.prisma"
      provides: "Enum NotificationType atualizado"
      contains: "CREDIT_GRANTED"
    - path: "apps/api/prisma/schema.prisma"
      provides: "Model SavedCard mínimo viável"
      contains: "model SavedCard"
    - path: "apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts"
      provides: "Stubs de teste para grantCredits (RED phase)"
      contains: "grantCredits"
  key_links:
    - from: "TransactionType enum"
      to: "CreditTransaction.type"
      via: "Prisma schema field type"
      pattern: "ADMIN_GRANT"
    - from: "NotificationType enum"
      to: "Notification.type"
      via: "Prisma schema field type"
      pattern: "CREDIT_GRANTED"
    - from: "CreditTransaction model"
      to: "adminId field"
      via: "optional ObjectId reference"
      pattern: "adminId.*String.*@db\\.ObjectId"
---

<objective>
Aplica o Schema v1.1 de uma vez — todos os campos que desbloqueiam as Phases 11–14 — via prisma db push seguido de prisma generate (nunca migrate dev). Adiciona também os test stubs (RED phase) para grantCredits no arquivo de testes existente do AdminClientsService, garantindo que o Plan 02 comece com testes reprovando corretamente.

Purpose: Uma única migração atômica evita drift entre fases e garante que todos os módulos downstream compilem contra o schema correto desde o início.

Output: schema.prisma atualizado com 8 mudanças de D-02, client Prisma regenerado, e stubs de teste grantCredits em estado RED no arquivo admin-clients.service.test.ts.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/STATE.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/prisma/schema.prisma
@/home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts

<interfaces>
<!-- Estado atual do schema.prisma — extraído em leitura única -->
<!-- Executor NÃO precisa re-ler o schema; use as definições abaixo como referência. -->

Schema atual — enums relevantes:

  enum TransactionType {
    PURCHASE
    DELIVERY
    REFUND
    EXPIRY
    // Adicionar: ADMIN_GRANT
  }

  enum NotificationType {
    DELIVERY_EVE
    DELIVERY_DONE
    LOW_CREDIT
    CUTOFF
    RECONFIGURE
    CREDIT_PURCHASED
    // Adicionar: CREDIT_GRANTED
  }

Schema atual — models relevantes (campos a adicionar):

  model CreditTransaction {  // adicionar adminId e reason
    id          String          @id @default(auto()) @map("_id") @db.ObjectId
    userId      String          @db.ObjectId
    type        TransactionType
    quantity    Int
    referenceId String?
    description String?
    createdAt   DateTime        @default(now())
    // Adicionar:
    // adminId String? @db.ObjectId
    // reason  String?
  }

  model User {  // adicionar mpCustomerId
    // ... campos existentes ...
    cardTokenMp       String?
    oneSignalPlayerId String?
    // Adicionar: mpCustomerId String?
    createdAt         DateTime @default(now())
    updatedAt         DateTime @updatedAt
  }

  model Condominium {  // adicionar deliverySlots
    // ... campos existentes ...
    isActive  Boolean   @default(true)
    // Adicionar: deliverySlots String[]
    createdAt DateTime  @default(now())
    updatedAt DateTime  @updatedAt
  }

  model Schedule {  // adicionar days
    // ... campos existentes ...
    isActive          Boolean  @default(true)
    // Adicionar: days Json?
    createdAt         DateTime @default(now())
    updatedAt         DateTime @updatedAt
  }

  // Novo model a criar (D-07):
  model SavedCard {
    id        String   @id @default(auto()) @map("_id") @db.ObjectId
    userId    String   @db.ObjectId
    mpCardId  String
    brand     String
    lastFour  String
    expiresAt String
    isDefault Boolean  @default(false)
    createdAt DateTime @default(now())
  }

Arquivo de testes existente — describe blocks presentes:
  describe('AdminClientsService') → describe('list'), describe('getDetail'), describe('blockToggle')
  // Adicionar: describe('grantCredits') com 4 stubs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aplicar Schema v1.1 (D-01, D-02) e executar prisma db push + generate</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (schema completo atual — leitura única, extrair tudo de uma vez)
  </read_first>
  <action>
    Editar apps/api/prisma/schema.prisma aplicando exatamente as 8 mudanças de D-02. Não alterar nenhum campo existente, só adicionar.

    Mudança 1 — enum TransactionType: adicionar valor ADMIN_GRANT após EXPIRY.

    Mudança 2 — enum NotificationType: adicionar valor CREDIT_GRANTED após CREDIT_PURCHASED.

    Mudança 3 — model CreditTransaction: após o campo description String?, adicionar dois campos novos:
      adminId String? @db.ObjectId
      reason  String?
    Manter o campo createdAt e o comentário de imutabilidade no topo do model.

    Mudança 4 — model User: após oneSignalPlayerId String?, adicionar:
      mpCustomerId String?
    Inserir antes de createdAt para manter agrupamento de campos opcionais.

    Mudança 5 — model SavedCard: criar novo model completo após o model Payment (que termina na linha do segundo updatedAt). O model deve ter exatamente os campos de D-07:
      id String @id @default(auto()) @map("_id") @db.ObjectId
      userId String @db.ObjectId
      mpCardId String
      brand String
      lastFour String
      expiresAt String
      isDefault Boolean @default(false)
      createdAt DateTime @default(now())
    Adicionar comentário de linha acima: "// 18. SavedCard — cartões salvos do cliente no Mercado Pago"
    Usar numeração 18 (schema já tem 17 models: User...OtpCode).

    Mudança 6 — model Condominium: após isActive Boolean @default(true), adicionar:
      deliverySlots String[]

    Mudança 7 — model Schedule: após isActive Boolean @default(true), adicionar:
      days Json?

    Mudança 8 — Não há mudança 8 explícita além das 7 acima; D-02 lista 8 itens mas SavedCard é o item 5 e cobre o SavedCard mínimo viável.

    Após salvar o schema, executar no terminal a partir do diretório apps/api:
      cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma db push
    Depois, executar prisma generate (Pitfall 3):
      npx prisma generate

    Se prisma db push retornar erro de validação, corrigir o schema antes de continuar.
    Nunca usar prisma migrate dev (header do schema proíbe explicitamente).
  </action>
  <verify>
    <automated>
      cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && grep -c "ADMIN_GRANT" prisma/schema.prisma && grep -c "CREDIT_GRANTED" prisma/schema.prisma && grep -c "adminId" prisma/schema.prisma && grep -c "mpCustomerId" prisma/schema.prisma && grep -c "model SavedCard" prisma/schema.prisma && grep -c "deliverySlots" prisma/schema.prisma && grep -c "days Json" prisma/schema.prisma
    </automated>
  </verify>
  <acceptance_criteria>
    Cada grep retorna 1 (ou mais para adminId que aparece na definição e no campo).
    prisma db push conclui sem erros.
    prisma generate conclui sem erros TypeScript.
    npx prisma validate passa sem warnings.
  </acceptance_criteria>
  <done>Schema v1.1 aplicado com 8 mudanças, client Prisma regenerado, zero erros de compilação.</done>
</task>

<task type="auto">
  <name>Task 2: Adicionar test stubs grantCredits (RED phase) ao arquivo de testes existente</name>
  <files>apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts</files>
  <read_first>
    - apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts (arquivo completo — leitura única)
  </read_first>
  <action>
    Editar admin-clients.service.test.ts adicionando um novo describe block 'grantCredits' ao final do describe('AdminClientsService'), após o describe('blockToggle') existente.

    O describe block deve conter 4 testes escritos em RED phase — os testes descrevem o comportamento esperado mas falham porque o método grantCredits ainda não existe na service.

    Atualizar também o makeFastifyMock para suportar os mocks necessários para grantCredits. Adicionar ao objeto prisma dentro de makeFastifyMock:
      - prisma.$transaction: vi.fn() que resolve com um array onde o segundo elemento é { id: 'user-01', name: 'João Cliente', creditBalance: 15 } (saldo atualizado após grant de 5 créditos)
      - No mock de prisma.creditTransaction: adicionar create: vi.fn().mockResolvedValue({ id: 'tx-02', userId: 'user-01', type: 'ADMIN_GRANT', quantity: 5 })
      - No mock de prisma.user: manter o update existente (que já resolve)

    Os 4 testes do describe('grantCredits'):

    Teste 1: 'cria CreditTransaction ADMIN_GRANT e incrementa creditBalance via $transaction'
    — chama service.grantCredits('user-01', { quantity: 5, reason: 'Acerto', adminId: 'admin-01' })
    — verifica que prisma.$transaction foi chamado
    — verifica que o resultado tem creditBalance igual a 15

    Teste 2: 'lança { statusCode: 404 } quando cliente não existe'
    — usar makeFastifyMock({ client: null })
    — await expect(service.grantCredits('id-inexistente', { quantity: 5, reason: 'Acerto', adminId: 'admin-01' })).rejects.toMatchObject({ statusCode: 404, message: expect.stringMatching(/não encontrado/i) })

    Teste 3: 'lança { statusCode: 404 } quando user não é CLIENT'
    — usar makeFastifyMock({ client: { id: 'admin-01', name: 'Admin User', role: 'ADMIN', isBlocked: false } })
    — mesma asserção de 404

    Teste 4: 'lança { statusCode: 400 } quando quantity é menor que 1'
    — usar makeFastifyMock()
    — await expect(service.grantCredits('user-01', { quantity: 0, reason: 'Acerto', adminId: 'admin-01' })).rejects.toMatchObject({ statusCode: 400 })

    Não importar nem referenciar grantCredits diretamente — o método não existe ainda. Os testes devem ser escritos para falhar com "service.grantCredits is not a function" quando executados.

    Manter todos os imports existentes. Não remover nenhum teste existente.
  </action>
  <verify>
    <automated>
      cd /home/joaopatrick/Projects/cheirin-de-pao-app && npx vitest run apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|grantCredits)"
    </automated>
  </verify>
  <acceptance_criteria>
    Os 4 novos testes de grantCredits falham (FAIL) com "is not a function" ou similar — confirma RED phase.
    Os testes existentes (list, getDetail, blockToggle) continuam passando (PASS).
    grep -c "grantCredits" retorna >= 4 no arquivo de testes.
  </acceptance_criteria>
  <done>4 stubs de teste grantCredits em RED, testes existentes em GREEN. Pronto para Plan 02 implementar a service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Prisma DB Push | Executor aplica schema ao MongoDB Atlas remoto; não há validação prévia de dados existentes |
| Schema → Prisma Client | Schema atualizado deve ser compilável pelo TypeScript client gerado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | prisma/schema.prisma | mitigate | Executor lê o schema antes de editar; verifica com prisma validate antes de db push |
| T-10-01-02 | Denial of Service | prisma db push para Atlas remoto | accept | Atlas remoto em dev — falha de rede abortará o comando; executor re-executa após resolução |
| T-10-01-SC | Tampering | npm/npx installs | mitigate | Apenas npx prisma (já em package.json); sem novas dependências neste plan |
</threat_model>

<verification>
cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma validate
cd /home/joaopatrick/Projects/cheirin-de-pao-app && grep -E "ADMIN_GRANT|CREDIT_GRANTED|adminId|mpCustomerId|SavedCard|deliverySlots|days Json" apps/api/prisma/schema.prisma | wc -l
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npx vitest run apps/api/src/modules/admin-clients/__tests__/admin-clients.service.test.ts 2>&1 | tail -20
</verification>

<success_criteria>
- prisma validate: zero erros
- 7 grep matches (um por mudança de D-02) no schema.prisma
- model SavedCard presente com exatamente os campos de D-07
- Testes existentes (list, getDetail, blockToggle): todos PASS
- 4 novos testes grantCredits: todos FAIL (RED confirmado)
- prisma generate concluído sem erros TypeScript
</success_criteria>

<output>
Criar .planning/phases/10-schema-v1-1-cr-dito-manual-admin-logout/10-01-SUMMARY.md ao concluir.
</output>
