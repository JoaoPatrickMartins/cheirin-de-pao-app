---
phase: 11-configura-es-e-perfil-do-cliente
plan: 05
type: execute
wave: 5
depends_on:
  - 11-03
  - 11-04
files_modified:
  - apps/web/src/pages/client/ScheduleScreen.tsx
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
must_haves:
  truths:
    - "ScheduleScreen exibe banner 'Você mudou de condomínio. Configure sua nova agenda semanal.' quando condominiumJustChanged=true"
    - "Banner desaparece após usuário configurar agenda com pelo menos 1 dia"
    - "Tela de configurações completa está acessível pelo 5º tab Perfil"
    - "Edição de contato com OTP funciona end-to-end no ambiente dev"
  artifacts:
    - path: "apps/web/src/pages/client/ScheduleScreen.tsx"
      provides: "Banner contextual de mudança de condomínio"
      contains: "condominiumJustChanged"
  key_links:
    - from: "apps/web/src/pages/client/ScheduleScreen.tsx"
      to: "apps/web/src/contexts/AuthContext.tsx"
      via: "user.condominiumJustChanged — campo adicionado no Plano 03"
      pattern: "condominiumJustChanged"
---

<objective>
Adiciona o banner contextual de mudança de condomínio na `ScheduleScreen` e executa verificação humana end-to-end da fase completa.

Purpose: Fecha o ciclo CONF-06 (banner informa que agenda foi desativada) e confirma que toda a UX de configurações funciona corretamente no dispositivo real.

Output: `ScheduleScreen.tsx` com banner adicionado + checkpoint humano de verificação final.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/11-configura-es-e-perfil-do-cliente/11-UI-SPEC.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/11-configura-es-e-perfil-do-cliente/11-04-SUMMARY.md

<interfaces>
<!-- AuthUser.condominiumJustChanged (adicionado no Plano 03) -->
condominiumJustChanged?: boolean  // true após PATCH com condominiumId mudado

<!-- ScheduleScreen padrão de acesso ao user (linhas 32-33) -->
const { user } = useAuth()

<!-- Banner visual — baseado em UI-SPEC.md seção "Banner de Agenda na ScheduleScreen" -->
// Fundo: --color-gold-soft (#F3DDA6)
// Borda esquerda: 3px solid var(--color-accent)
// Ícone: 'alert' (cor var(--color-accent))
// Texto: 15px (--text-base), var(--color-text), fontWeight 600
// Radius: 12px
// Padding: 12px 16px
// Posição: abaixo do AppBar, acima do conteúdo principal da agenda

<!-- Como limpar o banner: após usuário salvar agenda com weeklyQty com pelo menos 1 dia > 0 -->
// Chamar auth.updateUser({ condominiumJustChanged: false }) após PUT /schedules/me bem-sucedido
// ScheduleScreen já chama esse endpoint na função handleSave
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Banner contextual de condomínio na ScheduleScreen</name>
  <files>
    apps/web/src/pages/client/ScheduleScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/ScheduleScreen.tsx (arquivo completo — leitura obrigatória; entender onde inserir o banner e onde o handleSave chama PUT /schedules/me)
    - apps/web/src/contexts/AuthContext.tsx (confirmar que updateUser está disponível via useAuth)
  </read_first>
  <behavior>
    - `showCondoBanner` é verdadeiro apenas quando `user?.condominiumJustChanged === true`
    - Banner aparece entre o AppBar e o conteúdo principal da ScheduleScreen
    - Banner visual: fundo '#F3DDA6' (--color-gold-soft), borda esquerda 3px solid var(--color-accent), borderRadius 12, padding '12px 16px', texto 'Você mudou de condomínio. Configure sua nova agenda semanal.', ícone 'alert' cor var(--color-accent)
    - Após handleSave com sucesso (PUT /schedules/me retorna 200): chamar `updateUser({ condominiumJustChanged: false })` para limpar o banner
    - Nenhuma outra mudança em ScheduleScreen além da adição do banner e da linha no handleSave
  </behavior>
  <action>
    Em `apps/web/src/pages/client/ScheduleScreen.tsx`:

    1. Verificar se `updateUser` já está sendo desestruturado do useAuth. Se não estiver, adicionar ao desestruturamento existente: `const { user, updateUser } = useAuth()` (ou equivalente conforme assinatura atual do hook).

    2. Adicionar constante: `const showCondoBanner = user?.condominiumJustChanged === true`.

    3. Inserir o JSX do banner imediatamente após o AppBar e antes do conteúdo principal da agenda (o bloco do ScrollArea ou o primeiro div de conteúdo):
       ```
       {showCondoBanner && (
         <div style={{
           display: 'flex', alignItems: 'center', gap: 8,
           background: '#F3DDA6',
           borderLeft: '3px solid var(--color-accent)',
           borderRadius: 12,
           padding: '12px 16px',
           margin: '0 16px 16px',
           fontFamily: 'var(--font-body)',
           fontSize: 15,
           fontWeight: 600,
           color: 'var(--color-text)',
         }}>
           <Icon name="alert" size={18} color="var(--color-accent)" />
           <span>Você mudou de condomínio. Configure sua nova agenda semanal.</span>
         </div>
       )}
       ```

    4. Na função `handleSave` (ou equivalente que chama PUT /schedules/me), após o sucesso da requisição, adicionar: `updateUser({ condominiumJustChanged: false })`.

    Não alterar nada mais em ScheduleScreen.tsx além desses 4 pontos.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | grep "ScheduleScreen" | head -10</automated>
  </verify>
  <done>
    `npx tsc --noEmit` sem erros em ScheduleScreen.tsx. `grep -n "condominiumJustChanged" apps/web/src/pages/client/ScheduleScreen.tsx` retorna pelo menos 2 linhas: a constante `showCondoBanner` e a chamada `updateUser({ condominiumJustChanged: false })`.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Todos os 5 planos da Fase 11 executados:
    - 11-01: schema Prisma (OtpCode.purpose) + auth.repository.ts fix
    - 11-02: módulo backend client-profile (4 endpoints) + db push
    - 11-03: AuthContext enriquecido + 5º tab Perfil + rotas
    - 11-04: SettingsScreen + ContactEditScreen
    - 11-05: banner ScheduleScreen

    O servidor deve estar rodando em http://localhost:3001 e o frontend em http://localhost:5173.
    Usar um usuário do tipo CLIENT já cadastrado no banco dev.
  </what-built>
  <how-to-verify>
    Verificar CONF-01: Abrir o app em http://localhost:5173, fazer login como CLIENT. Confirmar que o tab bar exibe 5 abas: Início / Agenda / Créditos / Pedidos / **Perfil**.

    Verificar CONF-02 e CONF-03: Tocar na aba **Perfil**. Confirmar que a tela exibe os campos Nome completo, Data de nascimento (editáveis) e CPF (readonly com fundo cinza e cursor de bloqueio). Alterar o nome e tocar "Salvar dados" — confirmar toast 'Dados salvos!'.

    Verificar CONF-04: Na seção Contato, tocar em **Editar contato**. Confirmar tela com Step 1 "Qual é o novo contato?". Inserir um telefone diferente do atual e tocar "Enviar código". Confirmar transição para Step 2 com campo OTP. Em dev, o código é `1234` (OTP_DEV_CODE). Inserir 1234 e confirmar toast 'Contato atualizado!' e retorno à tela de Perfil.

    Verificar CONF-05: Na seção Condomínio, confirmar que CondoSearch está pré-selecionado com o condomínio atual. Alterar apartamento e tocar "Salvar endereço" (sem mudar condomínio) — toast 'Endereço atualizado!' sem dialog.

    Verificar CONF-06: Mudar para um condomínio diferente e tocar "Salvar endereço". Confirmar que o dialog aparece com texto 'Mudar de condomínio vai desativar sua agenda semanal ativa...'. Tocar "Confirmar mudança". Navegar para aba Agenda — confirmar banner amarelo 'Você mudou de condomínio. Configure sua nova agenda semanal.'.

    Verificar CONF-07: Tocar em **Sair** na seção Conta. Confirmar redirecionamento imediato para a tela de login (sem dialog de confirmação).
  </how-to-verify>
  <resume-signal>
    Digitar "aprovado" se todos os 7 requisitos CONF-01 a CONF-07 estiverem funcionando corretamente, ou descrever os problemas encontrados para que sejam corrigidos.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScheduleScreen → AuthContext.condominiumJustChanged | Flag local de UX — sem exposição a dados externos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | condominiumJustChanged persiste no localStorage via updateUser | accept | É um campo de UX local (boolean) — sem dados sensíveis; flag limpa automaticamente após configurar agenda |
| T-11-05-02 | Denial of Service | Banner sempre visível se updateUser não for chamado após save | mitigate | handleSave de ScheduleScreen chama `updateUser({ condominiumJustChanged: false })` após PUT /schedules/me bem-sucedido |
| T-11-05-SC | Tampering | npm installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `grep -n "condominiumJustChanged" apps/web/src/pages/client/ScheduleScreen.tsx` retorna 2+ linhas
- `grep -n "'F3DDA6'" apps/web/src/pages/client/ScheduleScreen.tsx` retorna cor do banner
- `grep -n "Você mudou de condomínio" apps/web/src/pages/client/ScheduleScreen.tsx` retorna o texto do banner
- `cd apps/web && npx tsc --noEmit` sem erros
- `cd apps/api && npx tsc --noEmit` sem erros
- `npm run test` (Turborepo root) passa com todos os stubs e testes existentes
- Checkpoint humano aprovado com verificação manual dos 7 requisitos CONF-01 a CONF-07
</verification>

<success_criteria>
- Banner aparece na ScheduleScreen quando `condominiumJustChanged === true` e desaparece após salvar agenda
- Checkpoint humano aprovado: todos os 7 requisitos CONF-01 a CONF-07 verificados manualmente
- `npm run test` full suite verde
- `npx tsc --noEmit` sem erros em apps/web e apps/api
</success_criteria>

<output>
Criar `.planning/phases/11-configura-es-e-perfil-do-cliente/11-05-SUMMARY.md` ao concluir.
</output>
