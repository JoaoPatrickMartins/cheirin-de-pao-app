---
phase: 11-configura-es-e-perfil-do-cliente
plan: 04
type: execute
wave: 4
depends_on:
  - 11-02
  - 11-03
files_modified:
  - apps/web/src/pages/client/SettingsScreen.tsx
  - apps/web/src/pages/client/ContactEditScreen.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
must_haves:
  truths:
    - "SettingsScreen renderiza seções Dados Pessoais, Contato, Condomínio, Conta"
    - "Campo CPF é readonly com estilo visual diferente (surface-2, opacity 0.7, cursor not-allowed)"
    - "Botão Sair chama auth.logout() diretamente sem dialog (D-09)"
    - "Seção Condomínio exibe CondoSearch inline + campo apartamento + campo bloco condicional (BLOCKS)"
    - "Ao salvar com condominiumId mudado, dialog de confirmação é exibido antes do PATCH"
    - "ContactEditScreen tem 2 steps visuais inline: Step 1 input contato, Step 2 OTP"
    - "ContactEditScreen Step 1 exibe erro 'Este contato já está associado a outra conta.' em caso de 422"
  artifacts:
    - path: "apps/web/src/pages/client/SettingsScreen.tsx"
      provides: "Tela de configurações completa com 4 seções"
      contains: "logout"
    - path: "apps/web/src/pages/client/ContactEditScreen.tsx"
      provides: "Wizard de 2 steps para edição de contato com OTP"
      contains: "CONTACT_CHANGE"
  key_links:
    - from: "apps/web/src/pages/client/SettingsScreen.tsx"
      to: "apps/api/src/modules/client-profile/client-profile.route.ts"
      via: "apiFetch PATCH /client/profile"
      pattern: "PATCH.*client/profile"
    - from: "apps/web/src/pages/client/ContactEditScreen.tsx"
      to: "apps/api/src/modules/client-profile/client-profile.route.ts"
      via: "apiFetch POST /client/profile/contact/request-change + confirm-change"
      pattern: "contact/request-change"
---

<objective>
Cria `SettingsScreen.tsx` (tela de configurações com seções Dados Pessoais, Contato, Condomínio, Conta) e `ContactEditScreen.tsx` (wizard de 2 steps inline para editar contato com OTP). Ambas as telas consomem os endpoints do Plano 02 e usam o `AuthContext` enriquecido do Plano 03.

Purpose: Entregar as telas visíveis ao cliente para CONF-01 a CONF-07. Este é o plano central de UI da fase.

Output: 2 novos arquivos de tela completos e funcionais.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/11-configura-es-e-perfil-do-cliente/11-UI-SPEC.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/11-configura-es-e-perfil-do-cliente/11-03-SUMMARY.md

<interfaces>
<!-- AuthUser (ampliado no Plano 03) -->
export interface AuthUser {
  id: string; role: 'CLIENT' | 'COURIER' | 'ADMIN'; name: string; creditBalance: number;
  phone?: string; email?: string; cpf?: string; birthDate?: string;
  condominiumId?: string; condominiumName?: string; apartment?: string; block?: string;
  condominiumJustChanged?: boolean;
}

<!-- AuthContextType (ampliado no Plano 03) -->
export interface AuthContextType {
  user: AuthUser | null; token: string | null; isLoading: boolean;
  login: (token: string, user: AuthUser) => void;
  logout: () => void;
  updateCreditBalance: (balance: number) => void;
  updateUser: (partial: Partial<AuthUser>) => void;
}

<!-- useAuth hook -->
import { useAuth } from '../../hooks/useAuth'
const { user, auth } = useAuth()

<!-- apiFetch assinatura -->
import { apiFetch } from '../../lib/apiFetch'
const res = await apiFetch('/client/profile', { method: 'PATCH', body: JSON.stringify(data) })
// res.ok: boolean, res.json(): Promise<any>, res.status: number

<!-- OtpInput API -->
export interface OtpInputProps { onComplete: (code: string) => void; disabled?: boolean }
import { OtpInput } from '../../components/auth/OtpInput'
<OtpInput onComplete={(code) => handleConfirmOtp(code)} disabled={loading} />

<!-- CondoSearch API — verificar real assinatura em OnboardingScreen -->
import { CondoSearch } from '../../components/auth/CondoSearch'

<!-- Toast pattern (ScheduleScreen.tsx linhas 49-58 + 71-92) -->
const [toast, setToast] = useState<{ message: string; ok: boolean } | null>(null)
// position: fixed, top: 16, left: 50%, transform: translateX(-50%), zIndex: 9999
// background: var(--color-espresso), color: var(--color-primary-btn-text), radius: 12, padding: 12px 16px
// setTimeout(() => setToast(null), 2500)

<!-- Dialog pattern (ConfirmDeliveryDialog.tsx) -->
// role="dialog" aria-modal="true", backdrop rgba(0,0,0,0.45)
// card: bg var(--color-surface), radius var(--radius-card) 22px, padding 24px, maxWidth calc(100vw - 48px)
// botão cancelar: bg var(--color-surface-2), texto var(--color-text-sec)
// botão confirmar: bg var(--color-espresso), texto var(--color-primary-btn-text)

<!-- Campo readonly CPF -->
// style: opacity 0.7, bg var(--color-surface-2), border 1.5px solid var(--color-border), cursor not-allowed
// mesmos padding/borderRadius dos outros campos

<!-- Botão Sair -->
// color '#C0392B', sem fundo, sem borda, minHeight 44px, fontWeight 600, fontSize 15
// centralizado no card Conta, SEM dialog de confirmação (D-09)

<!-- GET /condominiums — para CondoSearch -->
// apiFetch('/condominiums') retorna lista de condomínios
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SettingsScreen — 4 seções + dialog de condomínio + toast</name>
  <files>
    apps/web/src/pages/client/SettingsScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/ScheduleScreen.tsx (padrão completo: toast, useAuth, apiFetch, header, AppBar sem botão voltar, estrutura de tela)
    - apps/web/src/pages/auth/OnboardingScreen.tsx (uso de CondoSearch, bloco condicional BLOCKS, ChannelSelector)
    - apps/web/src/components/courier/ConfirmDeliveryDialog.tsx (padrão completo do dialog inline)
    - apps/web/src/components/auth/CondoSearch.tsx (interface do componente: props, callback de seleção)
    - apps/web/src/hooks/useAuth.ts (verificar assinatura — retorna { user, auth } ou { user, logout, updateUser }?)
  </read_first>
  <behavior>
    - Ao montar: carrega lista de condominiums via GET /condominiums para o CondoSearch; pré-popula todos os campos com dados de user do AuthContext (D-06 — sem fetch de profile ao abrir)
    - Seção "Dados Pessoais": campos Nome completo (input text editável, label 'Nome completo'), Data de nascimento (input date editável, label 'Data de nascimento'), CPF (input text readonly, label 'CPF', texto auxiliar 'O CPF não pode ser alterado.', estilo surface-2 opacity 0.7 cursor not-allowed)
    - Botão 'Salvar dados' (CTA primário, largura 100%) chama PATCH /client/profile com { name, birthDate }; em sucesso chama updateUser({ name, birthDate }) e exibe toast 'Dados salvos!'
    - Seção "Contato": linha Telefone (label + valor ou 'Não informado') com botão 'Editar contato' (chama navigate('/client/perfil/editar-contato')); linha E-mail (mesmo padrão)
    - Seção "Condomínio": CondoSearch pre-selecionado com user.condominiumId/condominiumName; campo Apartamento (input text); campo Bloco (condicional — exibe apenas se condo selecionado for do tipo BLOCKS, igual OnboardingScreen)
    - Botão 'Salvar endereço': se condominiumId mudou → exibe dialog de confirmação; se não mudou → PATCH direto. Dialog: título 'Mudar de condomínio', corpo 'Mudar de condomínio vai desativar sua agenda semanal ativa. Você precisará reconfigurar a agenda no novo endereço.', botões 'Cancelar' e 'Confirmar mudança'
    - Após confirmar PATCH com condominiumId mudado: se resposta contém `scheduleDeactivated: true` → chama updateUser({ condominiumId, condominiumName, apartment, block, condominiumJustChanged: true }); senão updateUser sem condominiumJustChanged. Toast 'Endereço atualizado!'
    - Seção "Conta": botão 'Sair' com cor de texto '#C0392B', sem fundo, sem borda, minHeight 44px — chama auth.logout() DIRETAMENTE sem dialog (D-09)
    - Toast: padrão idêntico ao ScheduleScreen — fixed top 16 left 50% transform translateX(-50%) zIndex 9999, bg var(--color-espresso), color var(--color-primary-btn-text), radius 12, padding 12px 16px, auto-dismiss 2500ms
    - Header: título 'Perfil' (heading 21px Bricolage weight 600 letterSpacing -0.02em) SEM botão voltar (D-03 — aba de nível superior)
  </behavior>
  <action>
    Criar `apps/web/src/pages/client/SettingsScreen.tsx` com export nomeado `SettingsScreen`.

    Imports: `{ useState, useEffect }` de 'react', `{ useNavigate }` de 'react-router', `{ useAuth }` de '../../hooks/useAuth', `{ apiFetch }` de '../../lib/apiFetch', `{ CondoSearch }` de '../../components/auth/CondoSearch', `{ Icon }` de '../../components/brand/Icon'.

    Estado local: `name` (string), `birthDate` (string), `selectedCondo` (objeto com id, name, type ou null), `apartment` (string), `block` (string), `condos` (array de condomínios para o CondoSearch), `showCondoDialog` (boolean), `loading` (boolean), `toast` (estado do padrão ScheduleScreen).

    useEffect de montagem: `apiFetch('/condominiums')` para popular `condos`. Pre-popular campos com `user?.name`, `user?.birthDate?.split('T')[0]`, `user?.apartment`, `user?.block` e setSelectedCondo com `{ id: user?.condominiumId, name: user?.condominiumName }`.

    Handler `handleSaveDados`: `apiFetch('/client/profile', { method: 'PATCH', body: JSON.stringify({ name, birthDate }) })`. Em sucesso: `updateUser({ name, birthDate }); setToast({ message: 'Dados salvos!', ok: true })`. Em erro: toast de erro.

    Handler `handleSaveEndereco`: Verificar se `selectedCondo?.id !== user?.condominiumId` — se sim, exibir `setShowCondoDialog(true)` e retornar. Se não mudou, chamar `doSaveEndereco` diretamente.

    Handler `doSaveEndereco`: PATCH com `{ condominiumId: selectedCondo.id, apartment, block }`. Em sucesso com `scheduleDeactivated: true`: `updateUser({ condominiumId: selectedCondo.id, condominiumName: selectedCondo.name, apartment, block, condominiumJustChanged: true })`. Sem schedule: `updateUser({ condominiumId: selectedCondo.id, condominiumName: selectedCondo.name, apartment, block })`. Toast 'Endereço atualizado!'.

    Cards de seção: cada card usa `background: 'var(--color-surface)'`, `borderRadius: 'var(--radius-card)'` (22px), `padding: 24`, `boxShadow: 'var(--shadow-soft)'`, `marginBottom: 32` (xl).

    Labels de campo: `fontFamily: 'var(--font-body)'`, `fontSize: 12.5`, `fontWeight: 600`, `color: 'var(--color-text-sec)'`, `marginBottom: 8` (sm).

    Campos input: `background: 'var(--color-surface)'`, `border: '1.5px solid var(--color-border)'`, `borderRadius: 'var(--radius-field)'` (14px), `padding: '12px 14px'`, `width: '100%'`, `fontFamily: 'var(--font-body)'`, `fontSize: 15`.

    Campo CPF readonly: adicionar `readOnly`, `style.opacity: 0.7`, `style.background: 'var(--color-surface-2)'`, `style.cursor: 'not-allowed'`. Texto auxiliar `'O CPF não pode ser alterado.'` em `fontSize: 12.5`, `color: 'var(--color-text-ter)'`.

    Botão CTA primário: `background: 'var(--color-espresso)'`, `color: 'var(--color-primary-btn-text)'`, `borderRadius: 'var(--radius-btn)'` (16px), `height: 52`, `width: '100%'`, `fontFamily: 'var(--font-body)'`, `fontSize: 15`, `fontWeight: 600`, `border: 'none'`, `opacity: loading ? 0.6 : 1`.

    Botão Sair: `color: '#C0392B'`, `background: 'none'`, `border: 'none'`, `minHeight: 44`, `fontFamily: 'var(--font-body)'`, `fontSize: 15`, `fontWeight: 600`, `cursor: 'pointer'`, `width: '100%'`. onClick: `auth.logout()` ou `logout()` conforme assinatura do useAuth. SEM dialog.

    Dialog de mudança de condomínio: quando `showCondoDialog === true`, renderizar overlay com `position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.45)', zIndex: 100`. Card interno com `role="dialog" aria-modal="true"`, `background: 'var(--color-surface)'`, `borderRadius: 22`, `padding: 24`, `width: 'calc(100vw - 48px)'`, `maxWidth: 320`. Título 'Mudar de condomínio' (heading), corpo conforme copywriting, botões 'Cancelar' (fecha dialog) e 'Confirmar mudança' (chama doSaveEndereco + fecha dialog).

    ScrollArea: `overflowY: 'auto'`, `padding: '0 16px'`, `paddingBottom: 48`. Header acima da scroll area com `paddingTop: 'calc(6px + env(safe-area-inset-top))'`, `padding: '6px 20px 14px'`.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | grep "SettingsScreen" | head -10</automated>
  </verify>
  <done>
    `npx tsc --noEmit` sem erros em SettingsScreen.tsx. Arquivo contém: `logout` (chamada a auth.logout), `'O CPF não pode ser alterado.'`, `'Salvar dados'`, `'Salvar endereço'`, `'Mudar de condomínio'`, `'Sair'`, `readOnly`. Campo CPF tem `readOnly` e `cursor: 'not-allowed'`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContactEditScreen — wizard 2 steps + OTP + feedback</name>
  <files>
    apps/web/src/pages/client/ContactEditScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/auth/OnboardingScreen.tsx (padrão step wizard inline, OtpInput, ChannelSelector, gestão de step state, botão voltar com navigate(-1))
    - apps/web/src/components/auth/OtpInput.tsx (API do componente: OtpInputProps, onComplete)
    - apps/web/src/components/auth/ResendTimer.tsx (API do componente: verificar props)
    - apps/web/src/components/auth/ChannelSelector.tsx (API do componente: verificar props de channel e onChange)
  </read_first>
  <behavior>
    - Step 1: indicator 'Passo 1 de 2', heading 'Qual é o novo contato?', subtitle 'Você receberá um código de verificação.', ChannelSelector (SMS/E-mail), input do contato, botão 'Enviar código' (disabled até campo preenchido)
    - Botão 'Enviar código': POST /client/profile/contact/request-change com { phone } ou { email }; em 422 exibe erro 'Este contato já está associado a outra conta.' abaixo do botão; em sucesso avança para step 2 com fade+translateY(-8px) 150ms
    - Step 2: indicator 'Passo 2 de 2', heading 'Confirme o código', subtitle 'Código enviado para {contato}.', OtpInput 4 dígitos (via onComplete), ResendTimer, botão 'Confirmar código' (disabled até 4 dígitos preenchidos)
    - Botão 'Confirmar código' (ou via OtpInput.onComplete): POST /client/profile/contact/confirm-change com { code }; em 401 exibe 'Código incorreto. Verifique e tente de novo.'; em sucesso chama updateUser({ phone } ou { email }) e navega para '/client/perfil'
    - Botão voltar no AppBar: `navigate(-1)` (ContactEditScreen é tela em stack — TEM botão voltar, D-03 não se aplica aqui)
    - Erro de OTP expirado: detectar se mensagem do backend contém 'expirado' → exibir 'Código expirado. Solicite um novo.'
  </behavior>
  <action>
    Criar `apps/web/src/pages/client/ContactEditScreen.tsx` com export nomeado `ContactEditScreen`.

    Imports: `{ useState }` de 'react', `{ useNavigate }` de 'react-router', `{ useAuth }` de '../../hooks/useAuth', `{ apiFetch }` de '../../lib/apiFetch', `{ OtpInput }` de '../../components/auth/OtpInput', `{ ResendTimer }` de '../../components/auth/ResendTimer', `{ ChannelSelector }` de '../../components/auth/ChannelSelector', `{ Icon }` de '../../components/brand/Icon'.

    Estado local: `step` (0 | 1), `channel` ('sms' | 'email', default 'sms'), `contactValue` (string), `error` (string | null), `loading` (boolean), `otpCode` (string — para habilitar botão Confirmar).

    AppBar: botão voltar (width 38, height 38, borderRadius 12, bg 'var(--color-surface-2)', border 'none', Icon 'arrow-left' size 20) + título 'Editar contato' (heading 21px Bricolage weight 600).

    Transição de step: usar estado CSS `opacity: step === n ? 1 : 0` + `transform: step === n ? 'translateY(0)' : 'translateY(-8px)'` + `transition: 'opacity 150ms ease-out, transform 150ms ease-out'`. Renderizar ambos os steps no DOM mas aplicar `display: 'none'` no step inativo para evitar layout shift.

    Step indicator: `fontFamily: 'var(--font-body)'`, `fontSize: 12.5`, `fontWeight: 600`, `color: 'var(--color-text-sec)'`, `marginBottom: 8`.

    Handler `handleRequestChange`: `setLoading(true); setError(null)`. POST `/client/profile/contact/request-change` com body `channel === 'sms' ? { phone: contactValue } : { email: contactValue }`. Se res.status === 422: `setError('Este contato já está associado a outra conta.')`. Se res.ok: `setStep(1)`. Em erro de rede: `setError('Algo deu errado. Tente novamente.')`.

    Handler `handleConfirmChange(code: string)`: `setLoading(true); setError(null)`. POST `/client/profile/contact/confirm-change` com body `{ code }`. Se res.ok: chamar `updateUser(channel === 'sms' ? { phone: contactValue } : { email: contactValue })`; `navigate('/client/perfil')`. Se res.status === 401: verificar mensagem — se contém 'expirado' → `setError('Código expirado. Solicite um novo.')`, senão `setError('Código incorreto. Verifique e tente de novo.')`.

    Botão 'Enviar código': `disabled={!contactValue || loading}`, opacity 0.45 quando disabled, cursor 'not-allowed'.

    Botão 'Confirmar código': `disabled={otpCode.length < 4 || loading}`. OtpInput.onComplete seta `otpCode` e chama `handleConfirmChange` automaticamente.

    Estilo geral: `padding: '0 24px'`, `background: 'var(--color-app-bg)'`, altura mínima full screen.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx tsc --noEmit 2>&1 | grep "ContactEditScreen" | head -10</automated>
  </verify>
  <done>
    `npx tsc --noEmit` sem erros em ContactEditScreen.tsx. Arquivo contém: `'Enviar código'`, `'Confirmar código'`, `'Passo 1 de 2'`, `'Passo 2 de 2'`, `'Este contato já está associado a outra conta.'`, `navigate(-1)`, `OtpInput`, `ResendTimer`, `ChannelSelector`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SettingsScreen → PATCH /client/profile | Dados de perfil do cliente; CPF nunca enviado |
| ContactEditScreen → POST /contact/request-change | Novo contato fornecido pelo usuário |
| ContactEditScreen → POST /contact/confirm-change | Código OTP fornecido pelo usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering | SettingsScreen PATCH envia cpf no body | mitigate | Campo CPF renderizado como `readOnly` — sem binding de onChange; não incluído no body do PATCH; verificado pelo Zod no backend |
| T-11-04-02 | Information Disclosure | Erro 422 exibe mensagem sobre contato existente | accept | Mensagem genérica 'Este contato já está associado a outra conta.' — não revela qual conta; risco residual aceito (D-12) |
| T-11-04-03 | Elevation of Privilege | OTP brute force via UI | mitigate | Rate limit 5 req/min no backend; botão desabilitado durante loading evita submissões paralelas |
| T-11-04-04 | Spoofing | Botão Sair sem confirmação | accept | D-09 é explícito: sem dialog para cliente. Risco mínimo — sessão limpa por logout() que remove token e navega para / |
| T-11-04-SC | Tampering | npm installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `grep -n "readOnly" apps/web/src/pages/client/SettingsScreen.tsx` retorna linha do campo CPF
- `grep -n "logout" apps/web/src/pages/client/SettingsScreen.tsx` retorna chamada direta (sem dialog)
- `grep -n "showCondoDialog" apps/web/src/pages/client/SettingsScreen.tsx` retorna estado e uso no dialog
- `grep -n "Confirmar mudança" apps/web/src/pages/client/SettingsScreen.tsx` retorna texto do botão do dialog
- `grep -n "navigate(-1)" apps/web/src/pages/client/ContactEditScreen.tsx` retorna botão voltar
- `grep -n "request-change" apps/web/src/pages/client/ContactEditScreen.tsx` retorna endpoint
- `cd apps/web && npx tsc --noEmit` sem erros
</verification>

<success_criteria>
- `SettingsScreen.tsx` criado com 4 seções (Dados Pessoais, Contato, Condomínio, Conta) funcionais
- CPF renderizado como readonly sem envio no PATCH (CONF-03)
- Dialog de mudança de condomínio com texto correto (D-17)
- Botão Sair chama logout() sem dialog (D-09, CONF-07)
- `ContactEditScreen.tsx` criado com 2 steps inline (D-11)
- Erro 422 exibe 'Este contato já está associado a outra conta.' (D-12)
- Após confirmação OTP: updateUser + navigate para /client/perfil (D-14)
</success_criteria>

<output>
Criar `.planning/phases/11-configura-es-e-perfil-do-cliente/11-04-SUMMARY.md` ao concluir.
</output>
