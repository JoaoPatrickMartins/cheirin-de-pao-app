---
phase: 11-configura-es-e-perfil-do-cliente
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/api/src/modules/client-profile/client-profile.schema.ts
  - apps/api/src/modules/client-profile/client-profile.repository.ts
  - apps/api/src/modules/client-profile/client-profile.service.ts
  - apps/api/src/modules/client-profile/client-profile.controller.ts
  - apps/api/src/modules/client-profile/client-profile.route.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
must_haves:
  truths:
    - "GET /client/profile retorna dados completos do cliente autenticado"
    - "PATCH /client/profile atualiza nome, birthDate, condomínio, apartamento, bloco — nunca CPF"
    - "POST /client/profile/contact/request-change valida conflito e envia OTP purpose=CONTACT_CHANGE"
    - "POST /client/profile/contact/confirm-change valida OTP e atualiza contato no banco"
    - "Mudança de condominiumId no PATCH desativa schedules ativos do usuário"
  artifacts:
    - path: "apps/api/src/modules/client-profile/client-profile.schema.ts"
      provides: "Zod schemas: UpdateProfileSchema, ContactChangeRequestSchema, ContactChangeConfirmSchema"
      contains: "UpdateProfileSchema"
    - path: "apps/api/src/modules/client-profile/client-profile.repository.ts"
      provides: "ClientProfileRepository com findActiveContactChangeOtp e deactivateSchedules"
      contains: "findActiveContactChangeOtp"
    - path: "apps/api/src/modules/client-profile/client-profile.service.ts"
      provides: "ClientProfileService com getProfile, updateProfile, requestContactChange, confirmContactChange"
      contains: "requestContactChange"
    - path: "apps/api/src/modules/client-profile/client-profile.controller.ts"
      provides: "ClientProfileController com role check CLIENT inline"
      contains: "role !== 'CLIENT'"
    - path: "apps/api/src/modules/client-profile/client-profile.route.ts"
      provides: "4 endpoints registrados com preHandler: [fastify.authenticate]"
      contains: "clientProfileRoute"
    - path: "apps/api/src/server.ts"
      provides: "clientProfileRoute registrado"
      contains: "clientProfileRoute"
  key_links:
    - from: "apps/api/src/modules/client-profile/client-profile.service.ts"
      to: "apps/api/src/modules/auth/otp.service.ts"
      via: "sendSmsOtp / sendEmailOtp para CONTACT_CHANGE OTP"
      pattern: "sendSmsOtp|sendEmailOtp"
    - from: "apps/api/src/modules/client-profile/client-profile.repository.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "prisma.schedule.updateMany + prisma.user.update"
      pattern: "schedule.updateMany"
---

<objective>
Cria o módulo backend `client-profile` completo (schema Zod, repository, service, controller, route) e o registra no server.ts. Executa `npx prisma generate` após a criação do módulo para que o TypeScript reconheça o campo `purpose` adicionado no Plano 01.

Purpose: Prover os 4 endpoints que o frontend de configurações precisa: GET profile, PATCH profile, POST request-change, POST confirm-change.

Output: 5 novos arquivos em `apps/api/src/modules/client-profile/`, `server.ts` atualizado, `prisma generate` executado.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/11-configura-es-e-perfil-do-cliente/11-01-SUMMARY.md

<interfaces>
<!-- Padrão de rota autenticada (credits.route.ts) -->
export const creditsRoute: FastifyPluginAsync = async (fastify) => {
  const ctrl = new CreditsController(fastify)
  fastify.get('/combos', { preHandler: [fastify.authenticate], schema: { tags: ['credits'], security: [{ bearerAuth: [] }] } }, ctrl.listCombos.bind(ctrl))
}

<!-- Rate limit OTP — copiar de auth.route.ts -->
const otpRateLimit = { config: { rateLimit: { max: 5, timeWindow: '1 minute' } } }

<!-- Padrão role check CLIENT inline (adaptar de admin-clients.controller.ts) -->
if (request.user?.role !== 'CLIENT') {
  return reply.status(403).send({ error: 'Acesso negado' })
}

<!-- Padrão de userId sempre do JWT (T-10-02-01) -->
const userId = request.user.id  // NUNCA do body ou params

<!-- Padrão timingSafeEqual para OTP (auth.service.ts linhas 79-83) -->
const expectedHash = Buffer.from(this.hashValue(code), 'hex')
const actualHash = Buffer.from(otp.code, 'hex')
const match = expectedHash.length === actualHash.length && timingSafeEqual(expectedHash, actualHash)
if (!match) return { error: 'Código inválido', status: 401 }

<!-- Padrão de desativação de agenda -->
await this.prisma.schedule.updateMany({ where: { userId, isActive: true }, data: { isActive: false } })

<!-- Padrão de conflito de contato (auth.service.ts) -->
const existing = await this.prisma.user.findFirst({ where: { phone } })
if (existing && existing.id !== userId) {
  return { error: 'Este contato já está associado a outra conta.', status: 422 }
}

<!-- server.ts linha atual de wiring — adicionar após courierRoute (linha 186) -->
await fastify.register(courierRoute)
// Phase 11 — adicionar após esta linha:
await fastify.register(clientProfileRoute)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Módulo client-profile — schema, repository, service</name>
  <files>
    apps/api/src/modules/client-profile/client-profile.schema.ts,
    apps/api/src/modules/client-profile/client-profile.repository.ts,
    apps/api/src/modules/client-profile/client-profile.service.ts
  </files>
  <read_first>
    - apps/api/src/modules/auth/auth.schema.ts (padrão Zod schemas)
    - apps/api/src/modules/auth/auth.repository.ts (padrão de classe repository, findActiveOtp modificado no Plano 01)
    - apps/api/src/modules/auth/auth.service.ts (padrão timingSafeEqual, hashValue, sendSmsOtp/sendEmailOtp, geração de OTP)
    - apps/api/src/modules/admin-clients/admin-clients.repository.ts (padrão de findById, update)
    - apps/api/src/modules/admin-clients/admin-clients.service.ts (padrão de estrutura de classe)
  </read_first>
  <behavior>
    - client-profile.schema.ts: Exporta `UpdateProfileSchema` (name min(2) optional, birthDate datetime optional, condominiumId optional, apartment optional, block optional — CPF NUNCA aqui), `ContactChangeRequestSchema` (phone ou email com .refine), `ContactChangeConfirmSchema` (code length(4))
    - client-profile.repository.ts: `findUserById`, `updateProfile`, `findCondominium`, `findActiveSchedule`, `deactivateSchedules`, `findActiveContactChangeOtp` (filtra purpose='CONTACT_CHANGE'), `createContactChangeOtp` (cria OtpCode com purpose='CONTACT_CHANGE'), `markOtpUsed`, `updateContact`
    - client-profile.service.ts: `getProfile` (retorna campos User + condominiumName via findCondominium), `updateProfile` (se condominiumId muda → chama deactivateSchedules, retorna `{ ..., scheduleDeactivated: boolean }`), `requestContactChange` (valida conflito antes de enviar OTP, usa sendSmsOtp/sendEmailOtp via import de otp.service), `confirmContactChange` (verifica OTP com timingSafeEqual, marca usedAt, atualiza User)
  </behavior>
  <action>
    Criar diretório `apps/api/src/modules/client-profile/` e os 3 arquivos.

    `client-profile.schema.ts`: Importar `z` de 'zod'. Definir e exportar:
    - `UpdateProfileSchema`: z.object com campos name (z.string().min(2).optional()), birthDate (z.string().datetime().optional()), condominiumId (z.string().optional()), apartment (z.string().optional()), block (z.string().optional()). Sem campo cpf — violação de CONF-03 se presente.
    - `ContactChangeRequestSchema`: z.object({ phone: z.string().optional(), email: z.string().email().optional() }).refine((d) => d.phone || d.email, { message: 'phone ou email obrigatório' })
    - `ContactChangeConfirmSchema`: z.object({ code: z.string().length(4) })
    - Exportar os tipos inferidos: `UpdateProfileBody`, `ContactChangeRequestBody`, `ContactChangeConfirmBody`

    `client-profile.repository.ts`: Classe `ClientProfileRepository` com `constructor(private fastify: FastifyInstance)` e getter `private get prisma()`. Métodos:
    - `findUserById(id: string)`: `prisma.user.findUnique({ where: { id } })`
    - `updateProfile(id: string, data: { name?: string; birthDate?: Date; condominiumId?: string; apartment?: string; block?: string })`: `prisma.user.update({ where: { id }, data, select: { id: true, name: true, birthDate: true, condominiumId: true, apartment: true, block: true, phone: true, email: true, cpf: true, creditBalance: true } })`
    - `updateContact(id: string, data: { phone?: string; email?: string })`: `prisma.user.update({ where: { id }, data })`
    - `findCondominium(id: string)`: `prisma.condominium.findUnique({ where: { id } })`
    - `findActiveSchedule(userId: string)`: `prisma.schedule.findFirst({ where: { userId, isActive: true } })`
    - `deactivateSchedules(userId: string)`: `prisma.schedule.updateMany({ where: { userId, isActive: true }, data: { isActive: false } })`
    - `findActiveContactChangeOtp(userId: string)`: `prisma.otpCode.findFirst({ where: { userId, usedAt: { isSet: false }, expiresAt: { gt: new Date() }, purpose: 'CONTACT_CHANGE' }, orderBy: { createdAt: 'desc' } })`
    - `createContactChangeOtp(data: { userId: string; code: string; channel: string; expiresAt: Date })`: `prisma.otpCode.create({ data: { ...data, purpose: 'CONTACT_CHANGE' } })`
    - `markOtpUsed(id: string)`: `prisma.otpCode.update({ where: { id }, data: { usedAt: new Date() } })`
    - `checkContactConflict(field: 'phone' | 'email', value: string, excludeUserId: string)`: `prisma.user.findFirst({ where: { [field]: value } })` — retorna o usuário encontrado ou null para o service verificar conflito

    `client-profile.service.ts`: Importar `FastifyInstance` de 'fastify', `ClientProfileRepository` local, `sendSmsOtp` e `sendEmailOtp` de '../auth/otp.service.js', `timingSafeEqual` de 'crypto', `UpdateProfileBody`, `ContactChangeRequestBody`, `ContactChangeConfirmBody` local. Classe `ClientProfileService` com:
    - `private repo: ClientProfileRepository` no constructor
    - `private hashValue(value: string): string` — usar SHA-256 com `crypto.createHash('sha256').update(value).digest('hex')` (mesmo padrão do auth.service)
    - `private generateOtpCode(): string` — retorna `process.env.OTP_DEV_CODE ?? Math.random().toString().slice(2, 6)` (4 dígitos)
    - `getProfile(userId: string)`: busca user + condominium, retorna `{ id, name, cpf, birthDate: user.birthDate?.toISOString(), phone, email, condominiumId, condominiumName: condo?.name ?? '', apartment, block, creditBalance }`
    - `updateProfile(userId: string, body: UpdateProfileBody)`: se `body.condominiumId` está presente E diferente do current user.condominiumId → chama `deactivateSchedules` e retorna `{ ...profileData, scheduleDeactivated: true }`. Caso contrário retorna `{ ...profileData, scheduleDeactivated: false }`. Converte `body.birthDate` para `new Date()` antes de salvar.
    - `requestContactChange(userId: string, body: ContactChangeRequestBody)`: verifica conflito via `checkContactConflict`; se conflito → `return { error: 'Este contato já está associado a outra conta.', status: 422 }`; senão gera OTP, cria via `createContactChangeOtp`, envia via `sendSmsOtp`/`sendEmailOtp` conforme canal. Retorna `{ ok: true }`.
    - `confirmContactChange(userId: string, body: ContactChangeConfirmBody)`: busca `findActiveContactChangeOtp`, verifica com `timingSafeEqual`, marca OTP como usado, atualiza contact via `updateContact`. Retorna o campo atualizado `{ phone?, email? }` ou `{ error, status }`.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx tsc --noEmit 2>&1 | grep "client-profile" | head -20</automated>
  </verify>
  <done>
    `npx tsc --noEmit` sem erros relativos a `client-profile`. Os 3 arquivos existem em `apps/api/src/modules/client-profile/`. `client-profile.service.ts` contém `requestContactChange`, `confirmContactChange`, `getProfile`, `updateProfile`.
  </done>
</task>

<task type="auto">
  <name>Task 2: controller + route + db push + wiring server.ts</name>
  <files>
    apps/api/src/modules/client-profile/client-profile.controller.ts,
    apps/api/src/modules/client-profile/client-profile.route.ts,
    apps/api/src/server.ts
  </files>
  <read_first>
    - apps/api/src/modules/credits/credits.route.ts (padrão FastifyPluginAsync, preHandler, schema tags)
    - apps/api/src/modules/admin-clients/admin-clients.controller.ts (padrão role check, Zod parse, try/catch duplo)
    - apps/api/src/modules/auth/auth.route.ts (otpRateLimit pattern, linhas 4-5)
    - apps/api/src/server.ts (onde adicionar o registro — após courierRoute, linha 186)
  </read_first>
  <action>
    `client-profile.controller.ts`: Classe `ClientProfileController` com constructor(private fastify: FastifyInstance). Importar ZodError, os 3 schemas Zod, `ClientProfileService`. Helper local `zodMessage(err: ZodError): string` — `err.issues.map((e) => e.message).join(', ')`. Handlers:
    - `getProfile(request, reply)`: role check CLIENT, userId=request.user.id, chama service.getProfile, retorna 200+dados ou 404.
    - `updateProfile(request, reply)`: role check CLIENT, parse `UpdateProfileSchema`, chama service.updateProfile, retorna 200+`{ ...result, scheduleDeactivated }` ou erros 400/404/500.
    - `requestContactChange(request, reply)`: role check CLIENT, parse `ContactChangeRequestSchema`, chama service.requestContactChange, retorna 204 se ok; 422 se conflito; 400 se Zod inválido; 500 em erro inesperado.
    - `confirmContactChange(request, reply)`: role check CLIENT, parse `ContactChangeConfirmSchema`, chama service.confirmContactChange, retorna 200+`{ phone?, email? }` ou 401 se OTP inválido/expirado; 400 se Zod inválido; 500 em erro inesperado.
    - Em TODOS os handlers: userId extraído de `request.user.id` — NUNCA do body (padrão T-10-02-01).

    `client-profile.route.ts`: `export const clientProfileRoute: FastifyPluginAsync = async (fastify) => { const ctrl = new ClientProfileController(fastify); const rateLimit = { config: { rateLimit: { max: 5, timeWindow: '1 minute' } } }` e registrar:
    - `fastify.get('/client/profile', { preHandler: [fastify.authenticate], schema: { tags: ['client-profile'], security: [{ bearerAuth: [] }], summary: 'Obter perfil do cliente' } }, ctrl.getProfile.bind(ctrl))`
    - `fastify.patch('/client/profile', { preHandler: [fastify.authenticate], schema: { ... } }, ctrl.updateProfile.bind(ctrl))`
    - `fastify.post('/client/profile/contact/request-change', { preHandler: [fastify.authenticate], ...rateLimit, schema: { ... } }, ctrl.requestContactChange.bind(ctrl))`
    - `fastify.post('/client/profile/contact/confirm-change', { preHandler: [fastify.authenticate], ...rateLimit, schema: { ... } }, ctrl.confirmContactChange.bind(ctrl))`

    `apps/api/src/server.ts`: Adicionar import `import { clientProfileRoute } from './modules/client-profile/client-profile.route.js'` no bloco de imports (após courierRoute import). Adicionar `await fastify.register(clientProfileRoute)  // Phase 11 — GET/PATCH /client/profile, POST contact/change` após a linha `await fastify.register(courierRoute)` (linha 186).

    Após criar todos os arquivos, executar `npx prisma generate` no diretório `apps/api` para gerar o cliente Prisma com o novo campo `purpose` do OtpCode (adicionado no Plano 01). Este passo é necessário para o TypeScript compilar sem erros.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma db push && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npx prisma db push` aplica o campo `purpose` ao schema Atlas sem erros. `npx tsc --noEmit` sem erros de TypeScript. `server.ts` contém import e registro de `clientProfileRoute`. `GET /client/profile`, `PATCH /client/profile`, `POST /client/profile/contact/request-change` e `POST /client/profile/contact/confirm-change` estão registrados na rota com `preHandler: [fastify.authenticate]`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLIENT request → /client/profile | Dados pessoais do cliente; userId sempre do JWT |
| CLIENT request → /contact/request-change | Novo contato fornecido pelo usuário; validar conflito antes de enviar OTP |
| CLIENT request → /contact/confirm-change | Código OTP fornecido pelo usuário; verificar com timingSafeEqual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Spoofing | GET /client/profile — acesso a dados de outro usuário | mitigate | userId extraído de request.user.id (JWT) — nunca de params/body (T-10-02-01) |
| T-11-02-02 | Tampering | PATCH /client/profile — sobrescrita de CPF | mitigate | UpdateProfileSchema Zod não inclui campo cpf — campo ignorado mesmo se enviado no body |
| T-11-02-03 | Information Disclosure | POST request-change retorna 422 com mensagem genérica | mitigate | Mensagem fixa: 'Este contato já está associado a outra conta.' — não revela qual conta (D-12) |
| T-11-02-04 | Elevation of Privilege | POST confirm-change — brute force do OTP | mitigate | Rate limit 5 req/min por IP no endpoint + timingSafeEqual (sem timing attack) |
| T-11-02-05 | Repudiation | updateProfile muda condominiumId sem auditoria | accept | Mudança registrada via Schedule.isActive=false + updateUser em AuthContext — rastreável no banco |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo neste plano |
</threat_model>

<verification>
- `curl -X GET http://localhost:3001/client/profile -H 'Authorization: Bearer {valid_token}'` retorna 200 com id, name, cpf, condominiumName
- `curl -X PATCH http://localhost:3001/client/profile -H 'Authorization: Bearer {token}' -d '{"cpf":"000"}'` não altera o CPF (campo ignorado pelo Zod schema)
- `curl -X POST http://localhost:3001/client/profile/contact/request-change -H 'Authorization: Bearer {token}' -d '{"phone":"...contato-duplicado..."}' ` retorna 422
- `npx tsc --noEmit` sem erros no diretório apps/api
- `npx prisma db push` bem-sucedido
</verification>

<success_criteria>
- 5 arquivos do módulo client-profile criados em `apps/api/src/modules/client-profile/`
- 4 endpoints registrados com autenticação JWT e role check CLIENT
- `npx prisma db push` aplicado com campo `purpose` no OtpCode
- Mudança de condominiumId desativa schedules ativos (CONF-06)
- CPF nunca aceito no PATCH (CONF-03)
- OTP de mudança de contato usa `purpose: 'CONTACT_CHANGE'` — não conflita com login (D-13)
</success_criteria>

<output>
Criar `.planning/phases/11-configura-es-e-perfil-do-cliente/11-02-SUMMARY.md` ao concluir.
</output>
