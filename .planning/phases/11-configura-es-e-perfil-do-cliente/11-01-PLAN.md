---
phase: 11-configura-es-e-perfil-do-cliente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/src/modules/auth/auth.repository.ts
  - apps/web/src/pages/client/__tests__/SettingsScreen.test.tsx
  - apps/web/src/pages/client/__tests__/ContactEditScreen.test.tsx
  - apps/api/src/modules/client-profile/__tests__/client-profile.service.test.ts
autonomous: true
requirements:
  - CONF-04
must_haves:
  truths:
    - "OtpCode com purpose opcional presente no schema Prisma"
    - "findActiveOtp do login filtra somente por purpose null ou LOGIN"
    - "Stubs de teste para SettingsScreen, ContactEditScreen e client-profile.service existem"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Campo purpose String? no model OtpCode"
      contains: "purpose   String?"
    - path: "apps/api/src/modules/auth/auth.repository.ts"
      provides: "findActiveOtp com filtro backward-compat"
      contains: "purpose: { in: [null, 'LOGIN'] }"
    - path: "apps/web/src/pages/client/__tests__/SettingsScreen.test.tsx"
      provides: "Stub de teste para SettingsScreen"
    - path: "apps/web/src/pages/client/__tests__/ContactEditScreen.test.tsx"
      provides: "Stub de teste para ContactEditScreen"
    - path: "apps/api/src/modules/client-profile/__tests__/client-profile.service.test.ts"
      provides: "Stub de teste para client-profile.service"
  key_links:
    - from: "apps/api/src/modules/auth/auth.repository.ts"
      to: "apps/api/prisma/schema.prisma"
      via: "prisma.otpCode.findFirst com filtro purpose"
      pattern: "purpose.*in.*null.*LOGIN"
---

<objective>
Adiciona o campo `purpose String?` ao model `OtpCode` no schema Prisma e adapta o `findActiveOtp` do login para filtrar backward-compat. Cria stubs de teste Wave 0 para as novas telas e serviço da fase.

Purpose: Bloquear conflito entre OTP de login e OTP de mudança de contato (D-13/D-18). Sem este plano, o módulo `client-profile` não pode criar OTPs `CONTACT_CHANGE` sem interferir no login.

Output: schema.prisma atualizado, auth.repository.ts com query corrigida, 3 arquivos de stub de teste.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/STATE.md

<interfaces>
<!-- Model OtpCode atual (schema.prisma linhas 314-323) — antes da modificação -->
model OtpCode {
  id        String    @id @default(auto()) @map("_id") @db.ObjectId
  userId    String    @db.ObjectId
  code      String
  channel   String
  expiresAt DateTime
  usedAt    DateTime?
  createdAt DateTime  @default(now())
  // ADICIONAR: purpose String?
}

<!-- auth.repository.ts findActiveOtp atual (linhas 32-37) — antes da modificação -->
findActiveOtp(userId: string) {
  return this.prisma.otpCode.findFirst({
    where: { userId, usedAt: { isSet: false }, expiresAt: { gt: new Date() } },
    orderBy: { createdAt: 'desc' },
  })
}

<!-- auth.repository.ts createOtp atual (linhas 39-41) — antes da modificação -->
createOtp(data: { userId: string; code: string; channel: string; expiresAt: Date }) {
  return this.prisma.otpCode.create({ data })
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar `purpose String?` ao OtpCode + adaptar findActiveOtp</name>
  <files>
    apps/api/prisma/schema.prisma,
    apps/api/src/modules/auth/auth.repository.ts
  </files>
  <read_first>
    - apps/api/prisma/schema.prisma (ler model OtpCode, linhas 314-323)
    - apps/api/src/modules/auth/auth.repository.ts (ler findActiveOtp e createOtp, linhas 32-41)
  </read_first>
  <behavior>
    - schema.prisma: OtpCode contém campo `purpose   String?` após `usedAt DateTime?`
    - auth.repository.ts: `findActiveOtp` usa `where: { ..., purpose: { in: [null, 'LOGIN'] } }` — documentos legados sem purpose (null) são tratados como LOGIN
    - auth.repository.ts: `createOtp` aceita `purpose?: string` opcional no tipo do parâmetro
  </behavior>
  <action>
    Em `apps/api/prisma/schema.prisma`, no model `OtpCode`, adicionar o campo `purpose   String?` após a linha de `usedAt`. O campo é opcional (String?) para backward-compat — documentos existentes sem o campo retornam null, tratado como LOGIN.

    Em `apps/api/src/modules/auth/auth.repository.ts`:
    - No método `findActiveOtp(userId: string)`: modificar o `where` para incluir `purpose: { in: [null, 'LOGIN'] }` — isso preserva OTPs legados (null) como válidos para login e exclui OTPs de CONTACT_CHANGE do fluxo de login.
    - No método `createOtp`: adicionar `purpose?: string` ao tipo do parâmetro `data`. O campo já será passado quando o módulo client-profile criar OTPs de CONTACT_CHANGE.

    Não alterar outros métodos de auth.repository.ts. Não tocar em auth.service.ts neste plano.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma validate</automated>
  </verify>
  <done>
    `npx prisma validate` passa sem erros. Schema contém `purpose   String?` em OtpCode. `auth.repository.ts` findActiveOtp contém `purpose: { in: [null, 'LOGIN'] }`. `createOtp` aceita `purpose?: string`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Stubs de teste Wave 0 para SettingsScreen, ContactEditScreen e client-profile.service</name>
  <files>
    apps/web/src/pages/client/__tests__/SettingsScreen.test.tsx,
    apps/web/src/pages/client/__tests__/ContactEditScreen.test.tsx,
    apps/api/src/modules/client-profile/__tests__/client-profile.service.test.ts
  </files>
  <read_first>
    - apps/web/src/pages/client/__tests__/ (verificar arquivos existentes para seguir padrão)
    - apps/api/src/modules/admin-clients/__tests__/ (padrão de teste de service existente, se houver)
  </read_first>
  <action>
    Criar três arquivos de stub de teste. Cada arquivo deve ter: import do componente/serviço alvo, bloco `describe` com nome do alvo, e pelo menos 1 test stub com `it.todo(...)` por requisito coberto. Os stubs com `it.todo` passam no Vitest sem falhar — são placeholders para implementação nas waves seguintes.

    `apps/web/src/pages/client/__tests__/SettingsScreen.test.tsx`:
    - `describe('SettingsScreen', ...)` com `it.todo('renderiza campo Nome completo editável')` (CONF-02), `it.todo('renderiza campo CPF como readonly')` (CONF-03), `it.todo('renderiza seção Condomínio com CondoSearch')` (CONF-05), `it.todo('chama logout() ao clicar em Sair')` (CONF-07).
    - Não importar nada que não existe ainda — apenas `it.todo` puro é suficiente.

    `apps/web/src/pages/client/__tests__/ContactEditScreen.test.tsx`:
    - `describe('ContactEditScreen', ...)` com `it.todo('renderiza Step 1 com input de novo contato')` (CONF-04), `it.todo('avança para Step 2 após enviar código')` (CONF-04), `it.todo('exibe erro 422 de contato já em uso')` (CONF-04).

    `apps/api/src/modules/client-profile/__tests__/client-profile.service.test.ts`:
    - `describe('ClientProfileService', ...)` com `it.todo('getProfile retorna dados completos do cliente')` (CONF-02/03/05), `it.todo('updateProfile com novo condominiumId desativa schedules ativos')` (CONF-06), `it.todo('requestContactChange retorna 422 se contato pertence a outra conta')` (CONF-04).
    - Criar diretório `apps/api/src/modules/client-profile/__tests__/` se não existir.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>
    `npm run test` passa. Os 3 novos arquivos existem. Cada um contém pelo menos 1 `it.todo(...)`. Nenhum erro de compilação TypeScript.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → MongoDB Atlas | Dados de OtpCode lidos/escritos; campo purpose opcional introduzido |
| auth.repository → Prisma query | Filtro backward-compat por `purpose` — documentos sem campo retornam null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Spoofing | findActiveOtp — null tratado como LOGIN | mitigate | Filtro `purpose: { in: [null, 'LOGIN'] }` — OTPs de CONTACT_CHANGE (purpose='CONTACT_CHANGE') nunca passam por este filtro |
| T-11-01-02 | Tampering | schema.prisma — campo purpose opcional | accept | MongoDB schema-less — campo ausente é null no Prisma; sem risco de sobrescrita de dados existentes |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado neste plano |
</threat_model>

<verification>
- `npx prisma validate` passa sem erros
- `npm run test` passa com stubs `it.todo` nos 3 novos arquivos
- `grep -n "purpose" apps/api/prisma/schema.prisma` retorna linha com `purpose   String?` no OtpCode
- `grep -n "purpose" apps/api/src/modules/auth/auth.repository.ts` retorna linha com `{ in: [null, 'LOGIN'] }`
</verification>

<success_criteria>
- Campo `purpose String?` presente no model OtpCode do schema Prisma
- `findActiveOtp` do login filtra `purpose: { in: [null, 'LOGIN'] }` — OTPs legados e novos de LOGIN funcionam; CONTACT_CHANGE isolados
- 3 arquivos de stub de teste criados e passando em `npm run test`
</success_criteria>

<output>
Criar `.planning/phases/11-configura-es-e-perfil-do-cliente/11-01-SUMMARY.md` ao concluir.
</output>
