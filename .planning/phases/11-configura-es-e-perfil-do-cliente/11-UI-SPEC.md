---
phase: 11
slug: configuracoes-e-perfil-do-cliente
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-19
---

# Phase 11 — UI Design Contract
# Configurações e Perfil do Cliente

> Visual and interaction contract for Phase 11. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — design tokens via Tailwind @theme (globals.css) |
| Preset | not applicable |
| Component library | none (custom inline styles + CSS vars) |
| Icon library | custom SVG paths via `Icon.tsx` / `Ic` registry (Lucide-derived) |
| Font — display | Bricolage Grotesque Variable (títulos e números grandes) |
| Font — body | Hanken Grotesk (texto e UI) |

**Source:** `apps/web/src/styles/globals.css` — tokens definidos e em uso desde Phase 1.

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Gap entre ícone e label no tab bar; gap interno de icon+text em linha |
| sm | 8px | Espaçamento entre campos inline; gap entre ícone e texto em botões de ação |
| sm+ | 12px | Gap entre botões lado a lado no dialog (Cancelar / Confirmar mudança); padding interno de toast e banner |
| md | 16px | Padding horizontal padrão de campos de formulário; gap entre label e campo |
| lg | 24px | Padding horizontal de seções e tela; padding vertical de seção dentro de card |
| xl | 32px | Separação entre seções de configuração (Dados Pessoais → Contato → Condomínio) |
| 2xl | 48px | Espaço abaixo do último card antes do rodapé (zona segura + tab bar) |
| 3xl | 64px | Não utilizado nesta fase |

Exceções (componentes existentes — não replicar em novos componentes):
- Tab bar: altura fixa 56px + `env(safe-area-inset-bottom)` (padrão existente — não alterar)
- Botão de voltar (AppBar): 38×38px visual, padding 3px externo para hit target de 44px (padrão existente — apenas herdar)
- Touch targets mínimos: 44px em todos os elementos interativos (UI-10)
- Header AppBar interno: padding `'6px 20px 14px'` + `env(safe-area-inset-top)` (padrão existente — apenas herdar)

**Regra de novos componentes:** valores fora da escala de 4px (38px, 3px, 6px, 14px) são herdados somente onde explicitamente indicado acima. Novos componentes criados nesta fase devem usar exclusivamente múltiplos de 4px.

---

## Typography

| Role | Size | Weight | Line Height | Font | Usage |
|------|------|--------|-------------|------|-------|
| Body | 15px (`--text-base`) | 400 (regular) | 1.5 | Hanken Grotesk | Valores de campos, texto descritivo de seção, corpo de dialogs, texto de toast, texto de banner |
| Label | 12.5px (`--text-sm`) | 600 (semibold) | 1.4 | Hanken Grotesk | Labels acima de campos, captions de seção, texto auxiliar de CPF bloqueado, step indicator ("Passo 1 de 2"), texto de erro inline abaixo de campos e botões |
| Heading | 21px (`--text-xl`) | 600 (semibold) | 1.2 | Bricolage Grotesque | Título da AppBar da tela ("Perfil" / "Editar contato") |
| Display | 32px (`--text-3xl`) | 600 (semibold) | 1.1 | Bricolage Grotesque | Não utilizado nesta fase |

**Letter-spacing do heading:** `-0.02em` (padrão existente em ScheduleScreen e HomeScreen).

**Regra de pesos:** somente 2 pesos em uso — 400 (body) e 600 (labels, headings e botões). Peso 700 não é utilizado nesta fase. Bricolage Grotesque 600 renderiza visualmente próximo ao 700, mantendo consistência com botões.

**Nota — tab bar label:** O label "Perfil" no tab bar exibido a ~10.5px é herança do componente `ClientTabBar` existente (tamanho herdado via CSS do componente, não uma role tipográfica nova). Não constitui um 5º tamanho de fonte — é o mesmo token `--text-sm` (12.5px) renderizado em espaço comprimido pelo flex layout de 5 tabs. Não alterar o CSS do tab bar para conformar: o label herda o padrão existente como os valores de espaçamento 38px/3px/6px.

---

## Color

Todos os tokens extraídos de `apps/web/src/styles/globals.css` — não criar novos tokens nesta fase.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) — fundo | `--color-app-bg` | `#FAF5EC` | Fundo de toda a SettingsScreen e EditContactScreen |
| Secondary (30%) — superfícies | `--color-surface` | `#FFFFFF` | Cards de seção; fundo de campos de input ativos |
| Secondary alt | `--color-surface-2` | `#F4EBDA` | Fundo de campos readonly (CPF bloqueado); fundo do botão voltar |
| Texto primário | `--color-text` | `#241608` | Nome, dados de campos preenchidos, título AppBar |
| Texto secundário | `--color-text-sec` | `#7C6A50` | Labels de campo, texto auxiliar, endereço de condomínio |
| Texto terciário | `--color-text-ter` | `#A89A82` | Placeholder de campos vazios, tab label inativo |
| Accent (10%) | `--color-accent` | `#B0702A` | **Reservado para:** label do tab ativo "Perfil"; botão primário CTA (Salvar, Enviar código, Confirmar código) |
| Accent dourado | `--color-gold` | `#E3AC3F` | Ícone do tab ativo "Perfil" (padrão tab bar existente) |
| Espresso | `--color-espresso` | `#1E1207` | Fundo de toast (sucesso e erro), texto de botão primário via `--color-primary-btn-text` |
| Destructive | `--color-good` seria N/A — usar `#C0392B` inline | N/A | **Reservado para:** texto do botão "Sair" na seção Conta (vermelho semântico); borda de campo com erro de validação |
| Good | `--color-good` | `#3E7C53` | Ícone de check no step OTP confirmado com sucesso |
| Good soft | `--color-good-soft` | `#DCEBDF` | Fundo do badge de sucesso no step OTP |
| Borda | `--color-border` | `rgba(43,26,12,0.10)` | Borda de campos de input, separadores de seção |
| Borda suave | `--color-border-2` | `rgba(43,26,12,0.06)` | Borda superior do tab bar (existente); separadores internos de card |
| Sombra soft | `--shadow-soft` | (CSS var) | Sombra dos cards de seção |

**Accent reservado para:** botão CTA primário (Salvar dados, Salvar endereço, Enviar código, Confirmar código), label e ícone do tab ativo "Perfil".
**Nunca usar accent em:** texto body, backgrounds de seção, bordas de campos, ícones decorativos.

**Nota sobre destructive:** O projeto não define um token `--color-destructive`. Usar `color: '#C0392B'` inline exclusivamente no texto do botão "Sair". Nenhum background vermelho — apenas a cor do texto.

---

## Border Radii

Tokens existentes — usar conforme contexto:

| Token | Value | Usage nesta fase |
|-------|-------|-----------------|
| `--radius-field` | 14px | Campos de input (nome, data, apartamento, bloco, novo contato) |
| `--radius-btn` | 16px | Botões primários (Salvar, Enviar código, Confirmar código) |
| `--radius-card` | 22px | Cards de seção (Dados Pessoais, Contato, Condomínio, Conta) |
| `--radius-pill` | 999px | Chips de seleção de canal (SMS / E-mail) na EditContactScreen |

---

## Screens e Layout

### SettingsScreen (`/client/perfil`)

**Estrutura:** Tab de nível superior — sem AppBar com botão voltar. Título da página exibido no topo da área scrollável com padding top seguro.

**Focal point:** Card "Dados Pessoais" é o primeiro ponto de atenção ao abrir a tela — posicionado imediatamente abaixo do título, sem elementos que disputem atenção acima da dobra.

**Layout:**
```
SafeAreaTop padding
Título "Perfil" (heading 21px, Bricolage, weight 600, color-text)
─────────────────────
ScrollArea (padding horizontal 16px)
  Card — Dados Pessoais
    Campo: Nome completo (editável)
    Campo: Data de nascimento (editável)
    Campo: CPF (readonly — fundo surface-2, cursor not-allowed)
  Card — Contato
    Linha: Telefone (label + valor ou "Não informado") + botão 'Editar contato' (chevR)
    Linha: E-mail (label + valor ou "Não informado") + botão 'Editar contato' (chevR)
  Botão "Salvar dados" (CTA primário — span toda a largura)
  ─────────────────────
  Card — Condomínio
    CondoSearch inline (igual OnboardingScreen step 2)
    Campo: Apartamento
    Campo: Bloco (condicional — somente se condo.type === 'BLOCKS')
  Botão "Salvar endereço" (CTA primário)
  ─────────────────────
  Card — Conta
    Botão "Sair" (texto vermelho '#C0392B', sem fundo, sem borda — aparência de link/texto)
─────────────────────
PaddingBottom 48px (zona acima do tab bar)
```

**Comportamento de seção Condomínio:**
- Se o usuário não alterou o `condominiumId` ao salvar: nenhum dialog — salva silenciosamente apartamento e bloco.
- Se o `condominiumId` mudou: exibe dialog de confirmação antes de salvar (ver seção Dialog abaixo).

### EditContactScreen (`/client/perfil/editar-contato`)

**Estrutura:** Tela em stack com AppBar (botão voltar + título). Step wizard de 2 passos inline (mesma tela, sem nova rota).

**AppBar:** Idêntica ao padrão de ScheduleScreen — botão voltar 38×38px (surface-2, radius 12px) + título heading.

**Focal point (Step 1):** Heading "Qual é o novo contato?" ancora o olhar imediatamente abaixo do step indicator — campo de input segue na sequência visual natural.
**Focal point (Step 2):** Campo OTP centralizado ancora a atenção — quatro dígitos bem espaçados são o único elemento interativo visível no step.

**Layout — Step 1 (input do novo contato):**
```
AppBar: ← "Editar contato"
─────────────────────
Padding 24px horizontal
StepIndicator: "Passo 1 de 2" (label 12.5px, color-text-sec)
Heading: "Qual é o novo contato?" (21px, Bricolage, weight 600)
Subtitle: "Você receberá um código de verificação." (15px, color-text-sec)
─────────────────────
ChannelSelector (SMS / E-mail) — reutilizar componente existente
Campo de input (phone ou email conforme canal selecionado)
─────────────────────
Botão "Enviar código" (CTA primário, disabled até campo preenchido)
Erro inline: texto 12.5px (`--text-sm`) color '#C0392B' abaixo do botão
```

**Layout — Step 2 (OTP):**
```
StepIndicator: "Passo 2 de 2"
Heading: "Confirme o código"
Subtitle: "Código enviado para {novo contato}." (15px, color-text-sec)
─────────────────────
OtpInput 4 dígitos (componente existente — reutilizar)
ResendTimer (componente existente — reutilizar)
─────────────────────
Botão "Confirmar código" (CTA primário, disabled até 4 dígitos)
Erro inline abaixo do botão
```

**Transição Step 1 → Step 2:** Fade + slide para cima suave (150ms ease-out). Sem nova navegação — estado interno `step: 1 | 2` no componente.

---

## Componentes Reutilizados (sem modificação)

| Componente | Arquivo | Uso nesta fase |
|------------|---------|----------------|
| `OtpInput` | `components/auth/OtpInput.tsx` | Step 2 do EditContactScreen |
| `ResendTimer` | `components/auth/ResendTimer.tsx` | Step 2 do EditContactScreen |
| `CondoSearch` | `components/auth/CondoSearch.tsx` | Seção Condomínio da SettingsScreen |
| `ChannelSelector` | `components/auth/ChannelSelector.tsx` | Step 1 do EditContactScreen |
| `Icon` | `components/brand/Icon.tsx` | Ícone `user` no 5º tab; `chevR` nas linhas de contato; `arrowL` no botão voltar |
| `ClientTabBar` | `components/client/ClientTabBar.tsx` | Modificar: adicionar 5º tab `{ label: 'Perfil', icon: 'user', path: '/client/perfil' }` |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Tab label (5º tab) | `Perfil` |
| Título da SettingsScreen | `Perfil` |
| Título da EditContactScreen | `Editar contato` |
| Seção dados pessoais | `Dados pessoais` |
| Seção contato | `Contato` |
| Seção condomínio | `Condomínio` |
| Seção conta | `Conta` |
| Label campo CPF (readonly) | `CPF` |
| Texto auxiliar CPF | `O CPF não pode ser alterado.` |
| Label campo nome | `Nome completo` |
| Label campo data nascimento | `Data de nascimento` |
| Label campo telefone | `Telefone` |
| Label campo e-mail | `E-mail` |
| Placeholder telefone/email vazio | `Não informado` |
| Label campo apartamento | `Apartamento` |
| Label campo bloco | `Bloco / Torre` |
| Botão CTA dados pessoais | `Salvar dados` |
| Botão CTA condomínio | `Salvar endereço` |
| Botão de linha de contato | `Editar contato` |
| Botão sair | `Sair` |
| Step 1 — heading | `Qual é o novo contato?` |
| Step 1 — subtitle | `Você receberá um código de verificação.` |
| Step 1 — CTA | `Enviar código` |
| Step 2 — heading | `Confirme o código` |
| Step 2 — subtitle | `Código enviado para {contato}.` |
| Step 2 — CTA | `Confirmar código` |
| Toast sucesso dados | `Dados salvos!` |
| Toast sucesso condomínio (sem mudança de condo) | `Endereço atualizado!` |
| Toast sucesso contato | `Contato atualizado!` |
| Toast erro genérico | `Algo deu errado. Tente novamente.` |
| Erro contato já em uso | `Este contato já está associado a outra conta.` |
| Erro OTP incorreto | `Código incorreto. Verifique e tente de novo.` |
| Erro OTP expirado | `Código expirado. Solicite um novo.` |
| Empty state telefone | `Não informado` |
| Empty state e-mail | `Não informado` |
| **Dialog — título** | `Mudar de condomínio` |
| **Dialog — corpo** | `Mudar de condomínio vai desativar sua agenda semanal ativa. Você precisará reconfigurar a agenda no novo endereço.` |
| **Dialog — botão cancelar** | `Cancelar` |
| **Dialog — botão confirmar** | `Confirmar mudança` |
| **Banner ScheduleScreen** | `Você mudou de condomínio. Configure sua nova agenda semanal.` |

**Nota — "Editar contato":** O botão nas linhas de contato usa `Editar contato` (verbo + substantivo). Em mobile com layout de linha comprimida, o truncamento natural do flex não é esperado pois "Editar contato" ocupa ~14 caracteres em 12.5px; caso o espaço seja insuficiente em testes reais, reduzir para `Editar` como exceção de contexto visual declarada — o contexto da linha (label "Telefone" / "E-mail") fornece o substantivo implicitamente.

---

## Interactions e Estados

### Campo de input (padrão existente)

| Estado | Visual |
|--------|--------|
| Default | border 1px `--color-border`, radius `--radius-field` 14px, bg `--color-surface` |
| Focus | border 1.5px `--color-accent` |
| Readonly (CPF) | bg `--color-surface-2`, cursor `not-allowed`, opacity 0.7 |
| Erro | border 1.5px `#C0392B`; texto de erro 12.5px (`--text-sm`) `#C0392B` abaixo do campo |
| Disabled (botão CTA) | opacity 0.45, cursor `not-allowed` |

### Botão CTA primário (padrão existente)

- Fundo: `--color-espresso`
- Texto: `--color-primary-btn-text` (`#FBF3E4`)
- Radius: `--radius-btn` 16px
- Altura: 52px (hit target seguro)
- Largura: 100% dentro do padding da tela
- Loading state: spinner inline (substituir texto do botão por círculo animado)
- Font: `--font-body`, 15px, weight 600

### Botão "Sair"

- Aparência de texto/link: sem fundo, sem borda
- Cor do texto: `#C0392B`
- Font: `--font-body`, 15px, weight 600
- Hit target: altura mínima 44px
- Alinhamento: centralizado horizontalmente no card Conta
- Sem dialog de confirmação (D-09 — cliente vs Admin)

### Dialog de confirmação de mudança de condomínio

- Overlay: `rgba(0,0,0,0.45)` cobrindo toda a tela
- Card central: bg `--color-surface`, radius `--radius-card` 22px, padding 24px, shadow strong
- Máximo de largura: `calc(100vw - 48px)`
- Botão "Cancelar": secundário — bg `--color-surface-2`, texto `--color-text-sec`, radius `--radius-btn`
- Botão "Confirmar mudança": primário — bg `--color-espresso`, texto `--color-primary-btn-text`
- Botões lado a lado em linha (gap 12px — ver token `sm+` na tabela de espaçamento)

### Toast de feedback

Padrão existente (idêntico ao ScheduleScreen):
- Posição: `fixed`, `top: 16px`, `left: 50%`, `transform: translateX(-50%)`
- Fundo: `--color-espresso`
- Texto: `--color-primary-btn-text`, 15px (`--text-base`), weight 600
- Radius: 12px
- Padding: `12px 16px` (gap 12px — ver token `sm+` na tabela de espaçamento)
- Auto-dismiss: 2500ms
- z-index: 9999

### Step wizard inline (EditContactScreen)

- Transição entre steps: fade + translateY(-8px) para cima (150ms ease-out)
- Sem animação de slide horizontal — não há sensação de "próxima página"
- Step indicator ("Passo 1 de 2"): texto 12.5px (`--text-sm`), `--color-text-sec`

---

## Tab Bar — 5º Tab "Perfil"

Modificação no `ClientTabBar.tsx`:

```typescript
{ label: 'Perfil', icon: 'user', path: '/client/perfil' }
```

- Ícone: `user` (já existe em `Icon.tsx` — path de person/silhueta)
- Label: `'Perfil'`
- Rota: `/client/perfil`
- `isActive`: `location.pathname.startsWith('/client/perfil')`
- Cor ícone ativo: `var(--color-gold)` (padrão existente)
- Cor label ativo: `var(--color-accent)` (padrão existente)
- Peso label ativo: 600; inativo: 600 (padrão existente — sem distinção de peso entre estados)

Ao expandir de 4 para 5 tabs: cada tab recebe `flex: 1`. Com 5 tabs, largura de cada tab cai de ~25% para ~20% — espaço suficiente para label de 6 caracteres ("Perfil") no tamanho herdado do componente existente. O tamanho de fonte do tab label é herdado do `ClientTabBar` CSS existente e não constitui role tipográfica nova (ver nota em Typography).

---

## Banner de Agenda na ScheduleScreen

Inserir banner contextual entre o AppBar e o conteúdo da ScheduleScreen quando:
- `user.condominiumId` mudou desde o último acesso (detectado comparando com valor salvo), OU
- flag `scheduleDeactivated: true` presente no `AuthUser` após mudança de condomínio

Visual do banner (padrão `BannerInsuficiente` como referência):
- Fundo: `--color-gold-soft` (`#F3DDA6`)
- Borda esquerda: 3px solid `--color-accent`
- Ícone: `alert` (cor `--color-accent`)
- Texto: 15px (`--text-base`), `--color-text`, weight 600
- Radius: 12px
- Padding: `12px 16px` (gap 12px — ver token `sm+` na tabela de espaçamento)
- O banner desaparece após o usuário salvar nova agenda com pelo menos 1 dia configurado

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | nenhum | not applicable — projeto não usa shadcn |
| third-party | nenhum | not applicable |

Nenhum componente de terceiros adicionado nesta fase. Todos os componentes são internos ao projeto ou reutilizados de fases anteriores.

---

## Fontes de Pré-população

| Campo | Fonte | Decisão |
|-------|-------|---------|
| Design tokens (cores, tipografia, espaçamento) | `apps/web/src/styles/globals.css` | Leitura direta do arquivo |
| Padrão AppBar | `ScheduleScreen.tsx` linhas 94–135 | Replicar exatamente |
| Padrão tab bar | `ClientTabBar.tsx` + `Icon.tsx` | Ícone `user` já existe |
| Padrão toast | `ScheduleScreen.tsx` linhas 70–92 | Replicar exatamente |
| Padrão OTP input | `OtpInput.tsx` + `LoginScreen.tsx` | Reutilizar sem modificação |
| Padrão CondoSearch | `OnboardingScreen.tsx` step 2 | Reutilizar sem modificação |
| Padrão ChannelSelector | `OnboardingScreen.tsx` step 1 | Reutilizar sem modificação |
| D-01 a D-19 | `11-CONTEXT.md` §decisions | Todas as decisões de interação vêm do CONTEXT.md |
| Botão Sair sem dialog | CONTEXT.md D-09 | Explícito |
| OTP purpose CONTACT_CHANGE | STATE.md D-18 | Sem conflito com login |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
