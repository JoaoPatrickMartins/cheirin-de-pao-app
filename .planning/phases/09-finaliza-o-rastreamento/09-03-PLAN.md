---
phase: 09-finaliza-o-rastreamento
plan: 03
type: execute
wave: 2
depends_on:
  - 09-02
files_modified:
  - apps/web/src/pages/client/NotificationsScreen.tsx
  - apps/web/src/pages/client/HomeScreen.tsx
  - apps/web/src/pages/client/__tests__/NotificationsScreen.test.tsx
  - apps/web/src/pages/client/__tests__/TrackingScreen.test.tsx
autonomous: true
requirements:
  - ACOMP-01
  - ACOMP-04
  - ACOMP-05

must_haves:
  truths:
    - "Badge do sino na HomeScreen zera ao abrir NotificationsScreen (via refresh() do NotifContext)"
    - "NotificationsScreen exibe CTA 'Ver pedido' para DELIVERY_EVE e DELIVERY_DONE"
    - "NotificationsScreen exibe CTA 'Acompanhar' para OUT_FOR_DELIVERY"
    - "HomeScreen usa useNotif() do NotifContext em vez de useNotifBadge()"
    - "PATCH /notifications/read-all é chamado no mount de NotificationsScreen e seguido de refresh()"
    - "Stubs de teste para TrackingScreen e NotificationsScreen existem e passam"
  artifacts:
    - path: "apps/web/src/pages/client/NotificationsScreen.tsx"
      provides: "CTA_CONFIG completo (5 entradas) + badge sync via NotifContext"
      contains: "DELIVERY_EVE"
    - path: "apps/web/src/pages/client/HomeScreen.tsx"
      provides: "useNotif() substituindo useNotifBadge()"
      contains: "useNotif"
    - path: "apps/web/src/pages/client/__tests__/NotificationsScreen.test.tsx"
      provides: "stubs de teste Wave 0 para NotificationsScreen"
    - path: "apps/web/src/pages/client/__tests__/TrackingScreen.test.tsx"
      provides: "stubs de teste Wave 0 para TrackingScreen"
  key_links:
    - from: "NotificationsScreen.tsx useEffect"
      to: "NotifContext refresh()"
      via: "const { refresh } = useNotif()"
      pattern: "refresh\(\)"
    - from: "NotificationsScreen.tsx CTA_CONFIG"
      to: "navigate(cta.path)"
      via: "DELIVERY_EVE: { label: 'Ver pedido', path: '/client/pedidos' }"
      pattern: "DELIVERY_EVE"
    - from: "HomeScreen.tsx"
      to: "NotifContext unreadCount"
      via: "const { unreadCount } = useNotif()"
      pattern: "useNotif"
---

<objective>
Completar os gaps de frontend que dependem de NotifContext (plano 09-02): migrar HomeScreen de useNotifBadge para useNotif(); completar CTA_CONFIG na NotificationsScreen com as 5 entradas corretas; adicionar refresh() após o mark-all-read para sincronizar o badge; criar stubs de teste Wave 0 para TrackingScreen e NotificationsScreen.

Purpose: Com NotifContext provisionado (plano 09-02), este plano fecha o ciclo de sincronização do badge e corrige os CTAs das notificações. Sem estes gaps, badge não zera ao abrir notificações e DELIVERY_EVE não exibe o botão "Ver pedido".
Output: HomeScreen migrada; NotificationsScreen com CTA_CONFIG completo e badge sync; stubs de teste verdes.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-UI-SPEC.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-02-SUMMARY.md

<interfaces>
<!-- Contratos de NotifContext criados no Plano 09-02 -->

De apps/web/src/contexts/NotifContext.tsx (criado no Plano 09-02):
- export function useNotif(): { unreadCount: number; refresh: () => void }
- refresh() usa useCallback com deps [] — referência estável, safe no array de deps de useEffect

De apps/web/src/pages/client/NotificationsScreen.tsx (código atual — gaps a preencher):
CTA_CONFIG atual (incompleto — linhas ~38-42):
  const CTA_CONFIG = {
    LOW_CREDIT: { label: 'Comprar créditos', path: '/client/creditos' },
    DELIVERY_DONE: { label: 'Acompanhar', path: '/client/pedidos' },   // label ERRADO
    RECONFIGURE: { label: 'Ajustar agenda', path: '/client/agenda' },
  }
CTA_CONFIG alvo (5 entradas per D-10, D-11):
  LOW_CREDIT:       { label: 'Comprar créditos', path: '/client/creditos' }
  DELIVERY_DONE:    { label: 'Ver pedido',        path: '/client/pedidos' }  // corrigido
  DELIVERY_EVE:     { label: 'Ver pedido',        path: '/client/pedidos' }  // novo D-10
  OUT_FOR_DELIVERY: { label: 'Acompanhar',        path: '/client/pedidos' }  // novo D-11
  RECONFIGURE:      { label: 'Ajustar agenda',    path: '/client/agenda'  }

useEffect de montagem (linha ~62-80 — adicionar refresh()):
  apiFetch('/notifications/read-all', { method: 'PATCH' })
    .then(() => {
      setIsRead(true)
      refresh()   // D-08 — ADICIONAR esta linha
    })
    .catch(() => {})
useEffect deps: adicionar refresh no array — [refresh] (refresh é estável via useCallback)

De apps/web/src/pages/client/HomeScreen.tsx (linha 7 e 52 — gap a preencher):
  // REMOVER: import { useNotifBadge } from '../../hooks/useNotifBadge'
  // REMOVER: const { unreadCount } = useNotifBadge()
  // ADICIONAR: import { useNotif } from '../../contexts/NotifContext'
  // ADICIONAR: const { unreadCount } = useNotif()
  Badge visual (linhas ~171-201) — NÃO alterar visualmente; apenas trocar a fonte do unreadCount
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stubs Wave 0 para TrackingScreen e NotificationsScreen</name>
  <files>
    apps/web/src/pages/client/__tests__/TrackingScreen.test.tsx
    apps/web/src/pages/client/__tests__/NotificationsScreen.test.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/TrackingScreen.tsx (ler arquivo completo — entender estrutura atual: useOrderTracking, renderização condicional de order, timeline 3 estados, GET /orders/history)
    - apps/web/src/pages/client/NotificationsScreen.tsx (ler arquivo completo — entender CTA_CONFIG atual, useEffect de montagem, padrão de render dos cards)
    - apps/web/src/contexts/NotifContext.tsx (ver interface exportada — necessário para mock nos testes de NotificationsScreen)
    - .planning/phases/09-finaliza-o-rastreamento/09-VALIDATION.md (Tasks 09-01-01, 09-01-02 — casos esperados para cada arquivo de teste)
    - apps/web/src/hooks/__tests__/useOrderTracking.test.ts (padrão de mock de apiFetch existente — replicar vi.hoisted)
  </read_first>
  <behavior>
    TrackingScreen.test.tsx — casos:
    - Caso 1: renderiza AppBar "Sua entrega" quando ordem é null (sem entrega hoje) — mock useOrderTracking retornando { order: null, isLoading: false }; esperar texto "Sua entrega"
    - Caso 2: renderiza HeroCard e timeline quando há order com status SCHEDULED — mock useOrderTracking com order { id:'1', status:'SCHEDULED', quantity:4, scheduledDate:new Date().toISOString() }; esperar texto "Agendado"
    - Caso 3: renderiza estado DELIVERED na timeline — mock com status DELIVERED; esperar texto "Entregue"
    - Caso 4: renderiza lista de histórico quando /orders/history retorna dados — mock apiFetch para /orders/history retornar array com 1 order; esperar item na lista
    - Caso 5: renderiza empty state do histórico quando /orders/history retorna array vazio — esperar texto "Nenhuma entrega ainda"

    NotificationsScreen.test.tsx — casos:
    - Caso 1: renderiza AppBar "Notificações" — mock useNotif retornando { unreadCount: 0, refresh: vi.fn() }; mock apiFetch /notifications/me retornando []; esperar texto "Notificações"
    - Caso 2: renderiza empty state "Tudo tranquilo por aqui" quando lista vazia — verificar heading
    - Caso 3: CTA_CONFIG inclui DELIVERY_EVE — renderizar card com type='DELIVERY_EVE'; esperar botão com label "Ver pedido"
    - Caso 4: CTA_CONFIG inclui OUT_FOR_DELIVERY — renderizar card com type='OUT_FOR_DELIVERY'; esperar botão com label "Acompanhar"
    - Caso 5: refresh() chamado após PATCH mark-all-read — mock apiFetch para PATCH retornar ok; esperar que refreshMock foi chamado
  </behavior>
  <action>
    CRIAR apps/web/src/pages/client/__tests__/TrackingScreen.test.tsx:
    Usar vi.hoisted para mockApiFetch e vi.mock('../../hooks/useOrderTracking') retornando o mock configurado por caso. Mock de react-router useNavigate retornando vi.fn(). Importar e renderizar TrackingScreen em cada caso com os mocks configurados antes. Os 5 casos devem inicialmente PASSAR (tela já existe) — se algum falhar por gap real, documentar no SUMMARY.

    CRIAR apps/web/src/pages/client/__tests__/NotificationsScreen.test.tsx:
    Usar vi.hoisted para mockApiFetch. Mock de '../../contexts/NotifContext' retornando { useNotif: () => ({ unreadCount: 0, refresh: vi.fn() }) }. Mock de react-router useNavigate. Renderizar NotificationsScreen com cada configuração. Casos 3 e 4 verificam CTA_CONFIG — se falharem agora (antes da Task 2), é esperado e documenta o gap; a Task 2 os tornará verdes.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx vitest run --reporter=verbose 2>&1 | grep -E "TrackingScreen|NotificationsScreen|passed|failed"</automated>
  </verify>
  <acceptance_criteria>
    - TrackingScreen.test.tsx existe em apps/web/src/pages/client/__tests__/
    - NotificationsScreen.test.tsx existe em apps/web/src/pages/client/__tests__/
    - Casos 1-2 de TrackingScreen passam (tela já renderiza corretamente)
    - Casos 1-2 de NotificationsScreen passam (AppBar e empty state já corretos)
    - Casos 3-4 de NotificationsScreen podem estar vermelhos (gap de CTA_CONFIG — serão corrigidos na Task 2)
    - cd apps/web && npx vitest run não regride testes pré-existentes
  </acceptance_criteria>
  <done>Stubs criados; casos básicos verdes; gaps de CTA_CONFIG documentados via testes vermelhos</done>
</task>

<task type="auto">
  <name>Task 2: NotificationsScreen CTA_CONFIG + badge sync; HomeScreen migrar useNotifBadge → useNotif</name>
  <files>
    apps/web/src/pages/client/NotificationsScreen.tsx
    apps/web/src/pages/client/HomeScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/client/NotificationsScreen.tsx (ler arquivo completo — localizar CTA_CONFIG, useEffect de montagem com PATCH read-all, confirmar que useNotif() ainda não está importado)
    - apps/web/src/pages/client/HomeScreen.tsx (ler arquivo completo — localizar import e uso de useNotifBadge; identificar exatamente as linhas a substituir sem alterar a lógica do badge visual)
    - apps/web/src/contexts/NotifContext.tsx (confirmar nomes de export: useNotif, NotifProvider — usar o arquivo criado no Plano 09-02)
    - .planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md (seções NotificationsScreen.tsx e HomeScreen.tsx — diff exato antes/depois)
    - .planning/phases/09-finaliza-o-rastreamento/09-UI-SPEC.md (seção CTA_CONFIG — labels e paths mandatórios; seção HomeScreen badge — confirmar sem mudança visual)
    - .planning/phases/09-finaliza-o-rastreamento/09-RESEARCH.md (Pitfall 1: refresh como dep estável; Pattern 3: mark-all-read + badge sync)
  </read_first>
  <action>
    NOTIFICATIONSSCREEN.TSX — 3 mudanças:

    1. Adicionar import: import { useNotif } from '../../contexts/NotifContext'
    2. No topo do componente, adicionar: const { refresh } = useNotif()
    3. Substituir CTA_CONFIG completo pela versão com 5 entradas:
       - LOW_CREDIT: { label: 'Comprar créditos', path: '/client/creditos' }
       - DELIVERY_DONE: { label: 'Ver pedido', path: '/client/pedidos' } (corrigir label de 'Acompanhar')
       - DELIVERY_EVE: { label: 'Ver pedido', path: '/client/pedidos' } (nova entrada — D-10)
       - OUT_FOR_DELIVERY: { label: 'Acompanhar', path: '/client/pedidos' } (nova entrada — D-11)
       - RECONFIGURE: { label: 'Ajustar agenda', path: '/client/agenda' }
    4. No useEffect de montagem, após o PATCH /notifications/read-all, adicionar refresh() no .then():
       apiFetch('/notifications/read-all', { method: 'PATCH' }).then(() => { setIsRead(true); refresh() }).catch(() => {})
    5. Atualizar deps do useEffect para incluir refresh: useEffect(() => { ... }, [refresh])
    Não alterar: estrutura dos cards, formatTimestamp, tone map, skeleton, empty state.

    HOMESCREEN.TSX — 2 mudanças:
    1. Remover: import { useNotifBadge } from '../../hooks/useNotifBadge'
    2. Adicionar: import { useNotif } from '../../contexts/NotifContext'
    3. Substituir: const { unreadCount } = useNotifBadge() por const { unreadCount } = useNotif()
    Não alterar: nenhuma lógica visual do badge (divs, estilos, condicionais unreadCount > 0); não remover nenhum outro import; não alterar o botão do sino ou navegação para /client/notificacoes.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx vitest run --reporter=verbose 2>&1 | grep -E "NotificationsScreen|TrackingScreen|passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - grep "DELIVERY_EVE" NotificationsScreen.tsx retorna linha com 'Ver pedido' e '/client/pedidos'
    - grep "OUT_FOR_DELIVERY" NotificationsScreen.tsx retorna linha com 'Acompanhar' e '/client/pedidos'
    - grep "DELIVERY_DONE" NotificationsScreen.tsx retorna linha com 'Ver pedido' (não 'Acompanhar')
    - grep "refresh()" NotificationsScreen.tsx retorna linha dentro do .then() do PATCH read-all
    - grep "useNotif" NotificationsScreen.tsx retorna 2 linhas (import + uso)
    - grep "useNotif" HomeScreen.tsx retorna 2 linhas (import + uso)
    - grep "useNotifBadge" HomeScreen.tsx retorna 0 linhas (foi removido)
    - Todos os 5 casos de NotificationsScreen.test.tsx passam (incluindo casos 3-4 de CTA_CONFIG)
    - Todos os 5 casos de TrackingScreen.test.tsx passam
    - cd apps/web && npx vitest run retorna 0 (suite completa verde)
  </acceptance_criteria>
  <done>CTA_CONFIG completo com 5 entradas corretas; badge sincronizado via refresh(); HomeScreen migrada para useNotif(); todos os 10 testes (5+5) verdes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → PATCH /notifications/read-all | Sem payload — apenas operação de estado; userId enforçado via JWT no backend |
| NotificationsScreen → navigate(cta.path) | Input de data layer (type da notificação) → rota de navegação interna |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | GET /notifications/me | accept | preHandler: [fastify.authenticate] — userId do JWT restringe notificações ao próprio usuário [VERIFICADO] |
| T-09-05 | Tampering | CTA navigate(cta.path) | accept | cta.path vem de CTA_CONFIG hardcoded no frontend — não vem do servidor; tipo da notificação determina o path; não há injeção de rota externa |
| T-09-06 | Denial of Service | useEffect [refresh] loop | mitigate | refresh() em NotifContext usa useCallback com deps [] — referência estável; sem loop infinito; Pitfall 1 do RESEARCH.md documenta este risco explicitamente |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
CTA_CONFIG completo: grep "DELIVERY_EVE\|OUT_FOR_DELIVERY" apps/web/src/pages/client/NotificationsScreen.tsx retorna 2 linhas.
Refresh após mark-all-read: grep -A3 "read-all" apps/web/src/pages/client/NotificationsScreen.tsx mostra refresh() no .then().
HomeScreen migrada: grep "useNotif\b" apps/web/src/pages/client/HomeScreen.tsx retorna 2 linhas; grep "useNotifBadge" retorna 0.
Testes verdes: cd apps/web && npx vitest run 2>&1 | grep -E "NotificationsScreen|TrackingScreen" mostra passed.
Suite completa: cd apps/web && npx vitest run retorna 0.
</verification>

<success_criteria>
- NotificationsScreen.tsx: CTA_CONFIG tem 5 entradas (LOW_CREDIT, DELIVERY_DONE com 'Ver pedido', DELIVERY_EVE, OUT_FOR_DELIVERY, RECONFIGURE); refresh() chamado após PATCH read-all
- HomeScreen.tsx: usa useNotif() — import e uso substituídos, badge visual idêntico
- cd apps/web && npx vitest run retorna 0 — todos os testes dos 2 stubs + suite pré-existente verdes
</success_criteria>

<output>
Criar .planning/phases/09-finaliza-o-rastreamento/09-03-SUMMARY.md ao concluir.
</output>
