---
phase: 09-finaliza-o-rastreamento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/contexts/NotifContext.tsx
  - apps/web/src/pages/client/ClientLayout.tsx
  - apps/web/src/hooks/useOneSignalDeepLink.ts
  - apps/web/src/contexts/__tests__/NotifContext.test.tsx
autonomous: true
requirements:
  - ACOMP-05

must_haves:
  truths:
    - "NotifContext fornece unreadCount e refresh() para toda a subárvore do ClientLayout"
    - "HomeScreen e NotificationsScreen podem consumir useNotif() e receber o mesmo unreadCount"
    - "Tocar em push com screen='pedidos' navega para /client/pedidos"
    - "refresh() em NotifContext usa useCallback com deps [] — sem loop infinito em consumers"
    - "NotifProvider está dentro do ClientLayout envolvendo Outlet e ClientTabBar"
  artifacts:
    - path: "apps/web/src/contexts/NotifContext.tsx"
      provides: "NotifProvider + useNotif() — unreadCount + refresh()"
      exports: ["NotifProvider", "useNotif"]
    - path: "apps/web/src/pages/client/ClientLayout.tsx"
      provides: "NotifProvider provisionado envolvendo Outlet + ClientTabBar"
      contains: "NotifProvider"
    - path: "apps/web/src/hooks/useOneSignalDeepLink.ts"
      provides: "handler de deep link com case 'pedidos'"
      contains: "screen === 'pedidos'"
    - path: "apps/web/src/contexts/__tests__/NotifContext.test.tsx"
      provides: "stubs de teste Wave 0 para NotifContext"
  key_links:
    - from: "ClientLayout.tsx"
      to: "NotifContext.tsx NotifProvider"
      via: "import { NotifProvider } from '../../contexts/NotifContext'"
      pattern: "NotifProvider"
    - from: "useOneSignalDeepLink.ts handleClick"
      to: "navigate('/client/pedidos')"
      via: "screen === 'pedidos'"
      pattern: "screen === 'pedidos'"
---

<objective>
Criar NotifContext (único artefato novo desta fase), provisionar no ClientLayout e estender useOneSignalDeepLink com o case 'pedidos'. Este plano estabelece a infraestrutura de contexto que o Plano 09-03 usa para sincronizar o badge entre HomeScreen e NotificationsScreen.

Purpose: Sem NotifContext, HomeScreen e NotificationsScreen ficam dessincronizados — badge não zera ao abrir notificações (D-06). Sem o case 'pedidos' no deep link, tocar numa push de véspera ou entrega não navega para TrackingScreen (D-05).
Output: NotifContext.tsx criado; ClientLayout com provider; useOneSignalDeepLink com case 'pedidos'; testes stub verdes.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase — verificados via leitura direta no RESEARCH.md e PATTERNS.md -->

De apps/web/src/contexts/AuthContext.tsx (padrão a seguir para NotifContext):
- import { createContext, useState, useEffect, useMemo } from 'react'
- export const AuthContext = createContext<AuthContextType | null>(null)
- export function AuthProvider() { return <AuthContext.Provider value={value}><Outlet /></AuthContext.Provider> }
- DIFERENÇA: NotifProvider recebe { children: React.ReactNode } — não usa Outlet

De apps/web/src/hooks/useNotifBadge.ts (lógica a migrar para NotifContext):
- import { apiFetch } from '../lib/apiFetch'
- const refresh = useCallback(async () => { ... }, [])  // deps [] obrigatório
- apiFetch('/notifications/unread-count') → { count: number }

De apps/web/src/pages/client/ClientLayout.tsx (estrutura atual completa — extraída do PATTERNS.md):
- import { Outlet } from 'react-router'
- import { useOneSignalRegister } from '../../hooks/useOneSignalRegister'
- import { useOneSignalDeepLink } from '../../hooks/useOneSignalDeepLink'
- return <div style={{ minHeight: '100dvh', background: 'var(--color-app-bg)', paddingBottom: 'calc(56px + env(safe-area-inset-bottom))' }}><Outlet /><ClientTabBar /></div>
- MODIFICAR: envolver <Outlet /> e <ClientTabBar /> com <NotifProvider>

De apps/web/src/hooks/useOneSignalDeepLink.ts (estrutura do handleClick — extraída do PATTERNS.md):
- const screen = event?.notification?.additionalData?.screen
- if (screen === 'creditos') { navigate('/client/creditos') }
- ADICIONAR: } else if (screen === 'pedidos') { navigate('/client/pedidos') }
- Padrão de cleanup: removeEventListener no return do useEffect — NÃO alterar

NotifContext — interface alvo:
  interface NotifContextValue { unreadCount: number; refresh: () => void }
  - createContext com valor default { unreadCount: 0, refresh: () => {} }
  - NotifProvider({ children: React.ReactNode }): fetch /notifications/unread-count no mount; refresh via useCallback
  - export function useNotif(): return useContext(NotifContext)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar NotifContext + stubs de teste Wave 0</name>
  <files>
    apps/web/src/contexts/NotifContext.tsx
    apps/web/src/contexts/__tests__/NotifContext.test.tsx
  </files>
  <read_first>
    - apps/web/src/contexts/AuthContext.tsx (ler arquivo completo — padrão de createContext, interface, Provider, hook useAuth — replicar estrutura com adaptações)
    - apps/web/src/hooks/useNotifBadge.ts (ler arquivo completo — lógica de fetch /notifications/unread-count e useCallback com deps [] a migrar para NotifContext)
    - apps/web/src/lib/apiFetch.ts (verificar assinatura — import path correto para NotifContext)
    - .planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md (seção NotifContext.tsx — implementação completa anotada)
    - .planning/phases/09-finaliza-o-rastreamento/09-VALIDATION.md (Task ID 09-01-03 — casos de teste esperados)
  </read_first>
  <behavior>
    Casos de teste (stubs Wave 0 — RED antes de implementar):
    - Caso 1: NotifProvider renderiza children sem erro — renderizar com filho div; não lançar exceção
    - Caso 2: useNotif() fora de NotifProvider retorna valor default { unreadCount: 0 } — não lançar erro
    - Caso 3: NotifProvider faz fetch de /notifications/unread-count no mount — mockApiFetch configurado para retornar { count: 3 }; após render waitFor que unreadCount === 3
    - Caso 4: refresh() re-faz o fetch e atualiza unreadCount — chamar refresh(); waitFor que apiFetch foi chamado 2x
    - Caso 5: falha de fetch mantém estado anterior — mockApiFetch lança erro; unreadCount permanece 0 sem exceção propagada
  </behavior>
  <action>
    STUBS PRIMEIRO (RED): Criar apps/web/src/contexts/__tests__/NotifContext.test.tsx com os 5 casos acima usando vi.hoisted para mockApiFetch (mesmo padrão dos testes existentes de hooks). Os testes devem falhar com "Cannot find module" pois NotifContext ainda não existe.

    IMPLEMENTAR (GREEN): Criar apps/web/src/contexts/NotifContext.tsx com:
    - Imports: createContext, useState, useEffect, useCallback, useContext do 'react'; apiFetch de '../lib/apiFetch'
    - Interface NotifContextValue: { unreadCount: number; refresh: () => void }
    - NotifContext = createContext<NotifContextValue>({ unreadCount: 0, refresh: () => {} }) — valor default no-op (não null)
    - NotifProvider({ children: React.ReactNode }): useState<number>(0); refresh = useCallback(async () => { try { const res = await apiFetch('/notifications/unread-count'); if (res.ok) { const data = await res.json() as { count: number }; setUnreadCount(data.count) } } catch {} }, []); useEffect(() => { void refresh() }, [refresh]); return <NotifContext.Provider value={{ unreadCount, refresh }}>{children}</NotifContext.Provider>
    - export function useNotif(): return useContext(NotifContext)
    - Exportar também NotifProvider e NotifContext (named exports)

    Pitfall crítico: refresh DEVE ser useCallback com deps [] — evita loop infinito em NotificationsScreen que inclui refresh no array de deps do seu useEffect.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web && npx vitest run --reporter=verbose 2>&1 | grep -E "NotifContext|passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - NotifContext.tsx exporta NotifProvider, useNotif, e NotifContext
    - grep "useCallback" apps/web/src/contexts/NotifContext.tsx retorna linha com deps []
    - grep "unread-count" apps/web/src/contexts/NotifContext.tsx retorna linha com apiFetch('/notifications/unread-count')
    - Todos os 5 casos de teste passam (GREEN) — cd apps/web && npx vitest run NotifContext retorna passed
    - Suite web completa não regride: cd apps/web && npx vitest run retorna 0
  </acceptance_criteria>
  <done>NotifContext criado com useCallback estável; 5 testes verdes; suite web verde</done>
</task>

<task type="auto">
  <name>Task 2: Provisionar NotifProvider no ClientLayout + estender useOneSignalDeepLink</name>
  <files>
    apps/web/src/pages/client/ClientLayout.tsx
    apps/web/src/hooks/useOneSignalDeepLink.ts
  </files>
  <read_first>
    - apps/web/src/pages/client/ClientLayout.tsx (ler arquivo completo — ver estrutura atual com Outlet e ClientTabBar antes de adicionar NotifProvider)
    - apps/web/src/hooks/useOneSignalDeepLink.ts (ler arquivo completo — localizar handleClick e o bloco if (screen === 'creditos') para adicionar o else if; verificar padrão de cleanup do listener)
    - .planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md (seção ClientLayout.tsx e seção useOneSignalDeepLink.ts — código exato antes/depois)
    - .planning/phases/09-finaliza-o-rastreamento/09-RESEARCH.md (Pitfall 3: NotifProvider deve envolver Outlet, não substituí-lo; Pitfall Anti-pattern: não criar novo listener OneSignal)
  </read_first>
  <action>
    CLIENTLAYOUT.TSX:
    1. Adicionar import: import { NotifProvider } from '../../contexts/NotifContext'
    2. No return do componente, envolver o conteúdo interno com NotifProvider — a estrutura do div externo permanece idêntica:
       return (<div style={{ minHeight: '100dvh', background: 'var(--color-app-bg)', paddingBottom: 'calc(56px + env(safe-area-inset-bottom))' }}><NotifProvider><Outlet /><ClientTabBar /></NotifProvider></div>)
    3. Não alterar nenhuma outra parte do arquivo — hooks useOneSignalRegister e useOneSignalDeepLink continuam sendo chamados no corpo do componente.

    USEONESIGNALDEEPLINK.TS:
    1. Dentro de handleClick, localizar o bloco: if (screen === 'creditos') { navigate('/client/creditos') }
    2. Adicionar imediatamente após: } else if (screen === 'pedidos') { navigate('/client/pedidos') }
    3. Não alterar o bloco try/catch externo, não alterar o padrão de addEventListener/removeEventListener no cleanup.
    Anti-pattern a evitar: não criar segundo listener, não registrar o hook novamente em outro componente — a extensão é apenas um case novo no handleClick existente.
  </action>
  <verify>
    <automated>grep -n "NotifProvider\|screen === 'pedidos'" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/pages/client/ClientLayout.tsx /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/hooks/useOneSignalDeepLink.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "NotifProvider" ClientLayout.tsx retorna 2 linhas (import + uso no JSX)
    - grep "screen === 'pedidos'" useOneSignalDeepLink.ts retorna linha com navigate('/client/pedidos')
    - grep "screen === 'creditos'" useOneSignalDeepLink.ts ainda retorna linha (case existente não foi removido)
    - ClientLayout.tsx ainda contém useOneSignalRegister() e useOneSignalDeepLink() no corpo do componente (não foram movidos)
    - cd apps/web && npx vitest run retorna 0 (sem regressão)
  </acceptance_criteria>
  <done>NotifProvider provisionado no ClientLayout; deep link para 'pedidos' funcional; suite web verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → GET /notifications/unread-count | Requisição autenticada via JWT cookie; resposta é apenas { count: N } |
| OneSignal push click → navigate() | Input externo (notification.additionalData.screen) processado pelo handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | GET /notifications/unread-count | accept | preHandler: [fastify.authenticate] enforçado no endpoint; userId do JWT restringe o count ao próprio usuário [VERIFICADO no RESEARCH.md] |
| T-09-02 | Tampering | deep link via additionalData.screen | accept | useOneSignalDeepLink usa whitelist implícita hardcoded: 'creditos' → /client/creditos, 'pedidos' → /client/pedidos; qualquer outro valor ignorado; sem exec de código externo |
| T-09-04 | Denial of Service | NotifContext polling no mount | accept | Fetch único no mount via useEffect; refresh() chamado sob demanda; sem polling contínuo — sem risco de amplificação |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase |
</threat_model>

<verification>
NotifContext exporta corretamente: grep -n "export function NotifProvider\|export function useNotif" apps/web/src/contexts/NotifContext.tsx retorna 2 linhas.
useCallback com deps []: grep -A2 "useCallback" apps/web/src/contexts/NotifContext.tsx retorna "}, [])".
NotifProvider no ClientLayout: grep "NotifProvider" apps/web/src/pages/client/ClientLayout.tsx retorna import e JSX.
Deep link 'pedidos': grep "pedidos" apps/web/src/hooks/useOneSignalDeepLink.ts retorna navigate('/client/pedidos').
Stubs verdes: cd apps/web && npx vitest run --reporter=verbose 2>&1 | grep "NotifContext" mostra "passed".
Suite web verde: cd apps/web && npx vitest run retorna 0.
</verification>

<success_criteria>
- NotifContext.tsx criado com NotifProvider (children prop), useNotif() e refresh() estável (useCallback deps [])
- ClientLayout.tsx provisiona NotifProvider envolvendo Outlet + ClientTabBar
- useOneSignalDeepLink.ts trata screen === 'pedidos' → navigate('/client/pedidos') sem remover case 'creditos'
- cd apps/web && npx vitest run retorna 0 (5 testes do NotifContext + suite existente)
</success_criteria>

<output>
Criar .planning/phases/09-finaliza-o-rastreamento/09-02-SUMMARY.md ao concluir.
</output>
