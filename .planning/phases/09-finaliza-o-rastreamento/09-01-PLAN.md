---
phase: 09-finaliza-o-rastreamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/schedules/schedules.service.ts
  - apps/api/src/modules/admin-orders/admin-orders.service.ts
autonomous: true
requirements:
  - ACOMP-02
  - ACOMP-03

must_haves:
  truths:
    - "Push de véspera enviado por sendEveReminders tem notification.data = { screen: 'pedidos' }"
    - "Notificação DELIVERY_EVE persistida com actionRoute '/client/pedidos' e title 'Entrega amanhã 🍞'"
    - "Push de entrega confirmada enviado por notifyAndPersist tem notification.data = { screen: 'pedidos' }"
    - "Push de véspera tem headings.pt = 'Entrega amanhã 🍞' (alinhado ao copywriting contract)"
    - "Push de entrega tem headings.pt = 'Entrega realizada! 🎉' (alinhado ao copywriting contract)"
  artifacts:
    - path: "apps/api/src/modules/schedules/schedules.service.ts"
      provides: "sendEveReminders com notification.data e actionRoute preenchidos"
      contains: "screen: 'pedidos'"
    - path: "apps/api/src/modules/admin-orders/admin-orders.service.ts"
      provides: "notifyAndPersist com notification.data preenchido"
      contains: "screen: 'pedidos'"
  key_links:
    - from: "schedules.service.ts sendEveReminders"
      to: "osClient.createNotification"
      via: "notification.data = { screen: 'pedidos' }"
      pattern: "notification\\.data"
    - from: "schedules.service.ts sendEveReminders"
      to: "notificationsService.createAndTrim"
      via: "actionRoute: '/client/pedidos'"
      pattern: "actionRoute.*pedidos"
    - from: "admin-orders.service.ts notifyAndPersist"
      to: "osClient.createNotification"
      via: "notification.data = { screen: 'pedidos' }"
      pattern: "notification\\.data"
---

<objective>
Preencher os 3 gaps de backend identificados na auditoria da Fase 9: (1) sendEveReminders não propaga notification.data ao push OneSignal e não passa actionRoute ao createAndTrim; (2) notifyAndPersist não propaga notification.data ao push de DELIVERY_DONE. Ambas as funções também precisam ter headings.pt alinhados ao copywriting contract do UI-SPEC.

Purpose: Sem notification.data, tocar na notificação push não navega para /client/pedidos — o deep link não funciona. Sem actionRoute na DELIVERY_EVE persistida, o CTA da NotificationsScreen não exibe o botão "Ver pedido". As correções são cirúrgicas: 2-3 linhas por arquivo.
Output: schedules.service.ts e admin-orders.service.ts corrigidos; deep link funcional no push; CTA de notificação in-app funcionando para DELIVERY_EVE.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/../../../.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase — verificados via leitura direta no RESEARCH.md e PATTERNS.md -->

De apps/api/src/modules/schedules/schedules.service.ts (sendEveReminders — gaps a preencher):
- Bloco push (linhas ~196-208): new OneSignal.Notification() com app_id, include_subscription_ids, headings, contents
- ADICIONAR após contents: notification.data = { screen: 'pedidos' }
- ATUALIZAR headings.pt: 'Entrega amanhã 🍞' (atualmente 'Cheirin de Pão')
- Bloco createAndTrim (linhas ~211-216): objeto { userId, type: 'DELIVERY_EVE', title, body }
- ADICIONAR ao objeto: actionRoute: '/client/pedidos'
- ATUALIZAR title: 'Entrega amanhã 🍞' (para consistência com o push)

De apps/api/src/modules/admin-orders/admin-orders.service.ts (notifyAndPersist — gap a preencher):
- Bloco push (linhas ~93-101): new OneSignal.Notification() com app_id, include_subscription_ids, headings, contents
- ADICIONAR após contents: notification.data = { screen: 'pedidos' }
- ATUALIZAR headings.pt: 'Entrega realizada! 🎉' (atualmente 'Cheirin de Pão')
- Nota: createAndTrim de DELIVERY_DONE (linhas ~111-117) JÁ tem actionRoute: '/client/pedidos' — não alterar

De apps/api/src/modules/notifications/notifications.service.ts (createAndTrim — assinatura atual):
- Aceita { userId, type, title, body, actionRoute? } — campo actionRoute já existe como opcional

Padrão OneSignal backend SDK (@onesignal/node-onesignal):
- notification.data = { screen: 'pedidos' }  // campo 'data' no SDK de backend
- Frontend recebe como event.notification.additionalData.screen — mapeamento automático pelo SDK

Padrão de isolamento push/persist (NÃO alterar):
- Push dentro de try/catch — falha é silenciosa (best-effort)
- createAndTrim FORA do try — persist obrigatório mesmo sem oneSignalPlayerId
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gap backend — sendEveReminders (schedules.service.ts)</name>
  <files>apps/api/src/modules/schedules/schedules.service.ts</files>
  <read_first>
    - apps/api/src/modules/schedules/schedules.service.ts (ler arquivo completo — localizar sendEveReminders, identificar as linhas exatas do bloco push e do bloco createAndTrim antes de qualquer edição)
    - apps/api/src/modules/admin-orders/admin-orders.service.ts (ler o bloco notifyAndPersist para ver o padrão notification.data já estabelecido no codebase — servir como referência de estilo)
    - apps/api/src/modules/notifications/notifications.service.ts (verificar assinatura de createAndTrim — confirmar que actionRoute já é aceito)
    - .planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md (seção schedules.service.ts — padrão exato antes/depois)
  </read_first>
  <action>
    Em schedules.service.ts, dentro de sendEveReminders():

    1. No bloco de criação do OneSignal.Notification (dentro do try/catch de push), após a linha notification.contents:
       - Atualizar notification.headings para { pt: 'Entrega amanhã 🍞' } (per D-03, copywriting contract UI-SPEC)
       - Adicionar notification.data = { screen: 'pedidos' } (per D-03 — campo 'data' do SDK @onesignal/node-onesignal)

    2. Na chamada a notificationsService.createAndTrim() (FORA do bloco try do push):
       - Atualizar title para 'Entrega amanhã 🍞' (consistência com o push)
       - Adicionar campo actionRoute: '/client/pedidos' ao objeto passado (per D-09)

    Não alterar: estrutura try/catch do push, localização do createAndTrim fora do try, nenhum outro método do serviço.
  </action>
  <verify>
    <automated>grep -n "notification\.data\|actionRoute.*pedidos\|Entrega amanhã" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/schedules/schedules.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "notification\.data" schedules.service.ts retorna linha com { screen: 'pedidos' }
    - grep "actionRoute" schedules.service.ts retorna linha com '/client/pedidos' dentro do bloco createAndTrim de sendEveReminders
    - grep "Entrega amanhã" schedules.service.ts retorna 2 linhas (headings do push + title do createAndTrim)
    - O padrão de isolamento push/persist é preservado: createAndTrim continua FORA do try/catch do push
    - cd apps/api && npx vitest run 2>&1 | grep -E "passed|failed" não mostra regressão de testes existentes
  </acceptance_criteria>
  <done>sendEveReminders envia push com deep link e persiste DELIVERY_EVE com actionRoute; copywriting alinhado ao UI-SPEC</done>
</task>

<task type="auto">
  <name>Task 2: Gap backend — notifyAndPersist (admin-orders.service.ts)</name>
  <files>apps/api/src/modules/admin-orders/admin-orders.service.ts</files>
  <read_first>
    - apps/api/src/modules/admin-orders/admin-orders.service.ts (ler arquivo completo — localizar notifyAndPersist, identificar bloco push com OneSignal.Notification e bloco createAndTrim; confirmar que createAndTrim JÁ tem actionRoute antes de editar)
    - .planning/phases/09-finaliza-o-rastreamento/09-PATTERNS.md (seção admin-orders.service.ts — padrão exato antes/depois com nota "createAndTrim DELIVERY_DONE já tem actionRoute — não alterar")
  </read_first>
  <action>
    Em admin-orders.service.ts, dentro de notifyAndPersist():

    1. No bloco de criação do OneSignal.Notification (dentro do try/catch de push), após a linha notification.contents:
       - Atualizar notification.headings para { pt: 'Entrega realizada! 🎉' } (copywriting contract UI-SPEC)
       - Adicionar notification.data = { screen: 'pedidos' } (per D-04 — campo 'data' do SDK @onesignal/node-onesignal)

    2. NÃO alterar o bloco createAndTrim de DELIVERY_DONE — já tem actionRoute: '/client/pedidos' corretamente configurado.

    Não alterar: estrutura try/catch do push, padrão de isolamento push/persist, nenhum outro método do serviço.
  </action>
  <verify>
    <automated>grep -n "notification\.data\|Entrega realizada" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/admin-orders/admin-orders.service.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "notification\.data" admin-orders.service.ts retorna linha com { screen: 'pedidos' } dentro de notifyAndPersist
    - grep "Entrega realizada" admin-orders.service.ts retorna linha com 'Entrega realizada! 🎉' no headings.pt
    - grep "actionRoute.*pedidos" admin-orders.service.ts retorna linha no bloco createAndTrim de DELIVERY_DONE (já existia — confirmar não foi removido)
    - cd apps/api && npx vitest run 2>&1 | grep -E "passed|failed" não mostra regressão de testes existentes
  </acceptance_criteria>
  <done>notifyAndPersist envia push DELIVERY_DONE com deep link para /client/pedidos; copywriting alinhado ao UI-SPEC</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schedules.service → OneSignal API | Saída de rede com REST_API_KEY; campo notification.data é payload controlado internamente (hardcoded) |
| admin-orders.service → OneSignal API | Mesma boundary; notification.data = { screen: 'pedidos' } não carrega input externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | notificações /notifications/* | accept | preHandler: [fastify.authenticate] já em todos os endpoints — userId extraído do JWT [VERIFICADO no RESEARCH.md] |
| T-09-02 | Tampering | deep link via notification.data.screen | accept | useOneSignalDeepLink no frontend usa whitelist implícita de rotas internas hardcoded — screen: 'pedidos' navega apenas para /client/pedidos; nenhuma ação destrutiva possível [VERIFICADO no RESEARCH.md] |
| T-09-03 | Information Disclosure | sendEveReminders — push para userId errado | accept | Query usa order.userId para buscar user.oneSignalPlayerId — escopo restrito ao dono do order; padrão não alterado por esta tarefa |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase — todas as libs auditadas em fases anteriores |
</threat_model>

<verification>
notification.data em sendEveReminders: grep -n "notification\.data" apps/api/src/modules/schedules/schedules.service.ts retorna linha com { screen: 'pedidos' }
actionRoute em DELIVERY_EVE: grep -n "actionRoute" apps/api/src/modules/schedules/schedules.service.ts retorna '/client/pedidos'
notification.data em notifyAndPersist: grep -n "notification\.data" apps/api/src/modules/admin-orders/admin-orders.service.ts retorna linha com { screen: 'pedidos' }
Copywriting sendEveReminders: grep "Entrega amanhã" apps/api/src/modules/schedules/schedules.service.ts retorna 2 linhas
Copywriting notifyAndPersist: grep "Entrega realizada" apps/api/src/modules/admin-orders/admin-orders.service.ts retorna linha no headings.pt
Suite API verde: cd apps/api && npx vitest run retorna 0
</verification>

<success_criteria>
- sendEveReminders: notification.data = { screen: 'pedidos' } + actionRoute: '/client/pedidos' + headings.pt = 'Entrega amanhã 🍞'
- notifyAndPersist: notification.data = { screen: 'pedidos' } + headings.pt = 'Entrega realizada! 🎉'
- Padrão de isolamento push/persist preservado nos dois serviços
- cd apps/api && npx vitest run retorna 0 (sem regressão)
</success_criteria>

<output>
Criar .planning/phases/09-finaliza-o-rastreamento/09-01-SUMMARY.md ao concluir.
</output>
