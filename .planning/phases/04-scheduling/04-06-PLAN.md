---
phase: 04-scheduling
plan: 06
type: execute
wave: 3
depends_on:
  - "04-03"
  - "04-05"
files_modified:
  - apps/web/src/pages/client/SingleScreen.tsx
  - apps/web/src/components/client/DateChips.tsx
  - apps/web/src/routes/router.tsx
  - apps/web/src/hooks/useOneSignalRegister.ts
  - apps/web/src/pages/client/ClientLayout.tsx
autonomous: true
requirements:
  - SCHED-01
  - SCHED-04
must_haves:
  truths:
    - "SingleScreen renderiza QuantityStepper (min=1, max=20) com número em accent quando qtd <= saldo e em warn (#B0702A) quando qtd > saldo"
    - "DateChips exibe chip 'Amanhã cedo' desabilitado (opacity 0.4, não interativo) quando hora atual >= 21h (D-05)"
    - "DateChips chip 'Outra data' aciona input[type=date] nativo com min=amanhã e max=30dias (D-04, D-06)"
    - "BannerInsuficiente é exibido com props onComprar e onAjustar quando qtd > saldo"
    - "CreditCard (estado suficiente) é exibido quando qtd <= saldo mostrando 'Sobram {saldo-qtd} de {saldo} créditos'"
    - "Botão 'Reservar e confirmar' está disabled quando qtd > saldo"
    - "Submissão bem-sucedida exibe toast 'Pedido agendado!' e navega para /client/agenda"
    - "router.tsx aponta /client/agenda para ScheduleScreen e /client/agenda/pedido-unico para SingleScreen"
    - "useOneSignalRegister registra o player_id via POST /users/push-token quando autenticado"
  artifacts:
    - path: "apps/web/src/pages/client/SingleScreen.tsx"
      provides: "tela de pedido único com DateChips, stepper, BannerInsuficiente e CreditCard"
      contains: "SingleScreen"
    - path: "apps/web/src/components/client/DateChips.tsx"
      provides: "chips de data com regra de corte 21h e input nativo"
      contains: "DateChips"
    - path: "apps/web/src/routes/router.tsx"
      provides: "rota /client/agenda → ScheduleScreen e /client/agenda/pedido-unico → SingleScreen"
      contains: "ScheduleScreen"
    - path: "apps/web/src/hooks/useOneSignalRegister.ts"
      provides: "hook que registra player_id do OneSignal após autenticação"
      contains: "useOneSignalRegister"
  key_links:
    - from: "apps/web/src/pages/client/SingleScreen.tsx"
      to: "/orders"
      via: "apiFetch POST"
      pattern: "apiFetch.*orders"
    - from: "apps/web/src/hooks/useOneSignalRegister.ts"
      to: "/users/push-token"
      via: "apiFetch POST"
      pattern: "apiFetch.*push-token"
    - from: "apps/web/src/pages/client/ClientLayout.tsx"
      to: "apps/web/src/hooks/useOneSignalRegister.ts"
      via: "useOneSignalRegister()"
      pattern: "useOneSignalRegister"
---

<objective>
Criar a SingleScreen com todos os subcomponentes (DateChips, reutilização de QuantityStepper e BannerInsuficiente, novo CreditCard), atualizar o router.tsx para apontar a aba Agenda para a ScheduleScreen real, e criar o hook useOneSignalRegister que registra o player_id do PWA no backend.

Purpose: Completar o ciclo do cliente — agora pode criar pedidos únicos além de configurar a agenda semanal. A aba Agenda deixa de ser placeholder. Push notifications ficam registradas para funcionar na Fase 5.

Output: SingleScreen + DateChips + router atualizado + useOneSignalRegister chamado no ClientLayout.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-scheduling/04-CONTEXT.md
@.planning/phases/04-scheduling/04-UI-SPEC.md
@.planning/phases/04-scheduling/04-05-SUMMARY.md
@.planning/phases/04-scheduling/04-03-SUMMARY.md

<interfaces>
<!-- Componentes reutilizáveis (RESEARCH.md seção 7) -->

QuantityStepper interface (apps/web/src/components/client/QuantityStepper.tsx):
```typescript
interface QuantityStepperProps {
  min: number
  max: number
  value: number
  onChange: (v: number) => void
}
export default function QuantityStepper({ min, max, value, onChange }: QuantityStepperProps)
```

BannerInsuficiente interface (apps/web/src/components/client/BannerInsuficiente.tsx):
```typescript
interface BannerInsuficienteProps {
  saldo: number
  requerido: number
  onComprar: () => void
  onAjustar: (novaQtd: number) => void
}
export default function BannerInsuficiente({ saldo, requerido, onComprar, onAjustar }: BannerInsuficienteProps)
// retorna null quando requerido <= saldo
```

AuthContext (apps/web/src/contexts/AuthContext.tsx):
```typescript
export function useAuth(): { user: AuthUser | null, updateCreditBalance: (balance: number) => void }
```

apiFetch (apps/web/src/lib/apiFetch.ts):
```typescript
export function apiFetch(path: string, options: RequestInit = {}): Promise<Response>
```

OneSignal Web SDK (já inicializado em main.tsx):
```typescript
// Disponível globalmente via react-onesignal (já instalado v3.5.5)
// OneSignal.User.PushSubscription.id: string | null
// OneSignal.User.PushSubscription.addEventListener('change', handler)
```

<!-- Tokens de design da UI-SPEC seções 9-12 -->
DateChips:
- Label "Para quando?": 12.5px, 700, textSec, marginBottom 9px
- Chip inativo: padding '13px 14px', borderRadius 16px, border '1.5px solid var(--color-border)', background surface
- Chip ativo: border '1.5px solid var(--color-accent)', background var(--color-gold-soft)
- Chip título ativo: fontWeight 700, fontSize 14px, color accent
- Sub-label: fontSize 12px, color textTer
- Chip "Amanhã" desabilitado após 21h: opacity 0.4, cursor default, sub-label "Disponível até 21:00"

QuantityStepper na SingleScreen:
- Card pad={22}, label "QUANTOS PÃES?" 12.5px 700 textSec marginBottom 16px
- Número 56px Bricolage 800 — cor accent quando qtd <= saldo, warn (#B0702A) quando qtd > saldo

CreditCard (estado suficiente) — spec UI-SPEC seção 11:
- Card pad={16}, layout flex justify space-between
- Lado esq: flex gap 11px, ícone wallet size 20 color accent, título "Usar créditos" 700 14px, subtexto "Sobram {saldo-qtd} de {saldo} créditos" 12px textTer
- Lado dir: Bricolage 800 18px color text "{qtd} 🥖"

CUTOFF_HOUR = 21 (D-05)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: DateChips + SingleScreen</name>
  <files>
    apps/web/src/components/client/DateChips.tsx,
    apps/web/src/pages/client/SingleScreen.tsx
  </files>
  <read_first>
    - .planning/phases/04-scheduling/04-UI-SPEC.md (seções 7-12) — spec completa de SingleScreen, DateChips, QuantityStepper na SingleScreen, BannerInsuficiente, CreditCard e Footer
    - apps/web/src/components/client/QuantityStepper.tsx — ver o componente antes de usar
    - apps/web/src/components/client/BannerInsuficiente.tsx — ver props e comportamento de retorno null
    - apps/web/src/pages/client/ScheduleScreen.tsx (criado no 04-05) — ver padrão de estrutura de tela para seguir
  </read_first>
  <action>
Criar DateChips.tsx: componente que recebe props { value: string | null, onChange: (v: string) => void, deliveryTime: string }. Constante CUTOFF_HOUR = 21 no arquivo. Lógica: const now = new Date(); const isAfterCutoff = now.getHours() >= CUTOFF_HOUR.

Calcular chips dinâmicos: amanhã (date.setDate + 1), depois de amanhã (+2). Quando isAfterCutoff: chip amanhã com disabled=true, opacity 0.4, cursor default, sub-label "Disponível até 21:00". Chip ativo: border '1.5px solid var(--color-accent)', background var(--color-gold-soft). Sub-label dos chips ativos/inativos normais: dia da semana abreviado + hora de entrega (usar deliveryTime).

Chip "Outra data": ícone calendar size 16 + texto "Outra data". Usa um ref para um <input type="date" style={{ display: 'none' }} min={tomorrow} max={in30Days}>. onClick do chip: dateInputRef.current?.click(). onChange do input: onChange(e.target.value). Se value é uma data (não um dos chips pré-definidos): exibir como "DD mmm" formatado.

Calcular tomorrow e in30Days para os atributos min e max do input (D-04 e D-06): const tomorrow = new Date(); tomorrow.setDate(tomorrow.getDate() + 1); const in30Days = new Date(); in30Days.setDate(in30Days.getDate() + 30). Formatar como 'YYYY-MM-DD'.

Criar SingleScreen.tsx seguindo a estrutura da UI-SPEC seção 7:
1. StatusBar
2. AppBar "Pedido único" com botão voltar
3. Área scrollável com paddingBottom 90px:
   - Subtexto 14px textSec: "Agende uma entrega avulsa para uma data. Os créditos são reservados na hora."
   - Card pad={22}: label "QUANTOS PÃES?" + QuantityStepper min={1} max={20} value={qtd} onChange={setQtd}. Cor do número: se qtd > creditBalance usar var(--color-accent) (warn === accent no tema claro)
   - DateChips value={quando} onChange={setQuando} deliveryTime={schedule?.deliveryTime ?? '07:00'}
   - Condicional: se qtd > creditBalance → BannerInsuficiente saldo={creditBalance} requerido={qtd} onComprar={() => navigate('/client/creditos')} onAjustar={(v) => setQtd(v)}
   - Se qtd <= creditBalance → CreditCard (inline no SingleScreen): Card pad={16}, layout conforme UI-SPEC seção 11
4. Footer fixo: Btn full size="lg" icon="check" disabled={qtd > creditBalance || !quando} texto "Reservar e confirmar"

Submissão: chamar apiFetch('/orders', { method: 'POST', body: JSON.stringify({ quantity: qtd, scheduledDate: quando }), headers: {'Content-Type':'application/json'} }). Se 201: toast "Pedido agendado!" + navigate('/client/agenda'). Se 400: exibir card de erro inline com borda border e ícone alert (não usa BannerInsuficiente — per UI-SPEC seção Estado de UI SingleScreen). Atualizar creditBalance via updateCreditBalance após sucesso.

BannerInsuficiente: o texto do botão "Comprar mais" existente no componente deve ser renderizado como "Comprar créditos" — passar a prop ou verificar se o componente já usa a prop diretamente. Se o componente usa texto fixo "Comprar mais", criar o componente com variante de texto ou adaptar inline.
  </action>
  <verify>
    <automated>npm run build --workspace=apps/web 2>&1 | grep -E "error TS|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - DateChips.tsx e SingleScreen.tsx existem nos caminhos corretos
    - grep "CUTOFF_HOUR" apps/web/src/components/client/DateChips.tsx confirma constante 21
    - grep "disabled\|opacity.*0.4" apps/web/src/components/client/DateChips.tsx confirma chip desabilitado após corte
    - grep "type=\"date\"" apps/web/src/components/client/DateChips.tsx confirma input nativo
    - grep "min={1}" apps/web/src/pages/client/SingleScreen.tsx e grep "max={20}" confirmam limites do stepper
    - grep "BannerInsuficiente" apps/web/src/pages/client/SingleScreen.tsx confirma uso do componente existente
    - grep "disabled.*qtd.*creditBalance\|qtd.*saldo" apps/web/src/pages/client/SingleScreen.tsx confirma CTA disabled quando insuficiente
    - npm run build --workspace=apps/web retorna sem erros TS
  </acceptance_criteria>
  <done>DateChips e SingleScreen criados com design de alta fidelidade e lógica de corte às 21h.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: router.tsx atualizado + useOneSignalRegister + ClientLayout wiring</name>
  <files>
    apps/web/src/routes/router.tsx,
    apps/web/src/hooks/useOneSignalRegister.ts,
    apps/web/src/pages/client/ClientLayout.tsx
  </files>
  <read_first>
    - apps/web/src/routes/router.tsx — ler o arquivo completo para ver as rotas /client/agenda atuais e o padrão de lazy import antes de modificar
    - apps/web/src/pages/client/ClientLayout.tsx — ler para entender onde adicionar a chamada ao useOneSignalRegister
    - apps/web/src/main.tsx — confirmar que OneSignal já está inicializado (linhas 24-28 per RESEARCH.md seção 3)
  </read_first>
  <action>
Atualizar router.tsx: substituir as 2 ocorrências de PlaceholderScreen na rota agenda:
  - Rota '/client/agenda' (index): mudar para lazy import de '../pages/client/ScheduleScreen' com Component: ScheduleScreen
  - Sub-rota (se houver um placeholder para pedido único): mudar para lazy import de '../pages/client/SingleScreen' com Component: SingleScreen
  - Adicionar nova rota filha de '/client/agenda': { path: 'pedido-unico', lazy: () => import('../pages/client/SingleScreen').then((m) => ({ Component: m.SingleScreen })) } — se SingleScreen exporta como named export; ou usar default export.

Criar useOneSignalRegister.ts: hook React sem JSX. Lógica: (1) verificar se OneSignal está disponível (typeof OneSignal !== 'undefined'); (2) tentar obter o player_id via OneSignal.User.PushSubscription.id; (3) se disponível, chamar apiFetch('/users/push-token', { method: 'POST', body: JSON.stringify({ playerId }), headers: {...} }) com catch silencioso; (4) adicionar listener via OneSignal.User.PushSubscription.addEventListener('change', (event) => { const id = event.current.id; if (id) { /* registrar */ } }); (5) retornar função de cleanup que remove o listener.

O hook usa useEffect com dependência [] (executa uma vez na montagem). Importa apiFetch de '../lib/apiFetch'.

Atualizar ClientLayout.tsx: importar useOneSignalRegister e chamar useOneSignalRegister() uma vez (sem usar o valor de retorno — é um side-effect puro). Adicionar a chamada dentro do componente ClientLayout, antes do return. Não alterar a estrutura de renderização existente do layout.

Importante (RESEARCH.md pitfall 4): o hook funciona corretamente pois está em ClientLayout, que só renderiza quando o usuário está autenticado — garante que o token JWT está disponível para o POST /users/push-token.
  </action>
  <verify>
    <automated>npm run build --workspace=apps/web 2>&1 | grep -E "error TS|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "ScheduleScreen" apps/web/src/routes/router.tsx confirma rota agenda apontando para a tela real
    - grep "PlaceholderScreen" apps/web/src/routes/router.tsx retorna 0 resultados para a rota /client/agenda (placeholder substituído)
    - grep "pedido-unico" apps/web/src/routes/router.tsx confirma sub-rota para SingleScreen
    - apps/web/src/hooks/useOneSignalRegister.ts existe
    - grep "push-token" apps/web/src/hooks/useOneSignalRegister.ts confirma chamada ao endpoint correto
    - grep "useOneSignalRegister" apps/web/src/pages/client/ClientLayout.tsx confirma que o hook é chamado no layout autenticado
    - npm run build --workspace=apps/web retorna sem erros TS
    - npm run test --workspace=apps/web -- --run retorna sem FAIL (stubs de BannerInsuficiente.test.tsx ainda pending — OK)
  </acceptance_criteria>
  <done>router.tsx atualizado (aba Agenda deixa de ser placeholder). useOneSignalRegister chamado no ClientLayout. Build e testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SingleScreen → POST /orders | scheduledDate vem do input nativo ou chip — validado no backend |
| useOneSignalRegister → POST /users/push-token | player_id capturado do OneSignal SDK — não manipulável pelo usuário diretamente |
| ClientLayout → useOneSignalRegister | Executado apenas após autenticação — JWT disponível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | scheduledDate manipulado via DevTools | mitigate | Backend valida scheduledDate >= tomorrow e quantity 1-20 via CreateOrderSchema Zod |
| T-04-06-02 | Tampering | input[type=date] com date fora do range 30 dias | mitigate | Atributos min/max no input restringem UI; backend valida também (D-06) |
| T-04-06-03 | Spoofing | player_id injetado via patch de estado React | accept | O player_id é enviado para o backend que o associa ao userId do JWT — não há ganho em forjar um player_id |
| T-04-06-04 | Denial of Service | useOneSignalRegister em loop (addEventListener vazando) | mitigate | useEffect retorna cleanup function que remove o listener via removeEventListener |
  | T-04-SC | Tampering | npm installs | mitigate | Nenhum novo pacote instalado neste plano — react-onesignal já instalado desde a Fase 1 |
</threat_model>

<verification>
1. npm run build --workspace=apps/web sem erros TS
2. npm run test --workspace=apps/web -- --run sem FAIL
3. grep "ScheduleScreen" apps/web/src/routes/router.tsx confirma substituição do PlaceholderScreen
4. grep "pedido-unico" apps/web/src/routes/router.tsx confirma nova rota
5. grep "CUTOFF_HOUR" apps/web/src/components/client/DateChips.tsx retorna 21
6. grep "useOneSignalRegister" apps/web/src/pages/client/ClientLayout.tsx retorna resultado
</verification>

<success_criteria>
SingleScreen completa com DateChips (corte às 21h), QuantityStepper, BannerInsuficiente e CreditCard. Router atualizado — aba Agenda funcional. useOneSignalRegister ativo no ClientLayout. Fase 4 front-end completa.
</success_criteria>

<output>
Criar `.planning/phases/04-scheduling/04-06-SUMMARY.md` ao concluir.
</output>
