---
phase: 04-scheduling
plan: 04
type: execute
wave: 2
depends_on:
  - "04-02"
  - "04-03"
files_modified:
  - apps/api/src/plugins/cron.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - SCHED-03
  - SCHED-04
  - CRED-07
must_haves:
  truths:
    - "plugin cron.ts existe em apps/api/src/plugins/ e registra 3 expressões cron"
    - "cron de meia-noite usa timezone America/Sao_Paulo e chama schedulesService.createDailyOrders() e schedulesService.processAutoBuy()"
    - "cron de domingo 20h usa timezone America/Sao_Paulo e chama schedulesService.sendReconfigureReminders()"
    - "cronPlugin é registrado no server.ts APÓS prismaPlugin e APÓS todos os módulos de rotas"
    - "schedulesRoute, ordersRoute e notificationsRoute estão registrados no server.ts"
    - "ONESIGNAL_APP_ID e ONESIGNAL_REST_API_KEY estão no envSchema do server.ts"
  artifacts:
    - path: "apps/api/src/plugins/cron.ts"
      provides: "plugin Fastify que registra os 3 crons usando node-cron"
      contains: "node-cron"
    - path: "apps/api/src/server.ts"
      provides: "wiring de schedulesRoute, ordersRoute, notificationsRoute e cronPlugin"
      contains: "cronPlugin"
  key_links:
    - from: "apps/api/src/plugins/cron.ts"
      to: "apps/api/src/modules/schedules/schedules.service.ts"
      via: "SchedulesService constructor(fastify)"
      pattern: "SchedulesService"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/plugins/cron.ts"
      via: "fastify.register(cronPlugin)"
      pattern: "register.*cronPlugin"
---

<objective>
Criar o plugin cron.ts que registra os 3 cron jobs da Fase 4, e atualizar server.ts para registrar os novos módulos (schedules, orders, notifications) e o plugin cron.

Purpose: Ativar a automação — os Orders do dia seguinte serão criados automaticamente à meia-noite, a compra automática será verificada diariamente, e o lembrete de reconfiguração semanal será enviado toda domingo às 20h.

Output: cron.ts com 3 schedules registrados + server.ts atualizado com os 4 novos registros.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-scheduling/04-CONTEXT.md
@.planning/phases/04-scheduling/04-RESEARCH.md
@.planning/phases/04-scheduling/04-02-SUMMARY.md
@.planning/phases/04-scheduling/04-03-SUMMARY.md

<interfaces>
<!-- node-cron v4.2.1 API (RESEARCH.md seção 1) -->
```typescript
import cron from 'node-cron'

cron.schedule(
  '0 0 * * *',
  async () => { /* handler */ },
  { scheduled: true, timezone: 'America/Sao_Paulo' }
)
cron.schedule('0 20 * * 0', handler, { timezone: 'America/Sao_Paulo' })
```

<!-- Padrão de plugin Fastify existente (plugins/prisma.ts) -->
```typescript
import fp from 'fastify-plugin'
import { FastifyPluginAsync } from 'fastify'

const cronPlugin: FastifyPluginAsync = fp(async (fastify) => {
  // fastify.prisma já disponível aqui pois cronPlugin é registrado após prismaPlugin
})

export default cronPlugin
```

<!-- server.ts atual (linhas 63-85) — ordem de registro estabelecida -->
```typescript
await fastify.register(prismaPlugin)           // linha 64
await fastify.register(authenticatePlugin)     // linha 71
await fastify.register(healthRoute)            // linha 74
await fastify.register(authRoute)              // linha 77
await fastify.register(condominiumsRoute)      // linha 80
await fastify.register(paymentsRoute)          // linha 83
await fastify.register(creditsRoute)           // linha 84
await fastify.register(webhooksRoute)          // linha 85
// ADICIONAR AQUI: schedulesRoute, ordersRoute, notificationsRoute, cronPlugin
```

<!-- envSchema do server.ts — adicionar as novas vars -->
```typescript
// Acrescentar ao schema de env existente:
ONESIGNAL_APP_ID: { type: 'string' }
ONESIGNAL_REST_API_KEY: { type: 'string' }
```

<!-- SchedulesService interface (criado no 04-02) -->
```typescript
class SchedulesService {
  constructor(private fastify: FastifyInstance) {}
  async createDailyOrders(): Promise<void>
  async sendReconfigureReminders(): Promise<void>
  async processAutoBuy(): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar apps/api/src/plugins/cron.ts com os 3 cron jobs</name>
  <files>apps/api/src/plugins/cron.ts</files>
  <read_first>
    - apps/api/src/plugins/prisma.ts — ver padrão de plugin com fastify-plugin e FastifyPluginAsync
    - apps/api/src/modules/schedules/schedules.service.ts (criado no 04-02) — confirmar a interface dos métodos antes de criar as chamadas no cron
  </read_first>
  <action>
Criar apps/api/src/plugins/cron.ts como Fastify plugin usando fastify-plugin (fp). O plugin recebe a instância Fastify decorada com fastify.prisma (disponível porque será registrado após prismaPlugin).

Dentro do plugin, instanciar SchedulesService(fastify) e registrar 3 crons usando node-cron:

Cron 1 — meia-noite diário (criação de Orders + auto-buy "quando estiver acabando"):
  expressão: '0 0 * * *'
  timezone: 'America/Sao_Paulo'
  handler: chama await schedulesService.createDailyOrders() seguido de await schedulesService.processAutoBuy()
  encapsular em try/catch: logar erro com fastify.log.error mas não lançar (não derrubar o servidor)

Cron 2 — domingo 20h (lembrete de reconfiguração semanal):
  expressão: '0 20 * * 0'
  timezone: 'America/Sao_Paulo'
  handler: chama await schedulesService.sendReconfigureReminders()
  encapsular em try/catch: logar erro com fastify.log.error

Cron 3 — compra automática semanal (verifica usuários com modo 'semanal' no dia configurado):
  expressão: '0 0 * * *' (junto com o cron diário — a lógica interna do processAutoBuy filtra pelo dia da semana)
  timezone: 'America/Sao_Paulo'
  Nota: pode ser unificado com o Cron 1 se processAutoBuy já cobre os dois modos internamente (modo 'acabar' e modo 'semanal'). Nesse caso, manter apenas os 2 crons (meia-noite e domingo).

Importante: NÃO usar setInterval ou setTimeout — usar apenas node-cron. Não inicializar os crons em ambiente de teste (verificar NODE_ENV !== 'test' ou passar flag para o plugin).
  </action>
  <verify>
    <automated>npm run build --workspace=apps/api 2>&1 | grep -E "error TS|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/plugins/cron.ts existe e compila sem erros TS
    - grep -c "America/Sao_Paulo" apps/api/src/plugins/cron.ts retorna >= 2 (cada cron tem seu timezone)
    - grep -c "node-cron" apps/api/src/plugins/cron.ts retorna >= 1 (import presente)
    - grep "createDailyOrders" apps/api/src/plugins/cron.ts confirma chamada ao service
    - grep "sendReconfigureReminders" apps/api/src/plugins/cron.ts confirma chamada ao service
    - grep "processAutoBuy" apps/api/src/plugins/cron.ts confirma chamada ao service
    - grep "fastify-plugin\|fp(" apps/api/src/plugins/cron.ts confirma uso do padrão fp()
  </acceptance_criteria>
  <done>Plugin cron.ts criado com 2-3 cron jobs registrados com timezone America/Sao_Paulo.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atualizar server.ts — registrar novos módulos e cronPlugin + vars de ambiente OneSignal</name>
  <files>apps/api/src/server.ts</files>
  <read_first>
    - apps/api/src/server.ts — ler o arquivo completo para ver a ordem exata de registro e o envSchema antes de modificar
  </read_first>
  <action>
Atualizar server.ts com as seguintes modificações (na ordem):

1. Adicionar imports no topo (junto com os outros imports de rotas e plugins):
   import { schedulesRoute } from './modules/schedules/schedules.route.js'
   import { ordersRoute } from './modules/orders/orders.route.js'
   import { notificationsRoute } from './modules/notifications/notifications.route.js'
   import cronPlugin from './plugins/cron.js'

2. No envSchema (objeto passado para @fastify/env), adicionar as duas novas variáveis de ambiente após as existentes:
   ONESIGNAL_APP_ID: { type: 'string' }
   ONESIGNAL_REST_API_KEY: { type: 'string' }

3. Após o registro de webhooksRoute (linha ~85), adicionar os novos registros nesta ordem:
   await fastify.register(schedulesRoute)
   await fastify.register(ordersRoute)
   await fastify.register(notificationsRoute)
   await fastify.register(cronPlugin)   // DEVE ser o último — depende de fastify.prisma já registrado

Não alterar nenhum registro existente. Não modificar a ordem dos registros anteriores. Apenas acrescentar ao final da sequência de registros.

Adicionar ONESIGNAL_APP_ID e ONESIGNAL_REST_API_KEY ao apps/api/.env.example se o arquivo existir (com valores placeholder: ONESIGNAL_APP_ID=your-app-id e ONESIGNAL_REST_API_KEY=your-api-key).
  </action>
  <verify>
    <automated>npm run build --workspace=apps/api 2>&1 | grep -E "error TS|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "schedulesRoute" apps/api/src/server.ts confirma import e register
    - grep "ordersRoute" apps/api/src/server.ts confirma import e register
    - grep "notificationsRoute" apps/api/src/server.ts confirma import e register
    - grep "cronPlugin" apps/api/src/server.ts confirma import e register
    - grep "ONESIGNAL_APP_ID" apps/api/src/server.ts confirma adição ao envSchema
    - grep "ONESIGNAL_REST_API_KEY" apps/api/src/server.ts confirma adição ao envSchema
    - cronPlugin aparece APÓS webhooksRoute na ordem de registro (grep -n "cronPlugin\|webhooksRoute" apps/api/src/server.ts mostra cronPlugin em linha maior)
    - npm run build --workspace=apps/api retorna sem erros TS
  </acceptance_criteria>
  <done>server.ts atualizado com 4 novos registros. Build passa sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron → SchedulesService | Execução server-side sem input externo — confiável |
| Cron → OneSignal API | Credenciais via env vars, não hardcoded |
| server.ts envSchema | ONESIGNAL_REST_API_KEY exposta apenas no processo do servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | ONESIGNAL_REST_API_KEY | mitigate | Declarar em envSchema (validação obrigatória na startup) — nunca logar o valor; usar apenas no service |
| T-04-04-02 | Denial of Service | cron handler lança exceção não capturada | mitigate | Envolver cada handler de cron em try/catch com fastify.log.error; não re-lançar — servidor não cai |
| T-04-04-03 | Tampering | Timezone incorreto no cron | mitigate | Sempre passar { timezone: 'America/Sao_Paulo' } no terceiro argumento do cron.schedule (RESEARCH.md pitfall 1) |
  | T-04-SC | Tampering | npm installs | mitigate | node-cron e @onesignal/node-onesignal auditados em 04-RESEARCH.md |
</threat_model>

<verification>
1. npm run build --workspace=apps/api sem erros TS
2. grep -c "America/Sao_Paulo" apps/api/src/plugins/cron.ts >= 2
3. grep "cronPlugin" apps/api/src/server.ts aparece após "webhooksRoute" (ordem correta)
4. grep "ONESIGNAL_APP_ID" apps/api/src/server.ts retorna resultado no envSchema
</verification>

<success_criteria>
Plugin cron.ts criado com 3 crons configurados corretamente no timezone America/Sao_Paulo. server.ts atualizado com todos os módulos da Fase 4 registrados na ordem correta. Build compila sem erros.
</success_criteria>

<output>
Criar `.planning/phases/04-scheduling/04-04-SUMMARY.md` ao concluir.
</output>
