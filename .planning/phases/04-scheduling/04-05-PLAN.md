---
phase: 04-scheduling
plan: 05
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - apps/web/src/pages/client/ScheduleScreen.tsx
  - apps/web/src/components/client/DeliveryTimeChips.tsx
  - apps/web/src/components/client/BannerCobertura.tsx
  - apps/web/src/hooks/useSchedule.ts
  - apps/web/src/hooks/__tests__/useSchedule.test.ts
autonomous: true
requirements:
  - SCHED-02
  - SCHED-04
  - SCHED-05
  - SCHED-06
must_haves:
  truths:
    - "ScheduleScreen renderiza 7 Day-Rows com StepperInline (min=0, max=12) para cada dia da semana"
    - "DeliveryTimeChips renderiza 4 chips (06:30, 07:00, 07:30, 08:00) com seleção exclusiva e estilo ativo per UI-SPEC"
    - "BannerCobertura renderiza estado A (goldSoft, borda gold) quando semana > saldo e estado B (surface) quando semana <= saldo"
    - "BannerCobertura é ocultado quando consumoSemanal === 0"
    - "Footer mostra consumo semanal calculado localmente como soma das quantidades (D-03) no formato '{n} pães · {hora}'"
    - "useSchedule chama GET /schedules/me e PUT /schedules/me via apiFetch"
    - "Switch do card 'Lembrar de reconfigurar' salva notifyReconfigure no Schedule"
    - "Botão 'Salvar agenda' chama PUT /schedules/me e exibe toast 'Agenda salva!' no sucesso"
  artifacts:
    - path: "apps/web/src/hooks/useSchedule.ts"
      provides: "hook com schedule state, métodos saveSchedule, coverage calculation"
      contains: "useSchedule"
    - path: "apps/web/src/components/client/DeliveryTimeChips.tsx"
      provides: "componente com 4 chips de horário"
      contains: "DeliveryTimeChips"
    - path: "apps/web/src/components/client/BannerCobertura.tsx"
      provides: "componente com 2 estados (falta e cobre)"
      contains: "BannerCobertura"
    - path: "apps/web/src/pages/client/ScheduleScreen.tsx"
      provides: "tela de agenda semanal completa"
      contains: "ScheduleScreen"
  key_links:
    - from: "apps/web/src/hooks/useSchedule.ts"
      to: "/schedules/me"
      via: "apiFetch"
      pattern: "apiFetch.*schedules/me"
    - from: "apps/web/src/pages/client/ScheduleScreen.tsx"
      to: "apps/web/src/contexts/AuthContext.tsx"
      via: "useAuth().user.creditBalance"
      pattern: "useAuth"
---

<objective>
Criar a ScheduleScreen completa com todos os seus subcomponentes: DeliveryTimeChips, BannerCobertura e o hook useSchedule. A ScheduleScreen substitui o PlaceholderScreen na aba Agenda — mas o router.tsx só será atualizado no plano 04-06 para evitar conflito de arquivo.

Purpose: Entregar a interface de agenda semanal — o coração do produto. O cliente configura quantos pãezinhos quer em cada dia da semana, escolhe o horário de entrega e ativa o lembrete de reconfiguração.

Output: ScheduleScreen, DeliveryTimeChips, BannerCobertura, useSchedule — todos respeitando o UI-SPEC ao nível de pixel.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-scheduling/04-CONTEXT.md
@.planning/phases/04-scheduling/04-UI-SPEC.md
@.planning/phases/04-scheduling/04-01-SUMMARY.md

<interfaces>
<!-- Componentes reutilizáveis confirmados no RESEARCH.md seção 7 -->

StepperInline interface (apps/web/src/components/client/StepperInline.tsx):
```typescript
interface StepperInlineProps {
  min: number
  max: number
  value: number
  onChange: (v: number) => void
}
export default function StepperInline({ min, max, value, onChange }: StepperInlineProps)
```

AuthContext interface (apps/web/src/contexts/AuthContext.tsx):
```typescript
export interface AuthUser {
  id: string
  role: string
  creditBalance: number
  condominiumId?: string
}
export interface AuthContextType {
  user: AuthUser | null
  updateCreditBalance: (balance: number) => void
}
export function useAuth(): AuthContextType
```

apiFetch (apps/web/src/lib/apiFetch.ts):
```typescript
export function apiFetch(path: string, options: RequestInit = {}): Promise<Response>
// Injeta Authorization: Bearer <token> automaticamente
```

WeeklyQty type (a ser criado neste plano, coerente com o schema backend):
```typescript
interface WeeklyQty {
  seg: number  // 0-12
  ter: number
  qua: number
  qui: number
  sex: number
  sab: number
  dom: number
}
```

<!-- Tokens de design da UI-SPEC -->
Cores relevantes:
- appBg: #FAF5EC
- surface: #FFFFFF
- surface2: #F4EBDA
- gold: #E3AC3F
- goldSoft: #F3DDA6
- accent: #B0702A
- text: #241608
- textSec: #7C6A50
- textTer: #A89A82
- border: rgba(43,26,12,0.10)
- border2: rgba(43,26,12,0.06)

Tipografia relevante:
- AppBar: Bricolage Grotesque 700, 21px
- Labels seção uppercase: Hanken Grotesk 600-700, 12.5px, letterSpacing 0.04em
- Day-row nome: Bricolage Grotesque 700, 15px
- Chip horário: Hanken Grotesk 700, 14px
- Footer valor: Bricolage Grotesque 800, 18px
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hook useSchedule + stubs de teste implementados</name>
  <files>
    apps/web/src/hooks/useSchedule.ts,
    apps/web/src/hooks/__tests__/useSchedule.test.ts
  </files>
  <read_first>
    - apps/web/src/lib/apiFetch.ts — confirmar assinatura da função apiFetch
    - apps/web/src/contexts/AuthContext.tsx — confirmar interface AuthUser e useAuth()
    - apps/web/src/hooks/__tests__/useSchedule.test.ts (stub do Wave 0) — ver os it.todo a implementar
  </read_first>
  <behavior>
    - useSchedule retorna: { schedule, weeklyQty, deliveryTime, notifyReconfigure, setWeeklyQty, setDeliveryTime, setNotifyReconfigure, saveSchedule, isLoading, isSaving, consumoSemanal, cobre, falta }
    - consumoSemanal = Object.values(weeklyQty).reduce((a, b) => a + b, 0) (D-03)
    - cobre = Math.floor(creditBalance / (consumoSemanal || 1))
    - falta = consumoSemanal > creditBalance
    - Teste: consumoSemanal é soma correta de todos os dias em weeklyQty
    - Teste: cobre = Math.floor(saldo / consumoSemanal)
    - Teste: falta = true quando semana > saldo
    - Teste: sem divisão por zero quando consumoSemanal === 0 (retorna Infinity ou 0 de forma segura)
    - useSchedule chama GET /schedules/me em useEffect para pre-popular o estado
    - saveSchedule chama PUT /schedules/me com { weeklyQty, deliveryTime, notifyReconfigure }
    - deliveryTime default: '07:00'
    - weeklyQty default: { seg: 0, ter: 0, qua: 0, qui: 0, sex: 0, sab: 0, dom: 0 }
  </behavior>
  <action>
Criar useSchedule.ts como React hook (useState + useEffect). Estado local: weeklyQty (WeeklyQty), deliveryTime (string), notifyReconfigure (boolean), isLoading (bool), isSaving (bool). Definir tipo WeeklyQty no próprio arquivo ou em um types file colocado no hook.

No useEffect (dependência vazia — roda apenas na montagem): chamar apiFetch('/schedules/me') e se a resposta for 200 popular o estado local com os dados recebidos. Se 404 (sem schedule) manter os defaults zerados.

Derivar consumoSemanal, cobre e falta como variáveis calculadas (não estado) a cada render.

saveSchedule: marcar isSaving=true, chamar apiFetch('/schedules/me', { method: 'PUT', body: JSON.stringify({weeklyQty, deliveryTime, notifyReconfigure}), headers: {'Content-Type':'application/json'} }), aguardar resposta, marcar isSaving=false. Retornar { ok: boolean, error?: string }.

Substituir o stub useSchedule.test.ts pelos testes reais. Os testes unitários cobrem apenas as funções puras de cálculo (consumoSemanal, cobre, falta) — não mocam fetch pois testam lógica de cálculo, não chamadas de rede.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/web -- --run --reporter=verbose 2>&1 | grep -E "useSchedule|PASS|FAIL" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - npm run test --workspace=apps/web -- --run retorna PASS (não FAIL) para useSchedule.test.ts
    - grep "consumoSemanal" apps/web/src/hooks/useSchedule.ts confirma cálculo presente
    - grep "Math.floor" apps/web/src/hooks/useSchedule.ts confirma cálculo de cobertura (D-03)
    - grep "schedules/me" apps/web/src/hooks/useSchedule.ts confirma chamadas de API corretas
    - npm run build --workspace=apps/web retorna sem erros TS
  </acceptance_criteria>
  <done>hook useSchedule criado e 4 testes unitários de cálculo passando.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DeliveryTimeChips + BannerCobertura + ScheduleScreen</name>
  <files>
    apps/web/src/components/client/DeliveryTimeChips.tsx,
    apps/web/src/components/client/BannerCobertura.tsx,
    apps/web/src/pages/client/ScheduleScreen.tsx
  </files>
  <read_first>
    - apps/web/src/components/client/StepperInline.tsx — ver o componente antes de usá-lo nos Day-Rows
    - apps/web/src/components/client/BannerInsuficiente.tsx — ver padrão de componente de banner para seguir na BannerCobertura
    - .planning/phases/04-scheduling/04-UI-SPEC.md (seções 2-6) — spec exata de DeliveryTimeChips (seção 2), Day-Row (seção 3), Card "Lembrar de reconfigurar" (seção 4), BannerCobertura (seção 5) e Footer (seção 6)
    - apps/web/src/styles/globals.css — confirmar os tokens CSS --color-* exatos antes de usar
  </read_first>
  <action>
Criar DeliveryTimeChips.tsx: componente que recebe props { value: string, onChange: (v: string) => void }. Renderiza label "Horário de entrega" (fontSize 12.5px, fontWeight 700, color textSec, marginBottom 9px) e 4 chips em flex gap 9px. Valores: ['06:30','07:00','07:30','08:00']. Chip inativo: padding '11px 0', flex 1, borderRadius 13px, border '1.5px solid var(--color-border)', background surface, color text, fontWeight 700, fontSize 14px, fontFamily 'Hanken Grotesk'. Chip ativo (value === chip): border '1.5px solid var(--color-accent)', background var(--color-gold-soft), color var(--color-accent). Transição: background .15s, border-color .15s, color .15s. Hit target mínimo 44px (padding 11px topo+base já garante ~44px).

Criar BannerCobertura.tsx: componente que recebe props { semana: number, saldo: number, cobre: number, onCombos: () => void, onAutoBuy: () => void }. Retorna null se semana === 0. Estado A (falta: semana > saldo): container onClick → onCombos, background goldSoft, border '1.5px solid var(--color-gold)', borderRadius 16px, padding '14px 16px', marginTop 14px, display flex, gap 11px, ícone alert size 20 color accent, texto '13px fontWeight 600' com copy "Seu saldo ({saldo}) não cobre a semana ({semana}). Compre um combo ou ative a reposição automática.", chevron direito size 18 color accent. Estado B (cobre): container onClick → onAutoBuy, background surface, border '1px solid var(--color-border-2)', ícone repeat size 19 color accent, texto color textSec, copy "Saldo cobre ~{cobre} semanas. Ative a compra automática pra nunca faltar.", chevron size 18 color textTer.

Criar ScheduleScreen.tsx como componente de página. Usar useSchedule() para estado. Usar useAuth() para creditBalance. Estrutura (conforme UI-SPEC seção 1):
1. StatusBar (componente existente ou div com paddingTop safe area)
2. AppBar com título "Agenda semanal" e botão voltar
3. Área scrollável com paddingBottom 90px (para tab bar):
   - Subtexto 14px textSec: "Quantos pães em cada dia. A gente entrega sozinho, todo dia, no horário escolhido."
   - DeliveryTimeChips value={deliveryTime} onChange={setDeliveryTime}
   - Lista de 7 Day-Rows (Seg, Ter, Qua, Qui, Sex, Sáb, Dom): cada row é container flex gap 14px, background surface, borderRadius 18px, border '1px solid var(--color-border-2)', padding '12px 16px', opacidade 0.66 quando valor === 0. Coluna de label (width 44px): nome abrev Bricolage 700 15px + sub-label "folga" ou "{v} pães" (Hanken 11px textTer). StepperInline min={0} max={12} value={weeklyQty[dia]} onChange={(v) => setWeeklyQty({...weeklyQty, [key]: v})}.
   - Card "Lembrar de reconfigurar" (UI-SPEC seção 4): container Card pad={16}, layout flex gap 13px, ícone-container 42x42 borderRadius 12px background surface2 com ícone repeat size 20 color accent, título "Lembrar de reconfigurar" 14.5px 700, subtexto "Aviso no domingo à noite p/ ajustar a semana" 12px textTer, Switch on={notifyReconfigure} onChange={setNotifyReconfigure}
   - BannerCobertura semana={consumoSemanal} saldo={creditBalance} cobre={cobre} onCombos={() => navigate('/client/creditos')} onAutoBuy={() => navigate('/client/creditos/compra-automatica')}
4. Footer fixo (position sticky bottom 0): linha de resumo com label "Consumo semanal" (13.5px textSec 600) e valor "{consumoSemanal} pães · {deliveryTime}" (Bricolage 800 18px). Botão "Salvar agenda" (Btn full size="lg" icon="check") com onClick que chama saveSchedule() e exibe toast de sucesso/erro.

Toast: usar um estado local para o toast. Após saveSchedule retornar, exibir div fixo no topo com texto "Agenda salva!" (background espresso, color primaryBtnText, borderRadius 12px, padding '12px 16px', posição fixa no topo center) por 2.5s e depois remover. Se erro: "Não conseguimos salvar. Tente novamente."

Estado de carregamento inicial: skeleton das 7 day-rows com height 64px, borderRadius 18px, background surface2, animação de pulse (usar className ou keyframe inline).
  </action>
  <verify>
    <automated>npm run build --workspace=apps/web 2>&1 | grep -E "error TS|Error" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos existem nos caminhos corretos
    - grep "StepperInline" apps/web/src/pages/client/ScheduleScreen.tsx confirma uso do componente existente
    - grep "useSchedule" apps/web/src/pages/client/ScheduleScreen.tsx confirma uso do hook
    - grep "useAuth" apps/web/src/pages/client/ScheduleScreen.tsx confirma uso do AuthContext para creditBalance
    - grep "BannerCobertura" apps/web/src/pages/client/ScheduleScreen.tsx confirma uso do componente
    - grep "America/Sao_Paulo\|goldSoft\|var(--color-gold)" apps/web/src/components/client/BannerCobertura.tsx confirma tokens corretos
    - grep "borderRadius: 13" apps/web/src/components/client/DeliveryTimeChips.tsx confirma bordas per handoff (não 16px)
    - npm run build --workspace=apps/web retorna sem erros TS
  </acceptance_criteria>
  <done>DeliveryTimeChips, BannerCobertura e ScheduleScreen criados e compilando. Alta fidelidade de design implementada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScheduleScreen → PUT /schedules/me | weeklyQty construído no frontend — validado novamente no backend via Zod |
| useSchedule → GET /schedules/me | Resposta do servidor é parseada — não confiar cegamente nos dados |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | weeklyQty no estado local | accept | StepperInline enforça min=0 max=12 via UI; backend valida com Zod (defense in depth) |
| T-04-05-02 | Denial of Service | loop infinito em useEffect | mitigate | useEffect com dependência vazia [] — executa apenas na montagem, não em cada render |
| T-04-05-03 | Information Disclosure | creditBalance exposto no estado local | accept | creditBalance já existe no AuthContext — não é dado novo; contexto de risco baixo |
| T-04-SC | Tampering | npm installs | mitigate | Nenhum novo pacote instalado neste plano |
</threat_model>

<verification>
1. npm run build --workspace=apps/web sem erros TS
2. npm run test --workspace=apps/web -- --run retorna PASS para useSchedule.test.ts
3. grep "borderRadius: 13" apps/web/src/components/client/DeliveryTimeChips.tsx (handoff canônico)
4. grep "semana === 0" apps/web/src/components/client/BannerCobertura.tsx confirma estado oculto
5. grep "90px\|paddingBottom" apps/web/src/pages/client/ScheduleScreen.tsx confirma proteção da tab bar
</verification>

<success_criteria>
ScheduleScreen completa com design de alta fidelidade. DeliveryTimeChips e BannerCobertura criados. useSchedule calculando cobertura corretamente (D-03). 4 testes unitários de cálculo passando. Build sem erros.
</success_criteria>

<output>
Criar `.planning/phases/04-scheduling/04-05-SUMMARY.md` ao concluir.
</output>
