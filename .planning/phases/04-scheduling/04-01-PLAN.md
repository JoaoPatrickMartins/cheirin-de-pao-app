---
phase: 04-scheduling
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/package.json
  - package-lock.json
  - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
  - apps/api/src/modules/orders/__tests__/orders.service.test.ts
  - apps/web/src/components/client/__tests__/BannerInsuficiente.test.tsx
  - apps/web/src/hooks/__tests__/useSchedule.test.ts
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - SCHED-05
  - SCHED-06
must_haves:
  truths:
    - "campo oneSignalPlayerId do tipo String? existe no modelo User do schema"
    - "@@unique([userId, condominiumId]) existe no modelo Schedule"
    - "node-cron e @onesignal/node-onesignal estão no package.json de apps/api"
    - "npx prisma db push retorna 0 após as alterações de schema"
    - "stubs de teste existem para todos os módulos novos da Fase 4 e passam (describe vazio)"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "User.oneSignalPlayerId + Schedule.@@unique"
      contains: "oneSignalPlayerId"
    - path: "apps/api/src/modules/schedules/__tests__/schedules.service.test.ts"
      provides: "stub de teste para schedules"
      min_lines: 5
    - path: "apps/api/src/modules/orders/__tests__/orders.service.test.ts"
      provides: "stub de teste para orders"
      min_lines: 5
    - path: "apps/web/src/components/client/__tests__/BannerInsuficiente.test.tsx"
      provides: "stub de teste para BannerInsuficiente"
      min_lines: 5
    - path: "apps/web/src/hooks/__tests__/useSchedule.test.ts"
      provides: "stub de teste para useSchedule"
      min_lines: 5
  key_links:
    - from: "apps/api/prisma/schema.prisma"
      to: "MongoDB Atlas"
      via: "npx prisma db push"
      pattern: "oneSignalPlayerId"
---

<objective>
Este plano é o Wave 0 bloqueante da Fase 4: faz todas as modificações de schema e instalações de pacotes que os planos subsequentes dependem. Nenhum plano da Fase 4 pode começar antes deste concluir.

Purpose: Preparar o terreno — schema Prisma atualizado e sincronizado com o Atlas, pacotes de cron e OneSignal instalados, stubs de teste criados para que os verificadores automatizados não falhem por ausência de arquivo.

Output: schema.prisma com oneSignalPlayerId + @@unique; node-cron e @onesignal/node-onesignal instalados; db push executado; 4 arquivos de teste stub criados e passando.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scheduling/04-CONTEXT.md
@.planning/phases/04-scheduling/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Atualizar schema Prisma (oneSignalPlayerId + @@unique) e sincronizar com Atlas</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma — ler o arquivo completo para ver os modelos User e Schedule exatos antes de modificar
  </read_first>
  <action>
No modelo User (a partir da linha 103), adicionar o campo oneSignalPlayerId do tipo String opcional após cardTokenMp (linha 117), preservando todos os outros campos exatamente como estão. O campo fica antes de createdAt.

No modelo Schedule (a partir da linha 179), adicionar @@unique([userId, condominiumId]) como diretiva de modelo após o campo updatedAt. Este índice composto é obrigatório para o upsert via Prisma funcionar (D-01 do RESEARCH.md seção 5).

Após salvar o schema, executar (em sequência, sem flags destrutivas — as alterações são apenas aditivas):
  npx prisma generate --schema=apps/api/prisma/schema.prisma
  npx prisma db push --schema=apps/api/prisma/schema.prisma

Se npx prisma db push solicitar confirmação interativa (não deve ocorrer em alterações aditivas), usar --accept-data-loss apenas se absolutamente necessário e justificar no log.

Verificar que a saída do db push contém "Your database is now in sync with your Prisma schema" sem erros.
  </action>
  <verify>
    <automated>grep -n "oneSignalPlayerId" apps/api/prisma/schema.prisma && grep -n "@@unique" apps/api/prisma/schema.prisma && npx prisma validate --schema=apps/api/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "oneSignalPlayerId" apps/api/prisma/schema.prisma retorna 1
    - grep -c "@@unique(\[userId, condominiumId\])" apps/api/prisma/schema.prisma retorna 1
    - npx prisma validate retorna sem erros
    - npx prisma db push retorna exit code 0 com mensagem "Your database is now in sync"
    - Os demais campos do modelo User permanecem inalterados (creditBalance, autoRecharge, cardTokenMp ainda presentes)
  </acceptance_criteria>
  <done>Schema atualizado, gerado e sincronizado com MongoDB Atlas. npx prisma db push saiu com código 0.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Instalar node-cron e @onesignal/node-onesignal no workspace apps/api</name>
  <files>apps/api/package.json, package-lock.json</files>
  <read_first>
    - apps/api/package.json — ler para confirmar que node-cron e @onesignal/node-onesignal ainda não estão presentes antes de instalar
  </read_first>
  <action>
Instalar os dois pacotes novos no workspace apps/api conforme RESEARCH.md Package Legitimacy Audit (ambos auditados e aprovados):

  npm install node-cron@4.2.1 @onesignal/node-onesignal@5.8.0 --workspace=apps/api
  npm install --save-dev @types/node-cron --workspace=apps/api

Confirmar que os pacotes aparecem corretamente no apps/api/package.json após a instalação.
  </action>
  <verify>
    <automated>grep -c "node-cron" apps/api/package.json && grep -c "onesignal" apps/api/package.json</automated>
  </verify>
  <acceptance_criteria>
    - grep "node-cron" apps/api/package.json mostra "node-cron": "4.2.1" (ou compatível ^4.2.1)
    - grep "@onesignal/node-onesignal" apps/api/package.json mostra a versão 5.8.0 (ou ^5.8.0)
    - grep "@types/node-cron" apps/api/package.json mostra a entrada em devDependencies
    - npm run build --workspace=apps/api não retorna erros de import de node-cron ou @onesignal/node-onesignal
  </acceptance_criteria>
  <done>Dois pacotes instalados no workspace apps/api. package.json e package-lock.json atualizados.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar stubs de teste Wave 0 para os 4 arquivos de teste da Fase 4</name>
  <files>
    apps/api/src/modules/schedules/__tests__/schedules.service.test.ts,
    apps/api/src/modules/orders/__tests__/orders.service.test.ts,
    apps/web/src/components/client/__tests__/BannerInsuficiente.test.tsx,
    apps/web/src/hooks/__tests__/useSchedule.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/auth/__tests__/auth.service.test.ts (se existir) ou qualquer arquivo de teste existente no projeto para entender o padrão de stub usado nas fases anteriores
    - apps/web/src/components/client/__tests__/ (verificar se existe algum teste de componente já criado para entender o padrão de import)
  </read_first>
  <action>
Criar os 4 arquivos de stub abaixo. Cada stub deve: (1) ter um describe com o nome do módulo; (2) ter pelo menos um it.todo para cada comportamento listado abaixo; (3) passar na execução do Vitest (it.todo conta como pending, não como falha).

apps/api/src/modules/schedules/__tests__/schedules.service.test.ts:
  - describe('SchedulesService') com it.todo para:
    - 'upsertSchedule cria novo Schedule se não existir'
    - 'upsertSchedule atualiza Schedule existente'
    - 'createDailyOrders cria Order para cada Schedule ativo com saldo suficiente'
    - 'createDailyOrders pula dias com quantidade 0'
    - 'createDailyOrders não cria Order quando saldo insuficiente'
    - 'sendReconfigureReminders chama OneSignal para usuários com notifyReconfigure true'
    - 'processAutoBuy detecta modo acabar e gera push quando saldo menor que consumo semanal'
    - 'processAutoBuy modo semanal dispara no dia correto da semana'

apps/api/src/modules/orders/__tests__/orders.service.test.ts:
  - describe('OrdersService') com it.todo para:
    - 'createSingleOrder reserva créditos e cria Order com type SINGLE e status SCHEDULED'
    - 'createSingleOrder lança erro 400 quando saldo insuficiente'
    - 'createSingleOrder rejeita scheduledDate no passado'

apps/web/src/components/client/__tests__/BannerInsuficiente.test.tsx:
  - describe('BannerInsuficiente') com it.todo para:
    - 'renderiza quando requerido maior que saldo'
    - 'retorna null quando requerido menor ou igual ao saldo'
    - 'chama onComprar ao clicar em Comprar créditos'
    - 'chama onAjustar com o valor do saldo ao clicar em Usar saldo'

apps/web/src/hooks/__tests__/useSchedule.test.ts:
  - describe('useSchedule coverage calculation') com it.todo para:
    - 'calcula consumoSemanal como soma de todos os dias em weeklyQty'
    - 'calcula cobre como Math.floor(saldo dividido por consumoSemanal)'
    - 'retorna falta true quando semana maior que saldo'
    - 'evita divisão por zero quando consumoSemanal é 0'

Os arquivos de teste usam Vitest (import { describe, it, expect } from 'vitest'). Para o arquivo React Testing Library use também import { render } from '@testing-library/react'.
  </action>
  <verify>
    <automated>npm run test --workspace=apps/api -- --run --reporter=verbose 2>&1 | grep -E "todo|pending|pass|fail" | head -20 && npm run test --workspace=apps/web -- --run --reporter=verbose 2>&1 | grep -E "todo|pending|pass|fail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos existem em seus caminhos corretos
    - npm run test --workspace=apps/api -- --run não retorna "FAIL" nos novos arquivos (todos os it.todo ficam pending)
    - npm run test --workspace=apps/web -- --run não retorna "FAIL" nos novos arquivos
    - grep -c "it.todo" apps/api/src/modules/schedules/__tests__/schedules.service.test.ts retorna 8
    - grep -c "it.todo" apps/api/src/modules/orders/__tests__/orders.service.test.ts retorna 3
    - grep -c "it.todo" apps/web/src/components/client/__tests__/BannerInsuficiente.test.tsx retorna 4
    - grep -c "it.todo" apps/web/src/hooks/__tests__/useSchedule.test.ts retorna 4
  </acceptance_criteria>
  <done>Quatro arquivos de stub criados. Suite de testes passa sem erros (todos os it.todo ficam pending).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → MongoDB Atlas | npx prisma db push envia alterações de schema para o banco remoto |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | schema.prisma db push | mitigate | Usar alterações aditivas (novos campos opcionais) — sem --accept-data-loss em campos existentes; verificar com npx prisma validate antes do push |
| T-04-SC | Tampering | npm install (node-cron, @onesignal/node-onesignal) | mitigate | Package Legitimacy Audit concluída em 04-RESEARCH.md — ambos os pacotes verificados no registry com slopcheck OK |
</threat_model>

<verification>
1. grep -c "oneSignalPlayerId" apps/api/prisma/schema.prisma == 1
2. grep -c "@@unique" apps/api/prisma/schema.prisma == 1 (Schedule)
3. grep "node-cron" apps/api/package.json e grep "@onesignal/node-onesignal" apps/api/package.json retornam versão
4. npm run test --workspace=apps/api -- --run não retorna FAIL
5. npm run test --workspace=apps/web -- --run não retorna FAIL
</verification>

<success_criteria>
Schema sincronizado com Atlas (oneSignalPlayerId + @@unique). Dois pacotes instalados. Quatro arquivos de stub criados e passando na suite. Fase 4 pode prosseguir para Wave 1.
</success_criteria>

<output>
Criar `.planning/phases/04-scheduling/04-01-SUMMARY.md` ao concluir.
</output>
