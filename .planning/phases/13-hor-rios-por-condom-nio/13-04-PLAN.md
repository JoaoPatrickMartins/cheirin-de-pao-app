---
phase: 13-hor-rios-por-condom-nio
plan: 04
type: execute
wave: 3
depends_on:
  - 13-02
  - 13-03
files_modified:
  - apps/web/src/components/client/DeliveryTimeChips.tsx
  - apps/web/src/screens/client/ScheduleScreen.tsx
autonomous: true
requirements:
  - SLOT-04
must_haves:
  truths:
    - "DeliveryTimeChips não usa mais DELIVERY_TIMES hardcoded — recebe slots[] como prop da API"
    - "ScheduleScreen busca GET /client/condominium/slots e passa slots para DeliveryTimeChips"
    - "Se API retornar array vazio, nenhum chip é exibido (sem crash)"
    - "Estilo visual dos chips é idêntico ao atual — apenas a fonte de dados muda"
  artifacts:
    - path: "apps/web/src/components/client/DeliveryTimeChips.tsx"
      provides: "Componente com prop slots: DeliverySlot[] em vez de DELIVERY_TIMES hardcoded"
      exports: ["DeliveryTimeChips"]
    - path: "apps/web/src/screens/client/ScheduleScreen.tsx"
      provides: "useEffect que busca /client/condominium/slots e passa para DeliveryTimeChips"
  key_links:
    - from: "ScheduleScreen.tsx"
      to: "GET /client/condominium/slots"
      via: "apiFetch ou fetch com Authorization header (padrão do projeto)"
      pattern: "client/condominium/slots"
    - from: "ScheduleScreen.tsx"
      to: "DeliveryTimeChips"
      via: "prop slots={slots}"
      pattern: "slots={"
---

<objective>
Substituir os chips de horário hardcoded em DeliveryTimeChips por dados dinâmicos vindos de GET /client/condominium/slots, e integrar essa busca na ScheduleScreen.

Purpose: Fecha o ciclo da fase — o cliente vê apenas os horários realmente ativos do seu condomínio, configurados pelo admin, em vez de 4 opções fixas que não refletem a realidade.

Output: DeliveryTimeChips aceita prop slots[], ScheduleScreen busca a API uma vez na montagem e passa os slots para o componente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-hor-rios-por-condom-nio/13-RESEARCH.md
@apps/web/src/components/client/DeliveryTimeChips.tsx
@apps/web/src/screens/client/ScheduleScreen.tsx
</context>

<interfaces>
<!-- DeliveryTimeChips atual -->
const DELIVERY_TIMES = ['06:30', '07:00', '07:30', '08:00']
interface DeliveryTimeChipsProps {
  value: string
  onChange: (v: string) => void
}

<!-- NOVA assinatura (após este plano) -->
interface DeliverySlot {
  name: string   // "manha" | "tarde"
  time: string   // "06:30"
  cutoffTime: string
  isActive: boolean
}
interface DeliveryTimeChipsProps {
  slots: DeliverySlot[]
  value: string
  onChange: (v: string) => void
}

<!-- GET /client/condominium/slots — resposta -->
// Array de DeliverySlot com isActive=true (filtrado no backend)
// Autenticado: Authorization: Bearer <token>
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refatorar DeliveryTimeChips para receber slots[] como prop</name>
  <files>apps/web/src/components/client/DeliveryTimeChips.tsx</files>
  <action>
Refatorar `DeliveryTimeChips.tsx`:

1. Remover a constante `DELIVERY_TIMES` hardcoded.
2. Atualizar a interface de props para incluir `slots: DeliverySlot[]` onde `DeliverySlot` é definido localmente no arquivo com os campos `name: string` e `time: string` (os demais campos não são necessários para renderização dos chips).
3. Substituir o `.map` de `DELIVERY_TIMES.map((time) => ...)` por `slots.map((slot) => ...)`, usando `slot.time` como valor e `slot.name` como key.
4. Manter EXATAMENTE o mesmo estilo visual atual dos chips — cores, bordas, padding, tipografia. Apenas a fonte de dados muda.
5. Se `slots` for array vazio, o componente renderiza o container sem chips (sem crash, sem mensagem de erro — ScheduleScreen tratará esse estado).

ATENÇÃO — Alta fidelidade de design é mandatória (CLAUDE.md). Não alterar nenhuma propriedade CSS/style inline dos chips existentes — apenas substituir a origem dos dados.

Exportar o tipo `DeliverySlot` do arquivo para que ScheduleScreen possa importar e tipar o estado.
  </action>
  <verify>
    <automated>grep -c "DELIVERY_TIMES" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/components/client/DeliveryTimeChips.tsx || true && grep -c "slots" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/components/client/DeliveryTimeChips.tsx</automated>
  </verify>
  <done>DELIVERY_TIMES removido; componente aceita prop `slots: DeliverySlot[]`; estilo visual idêntico ao anterior; compila sem erros TypeScript.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integrar busca de slots na ScheduleScreen</name>
  <files>apps/web/src/screens/client/ScheduleScreen.tsx</files>
  <action>
Em `ScheduleScreen.tsx`:

1. Adicionar estado: `const [slots, setSlots] = useState<DeliverySlot[]>([])` — importar `DeliverySlot` de `DeliveryTimeChips`.

2. Adicionar `useEffect` que executa uma vez na montagem (deps `[]`):
   - Buscar `GET /client/condominium/slots` usando o padrão de fetch já em uso na tela (apiFetch ou fetch com token do AuthContext — seguir o padrão exato das outras chamadas na mesma tela).
   - Em caso de sucesso: `setSlots(data)`.
   - Em caso de erro: logar via `console.warn` e manter `slots` como array vazio (chips não aparecem; não quebrar a tela).

3. Localizar o uso de `DeliveryTimeChips` na tela e adicionar a prop `slots={slots}`.

4. Se a tela exibia `value` inicializado com um dos valores de `DELIVERY_TIMES` hardcoded (ex: `'06:30'`): manter o valor inicial como string vazia `''` ou o primeiro slot da lista ao receber a resposta — verificar o estado atual antes de alterar para não quebrar a lógica de seleção existente.

ATENÇÃO — Não alterar nenhuma outra lógica da ScheduleScreen (steppers por dia, banner de cobertura, useSchedule hook, etc.). Escopo restrito a: adicionar estado `slots`, adicionar `useEffect` de fetch, e passar `slots` para `DeliveryTimeChips`.
  </action>
  <verify>
    <automated>grep -c "client/condominium/slots" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/screens/client/ScheduleScreen.tsx && grep -c "slots={slots}" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/screens/client/ScheduleScreen.tsx && cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ScheduleScreen busca slots na montagem e passa para DeliveryTimeChips; build do monorepo conclui sem erros TypeScript; nenhuma outra lógica da tela foi alterada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → /client/condominium/slots | Token JWT do cliente enviado no header — padrão existente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | GET /client/condominium/slots | accept | Endpoint retorna apenas slots do próprio condomínio do usuário (protegido no backend — Plano 03) |
| T-13-04-02 | Denial of Service | useEffect fetch na montagem | mitigate | Erro tratado com console.warn + estado vazio; tela não trava em falha de rede |
| T-13-04-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
1. ScheduleScreen abre e DeliveryTimeChips exibe os chips carregados da API (não mais 4 fixos)
2. Se API retornar 2 slots (manha 06:30, tarde 15:30): exibidos 2 chips
3. Estilo visual dos chips idêntico ao anterior
4. Build sem erros: `npm run build` na raiz do monorepo
</verification>

<success_criteria>
Cliente abre a tela de agenda e vê os chips de horário gerados dinamicamente dos slots ativos do seu condomínio. DeliveryTimeChips não contém mais nenhum array hardcoded. ScheduleScreen lida com erro de rede sem travar.
</success_criteria>

<output>
Criar `.planning/phases/13-hor-rios-por-condom-nio/13-04-SUMMARY.md` ao concluir.
</output>
