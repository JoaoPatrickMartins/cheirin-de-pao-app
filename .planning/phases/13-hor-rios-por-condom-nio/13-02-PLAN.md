---
phase: 13-hor-rios-por-condom-nio
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - apps/api/src/modules/admin-condominiums/admin-condominiums.schema.ts
  - apps/api/src/modules/admin-condominiums/admin-condominiums.repository.ts
  - apps/api/src/modules/admin-condominiums/admin-condominiums.service.ts
  - apps/api/src/modules/admin-condominiums/admin-condominiums.controller.ts
  - apps/api/src/modules/admin-condominiums/admin-condominiums.route.ts
autonomous: true
requirements:
  - SLOT-01
  - SLOT-02
  - SLOT-05
  - SLOT-06
must_haves:
  truths:
    - "PATCH /admin/condominiums/:id/slots/:slotName atualiza time, cutoffTime ou isActive de um slot sem remover o outro"
    - "POST /admin/condominiums injeta os 2 slots padrão automaticamente"
    - "Admin pode ativar/desativar slot individualmente via isActive"
    - "Validação Zod rejeita time/cutoffTime fora do formato HH:MM"
  artifacts:
    - path: "apps/api/src/modules/admin-condominiums/admin-condominiums.schema.ts"
      provides: "SlotUpdateSchema, SlotUpdateBody"
      exports: ["SlotUpdateSchema", "SlotUpdateBody"]
    - path: "apps/api/src/modules/admin-condominiums/admin-condominiums.repository.ts"
      provides: "updateSlot (read-modify-write), findById"
      exports: ["AdminCondominiumsRepository"]
    - path: "apps/api/src/modules/admin-condominiums/admin-condominiums.service.ts"
      provides: "create com DEFAULT_SLOTS, updateSlot"
      exports: ["AdminCondominiumsService"]
    - path: "apps/api/src/modules/admin-condominiums/admin-condominiums.route.ts"
      provides: "PATCH /admin/condominiums/:id/slots/:slotName"
  key_links:
    - from: "admin-condominiums.route.ts"
      to: "admin-condominiums.controller.ts"
      via: "preHandler authenticate + requireAdmin"
      pattern: "slots/:slotName"
    - from: "admin-condominiums.repository.ts"
      to: "prisma.condominium"
      via: "read-modify-write (findById + update)"
      pattern: "updatedSlots.*map"
---

<objective>
Implementar o CRUD de slots no módulo admin-condominiums: nova rota PATCH para atualização individual de slot e injeção de slots padrão na criação de condomínio.

Purpose: Permite ao admin editar horário, horário de corte e ativar/desativar cada slot individualmente por condomínio, e garante que novos condomínios nasçam com os 2 slots padrão.

Output: Endpoint `PATCH /admin/condominiums/:id/slots/:slotName` funcional e `POST /admin/condominiums` retornando condomínio com `deliverySlots` populados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-hor-rios-por-condom-nio/13-RESEARCH.md
@apps/api/src/modules/admin-condominiums/admin-condominiums.schema.ts
@apps/api/src/modules/admin-condominiums/admin-condominiums.repository.ts
@apps/api/src/modules/admin-condominiums/admin-condominiums.service.ts
@apps/api/src/modules/admin-condominiums/admin-condominiums.controller.ts
@apps/api/src/modules/admin-condominiums/admin-condominiums.route.ts
</context>

<interfaces>
<!-- Contratos existentes no módulo admin-condominiums -->

<!-- admin-condominiums.schema.ts (atual) -->
CreateCondominiumSchema — z.object({ name, address, type })
UpdateCondominiumSchema — CreateCondominiumSchema.partial()

<!-- admin-condominiums.repository.ts (atual) -->
findAll() → Condominium[]
findById(id: string) → Condominium | null
create(data: CreateCondominiumBody) → Condominium
update(id, data) → Condominium
remove(id) → Condominium

<!-- admin-condominiums.service.ts (atual) -->
list() → Condominium[]
create(data) → Condominium
update(id, data) → Condominium
remove(id) → void

<!-- Novos contratos a criar neste plano -->
SlotUpdateSchema — z.object({ time?, cutoffTime?, isActive? })
SlotUpdateBody — inferido do schema

repository.updateSlot(id, slotName, patch) → Condominium
service.updateSlot(id, slotName, patch) → Condominium
controller.updateSlot — handler do PATCH

DEFAULT_SLOTS = [
  { name: 'manha', time: '06:30', cutoffTime: '22:00', isActive: true },
  { name: 'tarde', time: '15:30', cutoffTime: '10:00', isActive: true },
]
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema + Repository — SlotUpdateSchema e updateSlot (read-modify-write)</name>
  <files>
    apps/api/src/modules/admin-condominiums/admin-condominiums.schema.ts
    apps/api/src/modules/admin-condominiums/admin-condominiums.repository.ts
  </files>
  <behavior>
    - SlotUpdateSchema aceita `{ time: "07:00" }` (parcial, só time) e valida HH:MM
    - SlotUpdateSchema rejeita `{ time: "99:99" }` (regex falha)
    - SlotUpdateSchema rejeita `{ time: "7:0" }` (formato sem zero à esquerda)
    - SlotUpdateSchema aceita `{ isActive: false }` (só isActive)
    - updateSlot com slotName "manha" atualiza apenas o slot manha; slot "tarde" permanece intacto
    - updateSlot com slotName inexistente lança { statusCode: 404, message: "Slot não encontrado" }
    - updateSlot com condomínio inexistente lança { statusCode: 404, message: "Condomínio não encontrado" }
  </behavior>
  <action>
Em `admin-condominiums.schema.ts`, adicionar após os exports existentes:

```typescript
export const SlotUpdateSchema = z.object({
  time: z.string().regex(/^\d{2}:\d{2}$/, 'Formato HH:MM esperado').optional(),
  cutoffTime: z.string().regex(/^\d{2}:\d{2}$/, 'Formato HH:MM esperado').optional(),
  isActive: z.boolean().optional(),
})
export type SlotUpdateBody = z.infer<typeof SlotUpdateSchema>
```

Em `admin-condominiums.repository.ts`, adicionar o método `updateSlot` seguindo o padrão read-modify-write (RESEARCH Pattern 3):

1. Chamar `this.findById(id)` para obter o condomínio e seu array de slots atual.
2. Se condomínio não encontrado: lançar `{ statusCode: 404, message: 'Condomínio não encontrado' }`.
3. Procurar o slot pelo `name === slotName`.
4. Se slot não encontrado: lançar `{ statusCode: 404, message: 'Slot não encontrado' }`.
5. Mapear o array substituindo apenas o slot alvo com `{ ...slot, ...patch }`.
6. Chamar `this.prisma.condominium.update({ where: { id }, data: { deliverySlots: updatedSlots } })`.

ATENÇÃO — slotName é sempre "manha" ou "tarde" (sem acento). Não aceitar IDs numéricos ou ObjectId.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test -- --run --reporter=verbose 2>&1 | grep -E "PASS|FAIL|admin-condominiums" | head -20</automated>
  </verify>
  <done>SlotUpdateSchema exportado com validação HH:MM; `updateSlot` no repository implementa read-modify-write preservando o outro slot; testes unitários passam.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Service + Controller + Route — create com slots padrão e PATCH slot</name>
  <files>
    apps/api/src/modules/admin-condominiums/admin-condominiums.service.ts
    apps/api/src/modules/admin-condominiums/admin-condominiums.controller.ts
    apps/api/src/modules/admin-condominiums/admin-condominiums.route.ts
  </files>
  <action>
Em `admin-condominiums.service.ts`:

1. Definir a constante `DEFAULT_SLOTS` no topo do arquivo (fora da classe):
   ```typescript
   const DEFAULT_SLOTS = [
     { name: 'manha', time: '06:30', cutoffTime: '22:00', isActive: true },
     { name: 'tarde', time: '15:30', cutoffTime: '10:00', isActive: true },
   ]
   ```

2. Modificar o método `create(data: CreateCondominiumBody)` para injetar os slots padrão:
   ```typescript
   async create(data: CreateCondominiumBody) {
     return this.repository.create({ ...data, deliverySlots: DEFAULT_SLOTS })
   }
   ```

3. Adicionar o método `updateSlot(id, slotName, patch)` que delega ao repository.

Em `admin-condominiums.controller.ts`, adicionar o handler `updateSlot`:
- Extrair `id` dos params e `slotName` dos params.
- Parse do body com `SlotUpdateSchema.parse(request.body)` (lança 400 se inválido — padrão do projeto).
- Chamar `service.updateSlot(id, slotName, parsed)`.
- Retornar o condomínio atualizado com `reply.send(updated)`.

Em `admin-condominiums.route.ts`, adicionar a rota:
```
PATCH /admin/condominiums/:id/slots/:slotName
preHandler: [fastify.authenticate, requireAdmin]
handler: controller.updateSlot
```

O `requireAdmin` é o guard padrão já usado nas outras rotas do mesmo arquivo — manter consistência.

ATENÇÃO — NÃO alterar as rotas existentes (GET, POST, PUT, DELETE). Apenas adicionar a nova rota PATCH de slots.
  </action>
  <verify>
    <automated>grep -c "slots/:slotName" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/admin-condominiums/admin-condominiums.route.ts && grep -c "DEFAULT_SLOTS" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/admin-condominiums/admin-condominiums.service.ts && cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>`PATCH /admin/condominiums/:id/slots/:slotName` registrado na rota; `create()` injeta DEFAULT_SLOTS; TypeScript compila sem erros.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → API | Body não confiável — validado por Zod antes de qualquer escrita |
| JWT → role check | Apenas ADMIN pode chamar a rota de atualização de slots |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | PATCH /admin/condominiums/:id/slots/:slotName | mitigate | Zod valida regex /^\d{2}:\d{2}$/ em time e cutoffTime antes de persistir |
| T-13-02-02 | Elevation of Privilege | admin-condominiums.route.ts | mitigate | preHandler requireAdmin — padrão D-11 do projeto; rota inacessível sem role ADMIN |
| T-13-02-03 | Tampering | updateSlot array rewrite | mitigate | Pattern 3 read-modify-write preserva o slot não alterado — não aceita array parcial do cliente |
| T-13-02-SC | Tampering | npm installs | accept | Nenhum pacote novo instalado |
</threat_model>

<verification>
1. `curl -X PATCH /admin/condominiums/:id/slots/manha -d '{"time":"07:00"}' -H "Authorization: Bearer ADMIN_TOKEN"` retorna 200 com slot manhã atualizado e slot tarde intacto
2. `curl -X PATCH /admin/condominiums/:id/slots/manha -d '{"time":"99:99"}' ...` retorna 400
3. `POST /admin/condominiums` com body válido retorna condomínio com `deliverySlots` contendo os 2 slots padrão
4. TypeScript compila sem erros: `cd apps/api && npx tsc --noEmit`
</verification>

<success_criteria>
Admin consegue criar um condomínio e receber automaticamente os 2 slots padrão (manhã 06:30 / tarde 15:30). Admin consegue editar horário, cutoffTime e isActive de cada slot via PATCH sem afetar o outro slot. Validação Zod bloqueia formatos inválidos com 400.
</success_criteria>

<output>
Criar `.planning/phases/13-hor-rios-por-condom-nio/13-02-SUMMARY.md` ao concluir.
</output>
