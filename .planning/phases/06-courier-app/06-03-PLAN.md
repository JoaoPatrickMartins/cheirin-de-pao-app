---
phase: 06-courier-app
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - apps/web/src/components/courier/CourierMap.tsx
  - apps/web/src/pages/courier/CourierRouteView.tsx
  - apps/web/src/pages/courier/CourierScreen.tsx
  - apps/web/src/main.tsx
autonomous: true
requirements:
  - COUR-03
  - COUR-04
  - COUR-05

must_haves:
  truths:
    - "Aba Rota exibe mapa Leaflet real com tiles OpenStreetMap e marcadores numerados dourados nos condomínios"
    - "Polyline dourada tracejada conecta os condomínios na ordem retornada pelo OSRM"
    - "Tooltip/overlay exibe distância total e número de paradas ('~X km · N paradas')"
    - "Se OSRM ou Nominatim falhar, aba Rota exibe 'Rota indisponível' sem bloquear a aba Lista"
    - "Lista de paradas abaixo do mapa exibe badge numerado, nome do condomínio e hora estimada"
    - "Mapa faz fitBounds nos marcadores ao montar (todos os condomínios visíveis)"
  artifacts:
    - path: "apps/web/src/components/courier/CourierMap.tsx"
      provides: "mapa react-leaflet com tiles OSM, marcadores DivIcon numerados dourados, polyline"
      exports: ["CourierMap"]
    - path: "apps/web/src/pages/courier/CourierRouteView.tsx"
      provides: "aba Rota: CourierMap + lista de paradas com hora estimada"
      exports: ["CourierRouteView"]
    - path: "apps/web/src/main.tsx"
      provides: "import leaflet/dist/leaflet.css antes de globals.css"
      contains: "leaflet/dist/leaflet.css"
  key_links:
    - from: "apps/web/src/pages/courier/CourierScreen.tsx"
      to: "apps/web/src/pages/courier/CourierRouteView.tsx"
      via: "tab === 'route' renderiza CourierRouteView passando data.route e data.condos como props"
      pattern: "CourierRouteView"
    - from: "apps/web/src/components/courier/CourierMap.tsx"
      to: "https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
      via: "TileLayer url"
      pattern: "tile.openstreetmap"
---

<objective>
Construir a aba Rota do app do entregador: mapa Leaflet real com tiles OSM, marcadores numerados dourados (DivIcon), polyline tracejada dourada, tooltip de distância/tempo e lista de paradas com hora estimada. Substituir o placeholder do Plano 02 pela implementação real.

Purpose: Completar o requisito COUR-03 (mapa com ordem de paradas entre condomínios) e COUR-04 (ordem sugerida — já calculada pelo backend, exibida visualmente no mapa). Após este plano, a fase 6 está feature-complete.
Output: CourierMap funcional com react-leaflet; CourierRouteView integrando mapa + lista; CourierScreen com aba Rota real; leaflet CSS importado em main.tsx.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-courier-app/06-CONTEXT.md
@.planning/phases/06-courier-app/06-RESEARCH.md
@.planning/phases/06-courier-app/06-UI-SPEC.md
@.planning/phases/06-courier-app/06-PATTERNS.md
@.planning/phases/06-courier-app/06-02-SUMMARY.md

<interfaces>
<!-- Interfaces críticas para implementação do mapa -->

De apps/web/src/pages/courier/CourierScreen.tsx (criado no Plano 02):
```typescript
// data.route pode ser null (graceful degradation):
type RouteData = {
  distanceKm: string    // ex: "4.7"
  durationMin: number   // ex: 22
  geometry: Array<[number, number]>  // [lat, lng] (convertidos pelo backend — ATENÇÃO: OSRM retorna [lng,lat], backend converte para [lat,lng])
} | null

// data.condos[i] contém lat/lng geocodificados pelo backend:
type CondoGroup = {
  condominiumId: string
  condominiumName: string
  address: string
  lat: number | null    // null se geocodificação falhou
  lng: number | null
  stops: Stop[]
}
```

Props de CourierMap:
```typescript
interface CourierMapProps {
  waypoints: Array<{ lat: number; lng: number; name: string; order: number }>
  geometry: Array<[number, number]>  // [lat, lng] para Polyline do react-leaflet
  distanceKm: string
  durationMin: number
}
```

Props de CourierRouteView:
```typescript
interface CourierRouteViewProps {
  condos: CondoGroup[]
  route: RouteData  // null se OSRM falhou
}
```

De RESEARCH.md — imports obrigatórios react-leaflet:
```typescript
// CRÍTICO: importar ANTES de globals.css em main.tsx
import 'leaflet/dist/leaflet.css'

// Imports react-leaflet v5:
import { MapContainer, TileLayer, Polyline, Marker, useMap } from 'react-leaflet'
import L from 'leaflet'
// (não usar L.Icon com iconUrl — usar apenas L.DivIcon com className: '')
```

De RESEARCH.md — criação de DivIcon com marcador numerado dourado:
```typescript
function createNumberedMarker(order: number): L.DivIcon {
  return L.divIcon({
    html: `<div style="width:36px;height:36px;border-radius:12px;background:#1E1207;border:2px solid #E3AC3F;color:#E3AC3F;font-family:'Bricolage Grotesque Variable',serif;font-size:15px;font-weight:800;display:flex;align-items:center;justify-content:center;">${order}</div>`,
    className: '',   // OBRIGATÓRIO: evita fundo branco padrão do leaflet-div-icon
    iconSize: [36, 36],
    iconAnchor: [18, 18],
  })
}
```

De RESEARCH.md — FitBounds como componente filho (react-leaflet v5 usa useMap hook):
```typescript
function FitBounds({ waypoints }: { waypoints: Array<{ lat: number; lng: number }> }) {
  const map = useMap()
  useEffect(() => {
    if (waypoints.length > 0) {
      const bounds = L.latLngBounds(waypoints.map(w => [w.lat, w.lng]))
      map.fitBounds(bounds, { padding: [20, 20] })
    }
  }, [map, waypoints])
  return null
}
```

De apps/web/src/pages/client/TrackingScreen.tsx — padrão de item em lista com badge:
```typescript
// Badge 32×32px, borderRadius 8, fundo gold, cor espresso, Bricolage 15px 800
// Nome 15px body 700, cor text
// Subtítulo 12px, cor textTer
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Importar leaflet CSS em main.tsx + criar CourierMap com react-leaflet</name>
  <files>
    apps/web/src/main.tsx,
    apps/web/src/components/courier/CourierMap.tsx
  </files>
  <read_first>
    - apps/web/src/main.tsx — ler o arquivo completo para ver a ordem de imports atuais e onde inserir leaflet CSS (deve ser antes de globals.css)
    - .planning/phases/06-courier-app/06-RESEARCH.md — seção "Padrão 4: react-leaflet setup crítico no Vite" e "Code Examples Exemplo 3" para os detalhes exatos de implementação
    - .planning/phases/06-courier-app/06-UI-SPEC.md — seção CourierRouteView (Mapa Leaflet) para especificações visuais do mapa
  </read_first>
  <action>
    1. Editar apps/web/src/main.tsx: inserir `import 'leaflet/dist/leaflet.css'` ANTES do import de globals.css (ou qualquer CSS do projeto). Esta ordem é crítica — CSS do Leaflet deve ser carregado antes dos estilos do app para não sobrescrever controles do mapa.

    2. Criar apps/web/src/components/courier/CourierMap.tsx com a seguinte estrutura:

       Imports: `import { MapContainer, TileLayer, Polyline, Marker, useMap } from 'react-leaflet'`, `import L from 'leaflet'`, `import 'leaflet/dist/leaflet.css'` (redundância intencional — garante CSS mesmo se main.tsx for modificado), `import { useEffect } from 'react'`.

       Função `createNumberedMarker(order: number): L.DivIcon`: usar o código exato do RESEARCH.md — DivIcon com HTML inline, espresso background, borda gold, className: '' (OBRIGATÓRIO — sem className padrão).

       Componente filho `FitBounds`: recebe waypoints, usa `useMap()` hook, em useEffect chama `map.fitBounds` com padding [20, 20]. Retorna null.

       Componente principal `CourierMap({ waypoints, geometry, distanceKm, durationMin })`:
       - Container: div borderRadius 22, overflow hidden, height 290, position relative.
       - `<MapContainer center={[waypoints[0]?.lat ?? -23.5, waypoints[0]?.lng ?? -46.6]} zoom={13} style={{ height: '100%', width: '100%' }} aria-label="Mapa de rota do entregador" zoomControl={false}>`
       - `<TileLayer url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" attribution='&copy; <a href="https://openstreetmap.org/copyright">OpenStreetMap</a>' />`
       - `<Polyline positions={geometry} color="#E3AC3F" weight={4} dashArray="2 9" lineCap="round" />`
       - Para cada waypoint: `<Marker key={wp.order} position={[wp.lat, wp.lng]} icon={createNumberedMarker(wp.order)} title={wp.name} />`
       - `<FitBounds waypoints={waypoints} />`
       - Tooltip de distância (overlay absoluto fora do MapContainer, position absolute bottom 12 left 12 zIndex 1000): div fundo 'var(--color-surface)', borderRadius 8, padding '8px 12px', boxShadow 'var(--shadow-soft)'. Conteúdo: "~{distanceKm} km · {waypoints.length} paradas" — 12px body 700, cor 'var(--color-text)'.

       Não usar `L.Icon` com `iconUrl` em nenhum marcador. Usar exclusivamente `L.DivIcon` com `className: ''`.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && grep -n "leaflet/dist/leaflet.css" apps/web/src/main.tsx && grep -n "className: ''" apps/web/src/components/courier/CourierMap.tsx && npm run typecheck --workspace=apps/web 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/main.tsx contém `import 'leaflet/dist/leaflet.css'` em posição anterior ao import de globals.css
    - CourierMap.tsx contém `className: ''` no L.divIcon (previne fundo branco do Pitfall 4)
    - CourierMap.tsx usa `useMap()` no FitBounds (não `ref` em MapContainer — API v5)
    - CourierMap.tsx não contém `L.Icon` nem `iconUrl` (usa apenas DivIcon)
    - CourierMap.tsx contém `aria-label="Mapa de rota do entregador"` no MapContainer
    - `npm run typecheck --workspace=apps/web` sai com código 0
  </acceptance_criteria>
  <done>CourierMap funcional com marcadores DivIcon numerados, polyline dourada e FitBounds automático.</done>
</task>

<task type="auto">
  <name>Task 2: CourierRouteView + integração em CourierScreen (substituir placeholder)</name>
  <files>
    apps/web/src/pages/courier/CourierRouteView.tsx,
    apps/web/src/pages/courier/CourierScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/courier/CourierScreen.tsx — ler o arquivo do Plano 02 para localizar o placeholder da aba Rota e substituir por CourierRouteView
    - apps/web/src/pages/client/TrackingScreen.tsx — padrão de card de item em lista (badge + nome + subtítulo + hora) para replicar na lista de paradas da aba Rota
    - .planning/phases/06-courier-app/06-UI-SPEC.md — seção CourierRouteView, especialmente "Lista de paradas (abaixo do mapa)"
    - apps/web/src/components/courier/CourierMap.tsx — ver a interface CourierMapProps que foi implementada na Task 1
  </read_first>
  <action>
    Criar apps/web/src/pages/courier/CourierRouteView.tsx:
    Props: `CourierRouteViewProps { condos: CondoGroup[]; route: RouteData }`.

    Caso route === null: div padding '20px', card surface borderRadius 22 padding 24. Ícone de aviso + "Rota indisponível" (Bricolage 18px 700, cor text) + "O cálculo da rota está temporariamente indisponível. Use a aba Lista para as paradas." (15px body, cor textSec). (per Pitfall 7 do RESEARCH.md — não bloquear uso da lista).

    Caso route !== null:
    - Filtrar waypoints válidos: `condos.filter(c => c.lat !== null && c.lng !== null).map((c, i) => ({ lat: c.lat!, lng: c.lng!, name: c.condominiumName, order: i + 1 }))`.
    - Renderizar `<CourierMap waypoints={waypoints} geometry={route.geometry} distanceKm={route.distanceKm} durationMin={route.durationMin} />` dentro de div padding '0 0 12px'.
    - Abaixo do mapa: label "ORDEM DE PARADAS" (12px body 700, letterSpacing 0.04em, cor textTer, padding '12px 0 8px').
    - Lista de condomínios: para cada condo com coordenadas (filtrar lat !== null):
      div display flex, gap 12, padding '12px 16px', background 'var(--color-surface)', borderRadius 16, border '1px solid var(--color-border-2)'.
      Badge 32×32px, borderRadius 8, fundo '#E3AC3F', cor '#1E1207', Bricolage 15px 800, texto = índice+1.
      Div flex 1: nome 15px body 700 cor text; subtítulo 12px cor textTer (ex: `${condo.stops.length} paradas`).
      Hora estimada (direita): calcular acumulado — somar `durationMin / condos.length * index` minutos à hora atual. Formatar como "HH:MM". Exibir em 12px body 700, cor textSec.

    Atualizar apps/web/src/pages/courier/CourierScreen.tsx:
    - Localizar o bloco da aba Rota (tab === 'route') que exibe o placeholder "Mapa disponível em breve".
    - Substituir por: `<CourierRouteView condos={data.condos} route={data.route} />` com padding '12px 20px 0'.
    - Adicionar import de CourierRouteView no topo do arquivo.
    - Se data === null (loading ou erro), a aba Rota exibe um estado de loading (div centralizado, spinner dourado) — estado de loading já existe no CourierScreen, garantir que condição route nula quando data não carregou ainda não quebre o render.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && grep -n "CourierRouteView" apps/web/src/pages/courier/CourierScreen.tsx && npm run typecheck --workspace=apps/web 2>&1 | tail -10 && npm run build --workspace=apps/web 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CourierScreen.tsx importa e renderiza `<CourierRouteView condos={data.condos} route={data.route} />` na aba Rota (não o placeholder)
    - CourierRouteView.tsx renderiza mensagem "Rota indisponível" quando route === null (não crasha)
    - CourierRouteView.tsx filtra condos com lat/lng null antes de passar para CourierMap (evita NaN no mapa)
    - CourierRouteView.tsx lista de paradas tem badges numerados dourados (fundo '#E3AC3F', cor '#1E1207')
    - `npm run typecheck --workspace=apps/web` sai com código 0
    - `npm run build --workspace=apps/web` sai com código 0 (sem erros de bundle — leaflet CSS importado corretamente)
  </acceptance_criteria>
  <done>Aba Rota funcional end-to-end: mapa OSM com marcadores, polyline e lista de paradas com hora estimada. Fase 6 feature-complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → Leaflet | Coordenadas lat/lng vêm do backend (geocodificação server-side) — não do usuário; validação de null antes de passar ao mapa |
| OSRM geometry → Polyline | Geometria retornada pelo OSRM via backend — não confiada diretamente do browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service | CourierMap — muitos waypoints | accept | Rotas MVP têm 2–10 condomínios; Leaflet lida com dezenas de markers sem problema |
| T-06-09 | Information Disclosure | Tiles OpenStreetMap | accept | Tiles são públicos e não contêm dados do app; URLs de tiles não expõem rotas específicas do entregador |
| T-06-10 | Tampering | geometry array de lat/lng | mitigate | Backend converte [lng,lat] para [lat,lng] antes de enviar ao frontend; CourierRouteView passa geometry direto ao CourierMap sem reprocessar |
| T-06-11 | Availability | Nominatim/OSRM offline | accept | Pitfall 7 implementado: route: null → "Rota indisponível" sem bloquear aba Lista; entregador ainda pode confirmar todas as paradas |
</threat_model>

<verification>
Verificações manuais do plano 03:

1. Login como entregador → /courier → clicar aba "Rota" → mapa Leaflet carrega com tiles OSM visíveis (não tela preta)
2. Marcadores aparecem como círculos espresso com borda dourada e número branco/dourado (não ícone padrão do Leaflet)
3. Polyline dourada tracejada conecta os marcadores
4. Mapa faz fitBounds automático mostrando todos os condomínios na viewport
5. Tooltip "~X km · N paradas" visível no canto inferior esquerdo do mapa
6. Lista de paradas abaixo do mapa: badges dourados numerados + nome + hora estimada
7. Simular route: null (desativar OSRM temporariamente no backend) → aba Rota exibe "Rota indisponível" sem crashar; aba Lista ainda funciona normalmente
8. `npm run build --workspace=apps/web` passa sem erros (CSS do leaflet resolvido pelo Vite)
</verification>

<success_criteria>
- Aba Rota exibe mapa Leaflet real com tiles OpenStreetMap (não SVG ou placeholder)
- Marcadores numerados dourados implementados como DivIcon (sem problema de icon path no Vite)
- Polyline tracejada dourada visível conectando condomínios
- FitBounds automático ao montar o mapa
- Graceful degradation quando OSRM/Nominatim falham (route: null → "Rota indisponível")
- `npm run build --workspace=apps/web` passa sem erros de bundle
- Fase 6 feature-complete: todos os requisitos COUR-01..COUR-05 implementados
</success_criteria>

<output>
Criar `.planning/phases/06-courier-app/06-03-SUMMARY.md` ao concluir este plano.
</output>
