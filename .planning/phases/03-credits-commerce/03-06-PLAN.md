---
phase: 03-credits-commerce
plan: 06
type: execute
wave: 5
depends_on:
  - "03-05"
files_modified:
  - apps/web/src/pages/client/PixWaitingScreen.tsx
  - apps/web/src/pages/client/CardPaymentScreen.tsx
  - apps/web/src/hooks/usePaymentPolling.ts
  - apps/web/main.tsx
autonomous: false
requirements:
  - PAY-01
  - PAY-02
  - CRED-01
  - CRED-11

user_setup:
  - service: mercadopago-sandbox
    why: "Testar fluxos de pagamento Pix e cartão end-to-end"
    env_vars:
      - name: MP_ACCESS_TOKEN
        source: "https://www.mercadopago.com.br/developers → Suas integrações → Credenciais → Credenciais de teste → Access token"
      - name: MP_WEBHOOK_SECRET
        source: "https://www.mercadopago.com.br/developers → Suas integrações → [sua integração] → Webhooks → Configurar → Chave secreta"
      - name: MP_PUBLIC_KEY
        source: "Mesma área de Credenciais de teste → Chave pública"
      - name: VITE_MP_PUBLIC_KEY
        source: "Mesmo valor de MP_PUBLIC_KEY, adicionado em apps/web/.env"
    dashboard_config:
      - task: "Configurar URL de webhook para ngrok/MP CLI"
        location: "https://www.mercadopago.com.br/developers → Suas integrações → [integração] → Webhooks → Adicionar URL"

must_haves:
  truths:
    - "PixWaitingScreen exibe QR code como img com prefixo data:image/png;base64,"
    - "PixWaitingScreen exibe código copia-e-cola com botão Copiar (feedback Copiado! por 2s)"
    - "usePaymentPolling faz polling a cada 3s, para em 5 tentativas ou ao receber approved/rejected"
    - "Polling cleanup retorna clearInterval ao desmontar o componente"
    - "Ao receber approved, saldo é atualizado via AuthContext.updateCreditBalance sem refetch completo"
    - "CardPaymentScreen renderiza CardPayment Brick do @mercadopago/sdk-react"
    - "initMercadoPago é chamado com VITE_MP_PUBLIC_KEY e locale pt-BR"
    - "CardPayment.onSubmit envia token para POST /payments/card e navega para sucesso"
  artifacts:
    - path: "apps/web/src/hooks/usePaymentPolling.ts"
      provides: "Hook de polling com cleanup e callback onApproved"
      exports: ["usePaymentPolling"]
    - path: "apps/web/src/pages/client/PixWaitingScreen.tsx"
      provides: "Tela de espera Pix com QR code + copia-e-cola + polling"
      exports: ["PixWaitingScreen"]
    - path: "apps/web/src/pages/client/CardPaymentScreen.tsx"
      provides: "Tela de pagamento com CardPayment Brick do Mercado Pago"
      exports: ["CardPaymentScreen"]
  key_links:
    - from: "apps/web/src/pages/client/PixWaitingScreen.tsx"
      to: "apps/web/src/hooks/usePaymentPolling.ts"
      via: "usePaymentPolling(paymentId, onApproved)"
      pattern: "usePaymentPolling"
    - from: "apps/web/src/hooks/usePaymentPolling.ts"
      to: "apps/api GET /payments/:id/status"
      via: "apiFetch(`/payments/${paymentId}/status`)"
      pattern: "payments.*status"
    - from: "apps/web/src/hooks/usePaymentPolling.ts"
      to: "apps/web/src/contexts/AuthContext.tsx"
      via: "updateCreditBalance(data.creditBalance) quando approved"
      pattern: "updateCreditBalance"

---

<objective>
Wave 5 — Fluxos de pagamento: implementar PixWaitingScreen com QR code inline + polling, CardPaymentScreen com Mercado Pago Bricks, e o hook usePaymentPolling com cleanup correto. Instalar @mercadopago/sdk-react no workspace web.

Purpose: Com este plano o ciclo completo de pagamento fica funcional: usuário compra → paga via Pix ou cartão → polling detecta aprovação → saldo atualizado na HomeA sem recarregar a página.

Output: 4 arquivos frontend; fluxo Pix end-to-end funcionando; fluxo cartão com Bricks integrado. Checkpoint de verificação manual ao final.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-PATTERNS.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-UI-SPEC.md

<interfaces>
<!-- usePaymentPolling pattern (RESEARCH.md Pattern 5): -->
function usePaymentPolling(paymentId: string | null, onApproved: (creditBalance: number) => void) {
  const [attempts, setAttempts] = useState(0)
  const MAX_ATTEMPTS = 5
  useEffect(() => {
    if (!paymentId || attempts >= MAX_ATTEMPTS) return
    const id = setInterval(async () => {
      const res = await apiFetch(`/payments/${paymentId}/status`)
      const data = await res.json()
      if (data.status === 'approved') {
        clearInterval(id)
        onApproved(data.creditBalance)
      } else if (data.status === 'rejected') {
        clearInterval(id)
      } else {
        setAttempts(a => a + 1)
      }
    }, 3000)
    return () => clearInterval(id)   // cleanup obrigatório
  }, [paymentId, attempts])
  return { isTimeout: attempts >= MAX_ATTEMPTS }
}

<!-- QR code inline no frontend (RESEARCH.md + UI-SPEC.md): -->
<img src={`data:image/png;base64,${qrCodeBase64}`} width={200} height={200} style={{ borderRadius: 12 }} alt="QR Code PIX" />

<!-- Código copia-e-cola: -->
// fontFamily: monospace, fontSize: 12px, wordBreak: break-all
// fundo surface2, borderRadius 10px, padding 12px
// botão "Copiar código" → feedback "Copiado!" por 2s via navigator.clipboard.writeText

<!-- Spinner de polling (UI-SPEC.md §Motion): -->
// CSS keyframes spin: from rotate(0deg) to rotate(360deg)
// div: width 32px, height 32px, border 3px solid goldSoft, border-top-color gold, borderRadius 50%
// animation: spin 800ms linear infinite

<!-- CardPayment Brick pattern (RESEARCH.md Pattern 4): -->
import { initMercadoPago, CardPayment } from '@mercadopago/sdk-react'
// initMercadoPago chamado UMA VEZ em main.tsx com VITE_MP_PUBLIC_KEY
<CardPayment
  initialization={{ amount: combo.price }}
  onSubmit={async (formData) => {
    // formData: { token, payment_method_id, installments, issuer_id, ... }
    const res = await apiFetch('/payments/card', {
      method: 'POST',
      body: JSON.stringify({ ...formData, comboId: selectedComboId }),
    })
    // navegar para sucesso ou polling
  }}
  onError={(err) => console.error(err)}
  onReady={() => setIsLoading(false)}
/>

<!-- Navigate state recebido de CombosScreen: -->
// Para PixWaitingScreen: { paymentId: string, qrCodeBase64: string, qrCode: string, comboQuantity: number }
// Para CardPaymentScreen: { comboId?: string, customQuantity?: number, amount: number }

<!-- AuthContext.updateCreditBalance (criado no plano 04): -->
const { updateCreditBalance } = useAuth()
// Chamar após polling aprovar: updateCreditBalance(data.creditBalance)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Instalar @mercadopago/sdk-react + hook usePaymentPolling + PixWaitingScreen</name>
  <files>
    apps/web/src/hooks/usePaymentPolling.ts
    apps/web/src/pages/client/PixWaitingScreen.tsx
  </files>
  <read_first>
    - apps/web/src/hooks/useInstallPrompt.ts — padrão de hook com useEffect + cleanup (análogo)
    - apps/web/src/lib/apiFetch.ts — verificar assinatura e uso
    - apps/web/src/contexts/AuthContext.tsx — verificar updateCreditBalance adicionado no plano 04
    - apps/web/src/pages/auth/LoginScreen.tsx — padrão de useState + useNavigate + estados de erro
    - .planning/phases/03-credits-commerce/03-UI-SPEC.md §8 Fluxo Pix, §Motion (spinner)
    - .planning/phases/03-credits-commerce/03-RESEARCH.md §Pitfall 4 (cleanup setInterval), §Pitfall 3 (prefixo MIME)
  </read_first>
  <behavior>
    - Antes de criar os arquivos: executar `npm install @mercadopago/sdk-react --workspace=apps/web` para instalar o SDK
    - usePaymentPolling: intervalo 3000ms, MAX_ATTEMPTS = 5, retorna { isTimeout: boolean }
    - usePaymentPolling: cleanup retorna clearInterval — sem leaks mesmo ao desmontar
    - usePaymentPolling: onApproved recebe creditBalance do response e chama updateCreditBalance
    - usePaymentPolling: ao receber rejected, chamar onRejected se fornecido (prop opcional)
    - PixWaitingScreen: lê { paymentId, qrCodeBase64, qrCode, comboQuantity } de useLocation().state
    - PixWaitingScreen: QR code com `src={data:image/png;base64,${qrCodeBase64}}` width=200 height=200 borderRadius=12
    - PixWaitingScreen: código copia-e-cola com botão "Copiar código" que muda para "Copiado!" por 2s via navigator.clipboard.writeText
    - PixWaitingScreen: spinner ativo enquanto polling está rodando (isTimeout = false e status != approved/rejected)
    - Spinner: CSS spin keyframes declarado inline via `<style>` tag ou className (sem import de CSS externo)
    - PixWaitingScreen estado timeout: banner goldSoft com borda gold, texto "Não detectamos o pagamento ainda. Verifique o app do banco e tente novamente.", botão "Verificar mais tarde" → navigate('/client/home')
    - PixWaitingScreen estado rejected: banner neutro, texto "Pagamento recusado.", CTAs para tentar de novo
    - Ao approved: chamar updateCreditBalance, navegar para '/client/creditos/sucesso' com state { quantity: comboQuantity }
    - prefers-reduced-motion: sem spinner animado; substituir por texto "Aguardando..." (checar globals.css que já tem media query)
  </behavior>
  <action>
Executar primeiro: `cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm install @mercadopago/sdk-react --workspace=apps/web`

Criar usePaymentPolling.ts: seguir EXATAMENTE o Pattern 5 do RESEARCH.md. O callback `onApproved` recebe `creditBalance: number`. Usar `useAuth().updateCreditBalance` internamente OU deixar o consumer chamar — preferir deixar o consumer chamar para manter o hook desacoplado do AuthContext. Exportar: `export function usePaymentPolling(paymentId: string | null, onApproved: (creditBalance: number) => void, onRejected?: () => void): { isTimeout: boolean, attempts: number }`.

Criar PixWaitingScreen.tsx: use `const location = useLocation()` para ler o navigate state. Guardar `{ paymentId, qrCodeBase64, qrCode, comboQuantity }` com fallback seguro (se state for null, navegar de volta para /client/creditos). Chamar `usePaymentPolling(paymentId, handleApproved, handleRejected)` onde `handleApproved = (creditBalance) => { updateCreditBalance(creditBalance); navigate('/client/creditos/sucesso', { state: { quantity: comboQuantity } }) }`.

Ícone do botão "Copiar código": usar ícone `list` do Icon component (conforme UI-SPEC.md §8: "não há 'copy' no Ic — usar list") ou verificar se 'copy' foi adicionado. Se não existir, adicionar um SVG path simples de clipboard ao Icon.tsx. NÃO importar biblioteca externa de ícones.

Spinner inline via tag `<style>` sem import de CSS externo para manter componente autocontido:
```html
<style>{`@keyframes spin { from { transform: rotate(0deg) } to { transform: rotate(360deg) } }`}</style>
```
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run typecheck --workspace=apps/web 2>&1 | tail -10</automated>
  </verify>
  <done>
    - @mercadopago/sdk-react instalado (verificar em apps/web/package.json)
    - usePaymentPolling tem cleanup clearInterval (grep no arquivo)
    - usePaymentPolling: attempts máximo = 5, intervalo = 3000ms
    - PixWaitingScreen: QR code usa prefixo `data:image/png;base64,` (RESEARCH.md Pitfall 3)
    - PixWaitingScreen: botão Copiar usa navigator.clipboard.writeText (sem biblioteca externa)
    - TypeScript sem erros em apps/web
  </done>
</task>

<task type="auto">
  <name>Task 2: CardPaymentScreen + initMercadoPago em main.tsx</name>
  <files>
    apps/web/src/pages/client/CardPaymentScreen.tsx
    apps/web/main.tsx
  </files>
  <read_first>
    - apps/web/main.tsx — ler arquivo completo para adicionar initMercadoPago sem quebrar a inicialização existente
    - apps/web/src/pages/auth/LoginScreen.tsx — padrão de tela com AppBar de volta, states de loading/error
    - apps/web/src/lib/apiFetch.ts — verificar uso para POST /payments/card
    - apps/web/src/contexts/AuthContext.tsx — verificar updateCreditBalance disponível
    - .planning/phases/03-credits-commerce/03-RESEARCH.md §Pattern 4 (CardPayment Brick), §Pitfall 5 (MP_ACCESS_TOKEN no frontend)
    - .planning/phases/03-credits-commerce/03-UI-SPEC.md §8 Tela de Pagamento Cartão
  </read_first>
  <action>
Em main.tsx: adicionar `import { initMercadoPago } from '@mercadopago/sdk-react'` e chamar `initMercadoPago(import.meta.env.VITE_MP_PUBLIC_KEY, { locale: 'pt-BR' })` UMA VEZ antes do `ReactDOM.createRoot(...).render(...)`. Usar um guard para não chamar em modo de teste: `if (import.meta.env.VITE_MP_PUBLIC_KEY) { initMercadoPago(...) }`.

ANTI-PADRÃO CRÍTICO: NUNCA usar `import.meta.env.VITE_MP_ACCESS_TOKEN` ou qualquer variável de access token no frontend. Apenas `VITE_MP_PUBLIC_KEY` é permitida (chave pública).

Criar CardPaymentScreen.tsx: ler navigate state `{ comboId?, customQuantity?, amount }` via useLocation. Exibir AppBar com título "Pagamento com cartão" e botão voltar (← /client/creditos). Nota de segurança: "Pagamento processado com segurança pelo Mercado Pago" com ícone card 12px textTer.

Renderizar `<CardPayment>` do SDK com:
- `initialization={{ amount }}` onde amount vem do navigate state
- `onSubmit={async (formData) => { ... }}` — no body de onSubmit: chamar `apiFetch('/payments/card', { method: 'POST', body: JSON.stringify({ token: formData.token, installments: formData.installments, issuerId: formData.issuer_id, comboId, customQuantity }) })`. Se resposta ok, navegar para '/client/creditos/sucesso' com state `{ quantity: comboQty }`. Para cartão, o Brick confirma internamente — sem polling separado (webhook MP confirma e credita).
- `onError={(err) => { setError(err.message ?? 'Erro no pagamento. Tente novamente.') }}`
- `onReady={() => setIsLoading(false)}`

Estado loading enquanto Brick carrega: spinner simples ou texto "Carregando formulário de pagamento..." centralizado. Container do Brick com `borderRadius: 16px, overflow: hidden, padding: '0 20px', maxWidth: 390px`.

NOTA sobre card token para compra recorrente (D-06): o Brick já captura o card token em formData.token. Após POST /payments/card retornar sucesso, se o usuário for CLIENT, salvar o token via `apiFetch('/users/me/card-token', { method: 'PUT', body: JSON.stringify({ token: formData.token }) })`. Isso requer um endpoint simples em credits.route.ts: `PUT /users/me/card-token` que faz `prisma.user.update({ where: { id: userId }, data: { cardTokenMp: token } })`. Adicionar esse endpoint ao credits.route.ts do plano 02 se não foi incluído — ou adicionar inline ao CardPaymentScreen como um fire-and-forget (não bloquear o fluxo principal).
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run typecheck --workspace=apps/web 2>&1 | tail -10</automated>
  </verify>
  <done>
    - main.tsx chama initMercadoPago com VITE_MP_PUBLIC_KEY (não MP_ACCESS_TOKEN)
    - CardPaymentScreen renderiza CardPayment Brick com initialization={{ amount }}
    - CardPayment.onSubmit envia token para POST /payments/card
    - Nenhuma referência a VITE_MP_ACCESS_TOKEN em apps/web (grep deve retornar vazio)
    - TypeScript sem erros em apps/web
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Fluxo completo de pagamento Pix e cartão: CombosScreen → seleção de combo → Pix com QR code inline → polling → saldo atualizado na HomeA. CardPayment Brick com Mercado Pago SDK React integrado.
  </what-built>
  <how-to-verify>
    Pré-requisito: configurar MP_ACCESS_TOKEN (sandbox), MP_WEBHOOK_SECRET, MP_PUBLIC_KEY e VITE_MP_PUBLIC_KEY nos .env dos apps.

    1. Acessar o app como cliente autenticado → verificar tab bar com 4 abas
    2. Ir para aba "Créditos" → CombosScreen deve carregar combos do banco (se vazio, criar combo via Admin ou MongoDB Atlas diretamente)
    3. Selecionar um combo → escolher "Pix" → tocar em "Comprar {combo.nome}"
    4. PixWaitingScreen deve aparecer com:
       - QR code como imagem PNG (não texto quebrado)
       - Código copia-e-cola abaixo do QR
       - Botão "Copiar código" → ao tocar deve mostrar "Copiado!" por 2 segundos
       - Spinner rodando com texto "Aguardando pagamento..."
    5. Simular aprovação do webhook via MP CLI ou ngrok: `curl -X POST https://{seu-ngrok}/webhooks/mercadopago -H "x-signature: ts=...,v1=..." -d '{"action":"payment.updated","data":{"id":"..."}}'`
    6. Verificar que o polling detecta o status e navega para PurchasedScreen com "+N pães adicionados"
    7. Voltar para Home → card espresso deve exibir o novo saldo
    8. Testar fluxo de cartão: selecionar combo → "Cartão" → CardPayment Brick deve renderizar o formulário inline
    9. Preencher com cartão de teste do MP Sandbox (ex: 5031 7557 3453 0604) → confirmar
    10. Verificar saldo atualizado na Home após aprovação
  </how-to-verify>
  <resume-signal>Digite "aprovado" para confirmar que os fluxos Pix e cartão funcionam, ou descreva os problemas encontrados</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → POST /payments/card | Token gerado pelo Brick MP no browser — dados do cartão nunca tocam o servidor do app |
| Mercado Pago CDN → Browser | SDK React carrega scripts do CDN oficial do MP |
| main.tsx → VITE_MP_PUBLIC_KEY | Chave pública exposta no bundle — seguro por design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | usePaymentPolling → GET /payments/:id/status | accept | Endpoint autenticado (Bearer token); usuário só consulta seus próprios pagamentos (verificado pelo server via userId) |
| T-03-05 | Information Disclosure | main.tsx VITE_MP_PUBLIC_KEY | accept | Chave pública — projetada para ser exposta; MP_ACCESS_TOKEN nunca em VITE_* |
| T-03-11 | Tampering | CardPayment Brick | accept | Brick é iframe isolado do CDN do MP; dados do cartão tokenizados antes de chegar ao servidor do app — PCI compliance delegada ao MP |
| T-03-12 | Denial of Service | usePaymentPolling setInterval | mitigate | Max 5 tentativas; cleanup clearInterval ao desmontar; sem polling infinito |
</threat_model>

<verification>
Após execução deste plano:

```bash
# Verificar instalação do SDK React do MP
grep "@mercadopago/sdk-react" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/package.json

# Verificar cleanup no polling hook
grep -n "clearInterval\|return () =>" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/hooks/usePaymentPolling.ts

# Verificar que MP_ACCESS_TOKEN não está no frontend
grep -rn "VITE_MP_ACCESS_TOKEN\|MP_ACCESS_TOKEN" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/
# Deve retornar VAZIO

# Verificar prefixo MIME no QR code
grep -n "data:image/png;base64" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/pages/client/PixWaitingScreen.tsx

# TypeScript build
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run typecheck --workspace=apps/web

# Suite completa de testes
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test
```
</verification>

<success_criteria>
- @mercadopago/sdk-react instalado em apps/web
- usePaymentPolling: 5 tentativas máximas, 3s de intervalo, clearInterval no cleanup
- PixWaitingScreen: QR code com prefixo data:image/png;base64,
- PixWaitingScreen: botão Copiar com feedback "Copiado!" sem biblioteca externa
- CardPaymentScreen: Brick renderizado com initMercadoPago(VITE_MP_PUBLIC_KEY)
- Nenhuma referência a MP_ACCESS_TOKEN em apps/web
- Checkpoint humano aprovado: fluxos Pix e cartão funcionando com sandbox MP
- TypeScript sem erros em apps/web
- Suite de testes completa (npm run test) verde
</success_criteria>

<output>
Criar `.planning/phases/03-credits-commerce/03-06-SUMMARY.md` quando done
</output>
