---
phase: 03-credits-commerce
plan: 05
type: execute
status: completed
wave: 2
depends_on:
  - "03-04"
files_modified:
  - apps/web/src/pages/client/CombosScreen.tsx
  - apps/web/src/components/client/QuantityStepper.tsx
  - apps/web/src/components/client/StepperInline.tsx
  - apps/web/src/components/client/ComboCard.tsx
  - apps/web/src/components/client/BannerInsuficiente.tsx
  - apps/web/src/pages/client/PurchasedScreen.tsx
  - apps/web/src/pages/client/AutoBuyScreen.tsx
autonomous: true
requirements:
  - CRED-01
  - CRED-02
  - CRED-03
  - CRED-04
  - CRED-05
  - CRED-06
  - CRED-07
  - CRED-08
  - CRED-09
  - CRED-10
  - UI-07

must_haves:
  truths:
    - "CombosScreen carrega combos via GET /combos e pricing via GET /pricing"
    - "Toggle Combos/Compra personalizada muda o painel de conteúdo"
    - "QuantityStepper não permite value > max nem < min; botões ficam desabilitados nos limites"
    - "Compra personalizada mostra preço unitário e comparativo % mais barato do combo"
    - "CombosScreen rejeita na UI compra personalizada com quantity >= avulsoLimite"
    - "Créditos não expiram — banner informativo visível na CombosScreen"
    - "PurchasedScreen exibe +N pães adicionados e CTA Montar minha agenda"
    - "AutoBuyScreen salva preferência via PUT /users/me/auto-recharge no banco"
    - "Navigate state enviado para PixWaitingScreen usa camelCase: { paymentId, qrCodeBase64, qrCode, comboQuantity }"
  artifacts:
    - path: "apps/web/src/components/client/QuantityStepper.tsx"
      provides: "Stepper grande 48px reutilizável com min/max"
      exports: ["QuantityStepper"]
    - path: "apps/web/src/components/client/StepperInline.tsx"
      provides: "Stepper pequeno 34px para agenda semanal (Fase 4)"
      exports: ["StepperInline"]
    - path: "apps/web/src/pages/client/CombosScreen.tsx"
      provides: "Tela de compra: combos + compra personalizada + seleção de método de pagamento"
      exports: ["CombosScreen"]
    - path: "apps/web/src/pages/client/AutoBuyScreen.tsx"
      provides: "Configuração de compra recorrente automática (apenas UI + save)"
      exports: ["AutoBuyScreen"]
  key_links:
    - from: "apps/web/src/pages/client/CombosScreen.tsx"
      to: "apps/api POST /payments/pix"
      via: "apiFetch('/payments/pix', { method: 'POST' }) → navigate('/client/creditos/pix', { state: { paymentId, qrCodeBase64, qrCode, comboQuantity } })"
      pattern: "navigate.*creditos/pix"
    - from: "apps/web/src/pages/client/AutoBuyScreen.tsx"
      to: "apps/api PUT /users/me/auto-recharge"
      via: "apiFetch('/users/me/auto-recharge', { method: 'PUT' })"
      pattern: "auto-recharge"
---

<objective>
Wave 4 — Tela de compra + componentes de crédito: implementar a CombosScreen completa (compra de combo e personalizada), os componentes QuantityStepper/StepperInline/ComboCard/BannerInsuficiente, a PurchasedScreen e a AutoBuyScreen. Esta é a tela principal de conversão do app.

Purpose: Com este plano o usuário consegue selecionar um combo, ver o preço, escolher o método de pagamento (Pix ou cartão) e avançar para o fluxo de pagamento. A AutoBuyScreen salva a preferência de compra recorrente via PUT /users/me/auto-recharge (endpoint criado no 03-02 T2).

Output: 7 arquivos frontend; CombosScreen com toggle funcionando; QuantityStepper reutilizável (será reutilizado na Fase 4); testes de QuantityStepper GREEN.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-UI-SPEC.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-PATTERNS.md

<interfaces>
<!-- Tipo Combo retornado por GET /combos: -->
interface Combo {
  id: string
  name: string
  quantity: number    // quantidade de pães
  price: number       // Float em reais
  isActive: boolean
  tag?: string        // badge como "Mais popular" ou "Melhor valor"
  antes?: number      // preço riscado (se houver promoção — exibir se presente, criar promoção é Fase 7)
}

<!-- Tipo Pricing retornado por GET /pricing: -->
interface Pricing {
  avulsoLimite: number   // limite máximo de compra personalizada
  avulsoUnit: number     // preço unitário da compra personalizada
}

<!-- apiFetch wrapper (apps/web/src/lib/apiFetch.ts): -->
import { apiFetch } from '../../lib/apiFetch'
const res = await apiFetch('/combos')
if (res.ok) {
  const data = await res.json() as Combo[]
}

<!-- QuantityStepper props (UI-SPEC.md §5): -->
interface QuantityStepperProps {
  min: number
  max: number
  value: number
  onChange: (v: number) => void
}
// Botões: width/height 48px, borderRadius 16px, border 1.5px solid border
// Número: Bricolage Grotesque 800, fontSize 56px (token display-stepper), color accent (#B0702A)
// Gap entre botões e número: 24px
// Botão − desabilitado quando value <= min; botão + desabilitado quando value >= max

<!-- StepperInline props (UI-SPEC.md §6): -->
interface StepperInlineProps {
  min: number
  max: number
  value: number
  onChange: (v: number) => void
}
// Botões: width/height 34px (override de alta fidelidade), borderRadius 11px
// Número: Bricolage 800 18px, color accent quando value>0, textTer quando 0
// Gap: 12px

<!-- ComboCard props (UI-SPEC.md §4): -->
interface ComboCardProps {
  combo: Combo
  selected: boolean
  onSelect: () => void
}
// Container: background surface, borderRadius 22px, padding 18px
// Borda: 2px solid border2 (inativo), 2px solid accent (ativo)
// Sombra: shadowSoft (inativo), shadow strong (ativo)
// Nome: Bricolage 700 18px letterSpacing -0.02em
// Preço principal: Bricolage 800 22px color accent (ativo) ou text (inativo)
// Tag badge: posição absolute top -10px left 18px, background gold, color espresso, 11px 700 borderRadius 999px padding 3px 10px

<!-- BannerInsuficiente props (UI-SPEC.md §12): -->
interface BannerInsuficienteProps {
  saldo: number
  requerido: number
  onComprar: () => void
  onAjustar: (novaQtd: number) => void
}
// background goldSoft, border 1.5px solid gold, borderRadius 16px, padding 13px
// Texto: "Você tem {saldo} créditos e precisa de {requerido}. Compre mais ou ajuste a quantidade."
// CTAs: Btn variant=gold size=sm + Btn variant=ghost size=sm em flex row gap 10px

<!-- Contrato de navigate state enviado para PixWaitingScreen (camelCase obrigatório): -->
// { paymentId: string, qrCodeBase64: string, qrCode: string, comboQuantity: number }
// A API retorna qr_code_base64 e qr_code (snake_case) — renomear para camelCase ANTES de navegar
// PixWaitingScreen (03-06 T1) lê exatamente esses nomes do useLocation().state

<!-- Endpoint PUT /users/me/auto-recharge (criado em 03-02 T2): -->
// preHandler: [fastify.authenticate]
// body: { mode: 'acabar' | 'semanal', weekday?: string, comboId: string }
// resposta: 200 { ok: true }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QuantityStepper + StepperInline + ComboCard + BannerInsuficiente</name>
  <files>
    apps/web/src/components/client/QuantityStepper.tsx
    apps/web/src/components/client/StepperInline.tsx
    apps/web/src/components/client/ComboCard.tsx
    apps/web/src/components/client/BannerInsuficiente.tsx
  </files>
  <read_first>
    - apps/web/src/components/auth/OtpInput.tsx — padrão de componente controlado com interface de props TypeScript
    - apps/web/src/components/brand/Icon.tsx — verificar ícones disponíveis (minus, plus, spark, alert)
    - apps/web/src/styles/globals.css — tokens de cor, raio e sombra
    - apps/web/src/components/client/__tests__/QuantityStepper.test.tsx — stubs que devem ficar GREEN
    - .planning/phases/03-credits-commerce/03-UI-SPEC.md §5 (QuantityStepper), §6 (StepperInline), §4 (ComboCard), §12 (BannerInsuficiente)
  </read_first>
  <behavior>
    - QuantityStepper: clicar − quando value <= min não altera o state (botão visualmente disabled, opacity 0.5)
    - QuantityStepper: clicar + quando value >= max não altera o state (botão visualmente disabled, opacity 0.5)
    - QuantityStepper: clicar − quando value > min decrementa value em 1 e chama onChange(value - 1)
    - QuantityStepper: clicar + quando value < max incrementa value em 1 e chama onChange(value + 1)
    - StepperInline: mesma lógica mas dimensões 34px e número 18px (para ser reutilizado na Fase 4)
    - ComboCard: quando selected=true aplicar borda accent e sombra strong; quando false aplicar borda border2 e sombra soft
    - ComboCard: exibir preço riscado `antes` somente se combo.antes existe e antes > combo.price
    - BannerInsuficiente: exibir somente quando requerido > saldo; botão "Usar {saldo}" chama onAjustar(saldo)
    - Todos os botões interativos têm minHeight: 44px (UI-10)
  </behavior>
  <action>
Criar QuantityStepper.tsx como componente funcional puro com a interface de props descrita. Os botões − e + são `<button>` elements com `onClick={() => value > min && onChange(value - 1)}` e similar para +. Usar `style={{ opacity: value <= min ? 0.45 : 1, cursor: value <= min ? 'default' : 'pointer' }}` no botão − (e inverso para +). O número usa `fontFamily: 'var(--font-display)'`, `fontSize: 56`, `fontWeight: 800`, `color: 'var(--color-accent)'`.

Criar StepperInline.tsx com a mesma lógica mas dimensões 34×34px, borderRadius 11px, número 18px Bricolage 800 com color accent quando value > 0 e textTer quando 0.

Criar ComboCard.tsx com posição `relative` no container para o badge ficar `position: absolute, top: -10px, left: 18px`. Usar `transition: 'border-color .15s, box-shadow .15s'` para suavizar a seleção. O rádio indicator é um div circular 26×26px com borda accent quando selected, com ponto interno 13×13px de background accent visível apenas quando selected.

Criar BannerInsuficiente.tsx verificando que requerido > saldo antes de renderizar o conteúdo (se não, retornar null). Ícone `spark` ou `alert` 20px color accent. CTAs em flex row com gap 10px.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web -- --reporter=verbose 2>&1 | grep -E "QuantityStepper|PASS|FAIL" | head -20</automated>
  </verify>
  <done>
    - QuantityStepper.test.tsx GREEN: value não ultrapassa max, não cai abaixo de min
    - StepperInline compila sem erros (será testado na Fase 4)
    - ComboCard exibe badge somente se combo.tag existe; preço riscado somente se combo.antes existe
    - BannerInsuficiente retorna null quando requerido <= saldo
    - Todos os botões têm minHeight: 44px
  </done>
</task>

<task type="auto">
  <name>Task 2: CombosScreen + PurchasedScreen + AutoBuyScreen</name>
  <files>
    apps/web/src/pages/client/CombosScreen.tsx
    apps/web/src/pages/client/PurchasedScreen.tsx
    apps/web/src/pages/client/AutoBuyScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/auth/LoginScreen.tsx — padrão de estado local + apiFetch + navegação
    - apps/web/src/components/client/ComboCard.tsx — criado na Task 1; verificar props
    - apps/web/src/components/client/QuantityStepper.tsx — criado na Task 1; verificar props
    - apps/web/src/lib/apiFetch.ts — padrão de uso com headers Authorization
    - apps/web/src/contexts/AuthContext.tsx — verificar user.creditBalance e useAuth()
    - .planning/phases/03-credits-commerce/03-UI-SPEC.md §3 (Segmented Control), §8 (PurchasedScreen), §9 (AutoBuyScreen), §Contrato de Copywriting
    - .planning/phases/03-credits-commerce/03-CONTEXT.md §D-07 (Aba Créditos abre CombosScreen diretamente)
    - apps/web/src/pages/client/__tests__/CombosScreen.test.tsx — stub criado no 03-01 T2; deve ficar GREEN para lógica de navegação Pix
  </read_first>
  <action>
Criar CombosScreen.tsx com estado `tab: 'combos' | 'avulso'` para o Segmented Control. No mount, buscar via apiFetch: GET /combos (lista de Combo[]) e GET /pricing ({ avulsoLimite, avulsoUnit }). Estados: `isLoading`, `combos: Combo[]`, `pricing: Pricing`, `selectedCombo: Combo | null`, `customQty: number` (init: 1), `paymentMethod: 'pix' | 'card'`.

Segmented Control (UI-SPEC.md §3): container background surface2, borderRadius 14px, padding 4px, gap 6px (override de alta fidelidade). Tab ativa: background surface, boxShadow shadowSoft, transição 150ms. Tab inativa: transparent, sem sombra.

Loading state: 3 skeletons height:90 borderRadius:22 background:surface2.
Empty state: ícone bag 48px textTer + "Nenhum combo disponível no momento."

No painel de combos: lista de ComboCard com gap 12px entre cards.
No painel de compra personalizada: label "QUANTOS PÃES?" (maiúsculas), QuantityStepper com min=1, max=avulsoLimite-1. Preço unitário "{BRL(avulsoUnit)} por pão". Comparativo com melhor combo: calcular melhor combo/pão = min(combos.map(c => c.price/c.quantity)), calcular economia = ((avulsoUnit - melhorUnit) / avulsoUnit * 100).toFixed(0), exibir "{economia}% mais barato por pão" em cor accent. Banner "CRED-06: Créditos não expiram. Pause quando viajar." — texto informativo em good.

CTA bar fixo no bottom (acima da tab bar): Pill "Pix ou cartão" + botão "Comprar {combo.nome}" (variant primary, full) quando tab=combos; "Comprar {qtd} pães" quando tab=avulso.

CONTRATO DE NAVIGATE STATE (crítico — deve coincidir exatamente com o que PixWaitingScreen consome):
Ao clicar em "Comprar" via Pix: chamar apiFetch('POST /payments/pix', body: { comboId? ou customQuantity }). A API retorna `{ paymentId, qr_code_base64, qr_code }` (snake_case). Renomear para camelCase ANTES de navegar:
  const { paymentId, qr_code_base64: qrCodeBase64, qr_code: qrCode } = await res.json()
  const comboQuantity = selectedCombo?.quantity ?? customQty
  navigate('/client/creditos/pix', { state: { paymentId, qrCodeBase64, qrCode, comboQuantity } })
NUNCA passar qr_code_base64 (snake_case) no navigate state — PixWaitingScreen lê qrCodeBase64 (camelCase).

Se paymentMethod = 'card': navegar para '/client/creditos/cartao' passando { comboId: selectedCombo?.id, customQuantity: customQty, amount: selectedCombo?.price ?? avulsoUnit * customQty } via navigate state.

Criar PurchasedScreen.tsx: centralizado na tela. Ícone check: div 96×96px borderRadius 30%, background goodSoft, ícone check size:48 color:good stroke:2.4, com transform scale animado (0.8 → 1.0, 250ms ease-out via CSS transition em mount). Título "Créditos na conta!" Bricolage 700 26px. Subtexto "+{N} pães adicionados. Agora é só deixar a agenda no jeito." onde N vem de navigate state. Dois CTAs: "Montar minha agenda" (Btn full size=lg icon=calendar → /client/agenda) + "Voltar ao início" (Btn variant=ghost full → /client/home). Ler navigate state via `useLocation().state as { quantity: number }`.

Criar AutoBuyScreen.tsx: toggle mestre (Switch component ou toggle nativo). Quando on=true, mostrar: duas opções de modo ("Quando estiver acabando" / "Toda semana") em cards selecionáveis com rádio 26×26px. Modo "toda semana": chips de dia da semana (Dom/Seg/Ter/Qua/Qui/Sex/Sáb) com scroll horizontal. Lista de combos com rádio 22×22px para seleção. Nota de cobrança: "Cobramos no Pix salvo. Você recebe um aviso a cada compra automática." CTA dinâmico: se on=true "Ativar — {combo.nome} ({BRL(combo.preco)})"; se on=false "Salvar". Ao salvar: PUT /users/me/auto-recharge com body `{ mode: 'acabar' | 'semanal', weekday?: string, comboId: string }` via apiFetch. Este endpoint já está criado em 03-02 T2 no credits.route.ts.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web -- --reporter=verbose 2>&1 | grep -E "CombosScreen|PASS|FAIL" | head -20</automated>
  </verify>
  <done>
    - CombosScreen carrega combos e pricing com estados loading/error/empty corretos
    - Segmented Control: gap 6px (override de alta fidelidade conforme UI-SPEC.md §Exceções de Espaçamento)
    - QuantityStepper na compra personalizada tem max=avulsoLimite-1 (CRED-03: abaixo do limite)
    - CTA "Comprar" via Pix navega para /client/creditos/pix com { paymentId, qrCodeBase64, qrCode, comboQuantity } em camelCase no navigate state
    - PurchasedScreen exibe "+{N} pães adicionados" lendo N do navigate state
    - AutoBuyScreen salva via PUT /users/me/auto-recharge (CRED-07, CRED-08, CRED-10)
    - Banner informativo de créditos não expiram visível na CombosScreen (CRED-06)
    - Testes de CombosScreen passam (ou stubs mostram RED determinístico)
    - TypeScript compila sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CombosScreen → POST /payments/pix | Não envia preço — apenas comboId ou customQuantity; preço calculado no backend |
| navigate state (PixWaitingScreen) | Dados de QR code passados via navigate — não persistidos, não expostos em URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | CombosScreen.handleComprar | mitigate | Frontend não envia price — envia apenas comboId/customQuantity; backend calcula o transaction_amount pelo Combo.price no banco |
| T-03-10 | Tampering | AutoBuyScreen → PUT /auto-recharge | mitigate | Endpoint autenticado com authenticate preHandler; valida body com Zod; salva apenas preferência (sem pagamento) |
</threat_model>

<verification>
Após execução deste plano:

```bash
# Verificar stubs de QuantityStepper ficam GREEN
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web

# Verificar que CombosScreen usa camelCase no navigate state
grep -n "qrCodeBase64\|qr_code_base64" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/pages/client/CombosScreen.tsx
# Deve conter qrCodeBase64 (camelCase) na chamada navigate; qr_code_base64 apenas na desestruturação da resposta da API

# Verificar que CombosScreen não envia preço para API
grep -n "price\|amount\|transaction" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/pages/client/CombosScreen.tsx
# Deve retornar VAZIO para envio ao backend (preço não enviado do frontend)

# TypeScript check
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run typecheck --workspace=apps/web
```
</verification>

<success_criteria>
- QuantityStepper.test.tsx GREEN: botões desabilitados nos limites, value respeitado
- CombosScreen: toggle Combos/Avulso funcional, ComboCard selecionável, navegação para Pix/cartão correta
- Navigate state para PixWaitingScreen usa camelCase { paymentId, qrCodeBase64, qrCode, comboQuantity }
- Compra personalizada: preço unitário exibido, comparativo com melhor combo calculado, max=avulsoLimite-1
- PurchasedScreen: "+{N} pães adicionados" com N do navigate state
- AutoBuyScreen: salva preferência via PUT /users/me/auto-recharge
- Banner CRED-06 "Créditos não expiram" visível na CombosScreen
- TypeScript sem erros em apps/web
</success_criteria>

<output>
Criar `.planning/phases/03-credits-commerce/03-05-SUMMARY.md` quando done
</output>
