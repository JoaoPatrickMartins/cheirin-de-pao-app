---
phase: 03-credits-commerce
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/.env.example
  - apps/web/.env.example
  - apps/api/src/modules/payments/__tests__/payments.service.test.ts
  - apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts
  - apps/api/src/modules/credits/__tests__/credits.service.test.ts
  - apps/web/src/components/client/__tests__/QuantityStepper.test.tsx
  - apps/web/src/components/client/__tests__/ClientTabBar.test.tsx
  - apps/web/src/pages/client/__tests__/HomeScreen.test.tsx
autonomous: true
requirements:
  - CRED-01
  - CRED-03
  - CRED-04
  - CRED-06
  - CRED-07
  - CRED-11
  - PAY-01
  - UI-07
  - UI-08
  - UI-04

must_haves:
  truths:
    - "Schema Prisma contém campos creditBalance, autoRecharge e cardTokenMp no model User"
    - "Schema Prisma contém campo customQuantity no model Payment"
    - "prisma generate executa sem erros após as alterações"
    - "Variáveis MP_ACCESS_TOKEN, MP_WEBHOOK_SECRET, MP_PUBLIC_KEY documentadas no .env.example"
    - "Stubs de teste existem para payments, webhooks, credits e componentes UI"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Schema estendido com campos creditBalance, autoRecharge, cardTokenMp, customQuantity"
      contains: "creditBalance"
    - path: "apps/api/src/modules/payments/__tests__/payments.service.test.ts"
      provides: "Stubs failing tests para PAY-01, CRED-01, CRED-11"
    - path: "apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts"
      provides: "Stubs failing tests para PAY-01 HMAC, idempotência"
    - path: "apps/api/src/modules/credits/__tests__/credits.service.test.ts"
      provides: "Stubs failing tests para CRED-04"
  key_links:
    - from: "apps/api/prisma/schema.prisma"
      to: "apps/api/src/modules/payments/payments.repository.ts"
      via: "fastify.prisma.user.update creditBalance increment"
      pattern: "creditBalance"
---

<objective>
Wave 0 — Fundação bloqueante: estender o Prisma schema com os campos necessários para a Fase 3, rodar prisma generate, documentar as novas variáveis de ambiente e criar os stubs de teste que serão preenchidos nos planos seguintes.

Purpose: Nenhuma task de API ou frontend de créditos/pagamentos pode ser implementada sem o schema atualizado e sem os arquivos de teste criados. Este plano desbloqueie todos os outros.

Output: schema.prisma estendido + prisma generate executado sem erros + 6 arquivos de teste stub criados + .env.example atualizado.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-RESEARCH.md

<interfaces>
<!-- Modelo User existente (apps/api/prisma/schema.prisma, linhas 103-117): -->
model User {
  id            String    @id @default(auto()) @map("_id") @db.ObjectId
  email         String?   @unique
  phone         String?   @unique
  role          UserRole
  name          String
  cpf           String    @unique
  birthDate     DateTime?
  isBlocked     Boolean   @default(false)
  condominiumId String?   @db.ObjectId
  apartment     String?
  block         String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
}

<!-- Modelo Payment existente (apps/api/prisma/schema.prisma, linhas 260-271): -->
model Payment {
  id            String        @id @default(auto()) @map("_id") @db.ObjectId
  userId        String        @db.ObjectId
  amount        Float
  method        PaymentMethod
  status        PaymentStatus
  mercadoPagoId String?       @unique
  comboId       String?       @db.ObjectId
  createdAt     DateTime      @default(now())
  updatedAt     DateTime      @updatedAt
}

<!-- Padrão de mock Fastify para testes (apps/api/src/__tests__/auth.service.test.ts, linhas 7-33): -->
function createMockFastify(overrides = {}) {
  return {
    prisma: {
      user: { findUnique: vi.fn(), update: vi.fn() },
      otpCode: { findFirst: vi.fn(), create: vi.fn(), update: vi.fn() },
    },
    ...overrides,
  } as unknown as FastifyInstance
}

<!-- AuthUser atual (apps/web/src/contexts/AuthContext.tsx): -->
export interface AuthUser {
  id: string
  role: 'CLIENT' | 'COURIER' | 'ADMIN'
  name: string
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender schema Prisma + prisma generate + atualizar .env.example</name>
  <files>
    apps/api/prisma/schema.prisma
    apps/api/.env.example
    apps/web/.env.example
  </files>
  <read_first>
    - apps/api/prisma/schema.prisma — ler o arquivo completo para ver os modelos User e Payment existentes antes de editar
    - apps/api/.env.example — ler para ver variáveis existentes antes de adicionar
    - apps/web/.env.example — ler se existir; criar se não existir
    - .planning/phases/03-credits-commerce/03-RESEARCH.md §Schema Changes Required — campos exatos a adicionar
  </read_first>
  <action>
No model User, adicionar após o campo `block String?` (antes de `createdAt`) os três campos:
`creditBalance  Int     @default(0)`, `autoRecharge   Json?`, `cardTokenMp    String?`.

No model Payment, adicionar após o campo `comboId String? @db.ObjectId` (antes de `createdAt`) o campo:
`customQuantity Int?`.

IMPORTANTE: MongoDB com Prisma NÃO suporta migrations. Usar apenas `npx prisma generate` (nunca `prisma migrate dev`). O comentário de aviso já existe nas primeiras linhas do schema — respeitar.

Após editar o schema, executar em `apps/api/`:
`cd apps/api && npx prisma generate`

Se o generate falhar por erro de sintaxe, corrigir o schema e rodar novamente antes de prosseguir.

No arquivo `apps/api/.env.example`, adicionar ao final, abaixo das variáveis existentes, em seção comentada `# Mercado Pago`:
- `MP_ACCESS_TOKEN=TEST-...` (com instrução: "Obter em https://www.mercadopago.com.br/developers > Credenciais de teste")
- `MP_WEBHOOK_SECRET=...` (com instrução: "Segredo do webhook configurado no painel MP")
- `MP_PUBLIC_KEY=TEST-...` (com instrução: "Chave pública de teste — usada no frontend")

No arquivo `apps/web/.env.example` (criar se não existir), adicionar:
`VITE_MP_PUBLIC_KEY=TEST-...` (com instrução: "Mesma MP_PUBLIC_KEY do backend, prefixada com VITE_ para Vite")

NUNCA adicionar `MP_ACCESS_TOKEN` ou `MP_WEBHOOK_SECRET` no .env.example do frontend. Apenas `VITE_MP_PUBLIC_KEY` é seguro para o bundle Vite.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && grep -c "creditBalance" prisma/schema.prisma && grep -c "autoRecharge" prisma/schema.prisma && grep -c "cardTokenMp" prisma/schema.prisma && grep -c "customQuantity" prisma/schema.prisma</automated>
  </verify>
  <done>
    - grep retorna 1 para cada um dos 4 campos: creditBalance, autoRecharge, cardTokenMp, customQuantity
    - `npx prisma generate` executa sem erros (Prisma Client gerado com sucesso)
    - MP_ACCESS_TOKEN, MP_WEBHOOK_SECRET, MP_PUBLIC_KEY presentes em apps/api/.env.example
    - VITE_MP_PUBLIC_KEY presente em apps/web/.env.example
    - Nenhuma variável secreta MP no .env.example do frontend
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar stubs de teste Wave 0 (API + Web)</name>
  <files>
    apps/api/src/modules/payments/__tests__/payments.service.test.ts
    apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts
    apps/api/src/modules/credits/__tests__/credits.service.test.ts
    apps/web/src/components/client/__tests__/QuantityStepper.test.tsx
    apps/web/src/components/client/__tests__/ClientTabBar.test.tsx
    apps/web/src/pages/client/__tests__/HomeScreen.test.tsx
  </files>
  <read_first>
    - apps/api/src/__tests__/auth.service.test.ts — ler o arquivo completo para replicar o padrão de mock Fastify e estrutura describe/it com vitest
    - apps/api/vitest.config.ts — verificar configuração de caminhos e globals
    - apps/web/vitest.config.ts — verificar configuração jsdom + RTL
    - .planning/phases/03-credits-commerce/03-RESEARCH.md §Validation Architecture §Wave 0 Gaps — lista exata dos testes necessários
    - .planning/phases/03-credits-commerce/03-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
Criar os 6 arquivos de stub abaixo. Cada arquivo deve compilar sem erros (imports válidos) mas com testes marcados como `it.todo` ou com `expect(true).toBe(false)` para ficarem no estado RED correto. Usar `import { vi, describe, it, expect, beforeEach } from 'vitest'` em todos.

IMPORTANTE: criar os diretórios intermediários se não existirem (`__tests__/` dentro de cada módulo).

**`apps/api/src/modules/payments/__tests__/payments.service.test.ts`**
Cobrir: CRED-01 (PaymentsService.createPix cria Payment com status PENDING e retorna qr_code_base64), CRED-11 (getStatus retorna creditBalance quando approved).
Usar o padrão createMockFastify com overrides para `prisma.payment`, `prisma.user`, `prisma.creditTransaction`. Importar PaymentsService de `'../payments.service.js'` (caminho relativo com extensão `.js` conforme padrão do projeto ESM).

**`apps/api/src/modules/webhooks/__tests__/webhooks.service.test.ts`**
Cobrir: PAY-01 assinatura inválida retorna false (T-03-01), webhook duplicado não credita duas vezes (idempotência — T-03-01).
Importar WebhooksService de `'../webhooks.service.js'`.

**`apps/api/src/modules/credits/__tests__/credits.service.test.ts`**
Cobrir: CRED-04 (preço unitário avulso > melhor combo/pão), CRED-03 (rejeita quantity >= avulsoLimite).
Importar CreditsService de `'../credits.service.js'`.

**`apps/web/src/components/client/__tests__/QuantityStepper.test.tsx`**
Cobrir: UI-07 (value não ultrapassa max, não cai abaixo de min; botões desabilitados nos limites).
Usar RTL: `import { render, screen, fireEvent } from '@testing-library/react'`. Importar QuantityStepper de `'../QuantityStepper'`.

**`apps/web/src/components/client/__tests__/ClientTabBar.test.tsx`**
Cobrir: UI-08 (renderiza 4 abas com labels corretos; aba ativa recebe cor diferente).
Wrappear com `MemoryRouter` do `react-router` para fornecer contexto de rota. Importar ClientTabBar de `'../ClientTabBar'`.

**`apps/web/src/pages/client/__tests__/HomeScreen.test.tsx`**
Cobrir: UI-04 + CRED-11 (HomeScreen exibe o creditBalance do usuário autenticado).
Mockar `useAuth` via `vi.mock('../../hooks/useAuth')` retornando user com creditBalance = 42. Verificar que o número "42" aparece no DOM.

Todos os stubs devem ter pelo menos um `it('TODO: [behavior]', () => { expect(true).toBe(false) })` por comportamento listado, com label descritiva do que será implementado. Isso garante que os testes estejam no estado RED antes da implementação.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/api 2>&1 | grep -E "FAIL|fail|todo|TODO" | head -20</automated>
  </verify>
  <done>
    - 6 arquivos de stub criados nos caminhos especificados
    - `npm run test --workspace=apps/api` encontra e executa os 3 arquivos de teste da API (mesmo que todos falhem — estado RED esperado)
    - Nenhum erro de sintaxe TypeScript nos arquivos de stub (compilam, mas testes falham)
    - Caminhos de import usam extensão `.js` para arquivos ESM da API (padrão do projeto)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → banco | Campos creditBalance e autoRecharge introduzidos — nunca setados diretamente pelo cliente |
| .env.example → repositório | Documentação de variáveis sem valores reais — valores reais ficam em .env (não versionado) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | schema.prisma creditBalance | mitigate | Campo atualizado apenas via webhook service com $transaction atômico — nunca via request direto do cliente |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Pacotes MP verificados na Package Legitimacy Audit (RESEARCH.md) — nenhum pacote instalado neste plano; instalação ocorre no plano 02 |
</threat_model>

<verification>
Após execução deste plano:

```bash
# Verificar campos no schema
grep -E "creditBalance|autoRecharge|cardTokenMp|customQuantity" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/prisma/schema.prisma

# Verificar prisma generate (deve ter rodado com sucesso — checar node_modules/@prisma/client)
ls /home/joaopatrick/Projects/cheirin-de-pao-app/node_modules/.prisma/client/ 2>/dev/null | head -5

# Verificar arquivos de teste criados
ls /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/payments/__tests__/
ls /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/webhooks/__tests__/
ls /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api/src/modules/credits/__tests__/
ls /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/components/client/__tests__/
ls /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/pages/client/__tests__/
```
</verification>

<success_criteria>
- 4 novos campos adicionados ao schema Prisma (creditBalance, autoRecharge, cardTokenMp, customQuantity)
- `npx prisma generate` executa sem erros
- 6 arquivos de teste stub criados e encontrados pelo runner vitest
- Variáveis MP documentadas em .env.example sem vazar segredos
- Plano seguinte (Wave 1 — módulo payments) pode começar sem dependências pendentes
</success_criteria>

<output>
Criar `.planning/phases/03-credits-commerce/03-01-SUMMARY.md` quando done
</output>
