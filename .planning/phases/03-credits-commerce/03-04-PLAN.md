---
phase: 03-credits-commerce
plan: 04
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - apps/web/src/contexts/AuthContext.tsx
  - apps/web/src/pages/client/ClientLayout.tsx
  - apps/web/src/routes/router.tsx
  - apps/web/src/components/client/ClientTabBar.tsx
  - apps/web/src/pages/client/HomeScreen.tsx
  - apps/web/src/pages/client/PlaceholderScreen.tsx
  - apps/web/src/pages/client/CreditHistoryScreen.tsx
  - apps/web/src/components/client/CreditBalanceCard.tsx
autonomous: true
requirements:
  - CRED-11
  - UI-04
  - UI-08

must_haves:
  truths:
    - "Tab bar exibe Início / Agenda / Créditos / Pedidos fixada no bottom com safe-area"
    - "Aba ativa tem ícone gold (#E3AC3F) e label accent (#B0702A) peso 700"
    - "HomeA exibe creditBalance do usuário logado no card espresso com Bricolage Grotesque 800 52px"
    - "Botão Comprar créditos navega para /client/creditos"
    - "Botão Extrato navega para /client/creditos/extrato"
    - "Abas Agenda e Pedidos navegam para PlaceholderScreen com texto Em breve"
    - "AuthUser inclui creditBalance e AuthContextType inclui updateCreditBalance"
    - "Router tem todas as sub-rotas do /client com lazy loading"
  artifacts:
    - path: "apps/web/src/components/client/ClientTabBar.tsx"
      provides: "Tab bar fixa com 4 abas usando useLocation + useNavigate"
      exports: ["ClientTabBar"]
    - path: "apps/web/src/pages/client/HomeScreen.tsx"
      provides: "HomeA completa: card saldo + TodayDelivery placeholder + QuickActions + NextDays placeholder"
      exports: ["HomeScreen"]
    - path: "apps/web/src/components/client/CreditBalanceCard.tsx"
      provides: "Card espresso com saldo, BreadMark watermark, botões Comprar e Extrato"
      exports: ["CreditBalanceCard"]
    - path: "apps/web/src/contexts/AuthContext.tsx"
      provides: "AuthUser com creditBalance, AuthContextType com updateCreditBalance"
      contains: "creditBalance"
  key_links:
    - from: "apps/web/src/pages/client/HomeScreen.tsx"
      to: "apps/web/src/contexts/AuthContext.tsx"
      via: "useAuth() → user.creditBalance"
      pattern: "useAuth.*creditBalance"
    - from: "apps/web/src/components/client/ClientTabBar.tsx"
      to: "apps/web/src/routes/router.tsx"
      via: "useLocation().pathname.startsWith(tab.path)"
      pattern: "pathname\\.startsWith"
---

<objective>
Wave 1 — Frontend core: atualizar AuthContext com creditBalance, implementar ClientTabBar, HomeScreen (variação A "Carteira"), CreditHistoryScreen e router com todas as sub-rotas do cliente. Este plano roda em paralelo com o 03-02 (depende apenas do 03-01 para o schema e stubs).

Purpose: Entrega a tab bar funcional e a HomeA com saldo real — o usuário pode ver seus créditos logo após o backend (planos 02-03) ser conectado. UI-04, UI-08 e CRED-11 ficam cobertos aqui.

Output: 8 arquivos frontend; HomeA visualmente completa com saldo em tempo real; tab bar 100% funcional; testes de ClientTabBar e HomeScreen ficam GREEN.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/workflows/execute-plan.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/PROJECT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-UI-SPEC.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/03-credits-commerce/03-PATTERNS.md

<interfaces>
<!-- AuthUser atual (apps/web/src/contexts/AuthContext.tsx linhas 4-8) — MODIFICAR: -->
export interface AuthUser {
  id: string
  role: 'CLIENT' | 'COURIER' | 'ADMIN'
  name: string
  // Adicionar:
  creditBalance: number
}

<!-- AuthContextType atual (apps/web/src/contexts/AuthContext.tsx linhas 10-16) — MODIFICAR: -->
export interface AuthContextType {
  user: AuthUser | null
  token: string | null
  isLoading: boolean
  login: (token: string, user: AuthUser) => void
  logout: () => void
  // Adicionar:
  updateCreditBalance: (balance: number) => void
}

<!-- ClientLayout atual (apps/web/src/pages/client/ClientLayout.tsx linhas 1-20) — MODIFICAR: -->
export function ClientLayout() {
  const { user, isLoading } = useAuth()
  if (isLoading) return <LoadingScreen />
  if (!user || user.role !== 'CLIENT') return <Navigate to="/" replace />
  return (
    <div style={{ minHeight: '100dvh', background: 'var(--color-app-bg)' }}>
      <div style={{ padding: '1rem' }}>Área do Cliente — Fase 3<Outlet /></div>
    </div>
  )
}

<!-- Padrão de rota lazy (apps/web/src/routes/router.tsx): -->
{ path: '/client', lazy: () => import('../pages/client/ClientLayout').then(m => ({ Component: m.ClientLayout })) }

<!-- ClientTabBar pattern (PATTERNS.md): -->
const TABS = [
  { label: 'Início',   icon: 'home',     path: '/client/home'     },
  { label: 'Agenda',   icon: 'calendar', path: '/client/agenda'   },
  { label: 'Créditos', icon: 'coin',     path: '/client/creditos' },
  { label: 'Pedidos',  icon: 'bag',      path: '/client/pedidos'  },
] as const

<!-- Design tokens (apps/web/src/styles/globals.css): -->
--color-gold: #E3AC3F; --color-accent: #B0702A; --color-text-ter: #A89A82;
--color-surface: #FFFFFF; --color-border-2: rgba(43,26,12,0.06);
--color-espresso: #1E1207; --color-app-bg: #FAF5EC;
--font-display: "Bricolage Grotesque Variable"; --font-body: "Hanken Grotesk";
--shadow-soft: 0 1px 2px rgba(43,26,12,0.05), 0 4px 14px -8px rgba(43,26,12,0.18);
--radius-card: 22px;

<!-- CreditBalanceCard specs (UI-SPEC.md §Card de Saldo): -->
// gradient: linear-gradient(135deg, #1E1207, #2E1D0D)
// padding: 22px 22px 20px (override de alta fidelidade)
// Número saldo: Bricolage Grotesque 800, fontSize: 52px, color: #FAF5EC, letterSpacing: -0.03em
// Label "SEUS CRÉDITOS": 12.5px 600 color #C7B595 letterSpacing: 0.04em
// Sufixo "pães": 16px 700 color #E3AC3F (gold)
// BreadMark watermark: position absolute, bottom: -50px, right: -30px, opacity: 0.1, size=200, color="#E3AC3F"
// Footer: padding 12px, gap: 10px (override de alta fidelidade)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthContext + ClientTabBar + router + ClientLayout</name>
  <files>
    apps/web/src/contexts/AuthContext.tsx
    apps/web/src/components/client/ClientTabBar.tsx
    apps/web/src/routes/router.tsx
    apps/web/src/pages/client/ClientLayout.tsx
  </files>
  <read_first>
    - apps/web/src/contexts/AuthContext.tsx — ler arquivo completo para adicionar creditBalance sem quebrar lógica existente de login/logout/localStorage
    - apps/web/src/routes/router.tsx — ler arquivo completo para entender rotas existentes antes de adicionar sub-rotas /client
    - apps/web/src/pages/client/ClientLayout.tsx — ler arquivo completo para substituir o placeholder pelo layout com tab bar
    - apps/web/src/components/brand/Icon.tsx — verificar quais ícones estão disponíveis (home, calendar, coin, bag)
    - apps/web/src/components/client/__tests__/ClientTabBar.test.tsx — stubs que devem ficar GREEN após esta task
  </read_first>
  <behavior>
    - AuthUser.creditBalance: number — adicionado com default 0 quando deserializar do localStorage (backward compat: `user.creditBalance ?? 0`)
    - updateCreditBalance(balance): atualiza state user.creditBalance = balance E atualiza o localStorage com o mesmo padrão try/catch do AuthContext existente
    - ClientTabBar usa useLocation() para detectar aba ativa via pathname.startsWith(tab.path)
    - Aba ativa: ícone color: var(--color-gold), label color: var(--color-accent) fontWeight: 700
    - Aba inativa: ícone color: var(--color-text-ter), label color: var(--color-text-ter) fontWeight: 600
    - Tab bar: position fixed, bottom: 0, z-index: 50, height: 56px, paddingBottom: env(safe-area-inset-bottom)
    - Cada botão de aba tem minHeight: 44px (UI-10: hit target mínimo)
    - ClientLayout: paddingBottom: calc(56px + env(safe-area-inset-bottom)) para não sobrepor conteúdo
    - Router: /client tem children com index → HomeScreen, home → HomeScreen, creditos → CombosScreen, agenda → PlaceholderScreen, pedidos → PlaceholderScreen, creditos/pix → PixWaitingScreen, creditos/cartao → CardPaymentScreen, creditos/sucesso → PurchasedScreen, creditos/extrato → CreditHistoryScreen, creditos/recorrente → AutoBuyScreen
    - Todas as sub-rotas usam lazy loading com o padrão import().then(m => ({ Component: m.X }))
  </behavior>
  <action>
Em AuthContext.tsx: adicionar `creditBalance: number` à interface AuthUser. Adicionar `updateCreditBalance: (balance: number) => void` à interface AuthContextType. Na função `login`, garantir que `creditBalance: userData.creditBalance ?? 0` é incluído. Implementar `updateCreditBalance` atualizando o estado e o localStorage dentro de try/catch. Não alterar a lógica de `logout` nem a sessão existente.

Em ClientTabBar.tsx: arquivo novo. Usar o padrão exato do PATTERNS.md §ClientTabBar. Quatro abas: home (ícone home), calendar (ícone calendar), coin (ícone coin), bag (ícone bag). Label font-size: 10.5px, font-family: var(--font-body). Background tab bar: var(--color-surface). Border-top: 1px solid var(--color-border-2). Label "Início", "Agenda", "Créditos", "Pedidos" — exatamente como no UI-SPEC.md §Contrato de Copywriting.

Em router.tsx: substituir a rota `/client` existente (sem children) pelo objeto com `children` contendo todas as sub-rotas lazy. Manter todas as outras rotas existentes intactas.

Em ClientLayout.tsx: remover o `<div style={{ padding: '1rem' }}>Área do Cliente — Fase 3</div>`. Adicionar `import { ClientTabBar }` e renderizar `<ClientTabBar />` como último filho. Adicionar `paddingBottom: 'calc(56px + env(safe-area-inset-bottom))'` no container principal.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web -- --reporter=verbose 2>&1 | grep -E "ClientTabBar|PASS|FAIL" | head -20</automated>
  </verify>
  <done>
    - ClientTabBar.test.tsx GREEN: renderiza 4 abas com labels Início/Agenda/Créditos/Pedidos
    - AuthContext.tsx tem creditBalance em AuthUser e updateCreditBalance em AuthContextType
    - router.tsx tem sub-rotas /client com lazy loading para todos os destinos
    - ClientLayout renderiza ClientTabBar e não tem mais o placeholder de texto
    - TypeScript compila sem erros em apps/web
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeScreen + CreditBalanceCard + CreditHistoryScreen + PlaceholderScreen</name>
  <files>
    apps/web/src/pages/client/HomeScreen.tsx
    apps/web/src/components/client/CreditBalanceCard.tsx
    apps/web/src/pages/client/CreditHistoryScreen.tsx
    apps/web/src/pages/client/PlaceholderScreen.tsx
  </files>
  <read_first>
    - apps/web/src/pages/auth/LoginScreen.tsx — padrão de imports, useNavigate, estado local, apiFetch
    - apps/web/src/pages/splash/SplashScreen.tsx — padrão de card decorativo com gradiente
    - apps/web/src/components/brand/BreadMark.tsx — verificar props disponíveis (size, color)
    - apps/web/src/contexts/AuthContext.tsx — verificar campos do AuthUser (após Task 1) incluindo creditBalance
    - apps/web/src/pages/client/__tests__/HomeScreen.test.tsx — stub que deve ficar GREEN
    - .planning/phases/03-credits-commerce/03-UI-SPEC.md §Card de Saldo, §Tela Placeholder, §Contrato de Copywriting
  </read_first>
  <behavior>
    - HomeScreen lê user.creditBalance via useAuth() e passa para CreditBalanceCard
    - HomeScreen tem: saudação por hora do dia ("Bom dia/Boa tarde/Boa noite, {nome}"), condomínio placeholder, CreditBalanceCard, card TodayDelivery placeholder ("Nenhuma entrega agendada"), QuickActions (4 ações), NextDays placeholder ("Editar agenda")
    - CreditBalanceCard: background gradient(135deg, #1E1207, #2E1D0D), padding 22px 22px 20px, número 52px Bricolage 800 cor #FAF5EC, label "SEUS CRÉDITOS" 12.5px 600 cor #C7B595 letterSpacing 0.04em, sufixo "pães" 16px 700 cor #E3AC3F
    - CreditBalanceCard: BreadMark watermark posição absoluta bottom:-50px right:-30px opacity:0.1 size=200 cor #E3AC3F
    - CreditBalanceCard footer: botão "Comprar créditos" (variant gold, full, icon plus) → navigate('/client/creditos') + botão "Extrato" (variant soft, icon clock, flexShrink: 0) → navigate('/client/creditos/extrato')
    - Estado carregando no CreditBalanceCard: skeleton retangular rgba(255,255,255,0.15) width:120 height:52 borderRadius:8
    - Estado saldo zero: exibir 0 em #FAF5EC, subtexto "Adicione créditos para começar"
    - CreditHistoryScreen: lista CreditTransaction[] via GET /credits/history com apiFetch; exibir tipo (PURCHASE/CONSUMPTION), quantidade, data em pt-BR; estado loading com skeleton
    - PlaceholderScreen: recebe prop icon ('calendar' | 'bag') e title; ícone 48px color textTer, título 20px Bricolage 700 textSec, subtexto "Em breve — disponível na próxima atualização" 14px textTer lineHeight 1.5; layout centralizado com padding 32px
    - HomeScreen teste: mockar useAuth com creditBalance=42, verificar que "42" e "pães" aparecem no DOM
  </behavior>
  <action>
Criar HomeScreen.tsx: calcular saudação com `new Date().getHours()` — antes das 12 "Bom dia", 12-18 "Boa tarde", após 18 "Boa noite". Usar `user?.name.split(' ')[0]` para o primeiro nome. Card TodayDelivery é um div simples com texto "Nenhuma entrega agendada" (placeholder para Fase 5). QuickActions tem 4 botões: "Comprar créditos" (→ /client/creditos), "Minha agenda" (→ /client/agenda), "Histórico" (→ /client/creditos/extrato), "Configurações" (→ /client/settings ou placeholder). NextDays é um placeholder simples com link "Editar agenda".

Criar CreditBalanceCard.tsx: recebe props `{ creditBalance: number, isLoading?: boolean }`. Usar `position: 'relative', overflow: 'hidden'` no container da seção espresso para o watermark absoluto funcionar corretamente. Botões no footer: o botão "Comprar créditos" deve ter `flex: 1` e o "Extrato" deve ter `flexShrink: 0`. Subtexto ritmo: "Rende ~{N} dias no seu ritmo atual" — para a Fase 3, N é calculado como Math.floor(creditBalance / 1) (simplificado — 1 pão/dia default) ou mostrar apenas o saldo sem subtexto de ritmo se creditBalance === 0.

Criar CreditHistoryScreen.tsx: buscar `/credits/history` via apiFetch no useEffect. Lista com items mostrando: ícone (PURCHASE = seta para cima em gold, CONSUMPTION = seta para baixo em accent), quantidade com sinal (+N / -N), data formatada com `new Intl.DateTimeFormat('pt-BR').format(new Date(tx.createdAt))`.

Criar PlaceholderScreen.tsx: aceitar prop `icon: 'calendar' | 'bag'` via useLocation state OU via path (detectar se pathname inclui 'agenda' ou 'pedidos'). Layout `display: flex, flexDirection: column, alignItems: center, justifyContent: center, minHeight: calc(100dvh - 56px - env(safe-area-inset-bottom)), padding: 32px`.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web -- --reporter=verbose 2>&1 | grep -E "HomeScreen|CreditBalance|PASS|FAIL" | head -20</automated>
  </verify>
  <done>
    - HomeScreen.test.tsx GREEN: exibe creditBalance=42 do mock de useAuth
    - CreditBalanceCard exibe número com Bricolage Grotesque 800 52px (verificar no DOM via data-testid)
    - PlaceholderScreen renderiza texto "Em breve" (verificar nos testes de ClientTabBar)
    - CreditHistoryScreen compila sem erros; busca /credits/history via apiFetch
    - Todas as navegações (Comprar créditos → /client/creditos; Extrato → /client/creditos/extrato) usam navigate()
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AuthContext localStorage → state | creditBalance deserializado com fallback 0 (backward compat) |
| useAuth() → CreditBalanceCard | saldo lido do contexto — nunca aceito do URL/querystring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | AuthContext.updateCreditBalance | mitigate | Função apenas atualiza creditBalance localmente após o backend confirmar (chamada pelo polling hook no plano 06); não é exposta a rotas públicas |
| T-03-08 | Information Disclosure | localStorage auth_user | accept | Dados armazenados: id, role, name, creditBalance — sem dados sensíveis (sem token de cartão, sem PII além do nome) |
</threat_model>

<verification>
Após execução deste plano:

```bash
# Verificar que AuthUser tem creditBalance
grep -n "creditBalance" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/contexts/AuthContext.tsx

# Verificar que ClientTabBar tem 4 abas
grep -c "label:" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/components/client/ClientTabBar.tsx

# Verificar router tem children para /client
grep -n "children\|creditos\|agenda\|pedidos" /home/joaopatrick/Projects/cheirin-de-pao-app/apps/web/src/routes/router.tsx

# Rodar testes do frontend
cd /home/joaopatrick/Projects/cheirin-de-pao-app && npm run test --workspace=apps/web
```
</verification>

<success_criteria>
- AuthUser tem creditBalance: number; AuthContextType tem updateCreditBalance
- ClientTabBar renderiza 4 abas com labels corretos e estilos de aba ativa/inativa conformes à UI-SPEC
- HomeA exibe creditBalance do usuário com Bricolage Grotesque 800 52px no card espresso
- Botões Comprar créditos e Extrato navegam para as rotas corretas
- PlaceholderScreen em Agenda e Pedidos exibe "Em breve"
- Todos os testes relevantes (ClientTabBar, HomeScreen) no estado GREEN
- TypeScript compila sem erros em apps/web
</success_criteria>

<output>
Criar `.planning/phases/03-credits-commerce/03-04-SUMMARY.md` quando done
</output>
