---
phase: 05-delivery-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/admin-orders/admin-orders.schema.ts
  - apps/api/src/modules/admin-orders/admin-orders.service.ts
  - apps/api/src/modules/admin-orders/admin-orders.controller.ts
  - apps/api/src/modules/admin-orders/admin-orders.route.ts
  - apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts
autonomous: true
requirements:
  - ACOMP-01
  - ACOMP-03
  - ACOMP-05

must_haves:
  truths:
    - "Admin pode mudar status de SCHEDULED para OUT_FOR_DELIVERY via PATCH /admin/orders/:id/status"
    - "Admin pode mudar status de OUT_FOR_DELIVERY para DELIVERED"
    - "Transição reversa (DELIVERED → SCHEDULED) é rejeitada com HTTP 422"
    - "Cliente sem role ADMIN recebe HTTP 403 ao chamar o endpoint"
    - "Quando status muda para DELIVERED, o push OneSignal é disparado (best-effort) e Notification é salva no banco"
    - "Notification trim mantém máximo 30 por usuário"
  artifacts:
    - path: "apps/api/src/modules/admin-orders/admin-orders.service.ts"
      provides: "updateOrderStatus() com VALID_TRANSITIONS + notifyDelivered()"
      exports: ["AdminOrdersService"]
    - path: "apps/api/src/modules/admin-orders/admin-orders.route.ts"
      provides: "PATCH /admin/orders/:id/status"
      exports: ["adminOrdersRoute"]
    - path: "apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts"
      provides: "Stubs de teste para ACOMP-01 e ACOMP-03"
  key_links:
    - from: "admin-orders.controller.ts"
      to: "admin-orders.service.ts"
      via: "updateOrderStatus(orderId, body.status)"
      pattern: "updateOrderStatus"
    - from: "admin-orders.service.ts"
      to: "prisma.notification.create"
      via: "notifyDelivered() → NotificationsService.createAndTrim()"
      pattern: "createAndTrim"
---

<objective>
Criar o módulo admin-orders: endpoint PATCH /admin/orders/:id/status com validação de transições de estado, disparo OneSignal ao marcar DELIVERED e persistência de Notification no banco. Inclui stubs de teste (Nyquist Wave 0) antes da implementação.

Purpose: Habilitar o Admin a acionar manualmente as transições SCHEDULED → OUT_FOR_DELIVERY → DELIVERED durante a Fase 5 (sem app do entregador ainda). O endpoint é o gatilho central de toda a experiência de rastreamento.
Output: Módulo admin-orders completo com 4 arquivos + suite de testes unitários verdes.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/execute-plan.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-PATTERNS.md

<interfaces>
<!-- Contratos que o executor precisa. Extraídos do codebase. -->

De apps/api/src/modules/schedules/schedules.service.ts (padrão OneSignal a replicar):
- createOsClient() helper com OneSignal.createConfiguration({ restApiKey: process.env.ONESIGNAL_REST_API_KEY! })
- push individual com notification.include_subscription_ids = [user.oneSignalPlayerId]
- push em try/catch próprio — falha silenciosamente

De apps/api/src/modules/auth/auth.controller.ts (padrão inline role check):
- if (request.user?.role !== 'ADMIN') return reply.status(403).send({ error: 'Acesso negado: apenas administradores' })

De apps/api/src/modules/orders/orders.route.ts (padrão de rota):
- { preHandler: [fastify.authenticate] } em todos os endpoints autenticados

De apps/api/src/modules/notifications/notifications.service.ts (existente — será estendido no Plan 02):
- NotificationsService já existe; Plan 01 deve INJETAR NotificationsService via construtor em AdminOrdersService
- Métodos que existem: savePushToken(userId, playerId)
- Métodos que AINDA NÃO existem (serão criados no Plan 02): createAndTrim, markAllRead, getByUserId, countUnread
- SOLUÇÃO: AdminOrdersService implementa a lógica de persist + trim INTERNAMENTE (sem depender do Plan 02), usando fastify.prisma diretamente, para manter os planos Wave 1 paralelos e independentes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stubs de teste Wave 0 — admin-orders.service.test.ts</name>
  <files>apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts</files>
  <read_first>
    - apps/api/src/modules/orders/__tests__/orders.service.test.ts (padrão makeFastifyMock, vi.mock oneSignal)
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (padrão mock OneSignal SDK)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seção "Service test (admin-orders.service)" com makeFastifyMock e mock OneSignal)
    - .planning/phases/05-delivery-experience/05-VALIDATION.md (Task IDs 05-01, 05-02, 05-06, 05-07)
  </read_first>
  <behavior>
    - Teste 1 (05-01): updateOrderStatus('id', 'OUT_FOR_DELIVERY') chama prisma.order.update quando order.status === 'SCHEDULED' e retorna sem erro
    - Teste 2 (05-01): updateOrderStatus('id', 'SCHEDULED') quando order.status === 'DELIVERED' lança { statusCode: 422, message: /Transição inválida/ }
    - Teste 3 (05-01): updateOrderStatus('id', 'OUT_FOR_DELIVERY') quando order.status === 'OUT_FOR_DELIVERY' lança { statusCode: 422 }
    - Teste 4 (05-01): updateOrderStatus('id', 'ANY') quando order não existe lança { statusCode: 404, message: /não encontrado/ }
    - Teste 5 (05-06): updateOrderStatus('id', 'DELIVERED') quando status era OUT_FOR_DELIVERY — chama prisma.notification.create pelo menos uma vez
    - Teste 6 (05-07): createAndTrim chama prisma.notification.deleteMany quando total de notificações excede 30 — prisma.notification.findMany retorna array de 32 IDs, expectativa: deleteMany chamado com ids dos 2 mais antigos
  </behavior>
  <action>
    Criar o arquivo de teste com os 6 casos acima. Usar o padrão makeFastifyMock do analógico orders.service.test.ts. Mock do OneSignal SDK conforme padrão de schedules.service.test.ts (vi.mock('@onesignal/node-onesignal')). Os testes devem FALHAR inicialmente (RED) pois AdminOrdersService ainda não existe. Usar import da classe mock de './admin-orders.service.js' — o compilador TS aceitará porque o arquivo será criado na Task 2. Estrutura de describe: 'AdminOrdersService', describe aninhados por método ('updateOrderStatus', 'createAndTrim').
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Arquivo criado com 6 casos de teste; ao rodar, os testes falham por "Cannot find module" ou import não encontrado (RED confirmado — esperado neste passo)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Módulo admin-orders — schema + service + controller + route</name>
  <files>
    apps/api/src/modules/admin-orders/admin-orders.schema.ts
    apps/api/src/modules/admin-orders/admin-orders.service.ts
    apps/api/src/modules/admin-orders/admin-orders.controller.ts
    apps/api/src/modules/admin-orders/admin-orders.route.ts
  </files>
  <read_first>
    - apps/api/src/modules/orders/orders.schema.ts (padrão Zod enum)
    - apps/api/src/modules/orders/orders.route.ts (padrão FastifyPluginAsync)
    - apps/api/src/modules/orders/orders.controller.ts (padrão handler com Zod + try/catch)
    - apps/api/src/modules/auth/auth.controller.ts linhas 114-118 (inline role check)
    - apps/api/src/modules/schedules/schedules.service.ts (padrão OneSignal SDK + createOsClient)
    - apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts (testes que devem passar)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seções admin-orders completas)
    - .planning/phases/05-delivery-experience/05-RESEARCH.md (Padrão 3 e 4, anti-patterns)
  </read_first>
  <action>
    SCHEMA (admin-orders.schema.ts): Exportar UpdateOrderStatusSchema com z.object({ status: z.enum(['OUT_FOR_DELIVERY', 'DELIVERED'], { errorMap: () => ({ message: 'Status inválido. Use OUT_FOR_DELIVERY ou DELIVERED.' }) }) }). Exportar UpdateOrderStatusBody = z.infer.

    SERVICE (admin-orders.service.ts): Classe AdminOrdersService com construtor que recebe FastifyInstance. Propriedade privada get prisma(). Constante VALID_TRANSITIONS = { SCHEDULED: ['OUT_FOR_DELIVERY'], OUT_FOR_DELIVERY: ['DELIVERED'] } — definida fora da classe.

    Método updateOrderStatus(orderId: string, newStatus: string): Promise(void) — (a) busca order com prisma.order.findUnique({ where: { id: orderId } }), (b) lança { statusCode: 404, message: 'Pedido não encontrado' } se não existir, (c) valida VALID_TRANSITIONS[order.status] ?? [] e lança { statusCode: 422, message: Transição inválida: ${order.status} → ${newStatus} } se não permitido, (d) atualiza order com prisma.order.update({ where: { id: orderId }, data: { status: newStatus as OrderStatus } }), (e) se newStatus === 'DELIVERED' chama this.notifyAndPersist(order).

    Método privado notifyAndPersist(order: Order): (a) busca user com prisma.user.findUnique({ where: { id: order.userId }, select: { oneSignalPlayerId: true } }), (b) se user.oneSignalPlayerId, dispara push OneSignal em try/catch (falha silenciosamente — D-06) com headings.pt = 'Cheirin de Pão', contents.pt = Seus ${order.quantity} pães foram entregues. Bom apetite!, (c) FORA do try, chama this.createAndTrim({ userId: order.userId, type: 'DELIVERY_DONE', title: 'Entrega realizada', body: Seus ${order.quantity} pães foram entregues. Bom apetite!, actionRoute: '/client/pedidos' }).

    Método createAndTrim(data: { userId, type, title, body, actionRoute? }): (a) prisma.notification.create({ data: { ...data, isRead: false } }), (b) busca todos com prisma.notification.findMany({ where: { userId: data.userId }, orderBy: { createdAt: 'desc' }, select: { id: true } }), (c) se length > 30, prisma.notification.deleteMany({ where: { id: { in: all.slice(30).map(n => n.id) } } }).

    CONTROLLER (admin-orders.controller.ts): Classe AdminOrdersController com construtor que instancia AdminOrdersService. Método updateOrderStatus(request, reply): (1) check inline request.user?.role !== 'ADMIN' → 403, (2) Zod parse body via UpdateOrderStatusSchema.parse(request.body) com catch ZodError → 400, (3) extrair orderId de request.params as { id: string }, (4) chamar service.updateOrderStatus — tratar 404, 422 e 500.

    ROUTE (admin-orders.route.ts): FastifyPluginAsync com fastify.patch('/admin/orders/:id/status', { preHandler: [fastify.authenticate] }, ctrl.updateOrderStatus.bind(ctrl)). O role check fica no controller (per D-11).

    Após implementar, rodar testes: os 6 casos da Task 1 devem estar GREEN.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run apps/api/src/modules/admin-orders/__tests__/admin-orders.service.test.ts 2>&1 | grep -E "passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/modules/admin-orders/admin-orders.schema.ts exporta UpdateOrderStatusSchema com z.enum(['OUT_FOR_DELIVERY', 'DELIVERED'])
    - apps/api/src/modules/admin-orders/admin-orders.service.ts contém VALID_TRANSITIONS e updateOrderStatus(
    - apps/api/src/modules/admin-orders/admin-orders.service.ts contém createAndTrim( com lógica de slice(30)
    - apps/api/src/modules/admin-orders/admin-orders.controller.ts contém request.user?.role !== 'ADMIN'
    - apps/api/src/modules/admin-orders/admin-orders.route.ts contém fastify.patch('/admin/orders/:id/status'
    - Suite de testes admin-orders.service.test.ts: 6/6 passed (GREEN)
    - Nenhum teste pre-existente quebrado: cd apps/api && npx vitest run retorna 0
  </acceptance_criteria>
  <done>Módulo admin-orders criado; todos os 6 testes passando; suite completa da API verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → admin-orders.controller | Requisição HTTP chega sem garantia de autenticidade ou role válido |
| controller → service | Body validado pelo Zod; orderId do path param (não validado quanto à posse) |
| service → OneSignal | Credencial REST_API_KEY no env; payload sai da rede interna |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | PATCH /admin/orders/:id/status | mitigate | preHandler: [fastify.authenticate] + inline role check request.user?.role !== 'ADMIN' → 403 no controller |
| T-05-02 | Tampering | admin-orders.service.ts updateOrderStatus | mitigate | VALID_TRANSITIONS map — qualquer transição fora do mapa lança statusCode 422 antes do update |
| T-05-03 | Tampering | admin-orders.service.ts createAndTrim | mitigate | Trim inline a 30 notificações por userId — deleteMany com ids da fatia [30:] |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — todos auditados em fases anteriores |
</threat_model>

<verification>
Suite da API completa verde: `cd apps/api && npx vitest run` passa sem falhas.
PATCH /admin/orders/:id/status rota registrada: `grep -r "admin/orders" apps/api/src/modules/admin-orders/admin-orders.route.ts` retorna a linha do patch.
Inline role check presente: `grep "role !== 'ADMIN'" apps/api/src/modules/admin-orders/admin-orders.controller.ts` retorna resultado.
VALID_TRANSITIONS presente: `grep "VALID_TRANSITIONS" apps/api/src/modules/admin-orders/admin-orders.service.ts` retorna resultado.
</verification>

<success_criteria>
- Suite `cd apps/api && npx vitest run` retorna 0 (todos os testes passando incluindo os 6 novos)
- Arquivo admin-orders.route.ts registra PATCH /admin/orders/:id/status com preHandler: [fastify.authenticate]
- Transição DELIVERED → SCHEDULED retorna 422 (validada pelo teste 05-01 e pelo VALID_TRANSITIONS map)
- Role check inline bloqueia role !== 'ADMIN' com 403
- createAndTrim persiste Notification e aplica trim a 30 por usuário
</success_criteria>

<output>
Criar `.planning/phases/05-delivery-experience/05-01-SUMMARY.md` ao concluir.
</output>
