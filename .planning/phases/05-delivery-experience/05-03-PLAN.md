---
phase: 05-delivery-experience
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified:
  - apps/api/src/modules/schedules/schedules.service.ts
  - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
  - apps/api/src/plugins/cron.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - ACOMP-02
  - ACOMP-03

must_haves:
  truths:
    - "Cron 0 21 * * * executa sendEveReminders() em America/Sao_Paulo toda noite às 21h"
    - "sendEveReminders busca Order com scheduledDate = amanhã BRT (não Schedule.weeklyQty)"
    - "sendEveReminders envia push OneSignal para cada cliente com order amanhã + persiste Notification do tipo DELIVERY_EVE"
    - "Clientes sem oneSignalPlayerId têm push ignorado silenciosamente mas Notification é persistida (D-06)"
    - "Módulo admin-orders registrado em server.ts"
    - "Log de startup indica '3 cron jobs registrados'"
  artifacts:
    - path: "apps/api/src/modules/schedules/schedules.service.ts"
      provides: "sendEveReminders() — busca orders de amanhã + push + persist DELIVERY_EVE"
      exports: ["SchedulesService"]
    - path: "apps/api/src/plugins/cron.ts"
      provides: "3º cron job (0 21 * * *, America/Sao_Paulo, name: eve-reminders)"
    - path: "apps/api/src/server.ts"
      provides: "register(adminOrdersRoute) adicionado"
  key_links:
    - from: "cron.ts"
      to: "schedules.service.sendEveReminders"
      via: "schedulesService.sendEveReminders()"
      pattern: "sendEveReminders"
    - from: "schedules.service.ts sendEveReminders"
      to: "prisma.order.findMany"
      via: "scheduledDate = amanhã BRT"
      pattern: "getTomorrowUTC3|scheduledDate"
    - from: "server.ts"
      to: "admin-orders.route.ts"
      via: "fastify.register(adminOrdersRoute)"
      pattern: "adminOrdersRoute"
---

<objective>
Completar a camada backend da fase 5: adicionar o cron de véspera (21h), implementar sendEveReminders() no SchedulesService, e registrar o adminOrdersRoute em server.ts para que o endpoint PATCH /admin/orders/:id/status fique acessível.

Purpose: Fechar o ciclo automático de notificações — o sistema notifica clientes na véspera (cron 21h) e ao confirmar entrega (PATCH → DELIVERED, já implementado no Plan 01). Sem este plano o Admin não consegue chamar o endpoint de status pois ele não está registrado no servidor.
Output: server.ts com adminOrdersRoute registrado; cron.ts com 3 jobs; schedules.service.ts com sendEveReminders; testes verdes.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/execute-plan.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase -->

De apps/api/src/plugins/cron.ts (existente — ver estrutura antes de modificar):
- guard: if (process.env.NODE_ENV === 'test') { return }
- cron.schedule(expressão, handler, { timezone: 'America/Sao_Paulo', name: string })
- log final: fastify.log.info('[cron] 2 cron jobs registrados ...')
- schedulesService instanciado como: const schedulesService = new SchedulesService(fastify)

De apps/api/src/modules/schedules/schedules.service.ts (existente):
- getTomorrowUTC3(): Date — já implementado; sendEveReminders vai usar o MESMO helper
- sendReconfigureReminders() — padrão a seguir para sendEveReminders
- OneSignal: createOsClient() helper existente; padrão push individual com include_subscription_ids
- NotificationsService: Plan 02 criou createAndTrim no NotificationsService — importar e usar

De apps/api/src/server.ts (existente):
- Padrão: import { xRoute } from './modules/x/x.route.js'
- Padrão: await fastify.register(xRoute)
- Posição: registrar adminOrdersRoute após ordersRoute (linha ~95) e antes de cronPlugin

De .planning/phases/05-delivery-experience/05-PATTERNS.md (cron 3º job):
- expressão: '0 21 * * *', timezone: 'America/Sao_Paulo', name: 'eve-reminders'
- handler: log info antes + try/await/log info + catch log error — servidor mantido ativo
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stub de teste Wave 0 + implementar sendEveReminders</name>
  <files>
    apps/api/src/modules/schedules/__tests__/schedules.service.test.ts
    apps/api/src/modules/schedules/schedules.service.ts
  </files>
  <read_first>
    - apps/api/src/modules/schedules/__tests__/schedules.service.test.ts (ler arquivo completo — ver casos existentes antes de adicionar)
    - apps/api/src/modules/schedules/schedules.service.ts (ler arquivo completo — ver getTomorrowUTC3, sendReconfigureReminders, padrão OneSignal)
    - apps/api/src/modules/notifications/notifications.service.ts (ver assinatura createAndTrim criada no Plan 02)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seção "No Analog Found" — sendEveReminders derivado de sendReconfigureReminders)
    - .planning/phases/05-delivery-experience/05-RESEARCH.md (Pitfall 6: cron busca Order, não Schedule)
    - .planning/phases/05-delivery-experience/05-VALIDATION.md (Task ID 05-05)
  </read_first>
  <action>
    Stub RED (adicionar ao describe existente de SchedulesService):
    - Caso 05-05a: sendEveReminders() — prisma.order.findMany chamado com scheduledDate igual ao dia seguinte em BRT; status: { not: 'CANCELLED' } (não busca Schedule diretamente)
    - Caso 05-05b: sendEveReminders() com 2 orders amanhã — cria 2 Notifications (uma por userId), independentemente de oneSignalPlayerId

    Após escrever os stubs (RED), implementar sendEveReminders em schedules.service.ts:
    - Busca prisma.order.findMany onde scheduledDate gte/lte = getTomorrowUTC3() (início e fim do dia seguinte BRT). Usar o mesmo padrão BRAZIL_OFFSET_HOURS para calcular início e fim do dia — amanhã começa em Date.UTC(year, month, day+1, 3, 0, 0) e termina em Date.UTC(year, month, day+2, 2, 59, 59, 999).
    - Para cada order: busca user com prisma.user.findUnique({ where: { id: order.userId }, select: { oneSignalPlayerId: true } }).
    - Se user.oneSignalPlayerId: dispara push OneSignal em try/catch próprio (D-06 — falha silenciosa) com headings.pt = 'Cheirin de Pão', contents.pt = Lembrete: ${order.quantity} pães agendados para amanhã.
    - FORA do try push, chama notificationsService.createAndTrim({ userId: order.userId, type: 'DELIVERY_EVE', title: 'Entrega amanhã', body: Lembrete: ${order.quantity} pães agendados para amanhã. }).
    - notificationsService é instância de NotificationsService injetada no construtor de SchedulesService — verificar se já existe, adicionar se necessário.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run apps/api/src/modules/schedules/__tests__/schedules.service.test.ts 2>&1 | grep -E "passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - schedules.service.ts contém sendEveReminders(
    - schedules.service.ts busca Order (não Schedule): grep mostra prisma.order.findMany dentro de sendEveReminders
    - sendEveReminders usa getTomorrowUTC3 ou cálculo de offset BRT equivalente (não new Date().getHours())
    - Push OneSignal em try/catch separado; createAndTrim chamado FORA do try (persist obrigatório mesmo sem playerId)
    - Todos os casos de schedules.service.test.ts passam (incluindo casos pré-existentes): testes saem GREEN
  </acceptance_criteria>
  <done>sendEveReminders implementado; casos 05-05a,b verdes; suite completa da API verde</done>
</task>

<task type="auto">
  <name>Task 2: Cron 21h em cron.ts + wiring adminOrdersRoute em server.ts</name>
  <files>
    apps/api/src/plugins/cron.ts
    apps/api/src/server.ts
  </files>
  <read_first>
    - apps/api/src/plugins/cron.ts (ler arquivo completo — ver 2 crons existentes e log final antes de modificar)
    - apps/api/src/server.ts (ler arquivo completo — ver posição de registro de rotas e imports antes de adicionar adminOrdersRoute)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seção cron.ts — 3º cron e atualização do log)
    - .planning/phases/05-delivery-experience/05-RESEARCH.md (Padrão 2: cron véspera 21h)
  </read_first>
  <action>
    CRON.TS: Adicionar o 3º cron job após o 2º existente (domingo 20h). Expressão: '0 21 * * *'. Handler: log info '[cron] iniciando sendEveReminders', try { await schedulesService.sendEveReminders(); log info '[cron] sendEveReminders concluído' } catch (err) { log error '[cron] erro em sendEveReminders — servidor mantido ativo' }. Opções: { timezone: 'America/Sao_Paulo', name: 'eve-reminders' }. Atualizar o log final de "2 cron jobs registrados (meia-noite diário + domingo 20h)" para "3 cron jobs registrados (meia-noite diário + domingo 20h + diário 21h)".

    SERVER.TS: Adicionar import { adminOrdersRoute } from './modules/admin-orders/admin-orders.route.js'. Adicionar await fastify.register(adminOrdersRoute) após await fastify.register(ordersRoute) e antes do cronPlugin. Não alterar nenhum outro registro existente.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run 2>&1 | grep -E "passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - cron.ts contém cron.schedule('0 21 * * *', — exatamente essa expressão
    - cron.ts contém name: 'eve-reminders'
    - cron.ts contém '3 cron jobs registrados' (log atualizado)
    - server.ts contém import { adminOrdersRoute } from './modules/admin-orders/admin-orders.route.js'
    - server.ts contém fastify.register(adminOrdersRoute)
    - Suite completa da API retorna 0 (guard NODE_ENV === 'test' impede execução dos crons nos testes)
  </acceptance_criteria>
  <done>3 crons registrados; adminOrdersRoute wired em server.ts; suite completa verde</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron.ts → schedules.service | Processo interno; sem input externo — sem superfície de ataque |
| schedules.service → OneSignal API | Saída de rede com REST_API_KEY; payload controlado internamente |
| server.ts → admin-orders.route | Registro de rota — autenticação enforçada pelo preHandler no route file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Denial of Service | sendEveReminders — loop por orders | accept | Volume máximo = número de orders do dia seguinte; cron executa 1x/dia; sem input externo que possa amplificar |
| T-05-08 | Information Disclosure | sendEveReminders — push para userId errado | mitigate | Query usa order.userId para buscar user.oneSignalPlayerId — escopo restrito ao dono do order |
| T-05-09 | Tampering | cron.ts — execução em ambiente de teste | mitigate | Guard NODE_ENV === 'test' já existente no topo do plugin — crons não registrados em test |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — todos auditados em fases anteriores |
</threat_model>

<verification>
Cron 21h verificado: `grep "0 21 \* \* \*" apps/api/src/plugins/cron.ts` retorna a linha do schedule.
Log atualizado: `grep "3 cron jobs" apps/api/src/plugins/cron.ts` retorna resultado.
adminOrdersRoute registrado: `grep "adminOrdersRoute" apps/api/src/server.ts` retorna 2 linhas (import + register).
sendEveReminders busca Order (não Schedule): `grep -A5 "sendEveReminders" apps/api/src/modules/schedules/schedules.service.ts | grep "order.findMany"` retorna resultado.
Suite completa verde: `cd apps/api && npx vitest run` retorna 0.
</verification>

<success_criteria>
- `cd apps/api && npx vitest run` retorna 0 com todos os testes passando
- cron.ts registra 3 jobs; log indica "3 cron jobs registrados"
- sendEveReminders busca prisma.order.findMany (não prisma.schedule); cron de véspera referencia a função correta
- adminOrdersRoute wired em server.ts — PATCH /admin/orders/:id/status acessível ao servidor em execução
</success_criteria>

<output>
Criar `.planning/phases/05-delivery-experience/05-03-SUMMARY.md` ao concluir.
</output>
