---
phase: 05-delivery-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/orders/orders.service.ts
  - apps/api/src/modules/orders/orders.repository.ts
  - apps/api/src/modules/orders/orders.controller.ts
  - apps/api/src/modules/orders/orders.route.ts
  - apps/api/src/modules/orders/__tests__/orders.service.test.ts
  - apps/api/src/modules/notifications/notifications.service.ts
  - apps/api/src/modules/notifications/notifications.controller.ts
  - apps/api/src/modules/notifications/notifications.route.ts
  - apps/api/src/modules/notifications/__tests__/notifications.service.test.ts
autonomous: true
requirements:
  - ACOMP-01
  - ACOMP-04
  - ACOMP-05

must_haves:
  truths:
    - "GET /orders/today retorna o pedido do dia atual em timezone BRT (UTC-3) para o usuário autenticado"
    - "GET /orders/history?days=30 retorna os pedidos dos últimos 30 dias (excluindo CANCELLED), ordenados por scheduledDate desc"
    - "GET /notifications/me retorna as últimas 30 notificações do usuário autenticado (userId do JWT)"
    - "PATCH /notifications/read-all marca todas as notificações do usuário como isRead=true"
    - "GET /notifications/unread-count retorna { count: N } com o total de notificações isRead=false"
  artifacts:
    - path: "apps/api/src/modules/orders/orders.service.ts"
      provides: "getTodayOrder() e getOrderHistory() com offset BRT"
      exports: ["OrdersService"]
    - path: "apps/api/src/modules/notifications/notifications.service.ts"
      provides: "getByUserId(), markAllRead(), createAndTrim(), countUnread()"
      exports: ["NotificationsService"]
    - path: "apps/api/src/modules/orders/__tests__/orders.service.test.ts"
      provides: "Casos de teste para getTodayOrder (BRT) e getOrderHistory"
  key_links:
    - from: "orders.controller.ts"
      to: "orders.service.ts"
      via: "getTodayOrder(userId), getOrderHistory(userId, days)"
      pattern: "getTodayOrder|getOrderHistory"
    - from: "notifications.controller.ts"
      to: "notifications.service.ts"
      via: "getByUserId(userId), markAllRead(userId), countUnread(userId)"
      pattern: "getByUserId|markAllRead|countUnread"
---

<objective>
Estender os módulos orders e notifications com os endpoints necessários para rastreamento e central de notificações. Inclui extensão de testes existentes (Nyquist Wave 0) antes de cada implementação.

Purpose: Prover os endpoints que o frontend e o módulo admin-orders (Plan 01) consumirão — GET /orders/today para polling de status, GET /orders/history para o histórico, e os três endpoints de notificações para a central in-app.
Output: 5 novos endpoints distribuídos entre orders e notifications; testes unitários verdes para todos os comportamentos críticos de timezone e acesso.
</objective>

<execution_context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/execute-plan.md
</execution_context>

<context>
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/ROADMAP.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-CONTEXT.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-RESEARCH.md
@/home/joaopatrick/Projects/cheirin-de-pao-app/.planning/phases/05-delivery-experience/05-PATTERNS.md

<interfaces>
<!-- Contratos extraídos do codebase -->

De apps/api/src/modules/orders/orders.service.ts (padrão BRT existente):
- const BRAZIL_OFFSET_HOURS = 3
- function getTomorrowUTC3(): Date — padrão de cálculo offset; getTodayRange() segue o mesmo padrão
- Classe OrdersService com construtor(private fastify: FastifyInstance) e getter private get prisma()

De apps/api/src/modules/orders/orders.repository.ts:
- Classe OrdersRepository com padrão self — findById, findByUserId existentes
- Adicionar findTodayByUserId(userId, start, end) e findHistoryByUserId(userId, since)

De apps/api/src/modules/notifications/notifications.service.ts (existente):
- Classe NotificationsService com construtor(private fastify: FastifyInstance)
- Método existente: savePushToken(userId, playerId) — NÃO modificar
- Adicionar: getByUserId, markAllRead, createAndTrim, countUnread

De apps/api/src/modules/orders/orders.route.ts:
- fastify.get('/orders/:id', { preHandler: [fastify.authenticate] }, ctrl.getOrder.bind(ctrl))
- Adicionar: GET /orders/today e GET /orders/history com mesmo preHandler

De apps/api/src/modules/notifications/notifications.route.ts:
- Já registra POST /users/push-token — NÃO remover
- Adicionar: GET /notifications/me, PATCH /notifications/read-all, GET /notifications/unread-count
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stubs de teste Wave 0 — extensão orders.service.test.ts e notifications.service.test.ts</name>
  <files>
    apps/api/src/modules/orders/__tests__/orders.service.test.ts
    apps/api/src/modules/notifications/__tests__/notifications.service.test.ts
  </files>
  <read_first>
    - apps/api/src/modules/orders/__tests__/orders.service.test.ts (arquivo completo — ler padrão makeFastifyMock existente antes de adicionar casos)
    - apps/api/src/modules/notifications/notifications.service.ts (ler estrutura existente da classe antes de adicionar casos)
    - .planning/phases/05-delivery-experience/05-VALIDATION.md (Task IDs 05-03, 05-08, 05-09)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seção "Service test (admin-orders.service)" para padrão makeFastifyMock)
  </read_first>
  <behavior>
    orders.service.test.ts — adicionar casos ao describe existente:
    - Caso 05-03a: getTodayOrder('userId-abc') — prisma.order.findFirst chamado com scheduledDate.gte e scheduledDate.lte diferentes de undefined; ambas as datas são instâncias de Date
    - Caso 05-03b: getTodayOrder com order retornado — retorna o order sem transformação
    - Caso 05-03c: getTodayOrder quando findFirst retorna null — retorna null
    - Caso 05-08a: getOrderHistory('userId-abc', 30) — prisma.order.findMany chamado com scheduledDate.gte cujo valor é uma Date há 30 dias atrás (± 1s)
    - Caso 05-08b: getOrderHistory sem arg days usa 30 como default

    notifications.service.test.ts — criar ou estender:
    - Caso 05-09a: getByUserId('userId-x') — prisma.notification.findMany chamado com where.userId === 'userId-x' (nunca outro userId)
    - Caso 05-09b: markAllRead('userId-x') — prisma.notification.updateMany chamado com where: { userId: 'userId-x', isRead: false } e data: { isRead: true }
    - Caso 05-09c: countUnread('userId-x') — prisma.notification.count chamado com where: { userId: 'userId-x', isRead: false }
  </behavior>
  <action>
    EXTENSÃO orders.service.test.ts: Adicionar 5 novos casos ao describe 'OrdersService' existente. Usar o mesmo makeFastifyMock já definido no arquivo. O mock de prisma.order.findFirst deve ser adicionado ao mock existente (não substituir findMany). Não apagar testes existentes.

    EXTENSÃO/CRIAÇÃO notifications.service.test.ts: Se o arquivo já existe (verificar), adicionar 3 casos. Se não existe, criar com padrão makeFastifyMock análogo ao de orders. Os casos 05-09a,b,c verificam que userId é sempre extraído do argumento (não do body) — isolamento por userId é o ponto central.

    Todos os testes devem FALHAR (RED) porque getTodayOrder, getOrderHistory, getByUserId, markAllRead, countUnread ainda não foram adicionados às classes. "RED" aceitável: método is not a function ou TypeError.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run apps/api/src/modules/orders/__tests__/orders.service.test.ts apps/api/src/modules/notifications/__tests__/notifications.service.test.ts 2>&1 | grep -E "passed|failed|Tests"</automated>
  </verify>
  <done>Novos casos adicionados; casos pré-existentes continuam passando; novos casos falham com TypeError (RED confirmado)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar extensões de orders e notifications</name>
  <files>
    apps/api/src/modules/orders/orders.service.ts
    apps/api/src/modules/orders/orders.repository.ts
    apps/api/src/modules/orders/orders.controller.ts
    apps/api/src/modules/orders/orders.route.ts
    apps/api/src/modules/notifications/notifications.service.ts
    apps/api/src/modules/notifications/notifications.controller.ts
    apps/api/src/modules/notifications/notifications.route.ts
  </files>
  <read_first>
    - apps/api/src/modules/orders/orders.service.ts (ler inteiro — incluir BRAZIL_OFFSET_HOURS e getTomorrowUTC3 como referência para getTodayRange)
    - apps/api/src/modules/orders/orders.repository.ts (ler inteiro — ver métodos existentes antes de adicionar)
    - apps/api/src/modules/orders/orders.controller.ts (ler inteiro — ver handlers existentes para replicar padrão)
    - apps/api/src/modules/orders/orders.route.ts (ler inteiro — ver rotas existentes para não duplicar)
    - apps/api/src/modules/notifications/notifications.service.ts (ler inteiro)
    - apps/api/src/modules/notifications/notifications.controller.ts (ler inteiro)
    - apps/api/src/modules/notifications/notifications.route.ts (ler inteiro)
    - .planning/phases/05-delivery-experience/05-PATTERNS.md (seções orders.service.ts, orders.repository.ts, orders.controller.ts, notifications.*)
    - .planning/phases/05-delivery-experience/05-RESEARCH.md (Padrão 5: getTodayRange, Pitfall 1: timezone BRT)
  </read_first>
  <action>
    ORDERS — orders.service.ts: Adicionar função helper getTodayRange(): { start: Date; end: Date } após getTomorrowUTC3. Usar BRAZIL_OFFSET_HOURS = 3 já declarado. Lógica: start = Date.UTC(year, month, day, BRAZIL_OFFSET_HOURS, 0, 0, 0); end = Date.UTC(year, month, day + 1, BRAZIL_OFFSET_HOURS - 1, 59, 59, 999). Adicionar métodos à classe OrdersService: getTodayOrder(userId: string) usa prisma.order.findFirst com scheduledDate { gte: start, lte: end } e status: { not: 'CANCELLED' }. getOrderHistory(userId: string, days: number = 30) usa prisma.order.findMany com scheduledDate { gte: since } onde since = new Date() e since.setDate(since.getDate() - days), status: { not: 'CANCELLED' }, orderBy: { scheduledDate: 'desc' }.

    ORDERS — orders.repository.ts: Adicionar findTodayByUserId(userId: string, start: Date, end: Date) e findHistoryByUserId(userId: string, since: Date) seguindo padrão da classe (get prisma() { return this.fastify.prisma }).

    ORDERS — orders.controller.ts: Adicionar getTodayOrder(request, reply): userId = request.user!.id, chama service.getTodayOrder(userId), se null retorna 404 { error: 'Nenhuma entrega hoje' }, senão retorna 200 com o order. Adicionar getOrderHistory(request, reply): userId = request.user!.id, days = parseInt((request.query as { days?: string }).days ?? '30', 10), retorna 200 com array.

    ORDERS — orders.route.ts: Adicionar GET /orders/today com preHandler: [fastify.authenticate] → ctrl.getTodayOrder.bind(ctrl). Adicionar GET /orders/history com preHandler: [fastify.authenticate] → ctrl.getOrderHistory.bind(ctrl).

    NOTIFICATIONS — notifications.service.ts: Adicionar 4 métodos à classe NotificationsService: getByUserId(userId: string) → prisma.notification.findMany({ where: { userId }, orderBy: { createdAt: 'desc' }, take: 30 }). markAllRead(userId: string) → prisma.notification.updateMany({ where: { userId, isRead: false }, data: { isRead: true } }). createAndTrim(data: { userId, type, title, body, actionRoute? }) → create + findMany + deleteMany se > 30 (mesmo padrão do Plan 01). countUnread(userId: string) → prisma.notification.count({ where: { userId, isRead: false } }).

    NOTIFICATIONS — notifications.controller.ts: Adicionar 3 métodos: getNotifications(req, reply) → userId do JWT → service.getByUserId → 200. readAll(req, reply) → userId do JWT → service.markAllRead → 200 { ok: true }. getUnreadCount(req, reply) → userId do JWT → service.countUnread → 200 { count }.

    NOTIFICATIONS — notifications.route.ts: Adicionar GET /notifications/me, PATCH /notifications/read-all, GET /notifications/unread-count — todos com preHandler: [fastify.authenticate]. NÃO remover a rota POST /users/push-token existente.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx vitest run apps/api/src/modules/orders/__tests__/orders.service.test.ts apps/api/src/modules/notifications/__tests__/notifications.service.test.ts 2>&1 | grep -E "passed|failed|Tests"</automated>
  </verify>
  <acceptance_criteria>
    - orders.service.ts contém getTodayRange( e getTodayOrder( e getOrderHistory(
    - orders.service.ts contém BRAZIL_OFFSET_HOURS (não usa new Date().getHours() para timezone)
    - orders.route.ts contém GET /orders/today e GET /orders/history
    - notifications.service.ts contém getByUserId( e markAllRead( e createAndTrim( e countUnread(
    - notifications.route.ts contém GET /notifications/me e PATCH /notifications/read-all e GET /notifications/unread-count
    - notifications.route.ts ainda contém POST /users/push-token (não removida)
    - Todos os casos de teste novos passam (GREEN): `cd apps/api && npx vitest run` retorna 0
    - userId é sempre extraído de request.user!.id no controller — nunca de body ou query params
  </acceptance_criteria>
  <done>5 endpoints implementados; 8 novos casos de teste verdes; suite completa da API retorna 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP → orders.controller | GET com JWT; userId extraído do token, não de query params |
| HTTP → notifications.controller | GET/PATCH com JWT; userId extraído do token — nunca do body |
| notifications.service → prisma | findMany/updateMany com userId do JWT — escopo restrito ao usuário autenticado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | GET /notifications/me | mitigate | userId sempre de request.user!.id (JWT) — nunca de query param ou body; usuário A não acessa notificações do usuário B |
| T-05-05 | Information Disclosure | GET /orders/today e GET /orders/history | mitigate | userId sempre de request.user!.id (JWT); preHandler: [fastify.authenticate] obrigatório em ambas as rotas |
| T-05-06 | Denial of Service | notifications.service.ts createAndTrim | mitigate | Trim inline a 30 notificações por usuário — deleteMany com slice(30) após cada create |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo — todos auditados em fases anteriores |
</threat_model>

<verification>
Todos os testes novos e pré-existentes verdes: `cd apps/api && npx vitest run` passa.
Rotas de orders verificadas: `grep -n "orders/today\|orders/history" apps/api/src/modules/orders/orders.route.ts` retorna 2 linhas.
Rotas de notifications verificadas: `grep -n "notifications/me\|read-all\|unread-count" apps/api/src/modules/notifications/notifications.route.ts` retorna 3 linhas.
userId do JWT verificado: `grep -n "request.user!.id" apps/api/src/modules/notifications/notifications.controller.ts` retorna resultados em todos os handlers.
</verification>

<success_criteria>
- `cd apps/api && npx vitest run` retorna 0 com todos os testes novos e pré-existentes passando
- GET /orders/today e GET /orders/history registradas com preHandler: [fastify.authenticate]
- getTodayRange() usa BRAZIL_OFFSET_HOURS = 3 (sem new Date() direto para cálculo de "hoje")
- GET /notifications/me, PATCH /notifications/read-all, GET /notifications/unread-count registradas com preHandler: [fastify.authenticate]
- POST /users/push-token preservada (não removida da rota)
</success_criteria>

<output>
Criar `.planning/phases/05-delivery-experience/05-02-SUMMARY.md` ao concluir.
</output>
