---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/vitest.config.ts
  - packages/shared/src/schemas/index.ts
  - packages/shared/src/__tests__/cpf.test.ts
  - apps/api/src/__tests__/auth.service.test.ts
  - apps/api/src/__tests__/auth.schema.test.ts
  - apps/web/src/components/auth/__tests__/OtpInput.test.tsx
  - apps/api/.env.example
autonomous: true
requirements: [AUTH-02, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "Session and OtpCode Prisma models exist and schema validates cleanly"
    - "Prisma client is regenerated and typed Session + OtpCode are available"
    - "CPF módulo-11 validator is exported from packages/shared"
    - "Test infrastructure exists for api (Vitest node env) so auth logic can be unit-tested"
    - "Test stubs exist for CPF validation, OTP generation, session expiry, and OtpInput focus"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "Session and OtpCode Prisma models"
      contains: "model Session"
    - path: "apps/api/vitest.config.ts"
      provides: "API test runner (node environment)"
    - path: "packages/shared/src/schemas/index.ts"
      provides: "CpfSchema export with módulo-11 validation"
      exports: ["CpfSchema"]
    - path: "packages/shared/src/__tests__/cpf.test.ts"
      provides: "CPF unit test stubs"
    - path: "apps/api/src/__tests__/auth.service.test.ts"
      provides: "Auth service unit test stubs"
  key_links:
    - from: "apps/api/src/modules/auth/auth.service.ts"
      to: "prisma.session / prisma.otpCode"
      via: "Prisma generated types after db push"
      pattern: "prisma\\.session\\|prisma\\.otpCode"
    - from: "apps/api/src/modules/auth/auth.schema.ts"
      to: "packages/shared/src/schemas/index.ts"
      via: "CpfSchema import"
      pattern: "CpfSchema"
---

<objective>
Lay the foundation that every subsequent plan in this phase depends on: extend the Prisma schema with Session and OtpCode models, push the schema to MongoDB Atlas, regenerate the Prisma client, add the shared CPF validator, and scaffold all Wave 0 test stubs so automated tests can run from Plan 02 onwards.

Purpose: Without this plan, no auth business logic can be written (no Prisma types for Session/OtpCode) and no unit tests can run (api has no Vitest config).
Output: Validated schema, typed Prisma client, CpfSchema in shared, Vitest in api, four test stub files green.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-CONTEXT.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing codebase. -->

From apps/api/prisma/schema.prisma (current end of models section):
- 15 existing models. Session and OtpCode are models 16 and 17.
- All model IDs: `String @id @default(auto()) @map("_id") @db.ObjectId`
- All timestamps: `DateTime @default(now())`
- MongoDB adapter — NEVER run `prisma migrate dev`; run `npx prisma db push` then `npx prisma generate`

From packages/shared/src/schemas/index.ts:
```typescript
export const ObjectIdSchema = z.string().regex(/^[a-f\d]{24}$/i, 'Invalid ObjectId')
export const UserRoleSchema = z.enum(['CLIENT', 'COURIER', 'ADMIN'])
export const CondoTypeSchema = z.enum(['SINGLE_ENTRANCE', 'BLOCKS'])
```

From apps/web/vitest.config.ts (analog for api config):
```typescript
import { defineConfig } from 'vitest/config'
export default defineConfig({
  test: { environment: 'jsdom', setupFiles: ['./src/test-setup.ts'], globals: true }
})
```

From apps/web/src/pages/splash/SplashScreen.test.tsx (test stub analog):
```typescript
describe('SplashScreen [UI-05]', () => {
  it.todo('renders espresso background')
  it.todo('shows install CTA')
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Prisma schema with Session + OtpCode, push to Atlas, regenerate client</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - apps/api/prisma/schema.prisma (read full file to find the last model and append after it)
    - .planning/phases/02-authentication/02-RESEARCH.md (Pattern 7: exact model field definitions)
    - .planning/phases/02-authentication/02-PATTERNS.md (Pattern Assignments section for schema)
  </read_first>
  <behavior>
    - Session model fields: id (ObjectId), userId (ObjectId), token (String @unique — sha256 hash), deviceId (String), lastUsedAt (DateTime @default(now())), expiresAt (DateTime), isRevoked (Boolean @default(false)), createdAt (DateTime @default(now()))
    - OtpCode model fields: id (ObjectId), userId (ObjectId), code (String — sha256 hash), channel (String — "sms" | "email"), expiresAt (DateTime), usedAt (DateTime? — null until used), createdAt (DateTime @default(now()))
    - Both models use the same id/ObjectId pattern as every existing model
    - After appending models: run `npx prisma validate` (must exit 0), then `npx prisma db push`, then `npx prisma generate`
  </behavior>
  <action>
    Append Session (model 16) and OtpCode (model 17) to the end of apps/api/prisma/schema.prisma following the exact pattern used by existing models (id @id @default(auto()) @map("_id") @db.ObjectId, all DateTime fields use @default(now()) where applicable). Add a comment header above each model matching the existing comment format: `// 16. Session — persistent device sessions` and `// 17. OtpCode — one-time codes for login and registration`.

    After writing the file, run these commands in sequence:
    1. `npx prisma validate --schema=apps/api/prisma/schema.prisma` — must exit 0
    2. `npx prisma db push --schema=apps/api/prisma/schema.prisma` — pushes Session + OtpCode collections to MongoDB Atlas (per schema_push_requirement, this is BLOCKING)
    3. `npx prisma generate --schema=apps/api/prisma/schema.prisma` — regenerates client with Session + OtpCode types
  </action>
  <verify>
    <automated>npx prisma validate --schema=apps/api/prisma/schema.prisma && npx tsc --noEmit -p apps/api/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/prisma/schema.prisma contains `model Session {` with all 8 fields listed above
    - apps/api/prisma/schema.prisma contains `model OtpCode {` with all 7 fields listed above
    - `npx prisma validate --schema=apps/api/prisma/schema.prisma` exits 0 with no errors
    - `npx prisma db push` exits 0 and creates Session + OtpCode collections in MongoDB Atlas
    - `npx prisma generate` exits 0 — Prisma Client types for Session and OtpCode are generated
    - `npx tsc --noEmit -p apps/api/tsconfig.json` exits 0 (no TypeScript errors from schema change)
  </acceptance_criteria>
  <done>Session and OtpCode models are in schema.prisma, pushed to Atlas, and Prisma Client is regenerated with typed access to both collections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CpfSchema to packages/shared + scaffold all Wave 0 test stubs</name>
  <files>
    packages/shared/src/schemas/index.ts,
    packages/shared/src/__tests__/cpf.test.ts,
    apps/api/vitest.config.ts,
    apps/api/src/__tests__/auth.service.test.ts,
    apps/api/src/__tests__/auth.schema.test.ts,
    apps/web/src/components/auth/__tests__/OtpInput.test.tsx,
    apps/api/.env.example
  </files>
  <read_first>
    - packages/shared/src/schemas/index.ts (read current exports to append cleanly)
    - apps/web/vitest.config.ts (exact analog for api vitest config — node environment instead of jsdom)
    - apps/web/src/pages/splash/SplashScreen.test.tsx (exact stub pattern to replicate)
    - .planning/phases/02-authentication/02-VALIDATION.md (Wave 0 gaps section — exact test cases required)
    - .planning/phases/02-authentication/02-PATTERNS.md (Package Assignments — shared schemas section)
    - apps/api/.env (or apps/api/.env.example if exists — read to see existing vars before appending)
  </read_first>
  <behavior>
    - CpfSchema: z.string().transform(v => v.replace(/\D/g, '')).refine(length === 11).refine(módulo-11 both check digits). Export as `CpfSchema` from packages/shared/src/schemas/index.ts.
    - validateCpfDigits internal helper: (1) reject length !== 11, (2) reject all-same-digit pattern /^(\d)\1{10}$/, (3) compute first check digit via sum(digit[i] * (10-i) for i=0..8) % 11 → remainder < 2 → 0 else 11-remainder, (4) compute second check digit via sum(digit[i] * (11-i) for i=0..9) % 11 → same rule. Return true only if both match.
    - apps/api/vitest.config.ts: node environment, globals: true, no React plugin, no setupFiles
    - cpf.test.ts stubs: describe('validateCpf [AUTH-02]') with it.todo for: 'accepts valid CPF', 'rejects all-same-digit CPF (111.111.111-11)', 'strips formatting before validating', 'rejects CPF with wrong check digits'
    - auth.service.test.ts stubs: describe('AuthService [AUTH-05, AUTH-06]') with it.todo for: 'generateOtpCode returns 4-digit string between 1000 and 9999', 'dev mode OTP_DEV_CODE=1234 accepted', 'session expiresAt < now returns 401', 'device mismatch revokes session'
    - OtpInput.test.tsx stubs: at apps/web/src/components/auth/__tests__/OtpInput.test.tsx — describe('OtpInput [UI-06]') with it.todo for: 'renders 4 input elements', 'focus advances to next input when digit entered', 'calls onComplete when all 4 digits filled', 'rejects non-numeric characters'
    - auth.schema.test.ts stubs: at apps/api/src/__tests__/auth.schema.test.ts — describe('RegisterSchema [AUTH-03]') with it.todo for: 'requires bloco when condominium type is BLOCKS'
    - .env.example additions: ZENVIA_TOKEN=, ZENVIA_FROM=, RESEND_API_KEY=, RESEND_FROM=, OTP_DEV_CODE=1234, ADMIN_NAME=, ADMIN_PHONE=, ADMIN_EMAIL=, ADMIN_CPF= (with inline comments explaining each)
  </behavior>
  <action>
    1. Append CpfSchema and its internal validateCpfDigits helper function to packages/shared/src/schemas/index.ts after the existing CondoTypeSchema export. The helper is NOT exported (internal to the file). CpfSchema uses .transform() to strip /\D/g then two .refine() calls: length === 11, then the módulo-11 algorithm.

    2. Create packages/shared/src/__tests__/cpf.test.ts with the 4 it.todo stubs. No imports needed for stubs — the describe/it.todo pattern from SplashScreen.test.tsx is the exact analog.

    3. Create apps/api/vitest.config.ts with defineConfig({ test: { environment: 'node', globals: true } }). No react plugin, no setupFiles. Import from 'vitest/config'.

    4. Create apps/api/src/__tests__/auth.service.test.ts with the 4 it.todo stubs under describe('AuthService [AUTH-05, AUTH-06]').

    5. Create apps/web/src/components/auth/__tests__/OtpInput.test.tsx with the 4 it.todo stubs under describe('OtpInput [UI-06]').

    5b. Create apps/api/src/__tests__/auth.schema.test.ts with a single it.todo stub under describe('RegisterSchema [AUTH-03]'): it.todo('requires bloco when condominium type is BLOCKS'). This stub ensures the AUTH-03 conditional block validation is tracked and testable once auth.schema.ts is built in Plan 02-02.

    6. Append Phase 2 environment variables to apps/api/.env.example: ZENVIA_TOKEN (required for SMS in production), ZENVIA_FROM (Zenvia sender ID), RESEND_API_KEY (required for email in production), RESEND_FROM (e.g. "Cheirin de Pão <noreply@domain.com>"), OTP_DEV_CODE=1234 (dev bypass — any code accepted when NODE_ENV=development), ADMIN_NAME, ADMIN_PHONE, ADMIN_EMAIL, ADMIN_CPF (optional, defaults to placeholder if absent — used to seed first Admin on boot).
  </action>
  <verify>
    <automated>npm run test --workspace=@cheirin-de-pao/shared && npm run test --workspace=@cheirin-de-pao/api && npm run test --workspace=@cheirin-de-pao/web</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/schemas/index.ts exports `CpfSchema` (grep: `export const CpfSchema`)
    - CpfSchema rejects '111.111.111-11' (all-same-digit) — verified via manual node REPL check or by running tests once stubs are filled
    - CpfSchema accepts '529.982.247-25' (a known valid CPF) — same manual check
    - apps/api/vitest.config.ts exists and contains `environment: 'node'`
    - `npm run test --workspace=@cheirin-de-pao/shared` exits 0 (4 todos = pass in Vitest)
    - `npm run test --workspace=@cheirin-de-pao/api` exits 0 (4 todos = pass in Vitest)
    - `npm run test --workspace=@cheirin-de-pao/web` exits 0 (existing + new 4 todos = all pass)
    - apps/api/src/__tests__/auth.schema.test.ts exists with describe('RegisterSchema [AUTH-03]') containing 1 it.todo
    - apps/api/.env.example contains `ZENVIA_TOKEN=`, `RESEND_API_KEY=`, `OTP_DEV_CODE=1234`, `ADMIN_NAME=`
    - `npm test` exits 0 (full Turborepo suite)
  </acceptance_criteria>
  <done>CpfSchema with módulo-11 is exported from shared, all four Wave 0 test stub files exist and pass, API has Vitest configured, .env.example documents all Phase 2 env vars.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema file → MongoDB Atlas | Prisma db push modifies Atlas collections directly; destructive if misconfigured |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC-01 | Tampering | npm install (resend package — Wave 2) | mitigate | Package legitimacy audit already in RESEARCH.md — resend is [OK]. No installs in this plan. |
| T-02-SC-02 | Tampering | `npx prisma db push` | accept | Only adds new collections (Session, OtpCode); does not drop or modify existing collections. MongoDB Atlas schema is additive. |
| T-02-01 | Information Disclosure | apps/api/.env.example | accept | .env.example contains placeholder values only — no real credentials. Documented with inline comments. |
</threat_model>

<verification>
Run after both tasks complete:

```bash
npx prisma validate --schema=apps/api/prisma/schema.prisma
npm test
```

Both must exit 0. Confirm MongoDB Atlas has Session and OtpCode collections created (visible in Atlas UI or via mongosh).
</verification>

<success_criteria>
- `npx prisma validate` exits 0 with Session + OtpCode models present
- `npx prisma db push` has executed successfully (Atlas collections exist)
- `npm test` (full suite) exits 0 — all workspaces pass
- CpfSchema exported from packages/shared with módulo-11 validation
- apps/api/vitest.config.ts exists (node environment)
- All 5 Wave 0 test stub files exist and produce passing todo results (includes auth.schema.test.ts for AUTH-03)
- apps/api/.env.example documents all 9 Phase 2 env vars
</success_criteria>

<output>
Create `.planning/phases/02-authentication/02-01-SUMMARY.md` when done
</output>
