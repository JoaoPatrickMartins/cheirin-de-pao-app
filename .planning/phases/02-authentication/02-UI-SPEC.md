---
phase: 2
slug: authentication
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-13
---

# Phase 2 — Authentication UI Design Contract

> Visual and interaction contract for frontend authentication flows.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> All values are extracted directly from the codebase — no assumptions made.
> Sources: `apps/web/src/styles/globals.css`, `.projeto/design_handoff_cheirin_pao/app/brand.jsx`,
> `.projeto/design_handoff_cheirin_pao/app/screens-onboarding.jsx`, `apps/web/src/pages/splash/SplashScreen.tsx`

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none (hand-rolled CSS variables via Tailwind v4 `@theme`) | `apps/web/src/styles/globals.css` |
| Preset | not applicable | — |
| Component library | none (custom primitives: `Btn`, `Card`, `Field`, `Icon`) | `brand.jsx` |
| Icon library | custom inline SVG paths (`Ic` object in `brand.jsx`) — stroke-only, 24×24, `currentColor` | `brand.jsx` |
| Font — Display | Bricolage Grotesque Variable / Bricolage Grotesque, serif (`--font-display`) | `globals.css` |
| Font — Body | Hanken Grotesk, sans-serif (`--font-body`) | `globals.css` |

**shadcn gate result:** Not applicable. Stack is React + Vite + Tailwind CSS with an existing hand-rolled design system. No shadcn initialization required.

---

## Screens in Scope

| Screen | Route | Profile |
|--------|-------|---------|
| Splash / Install | `/` | all (unauthenticated) |
| Login — phone/email entry | `/login` | all |
| Login — OTP verification | `/login` (step 2) | all |
| Registration — 5-step stepper | `/register` | Client only |
| Courier registration | `/admin/couriers/new` | Admin only |
| Loading / redirecting state | route guards | all |

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Pill padding-y, dot indicator gap |
| sm | 8px | Icon-text inline gap, step dot internal spacing |
| md | 16px | Card padding default, form field gap, page horizontal padding |
| lg | 24px | Page horizontal padding (auth screens), OTP digit gap total |
| xl | 32px | Splash bottom margin, section vertical padding |
| 2xl | 48px | Vertical centering spacer |
| 3xl | 64px | Splash top icon margin |

Exceptions:
- Back button: 38×38px hit area (minimum; visual < 44px — acceptable for secondary nav)
- Back button border-radius: 12px (deviates from `--radius-btn`; matches `brand.jsx` `AppBar` back button)
- OTP digit box: 64×72px — fixed, mandatory; matches UI-06 spec exactly
- Status bar height placeholder: 44px

---

## Typography

All sizes are exact pixel values from `globals.css` `@theme` block and `brand.jsx` component definitions.

| Role | Family | Size | Weight | Line Height | Letter Spacing | Usage |
|------|--------|------|--------|-------------|----------------|-------|
| Display | `--font-display` | 32px | 700 | 1.1 | -0.03em | Splash app name, Login/Register screen main heading |
| Heading | `--font-display` | 28px (`--text-xl` is 21; this is display-tier) | 700 | 1.1 | -0.03em | "Bom dia. Vamos te identificar." — Login heading |
| Subheading | `--font-display` | 26px | 700 | 1.15 | -0.03em | Registration stepper titles ("Seus dados", "Como falamos com você?") |
| AppBar title | `--font-display` | 21px (`--text-xl`) | 700 | 1.2 | -0.02em | Back-navigation screen headers |
| Body | `--font-body` | 15px (`--text-base`) | 400–500 | 1.5 | 0 | Paragraphs, field hints, descriptions below headings |
| Body strong | `--font-body` | 15px (`--text-base`) | 700 | 1.5 | -0.01em | Button labels, field values, resend link |
| Label | `--font-body` | 12.5px (`--text-sm`) | 700 | 1.4 | 0.01em | Form field labels, section labels ("Receber o código por") |
| Caption | `--font-body` | 13px | 600 | 1.4 | 0 | "Não chegou? Reenviar em 0:28", secondary links |
| Tagline | `--font-body` | 12px | 700 | 1 | 0.26em | Splash "PÃO FRESCO NA PORTA" — uppercase |
| OTP digit | `--font-display` | 30px | 800 | 1 | 0 | OTP input box digit display (UI-06) |

**Constraint:** Never use a size outside this table. Two weights only in practice: 400/500 for body prose, 700/800 for interactive and heading elements.

---

## Color

All values are from `globals.css` `--color-*` tokens and `THEMES.light` in `brand.jsx`.

| Role | Token | Hex / Value | 60/30/10 |
|------|-------|-------------|----------|
| Page background | `--color-page-bg` | `#C9BBA2` | dominant wrapper (desktop viewport overflow) |
| App background | `--color-app-bg` | `#FAF5EC` | 60% — main app shell background |
| Surface (card) | `--color-surface` | `#FFFFFF` | 30% — cards, install banner, modal sheets |
| Surface alt | `--color-surface-alt` | `#FBF6EC` | secondary inputs, OTP box fill |
| Surface 2 | `--color-surface-2` | `#F4EBDA` | back button bg, inactive stepper elements |
| Espresso (dark) | `--color-espresso` | `#1E1207` | primary button bg, splash background |
| Text primary | `--color-text` | `#241608` | all headings and body text |
| Text secondary | `--color-text-sec` | `#7C6A50` | descriptions, field labels |
| Text tertiary | `--color-text-ter` | `#A89A82` | hints, icons, inactive states |
| Gold | `--color-gold` | `#E3AC3F` | 10% accent — CTA buttons (gold variant), BreadMark |
| Gold soft | `--color-gold-soft` | `#F3DDA6` | selected channel button bg, selected block/tower bg |
| Accent | `--color-accent` | `#B0702A` | 10% accent — active field borders, OTP box border (filled), selected card border, stepper active dot, links |
| Good | `--color-good` | `#3E7C53` | success confirmation state (not in Phase 2 flows) |
| Good soft | `--color-good-soft` | `#DCEBDF` | success state background |
| Border | `--color-border` | `rgba(43,26,12,0.10)` | field borders (idle), card borders |
| Border 2 | `--color-border-2` | `rgba(43,26,12,0.06)` | card separators, subtle dividers |
| Primary button text | `--color-primary-btn-text` | `#FBF3E4` | text on espresso-background buttons |
| Icon container | (inline) | `#160C04` | BreadMark icon container background |

**Accent reserved for:**
1. Active/focused field border (`border: 1.5px solid var(--color-accent)`)
2. OTP input box border when digit is entered (`border: 1.5px solid var(--color-accent)`)
3. Selected condominium card border
4. Selected block/tower chip border and text
5. Active step dot in registration stepper
6. Text links ("Reenviar em 0:28", "e-mail" switch link)
7. Selected channel button border and text

**Gold reserved for:**
1. Primary CTA button background on splash screen (`variant="gold"`)
2. BreadMark symbol color everywhere it appears
3. Resend countdown text color in `LoginScreen`

**Espresso (`#1E1207`) reserved for:**
1. Splash screen background
2. Primary button background in auth flows (non-splash)
3. Text on gold-background elements

**Destructive:** `--color-good` inverse is not applicable in Phase 2. No destructive actions exist in auth flows. The color `#B0702A` (accent) doubles as warn per `brand.jsx` (`warn: '#B0702A'`).

---

## Border Radius

All values from `globals.css` `--radius-*` tokens:

| Token | Value | Applied to |
|-------|-------|------------|
| `--radius-field` | 14px | Text input fields |
| `--radius-btn` | 16px | All buttons (primary, gold, ghost, soft variants) |
| `--radius-card` | 22px | Cards, install banner, bottom sheets |
| `--radius-pill` | 999px | Pill badges, switch track |
| `--radius-app-icon` | 30% | Splash BreadMark container (132×132px) |
| (back button) | 12px | Back button / AppBar back button chip |
| (OTP digit box) | 18px | OTP input squares |
| (block/tower chip) | 13px | Registration step 3 block selector buttons |
| (channel button) | 14px | Registration step 1 channel selector (SMS / E-mail) |

---

## Component Inventory (Phase 2)

### New Components to Build

| Component | File | Description |
|-----------|------|-------------|
| `LoginScreen` | `apps/web/src/pages/auth/LoginScreen.tsx` | 2-step: phone/email input → OTP. Mirrors `screens-onboarding.jsx → LoginScreen`. |
| `OnboardingScreen` | `apps/web/src/pages/auth/OnboardingScreen.tsx` | 5-step registration stepper. Mirrors `screens-onboarding.jsx → OnboardingScreen`. |
| `OtpInput` | `apps/web/src/components/auth/OtpInput.tsx` | 4 independent inputs with auto-focus. See Interaction Contract below. |
| `ResendTimer` | `apps/web/src/components/auth/ResendTimer.tsx` | 30-second countdown; "Reenviar em 0:28" → activates resend button. |
| `StepDots` | `apps/web/src/components/auth/StepDots.tsx` | 5-dot progress indicator with active dot animating width 7px → 22px. |
| `CondoSearch` | `apps/web/src/components/auth/CondoSearch.tsx` | Searchable list of condominiums with empty state. |
| `ChannelSelector` | `apps/web/src/components/auth/ChannelSelector.tsx` | SMS / E-mail toggle with auto-select logic. |
| `CourierRegisterScreen` | `apps/web/src/pages/admin/CourierRegisterScreen.tsx` | Admin registers courier. Simple form: name, CPF, phone, email. |
| `ProtectedRoute` | `apps/web/src/components/ProtectedRoute.tsx` | Role-aware route guard wrapping profile layouts. |
| `AuthContext` | `apps/web/src/contexts/AuthContext.tsx` | `AuthProvider` as layout route component with `useNavigate`. |
| `LoadingScreen` | `apps/web/src/pages/auth/LoadingScreen.tsx` | Shown during `isLoading` state in `AuthProvider`. See spec below. |

### Reused Existing Components

| Component | Reused From | Notes |
|-----------|-------------|-------|
| `BreadMark` | `apps/web/src/components/brand/BreadMark.tsx` | Use in `LoadingScreen` only; not in main auth forms |
| `Icon` | `apps/web/src/components/brand/Icon.tsx` | Used in fields, back buttons, channel selector |

---

## Interaction Contracts

### OTP Input (UI-06) — Critical

```
4 inputs in a row, gap: 12px between each.
Each input:
  width: 64px, height: 72px
  textAlign: center
  fontSize: 30px, fontWeight: 800, fontFamily: --font-display
  color: --color-text
  background: --color-surface-alt (#FBF6EC)
  borderRadius: 18px
  border: 1.5px solid (filled → --color-accent, empty → --color-border)
  outline: none
  maxLength: 1, inputMode: "numeric"

On change:
  - Reject non-digit input (/^\d?$/ guard)
  - If digit entered AND not last box → focus next ref
  - If last box filled → call onComplete(joined 4-digit string)

On backspace (keydown event, key === 'Backspace', current value is ''):
  - Focus previous ref (allows correction)
```

### Resend Timer

```
Initial state: countdown visible, resend action disabled.
Format: "Reenviar em M:SS" (e.g. "Reenviar em 0:28")
Timer: 30 seconds (useState + useEffect interval).
At 0: text changes to "Reenviar código" as a clickable link.
Color during countdown: --color-text-ter (#A89A82) — non-interactive appearance.
Color when active: --color-accent (#B0702A) — same as other text links.
Font: 13px, weight 600, family --font-body.
```

### Field Focus State

```
On focus: border changes from --color-border to --color-accent (1.5px solid)
On blur: border returns to --color-border
Transition: border-color 0.15s ease
Background: --color-surface-alt (#FBF6EC)
Border-radius: --radius-field (14px)
Padding: 12px 14px
```

### Button States

```
Primary (espresso):
  background: --color-espresso (#1E1207)
  color: --color-primary-btn-text (#FBF3E4)
  border-radius: --radius-btn (16px)
  padding: 16px 22px (size="lg")
  font: 15px / weight 700 / --font-body / letter-spacing -0.01em
  disabled: opacity 0.45, no pointer
  hover (desktop): translateY(-1px) + brightness(1.05), transition 0.15s

Gold (CTA on dark background):
  background: --color-gold (#E3AC3F)
  color: --color-espresso (#1E1207)
  Same radius/padding as primary.

Touch target: ALL interactive elements must be ≥44px tall (UI-10).
Back button is 38px visually but must be inside a 44px tap area via padding.
```

### Back Button

```
shape: square with border-radius 12px
size: 38px × 38px (visual) — pad to 44px minimum touch target via container
background: --color-surface-2 (#F4EBDA)
icon: "arrowL", 20px, color: --color-text (#241608)
behavior: go back one step in stepper, or navigate to previous screen
```

### Stepper Dots (Registration)

```
5 dots total.
Active dot: width 22px, height 7px, background: --color-accent, border-radius 99px
Inactive dot: width 7px, height 7px, background: --color-border, border-radius 99px
Transition: width + background — 0.25s ease
Gap between dots: 6px
Position: centered, padding 4px 0 16px (below back button, above step content)
```

### Condominium Search Empty State

```
Icon: "building", 28px, color: --color-text-ter
Heading: none (caption only)
Caption: "Seu condomínio ainda não é parceiro."
Second line: "Avise a gente que levamos o cheirin até aí!"
Font: 13.5px, color: --color-text-ter, line-height 1.5, textAlign: center
Padding: 24px 16px
```

### Loading / Redirecting Screen

```
Background: --color-app-bg (#FAF5EC)
Content: BreadMark centered, size 48px, color: --color-gold (#E3AC3F)
No text — pure visual feedback during auth rehydration from localStorage.
Minimum display: 0ms (disappears as soon as isLoading becomes false)
```

### Channel Selector (Registration step 1)

```
Two buttons side by side: "SMS" (icon: phone) and "E-mail" (icon: mail).
Layout: flex row, gap 10px, each flex: 1.
Selected state:
  border: 1.5px solid --color-accent
  background: --color-gold-soft (#F3DDA6)
  color: --color-accent (#B0702A)
Unselected state:
  border: 1.5px solid --color-border
  background: --color-surface (#FFFFFF)
  color: --color-text
Disabled state (channel has no value entered):
  opacity: 0.5, cursor: default
Auto-select logic:
  - Only phone entered → SMS pre-selected, E-mail disabled
  - Only email entered → E-mail pre-selected, SMS disabled
  - Both entered → default SMS, user can switch
Font: 14px, weight 700, --font-body.
Border-radius: 14px. Padding: 13px 0.
```

### Block/Tower Chip Selector (Registration step 3)

```
Visible only when selected condominium type === 'blocos'.
Chips rendered as flex-wrap row, gap: 9px.
Each chip:
  padding: 10px 16px
  border-radius: 13px
  font: 14px, weight 700, --font-body
Selected: border 1.5px --color-accent, background --color-gold-soft, color --color-accent
Unselected: border 1.5px --color-border, background --color-surface, color --color-text
```

---

## Screen Layout Contracts

### Login Screen layout

```
Container: flex column, full height, padding: 8px 24px 24px
Top: back button (38px) — left-aligned
Content area: flex-grow, centered vertically (justify: center)
Bottom: no sticky footer (CTA is inside the content area)

Step 1 (phone entry):
  - Heading: 28px display, "Bom dia.\nVamos te identificar."
  - Body: 14.5px, color text-sec, margin-top 12px, margin-bottom 28px
  - Field: phone number
  - Spacer: 18px
  - Primary CTA full-width: "Enviar código"
  - Link below: "ou entre com e-mail" (13px, text-ter; "e-mail" in color-accent)

Step 2 (OTP entry):
  - Heading: 28px display, "Digite o código"
  - Body: 14.5px, "Mandamos 4 dígitos para [phone bold]."
  - OTP 4-input row (see above)
  - Spacer: 24px
  - Primary CTA full-width: "Confirmar"
  - Resend timer link below (13px)
```

### Registration Stepper layout

```
Container: flex column, full height, padding: 6px 24px 22px, overflow: hidden
Top: back button (left-aligned, flex-shrink 0)
Step dots: margin-top 16px, margin-bottom 6px, centered
Content: flex-grow, flex column, min-height 0 (for step 2 which has scrollable list)
CTA area: padding-top 14px, sticky to bottom of flex column
```

### Step 0 — "Seus dados"
```
Title (26px display) + Sub (14px, margin-top 10px, margin-bottom 22px)
Form: 3 fields (nome, CPF, nascimento), gap 14px
Spacer: flex-1
CTA: "Continuar" (disabled until all 3 fields non-empty)
```

### Step 1 — "Como falamos com você?"
```
Title + Sub (with bold "ou")
Fields: phone + email, gap 14px
Channel selector section below fields, margin-top 20px
Section label: 12.5px, weight 700, text-sec, margin-bottom 9px
Channel buttons
Spacer: flex-1
CTA: "Continuar" (disabled until at least one contact field non-empty)
```

### Step 2 — "Onde você mora?"
```
Title (26px) + description (14px, margin-top 10px, margin-bottom 16px)
Search field (building icon)
Condominium list: flex-1, overflow-y auto, margin-top 14px, gap 10px
Empty state (see above)
CTA sticky at bottom: "Continuar" (disabled until condominium selected)
```

### Step 3 — "Seu endereço"
```
Title + Sub (shows selected condo name + bairro)
Block/tower chips (conditional on condo type === 'blocos')
  Section label: 12.5px, weight 700, text-sec, margin-bottom 9px
Apartment field (pin icon), label "Apartamento"
Spacer: flex-1
CTA: "Enviar código de confirmação"
  (disabled until: apartment filled AND if blocks condo — block selected)
```

### Step 4 — "Confirme seu cadastro" (OTP)
```
Title (26px) + Sub showing channel + destination (bold color-text)
OTP 4-input row
Resend timer (margin-top 18px, center-aligned)
Spacer: flex-1
CTA: "Criar conta e ver meu pão"
```

---

## Copywriting Contract

| Element | Copy (Portuguese) | Screen |
|---------|-------------------|--------|
| Splash primary CTA | "Instalar e criar conta" | InstallScreen |
| Splash secondary link | "Já tenho conta — entrar" | InstallScreen |
| Splash tagline | "PÃO FRESCO NA PORTA" (uppercase) | InstallScreen |
| Login heading (step 1) | "Bom dia.\nVamos te identificar." | LoginScreen |
| Login body (step 1) | "Enviamos um código por SMS para confirmar seu número. Sem senha pra decorar." | LoginScreen |
| Login CTA (step 1) | "Enviar código" | LoginScreen |
| Login email switch | "ou entre com e-mail" | LoginScreen |
| Login heading (step 2) | "Digite o código" | LoginScreen |
| Login body (step 2) | "Mandamos 4 dígitos para [phone/email]." | LoginScreen |
| Login CTA (step 2) | "Confirmar" | LoginScreen |
| Resend countdown | "Não chegou? Reenviar em M:SS" | Login + Registration step 4 |
| Resend active | "Não chegou? Reenviar código" | Login + Registration step 4 |
| Reg step 0 title | "Seus dados" | OnboardingScreen |
| Reg step 0 body | "Precisamos disso uma única vez, pra deixar sua conta pronta." | OnboardingScreen |
| Reg step 1 title | "Como falamos com você?" | OnboardingScreen |
| Reg step 1 body | "Informe telefone **ou** e-mail (pelo menos um). É por aí que enviamos o código e os avisos de entrega." | OnboardingScreen |
| Reg step 2 title | "Onde você mora?" | OnboardingScreen |
| Reg step 2 body | "Entregamos só nos condomínios parceiros já cadastrados." | OnboardingScreen |
| Reg step 3 title | "Seu endereço" | OnboardingScreen |
| Reg step 3 sub | "[condo.nome] · [condo.bairro]" | OnboardingScreen |
| Reg step 3 CTA | "Enviar código de confirmação" | OnboardingScreen |
| Reg step 4 title | "Confirme seu cadastro" | OnboardingScreen |
| Reg step 4 body | "Enviamos 4 dígitos por **[SMS/e-mail]** para **[phone/email]**." | OnboardingScreen |
| Reg final CTA | "Criar conta e ver meu pão" | OnboardingScreen |
| Condo stepper CTA | "Continuar" (steps 0, 1, 2) | OnboardingScreen |
| Empty condo list | "Seu condomínio ainda não é parceiro.\nAvise a gente que levamos o cheirin até aí!" | OnboardingScreen step 2 |
| Loading / redirecting | (no text — visual only) | LoadingScreen |
| Error — invalid OTP | "Código incorreto. Verifique e tente de novo." | OTP step (Login + Registration) |
| Error — OTP expired | "Código expirado. Solicite um novo." | OTP step |
| Error — network | "Algo deu errado. Verifique sua conexão e tente novamente." | Any form submission |
| Error — CPF already registered | "Esse CPF já tem uma conta. Faça login ou recupere o acesso." | Registration step 0 (API error) |
| Courier register — admin CTA | "Cadastrar entregador" | CourierRegisterScreen |
| Section channel label | "Receber o código por" | OnboardingScreen step 1 |
| Block selector label | "Bloco / Torre" | OnboardingScreen step 3 |
| Apartment field label | "Apartamento" | OnboardingScreen step 3 |

**No destructive actions exist in Phase 2 auth flows.** No deletion or revocation is initiated by the user — all revocation is automatic on device mismatch.

---

## Accessibility Requirements

| Rule | Value | Source |
|------|-------|--------|
| Minimum touch target | 44×44px (UI-10) | REQUIREMENTS.md UI-10 |
| OTP input `inputMode` | `"numeric"` | UI-06, `screens-onboarding.jsx` |
| OTP input `maxLength` | 1 per box | UI-06 |
| `aria-label` on icon buttons | required (back button: "Voltar") | WCAG 2.1 |
| Loading state announcement | `aria-live="polite"` on loading container | WCAG 2.1 |
| Field `<label>` association | explicit `htmlFor` + `id` OR `<label>` wrapping pattern | `brand.jsx` Field component wraps with `<label>` |
| Reduced motion | `prefers-reduced-motion` block in `globals.css` sets all transitions/animations to 0ms | `globals.css` |
| Form autocomplete hints | phone: `autoComplete="tel"`, email: `autoComplete="email"`, CPF: `autoComplete="off"` | usability standard |

---

## Animation / Motion

All transitions follow the values from `brand.jsx` and `SplashScreen.tsx`:

| Element | Property | Duration | Easing |
|---------|----------|----------|--------|
| Field border-color | border-color | 150ms | ease (default) |
| Button hover lift | transform + filter | 150ms | ease |
| Stepper dot width | width + background | 250ms | ease |
| Channel button state | border + background + color | 150ms | ease |
| Switch track | background | 200ms | ease |
| Switch thumb | transform | 200ms | ease |
| Reduced motion | all above → 0ms | — | `prefers-reduced-motion: reduce` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required — no shadcn installed |
| third-party | none | not applicable |

No third-party component registry blocks are used in Phase 2. All UI is built from the existing custom design system (`brand.jsx`) and new TypeScript components following the same patterns.

---

## Pre-Population Sources

| Source | Decisions Used |
|--------|---------------|
| `apps/web/src/styles/globals.css` | All color tokens, font tokens, radius tokens, text size tokens |
| `.projeto/design_handoff_cheirin_pao/app/brand.jsx` | Component patterns (Btn, Card, Field, Icon), button variants, all THEMES.light values, shadow tokens |
| `.projeto/design_handoff_cheirin_pao/app/screens-onboarding.jsx` | All screen layouts, copywriting, OTP input spec, stepper dots, channel selector, condominium search, block selector |
| `apps/web/src/pages/splash/SplashScreen.tsx` | Existing SplashScreen implementation confirming Phase 1 token usage |
| `02-CONTEXT.md` | OTP interaction specifics (30s resend timer, 4-digit code, auto-focus, `--color-accent` border on fill), channel auto-select, loading state requirement |
| `02-RESEARCH.md` | Pattern 4 OTP component spec (64×72px, radius 18, surfaceAlt bg), architecture (LoadingScreen during isLoading, ProtectedRoute) |
| `REQUIREMENTS.md` | UI-06 (OTP 4 inputs + auto-focus), UI-10 (44px hit targets), AUTH-01..08 (flows and screens) |
| User input | 0 (all values derived from existing codebase) |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
