---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/plugins/prisma.ts
  - apps/api/src/modules/health/health.route.ts
  - apps/api/prisma/schema.prisma
autonomous: true
requirements:
  - INFRA-04
  - INFRA-07
  - PWA-05

must_haves:
  truths:
    - "Running `cd apps/api && npx prisma generate` succeeds without errors"
    - "`cd apps/api && npx prisma validate` exits 0"
    - "Fastify server starts on port 3001 and logs startup confirmation"
    - "GET /health returns HTTP 200 with body {ok:true, db:'connected'} when MongoDB Atlas is reachable"
    - "GET /health returns HTTP 503 with body {ok:false} when DATABASE_URL is invalid"
    - "@fastify/cors allows requests from http://localhost:5173 in development"
  artifacts:
    - path: "apps/api/prisma/schema.prisma"
      provides: "All 15 MongoDB collections via Prisma v6.19.3"
      contains: "provider = \"mongodb\""
    - path: "apps/api/src/plugins/prisma.ts"
      provides: "PrismaClient as Fastify plugin decoration"
      contains: "fastify.decorate('prisma'"
    - path: "apps/api/src/modules/health/health.route.ts"
      provides: "GET /health endpoint with MongoDB ping"
      contains: "prisma.$runCommandRaw"
    - path: "apps/api/src/server.ts"
      provides: "Fastify server entry with CORS + env + Prisma plugins"
      contains: "fastify.listen"
  key_links:
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/plugins/prisma.ts"
      via: "fastify.register"
      pattern: "register.*prismaPlugin"
    - from: "apps/api/src/modules/health/health.route.ts"
      to: "fastify.prisma"
      via: "decorated PrismaClient"
      pattern: "fastify\\.prisma\\.\\$runCommandRaw"
    - from: "apps/api/prisma/schema.prisma"
      to: "MongoDB Atlas"
      via: "DATABASE_URL env var"
      pattern: "env\\(\"DATABASE_URL\"\\)"
---

<objective>
Build the complete Fastify API foundation: Prisma schema with all 15 MongoDB collections, prisma generate [BLOCKING], Prisma plugin as Fastify decoration, CORS configuration, environment variable validation, and the /health endpoint that confirms live MongoDB Atlas connectivity.

Purpose: Proves the "full stack" part of the Walking Skeleton. The /health endpoint doing a real MongoDB ping is the backend half of the phase success criterion. Without this, the skeleton cannot be declared working end-to-end.

Output: Fastify API running on :3001; GET /health returns {ok:true, db:'connected'} with real Atlas connection.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Patterns from RESEARCH.md for this plan. No existing codebase files — greenfield. -->

From RESEARCH.md Pattern 4 — Prisma v6 + MongoDB schema pattern:
  datasource db { provider = "mongodb"; url = env("DATABASE_URL") }
  generator client { provider = "prisma-client-js" }
  Every model: id String @id @default(auto()) @map("_id") @db.ObjectId
  Embedded documents: use `type` keyword (composite types)
  CRITICAL: prisma@6.19.3 exact — v7 does NOT support MongoDB

From RESEARCH.md Pattern 5 — Fastify v5 health route:
  await prisma.$runCommandRaw({ ping: 1 })
  Returns { ok: true, db: 'connected' } on success
  Returns 503 { ok: false, db: 'disconnected', error: String(err) } on failure

From RESEARCH.md Code Example — Fastify Prisma plugin:
  import fp from 'fastify-plugin'
  fastify.decorate('prisma', prisma)
  fastify.addHook('onClose', async (app) => { await app.prisma.$disconnect() })

From RESEARCH.md Shared Patterns — CORS:
  Dev: origin 'http://localhost:5173'
  Never allow '*' in any environment

From RESEARCH.md Common Pitfalls — Prisma:
  NEVER run prisma migrate dev — MongoDB does not support migrations
  Only: prisma generate (after schema changes) and prisma validate (CI)

15 Collections from CONTEXT.md D-01 + Requisitos_v01.md section 5.3:
  USERS, CONDOMINIUMS, COMBOS, PROMOTIONS, SETTINGS, CREDIT_TRANSACTIONS,
  SCHEDULES, ORDERS, DELIVERIES, DELIVERY_LISTS, SUPPLIERS, PURCHASE_ORDERS,
  PURCHASE_ORDER_ITEMS, PAYMENTS, NOTIFICATIONS
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prisma schema — all 15 MongoDB collections</name>
  <files>apps/api/prisma/schema.prisma</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Prisma v6 + MongoDB schema; Critical MongoDB + Prisma notes)
    - .planning/phases/01-foundation/01-PATTERNS.md (Pattern Assignment for schema.prisma — full field detail)
    - .projeto/Cheirin_de_Pao_Requisitos_v01.md sections 5.3 (collection definitions, all 15 names and purposes)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01: all 15 collections; D-02: MongoDB adapter patterns)
  </read_first>
  <action>
    Create apps/api/prisma/schema.prisma with the full Prisma v6 MongoDB schema. Critical rules:

    Header block (exact):
    - datasource db: provider "mongodb", url env("DATABASE_URL")
    - generator client: provider "prisma-client-js"

    Universal field pattern for ALL models — every model must have EXACTLY:
    - id String @id @default(auto()) @map("_id") @db.ObjectId
    - createdAt DateTime @default(now())
    - updatedAt DateTime @updatedAt

    Embedded composite types (not models, not collections) to define once at top:
    - type Address { street String; number String; complement String?; city String; state String; zip String }

    15 models with their essential fields (derive from Requisitos_v01.md section 5.3 + CONTEXT.md D-01):

    1. User: id, email String? @unique, phone String? @unique, role UserRole, name String, cpf String @unique, birthDate DateTime?, isBlocked Boolean @default(false), condominiumId String? @db.ObjectId, apartment String?, block String?, createdAt, updatedAt

    2. Condominium: id, name String, address Address (composite type), type CondoType, isActive Boolean @default(true), createdAt, updatedAt

    3. Combo: id, name String, quantity Int (pãezinhos), price Float, isActive Boolean @default(true), createdAt, updatedAt

    4. Promotion: id, comboId String @db.ObjectId, discountType DiscountType (PERCENT | FIXED), discountValue Float, startsAt DateTime, endsAt DateTime, isActive Boolean @default(true), createdAt, updatedAt

    5. Setting: id, key String @unique, value String, createdAt, updatedAt — stores cutoffTime, maxCustomQty, customUnitPrice, sessionExpiryDays

    6. CreditTransaction: id, userId String @db.ObjectId, type TransactionType (PURCHASE | DELIVERY | REFUND | EXPIRY), quantity Int, referenceId String? (orderId or paymentId), description String?, createdAt, updatedAt (no updatedAt needed — immutable log; remove updatedAt for this model)

    7. Schedule: id, userId String @db.ObjectId, condominiumId String @db.ObjectId, weeklyQty Json (stores { mon, tue, wed, thu, fri, sat, sun } as numbers), deliveryTime String, notifyReconfigure Boolean @default(false), isActive Boolean @default(true), createdAt, updatedAt

    8. Order: id, userId String @db.ObjectId, type OrderType (SINGLE | SCHEDULED), quantity Int, scheduledDate DateTime, status OrderStatus (SCHEDULED | OUT_FOR_DELIVERY | DELIVERED | CANCELLED), deliveryListId String? @db.ObjectId, createdAt, updatedAt

    9. Delivery: id, orderId String @db.ObjectId, courierId String @db.ObjectId, deliveredAt DateTime?, confirmedAt DateTime?, status DeliveryStatus (PENDING | CONFIRMED), createdAt, updatedAt

    10. DeliveryList: id, date DateTime, condominiumId String @db.ObjectId, courierId String? @db.ObjectId, status DeliveryListStatus (OPEN | CLOSED | COMPLETED), createdAt, updatedAt

    11. Supplier: id, name String, cnpj String @unique, phone String?, email String?, address Address, pricePerUnit Float, isActive Boolean @default(true), createdAt, updatedAt

    12. PurchaseOrder: id, date DateTime, totalQuantity Int, cutoffTime DateTime, status PurchaseOrderStatus (DRAFT | FINALIZED), createdAt, updatedAt

    13. PurchaseOrderItem: id, purchaseOrderId String @db.ObjectId, supplierId String @db.ObjectId, quantity Int, unitPrice Float, createdAt, updatedAt

    14. Payment: id, userId String @db.ObjectId, amount Float, method PaymentMethod (PIX | CREDIT_CARD | DEBIT_CARD), status PaymentStatus (PENDING | PAID | FAILED | REFUNDED), mercadoPagoId String? @unique, comboId String? @db.ObjectId, createdAt, updatedAt

    15. Notification: id, userId String @db.ObjectId, type NotificationType (DELIVERY_EVE | DELIVERY_DONE | LOW_CREDIT | CUTOFF | RECONFIGURE | CREDIT_PURCHASED), title String, body String, isRead Boolean @default(false), actionRoute String?, createdAt (no updatedAt — immutable log)

    Enums to define: UserRole (CLIENT COURIER ADMIN), CondoType (SINGLE_ENTRANCE BLOCKS), DiscountType (PERCENT FIXED), TransactionType (PURCHASE DELIVERY REFUND EXPIRY), OrderType (SINGLE SCHEDULED), OrderStatus (SCHEDULED OUT_FOR_DELIVERY DELIVERED CANCELLED), DeliveryStatus (PENDING CONFIRMED), DeliveryListStatus (OPEN CLOSED COMPLETED), PurchaseOrderStatus (DRAFT FINALIZED), PaymentMethod (PIX CREDIT_CARD DEBIT_CARD), PaymentStatus (PENDING PAID FAILED REFUNDED), NotificationType (DELIVERY_EVE DELIVERY_DONE LOW_CREDIT CUTOFF RECONFIGURE CREDIT_PURCHASED)

    AFTER writing the file, run the [BLOCKING] generate step — do not proceed to Task 2 until generate succeeds.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma validate 2>&1 | tail -5</automated>
  </verify>
  <done>prisma validate exits 0; schema.prisma contains all 15 model declarations; every model has @id @default(auto()) @map("_id") @db.ObjectId; datasource uses provider "mongodb"</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] prisma generate + Fastify server + Prisma plugin + health route</name>
  <files>
    apps/api/src/server.ts,
    apps/api/src/plugins/prisma.ts,
    apps/api/src/modules/health/health.route.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5: Fastify v5 health route with MongoDB ping; Code Example: Fastify server with Prisma plugin; Shared Patterns: CORS, env var handling)
    - .planning/phases/01-foundation/01-PATTERNS.md (Pattern Assignments for server.ts, plugins/prisma.ts, health.route.ts; Shared Patterns: CORS Configuration, Environment Variable Handling)
    - apps/api/prisma/schema.prisma (just created in Task 1 — verify datasource + models)
    - apps/api/package.json (verify fastify@5.8.5, @fastify/cors@11.2.0, @fastify/env@7.0.0, fastify-plugin@6.0.0 are present)
  </read_first>
  <action>
    STEP 0 [BLOCKING — must run before writing any .ts files]:
    Run: cd apps/api && npx prisma@6.19.3 generate
    This generates the Prisma client into node_modules/@prisma/client. The API TypeScript source CANNOT compile without it. If generate fails, fix the schema first before creating source files.

    STEP 1 — Create apps/api/src/plugins/prisma.ts:
    Import fp from 'fastify-plugin' and FastifyPluginAsync from 'fastify'. Import PrismaClient from '@prisma/client'.
    Declare module augmentation: declare module 'fastify' { interface FastifyInstance { prisma: PrismaClient } }
    Create prismaPlugin as FastifyPluginAsync wrapped in fp(). Inside: new PrismaClient(), call prisma.$connect(), decorate fastify with prisma, add onClose hook that calls app.prisma.$disconnect().
    Export as default.

    STEP 2 — Create apps/api/src/modules/health/health.route.ts:
    Import FastifyPluginAsync from 'fastify'. Export healthRoute as FastifyPluginAsync.
    Inside: fastify.get('/health', async handler). Handler: try await fastify.prisma.$runCommandRaw({ ping: 1 }), return reply.send({ ok: true, db: 'connected' }). Catch: return reply.status(503).send({ ok: false, db: 'disconnected', error: String(err) }).

    STEP 3 — Create apps/api/src/server.ts:
    Import Fastify from 'fastify', cors from '@fastify/cors', env from '@fastify/env'. Import prismaPlugin and healthRoute.
    Create fastify with { logger: true }.
    Register @fastify/env FIRST (before other plugins) with JSON schema requiring DATABASE_URL as string, API_PORT as integer (default 3001), API_HOST as string (default '0.0.0.0'). Use dotenv:true option so it reads from .env file.
    Register @fastify/cors with origin: process.env.NODE_ENV === 'production' ? false : 'http://localhost:5173'. Never use origin: true or origin: '*'.
    Register prismaPlugin.
    Register healthRoute.
    Call fastify.listen({ port: Number(process.env.API_PORT ?? 3001), host: process.env.API_HOST ?? '0.0.0.0' }).

    STEP 4 — Add apps/api/src/server.ts to apps/api/tsconfig.json include if not already there.

    STEP 5 — Create apps/api/.env by copying .env.example and asking developer to fill in DATABASE_URL. The server will fail to start without a real DATABASE_URL. Add a console.log('INFO: Copy .env.example to .env and fill DATABASE_URL before starting') as a startup hint.
  </action>
  <verify>
    <automated>cd /home/joaopatrick/Projects/cheirin-de-pao-app/apps/api && npx prisma generate 2>&1 | grep -E "Generated|error" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>prisma generate exits 0 and prints "Generated Prisma Client"; tsc --noEmit exits 0 with no type errors in apps/api; server.ts registers cors, env, prismaPlugin, and healthRoute; health route handler calls fastify.prisma.$runCommandRaw; CORS origin is restricted to localhost:5173 in dev</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API server → MongoDB Atlas | All traffic over TLS (mongodb+srv:// enforces TLS by default) |
| Client browser → Fastify API | CORS controls cross-origin access in dev |
| Environment → Process | DATABASE_URL loaded from .env file; never committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | DATABASE_URL in environment | mitigate | .env in .gitignore (enforced in Plan 01); .env.example has placeholder only |
| T-02-02 | Tampering | CORS allows all origins | mitigate | @fastify/cors with origin: 'http://localhost:5173' in dev (per RESEARCH.md security domain) — never origin: true or '*' |
| T-02-03 | Denial of Service | MongoDB Atlas connection exhaustion | accept | Prisma manages connection pool; Phase 1 has no auth or rate limiting — acceptable for dev skeleton |
| T-02-SC | Tampering | npm installs (fastify, prisma, @fastify/cors) | accept | All packages verified in RESEARCH.md Package Legitimacy Audit — all Approved |
</threat_model>

<verification>
1. `cd apps/api && npx prisma validate` exits 0 — schema has valid MongoDB syntax
2. `cd apps/api && npx prisma generate` exits 0 and prints "Generated Prisma Client (version 6.19.3)"
3. `cd apps/api && npx tsc --noEmit` exits 0 — no TypeScript errors in API source
4. `cat apps/api/prisma/schema.prisma | grep -c 'model '` returns 15 (all 15 models)
5. `grep -v '^#' apps/api/prisma/schema.prisma | grep 'migrate'` returns empty (no migration references)
6. `cat apps/api/src/plugins/prisma.ts | grep "fastify.decorate('prisma'"` returns the decoration line
7. `curl http://localhost:3001/health` (with valid DATABASE_URL) returns `{"ok":true,"db":"connected"}`
</verification>

<success_criteria>
- All 15 Prisma models defined in schema.prisma with @id @default(auto()) @map("_id") @db.ObjectId pattern
- prisma generate exits 0 and generates the Prisma client
- prisma validate exits 0 — no schema errors
- tsc --noEmit exits 0 in apps/api — no TypeScript errors
- Fastify server registers: env validation, CORS (localhost:5173 only), Prisma plugin, health route
- GET /health returns 200 {ok:true,db:'connected'} with real MongoDB Atlas connection
- GET /health returns 503 {ok:false} when DATABASE_URL is invalid
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done.
</output>
