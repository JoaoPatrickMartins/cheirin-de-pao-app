# Não enviar dependências instaladas nem builds locais ao contexto do Docker.
# O Dockerfile roda `npm ci` e `prisma generate` dentro da imagem.
**/node_modules
**/dist
**/build
**/.turbo
.git

# Nunca vazar segredos para dentro da imagem.
**/.env
**/.env.*

# Artefatos de dev / testes
**/coverage
**/*.tsbuildinfo
apps/web/dev-dist
.devcontainer
.planning
.projeto
